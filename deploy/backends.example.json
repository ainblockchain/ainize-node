{
  "_comment": [
    "Example backends block; merge only the services you actually run into config.json.",
    "Model IDs must match the backend served-model-name. Keep upstream URLs private.",
    "Serving and training GPUs must be separate. See serve-llm.sh, serve-stt.sh and serve-image.sh."
  ],
  "backends": [
    {
      "id": "llm",
      "modality": "chat",
      "upstream": "http://127.0.0.1:8000",
      "models": [
        "Qwen3.8-Flash-Next"
      ],
      "concurrency": 1
    },
    {
      "id": "stt",
      "modality": "transcription",
      "upstream": "http://127.0.0.1:8100",
      "models": [
        "qwen3-asr"
      ],
      "concurrency": 4
    },
    {
      "id": "image",
      "modality": "image",
      "upstream": "http://127.0.0.1:8200",
      "models": [
        "qwen-image-2512"
      ],
      "concurrency": 1
    }
  ]
}
