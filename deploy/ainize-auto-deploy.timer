[Unit]
Description=Check main for ainize web and node every few minutes

[Timer]
# 부팅 직후 한 번, 그다음 2분마다. `Persistent` 는 쓰지 않는다 — 기계가 꺼져 있던 동안의
# 배포를 몰아서 돌릴 이유가 없다. 다음 주기에 어차피 같은 것을 본다.
OnBootSec=2min
OnUnitActiveSec=2min
AccuracySec=30s

[Install]
WantedBy=timers.target
