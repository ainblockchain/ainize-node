[
 {
  "id": "AZ-001",
  "persona": "Visitor (knowledge user)",
  "title": "Read the landing hero and follow the two primary calls to action",
  "goal": "A first-time visitor understands what Ainize is and can jump to Explore or Live test without signing in.",
  "preconditions": [
   "No operator session (open a private window)",
   "Node http://localhost:3402 is running with the demo catalog fully verified (1 LISTED, 3 SUPERSEDED — GET /api/info counts.listed = 1, counts.verifying = 0)"
  ],
  "steps": [
   "Open http://localhost:3402/",
   "Read the top navigation and the hero block",
   "Hover the purple count card title to read its tooltip",
   "Click the primary pill 'Explore knowledge'",
   "Go back and click the secondary pill 'Try a live test'"
  ],
  "expected": [
   "Nav shows 'Explore knowledge', 'Live test', 'Node sign-in' (tooltip: 'Console for node operators and developers. You do not need to sign in to use knowledge.') and a locale button reading '한국어'",
   "Hero title 'Plug knowledge into your AI' and sub 'Ainize = AI + -ize, \"make it usable by AI\". Pick verified knowledge, check it live, load it into your model in seconds.'",
   "Count card reads '1 verified knowledge' (info.counts.listed); no 'being verified' sub-line is rendered because counts.verifying is 0 (the sub-line only appears while items are ANNOUNCED/VERIFYING); the card tooltip reads 'Knowledge that independent verifier nodes loaded into the real model and checked for accuracy and side effects. (verifier quorum reached (≥ 2 independent attestations))'",
   "Card note: 'No sign-up · pays automatically with a wallet (AIN) or node credit · removable any time'",
   "'Explore knowledge' navigates to /explore; 'Try a live test' navigates to /chat (which redirects to /chat/krx-all-2761, the first testable item)"
  ],
  "priority": "P0",
  "area": "landing",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/LandingPage.tsx (IntroSection, CountCard, PillRow)",
   "packages/web/src/i18n/pages/public.ts: landing.hero.title, landing.hero.sub, landing.hero.count, landing.hero.count_verifying, landing.hero.note, landing.nav.signin_help",
   "packages/web/src/i18n/pages/common.ts: common.locale",
   "packages/node/src/api.ts GET /api/info counts {patches, listed, verifying, superseded, rejected}"
  ]
 },
 {
  "id": "AZ-002",
  "persona": "Visitor (knowledge user)",
  "title": "Inspect the trending card for the only verified knowledge",
  "goal": "The 'Popular verified knowledge' section shows only LISTED items with human units, accuracy and an AIN price note.",
  "preconditions": [
   "No operator session",
   "krx-all-2761 is LISTED with two executed attestations (26/26); run only after both verifiers attested (GET /api/patches/krx-all-2761 passed = 2)",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5; it changes on a `cluster-restart.sh --fresh`)"
  ],
  "steps": [
   "Open http://localhost:3402/ and scroll to 'Popular verified knowledge'",
   "Count the cards and read the single card's lines",
   "Hover 'facts covered', 'accuracy' and 'Verified' labels for tooltips",
   "Click the card",
   "Go back and click 'See all'"
  ],
  "expected": [
   "Sub-title 'Passed independent verification and ready to load into the model.'",
   "Exactly 1 card (catalog status=LISTED): name 'KRX ticker codes for 2,761 listed companies (final)', meta 'Creator: node-a · Qwen3.8-Flash-Next'",
   "Lines: 'facts covered' → '2,761 facts'; 'accuracy' → '100% on a 26-question sample of 2,761' (the percentage is printed against the denominator the verifiers actually used, next to the coverage the anchor claims) with a green score bar; 'Verified' → 'Verified (2/2 independent verifiers)'; 'Price' → '25 AIN' with note 'AIN = AI Network token (this demo runs a local dev chain)'",
   "SUPERSEDED items (krx-all-2761-ep12, -ep6, pixelplus-087600) do not appear",
   "Clicking the card opens /0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761 (the node-a address segment); 'See all' opens /explore"
  ],
  "priority": "P0",
  "area": "landing",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/LandingPage.tsx useCatalogQuery({status:'LISTED', sort:'popular', limit:6}), TrendCard",
   "packages/web/src/components/public/PatchListItem.tsx executedAccuracy, usePriceLabel, useVerificationLabel",
   "packages/web/src/i18n/pages/public.ts: landing.trending.*, item.verified_by, item.price_ain",
   "packages/web/src/i18n/pages/common.ts: units.facts, price.ain_note",
   "packages/node/src/seed.ts (English knowledge names)"
  ]
 },
 {
  "id": "AZ-003",
  "persona": "Visitor (knowledge user)",
  "title": "Browse the Explore list and read every field of a knowledge row",
  "goal": "A visitor sees all non-draft knowledge with status, units, verification count, accuracy and price without technical jargon.",
  "preconditions": [
   "No operator session",
   "Demo catalog: 4 items, all topic krx-ticker-codes, model Qwen3.8-Flash-Next; krx-all-2761 LISTED, the other three SUPERSEDED",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/explore",
   "Read the title, intro, the count line and the line under it about hidden versions",
   "Read the row for 'KRX ticker codes for 2,761 listed companies (final)'",
   "Click 'show' on that line to bring the retired versions back",
   "Read the row for 'Pixelplus ticker code (single fact)'",
   "Look at the pagination bar at the bottom",
   "Click the first row"
  ],
  "expected": [
   "Title 'Explore knowledge'; intro 'Choose by verification status and accuracy. You can check any of it with a live test before buying.'; Explore opens on 'Show: Current only', so the count reads '1 knowledge' and the line under it reads '3 older versions hidden · show' (tooltip '\"Current only\" hides knowledge that a newer version replaced and knowledge that failed verification.'); clicking 'show' switches to 'All versions' and the count becomes '4 knowledge' with the hidden line gone",
   "Row krx-all-2761: name followed by the purple 'Verified' certified label and a green status chip 'For sale' (tooltip 'On sale as the current version for this topic. Whether it passed verification is what the \"Verified\" badge beside it says.') — one word no longer means two things on the same row; ident 'node-a / krx-all-2761'; 'Creator: node-a · Target model: Qwen3.8-Flash-Next · Topic: krx-ticker-codes'; '2,761 facts · 270,053 memory entries · Size 331.7 MB · 0 downloads'; 'Verified (2/2 independent verifiers) · 100% (26/26 checked) · asked as template + chat' — the denominator is the attestation's own score and the form is the one its verifiers used",
   "Row pixelplus-087600 (visible once 'All versions' is on): status chip 'Newer version: krx-all-2761' in the warning palette (#8a4b00 on #fff3e0), '8 facts · 2,992 memory entries', 'Verified (2/2 independent verifiers) · 100% (4/4 checked) · asked as template + natural', price '0.1 AIN'",
   "The 56 px seal on each card carries information: full colour ([data-testid=seal-sealed], filter none) only on the verified current version, greyed ([data-testid=seal-retired], grayscale(1), opacity .45) on a superseded one, and absent on anything that never reached quorum — not the same purple seal on all four rows",
   "No UI label shows raw words like 'rows', 'patch', 'LISTED' or 'SUPERSEDED' (the creator-written description text of the krx items may mention file names such as rows-pin.npz — that is data, not a label)",
   "Pagination shows 'First' ‹ '1 / 1' › 'Last' with all four buttons disabled (10 items per page)",
   "Clicking a row opens /{creator address}/{id}, i.e. /0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761"
  ],
  "priority": "P0",
  "area": "explore",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ExplorePage.tsx (ITEM_LIMIT=10, Count, Pagination)",
   "packages/web/src/components/public/PatchListItem.tsx",
   "packages/web/src/components/ui/Misc.tsx Pagination ('First'/'Last'), StatusChip, Certified",
   "packages/web/src/i18n/pages/public.ts: explore.title, explore.sub, explore.count, item.*",
   "packages/web/src/i18n/pages/common.ts: status.LISTED='Verified', status.SUPERSEDED='Newer version available', units.rows, units.facts",
   "packages/node/src/seed.ts; GET /api/catalog?limit=50 (total 4)"
  ]
 },
 {
  "id": "AZ-004",
  "persona": "Visitor (knowledge user)",
  "title": "Read the knowledge detail header and stat strip",
  "goal": "The detail page explains status, verification count, price unit and size in plain words and links to Live test and the same-subject page.",
  "preconditions": [
   "No operator session",
   "krx-all-2761 LISTED, passed 2/2, revenue 0, downloads 0",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5; the page resolves by knowledge id, so an outdated address segment still renders)"
  ],
  "steps": [
   "Open http://localhost:3402/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761",
   "Read the white header band (title, ID line, track line, right-hand buttons)",
   "Read the status row and meta line under the band",
   "Read each stat in the strip and hover 'accuracy', 'Memory entries', 'Facts'",
   "Click 'Other knowledge on this subject ›'",
   "Go back and click the 'Live test' button"
  ],
  "expected": [
   "Header: title 'KRX ticker codes for 2,761 listed companies (final)', 'ID krx-all-2761', 'Creator: node-a', 'Track finance/KRX-latest · topic finance/krx'",
   "Right side: purple button 'Live test' with sub-text 'Compare answers before and after'; link 'Other knowledge on this subject ›'",
   "Status chip 'For sale' followed by 'Verified on the real model 2/2 · Verified' — the chip is the listing state, the sentence is the verification",
   "Meta: 'By node-a · target model Qwen3.8-Flash-Next · verified {n}m ago' (or '{n}h ago' depending on age)",
   "Stats: 'Purchases' 0; 'accuracy' (lowercase glossary term) reads the measured pair '1/8 → 26/26' (tooltip 'The same verification run scored 1/8 before the knowledge was loaded and 26/26 after.') with the note '100% after loading' — the percentage alone could not say whether the model already knew the answers; 'Memory entries' 270,053; 'Facts' 2,761; 'Size' 331.7 MB; 'Price' '25 AIN' with note 'AIN = AI Network token (this demo runs a local dev chain)'; 'Revenue' renders 'Free' for the zero amount (shared price formatter) — flag as a copy issue if judged confusing",
   "Tab bar: 'Overview', 'Verification', 'Origins & derivatives', 'Buy', 'History'",
   "Subject link opens /benchmarks/krx-ticker-codes; 'Live test' opens /chat/krx-all-2761"
  ],
  "priority": "P0",
  "area": "patch",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/PatchPage.tsx (BandContent, NameRow, Stats, tabs)",
   "packages/web/src/pages/detail/recordText.ts priceLabel/priceNote",
   "packages/web/src/utils/format.ts bytes(), pct(), scoreText()",
   "packages/web/src/i18n/pages/detail.ts: detail.patch.*, detail.stat.*, detail.tab.*; glossary.ts term('accuracy')",
   "GET /api/patches/krx-all-2761 (branches[0] finance/KRX-latest, topic_path finance/krx)"
  ]
 },
 {
  "id": "AZ-005",
  "persona": "Visitor (knowledge user)",
  "title": "Read the Verification tab and confirm only real-model runs count",
  "goal": "A visitor can see who verified the knowledge, how, which results counted, and what actually backs a verification.",
  "preconditions": [
   "No operator session",
   "krx-all-2761 has two executed attestations (node-b and node-c; verified_on vllm:Qwen3.8-Flash-Next; restarts 0); verifier addresses come from GET /api/nodes (currently node-b 0x430b3A37…9935, node-c 0x199Ff870…bce9)"
  ],
  "steps": [
   "Open the detail page of krx-all-2761 and click the 'Verification' tab",
   "Read the summary row",
   "Read both table rows; hover the 'Side-effect check' and 'Counts' cells",
   "Read the three explanatory notes under the table"
  ],
  "expected": [
   "Summary: 'Run on the real model' 2/2; 'Integrity only' 0; 'Status' chip 'For sale'",
   "Table headers: 'Verifier node', 'Method', 'Before', 'Accuracy', 'Side-effect check', 'Restarts detected', 'Counts', 'Result', 'Time' — 'Before' is score.pre_apply, which every attestation carries and which used to reach the DOM only inside a title= attribute; 'Counts' replaced the old 'Deposit' column, which reported a bond that was never escrowed (critique 2 item 127)",
   "Rows for node-b and node-c (name with the shortened address underneath): Method 'run on the real model'; Before '1/8' (the same run's pre_apply, tooltip 'What the same verifier scored on the same questions BEFORE the knowledge was loaded (pre_apply) …'); Accuracy '26/26'; Side-effect check 'not reported' (no collateral_nat in the record); Restarts detected 'none'; Counts 'independent' (tooltip: the result is signed with the verifier node's key, no deposit is escrowed, any node can challenge it); Result 'Passed' in green; Time as '{n}h ago' with the full date in the tooltip",
   "Note 1 starts 'Verified — Only verifications run on the real model count toward Verified (currently 2/2). The 0 integrity-only checks are shown separately…'",
   "A note under the table explains the pair: '\"Before\" and \"Accuracy\" are the same questions scored twice in the same run — before the knowledge was loaded (pre_apply) and after it. The pair is what shows how much the knowledge changed; the second number alone cannot.'",
   "Note 2 starts 'Restarts detected: if the model server restarted mid-run…'; Note 3 is the honest backing statement: 'No deposit is at stake on a verification. Each result is signed with the verifier node's key and stays on the public record under that identity for ever. Any node that thinks a result is wrong can challenge it, and a challenge stops the sale until the knowledge is re-verified.'"
  ],
  "priority": "P0",
  "area": "verification",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/PatchPage.tsx Verification(), sideEffectCell()",
   "packages/web/src/pages/detail/recordText.ts howLabel()",
   "packages/node/src/verifier.ts (only executed attestations count; hash-only separate)",
   "packages/web/src/i18n/pages/detail.ts: detail.ver.*, detail.how.executed, detail.pass",
   "GET /api/patches/krx-all-2761 attestations",
   "packages/core/src/catalog.ts deriveCatalog (self-attestations excluded; the fraction is clamped to the quorum)"
  ]
 },
 {
  "id": "AZ-006",
  "persona": "Visitor (knowledge user)",
  "title": "Read the Buy tab as a visitor and probe the automatic-payment address",
  "goal": "A visitor understands how automatic payment works, sees the price quote address, and is told sign-in is needed to buy from this node.",
  "preconditions": [
   "No operator session",
   "krx-all-2761 LISTED (quorum 2/2) with gateway_url http://localhost:3402/x402/patch/krx-all-2761 — while it is still VERIFYING the gateway answers HTTP 423 'patch not listed yet (verification 1/2)' instead of 402"
  ],
  "steps": [
   "Open the detail page of krx-all-2761 and click the 'Buy' tab",
   "Read the 'Buy with automatic payment' section and its key/value rows",
   "Click the 'Purchase address' link (opens a new tab) and inspect the response",
   "Expand 'For node operators & developers: buy from the command line / API'",
   "Read the 'Buy from this node' section",
   "Click 'Operator & developer docs ›'"
  ],
  "expected": [
   "Heading 'Buy with automatic payment' with the explanation starting 'Ask for the knowledge and the selling node quotes a price; once payment is confirmed you download immediately. Pay with a wallet (AIN) or node credit — no sign-up…'",
   "Rows: 'Price' '25 AIN · pay once per download' with note 'AIN = AI Network token (this demo runs a local dev chain)'; 'Selling node' 'node-a 0xF7A9dE49…A1F5' (name + shortened node-a address); 'Purchase address' http://localhost:3402/x402/patch/krx-all-2761",
   "The new tab returns HTTP 402 with an x-payment-required header (price quote), not the file",
   "Details show comment lines '# 1) ask → price quote comes back', '# 2) pay, then retry with the proof', '# or let the CLI do the whole loop' (ainize patch buy krx-all-2761), '# AI agent: detect unknown answer → search → auto-pay → load'",
   "'Buy from this node' shows only 'Sign in as this node’s operator to buy with the node’s wallet.' — no Buy button, no 'Paying…' state for a visitor",
   "Blue box 'Buy with an AI agent — An AI agent can buy on your behalf…' and the docs link opens /docs"
  ],
  "priority": "P0",
  "area": "x402",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/PatchPage.tsx Buy() (isOperator=false branch)",
   "packages/web/src/i18n/pages/detail.ts: detail.buy.*",
   "packages/node/src/market.ts x402 402 flow; packages/node/src/api.ts GET /x402/patch/:id (423 while not listed)",
   "GET /api/patches/krx-all-2761 gateway_url"
  ]
 },
 {
  "id": "AZ-007",
  "persona": "Visitor (knowledge user)",
  "title": "Open Live test, pick knowledge and use the sample-question chips",
  "goal": "A visitor can start a live test without login, see what is testable, and pre-fill a benchmark question with one click.",
  "preconditions": [
   "No operator session",
   "Model server on node-a is available (GET /api/chat/patches runtime.available=true)"
  ],
  "steps": [
   "Open http://localhost:3402/chat",
   "Read the title, model chip, subtitle and the left panel",
   "Tick 'Pixelplus ticker code (single fact)' in the left list (checkbox), untick it, then tick 'KRX ticker codes for 2,761 listed companies (final)'",
   "In the composer read 'Sample questions this knowledge answers' and hover the first chip",
   "Click 'Show 18 more', then 'Show less'",
   "Click the chip '종목코드 삼성전자' (do not send)",
   "Open http://localhost:3402/chat/does-not-exist"
  ],
  "expected": [
   "URL is replaced with /chat/krx-all-2761 (first testable item); title 'Live test'; chip 'Test model Qwen3.8-Flash-Next'; subtitle 'Ask the same question before and after loading the knowledge and watch the answer change. It loads and unloads in seconds, no restart.'",
   "Left panel 'Knowledge to load (pick up to 3)' (checkboxes, help 'They load in the order you tick them. If two overlap, the one ticked last wins.') with hint 'Only knowledge whose body is on this node can be tested.'; 4 items each showing name, 'node-a/{id}', '{n} facts', '100% accuracy', price like '25 AIN' + 'AIN = AI Network token (this demo runs a local dev chain)', a status chip ('For sale' for the current version, 'Newer version available' for a retired one); the active item shows 'Selected'",
   "Ticking exactly one item changes the URL to /chat/{id} (several → /chat/{id1},{id2}) and the main header shows its name, status chip, '{n} facts' and a 'Details →' link; the transcript shows 'No questions yet' with 'Click a sample question below or type your own. You get two answers side by side: before and after loading the knowledge.'",
   "krx-all-2761 shows 8 chips + 'Show 18 more' (26 samples); the chip for '종목코드 픽셀플러스' carries 'Expected: 087600' both as its tooltip and as a visible second line inside the chip (its accessible name stays the bare prompt); 'Show less' collapses back",
   "Clicking a chip puts its text into the textarea (placeholder 'Type a question and press Enter (Shift+Enter for a new line)') and focuses it; footer reads 'Free tries are limited per hour. No sign-in needed.'",
   "Unknown id: yellow alert 'The knowledge in the address (does-not-exist) cannot be tested on this node, so the first one in the list was chosen.' and URL becomes /chat/krx-all-2761"
  ],
  "priority": "P0",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ChatPage.tsx (route preselect, missingId)",
   "packages/web/src/components/chat/KnowledgePicker.tsx",
   "packages/web/src/components/chat/ChatComposer.tsx (INITIAL_CHIPS=8)",
   "packages/web/src/i18n/pages/chat.ts: chat.title, chat.model, chat.picker.*, chat.samples.*, chat.input.placeholder, chat.quota.visitor, chat.empty.*",
   "GET /api/chat/patches"
  ]
 },
 {
  "id": "AZ-008",
  "persona": "Visitor (knowledge user)",
  "title": "Run a Compare test and read the correct-answer marker and quota counter",
  "goal": "A visitor sees the before/after answers side by side, an automatic ✓/✗ against the sealed expected answer, and how many free tries remain.",
  "preconditions": [
   "No operator session; fresh IP quota (fewer than 20 tests this hour)",
   "Model server available; no other test running"
  ],
  "steps": [
   "Open http://localhost:3402/chat/krx-all-2761",
   "Confirm 'View' is on 'Compare' and 'Enable thinking' is unchecked",
   "Click the chip '종목코드 삼성전자' and press Enter",
   "While pending, read the two bubbles and the row above the composer",
   "Wait for both answers",
   "Read the badges in each bubble header and hover the badge in 'After loading'",
   "Read the composer footer"
  ],
  "expected": [
   "Pending state: two bubbles 'Before loading' and 'After loading' with animated dots and note 'Includes loading and unloading — this can take tens of seconds.'; the row 'Waiting for the answer — you can cancel if it takes too long.' with a 'Cancel' button; the Send button reads 'Waiting for the answer…'; chips, mode buttons and textarea are disabled",
   "HTTP 200 from POST /api/chat with mode 'compare'; 'After loading' shows 'reply {n}s' and '· loaded in {n}s' (or '· was already loaded')",
   "'After loading' badge '✓ Correct' (answer contains 005930) with tooltip 'This question is one of the knowledge’s benchmark items, so the answer was checked automatically. Expected: 005930'; 'Before loading' shows '✓ Correct' or '✗ Wrong' depending on the base model; BOTH bubbles render 'Expected: 005930' as muted text under the verdict, so the tick or cross can be checked without a mouse (data-testid chat-expected-base / chat-expected-patched)",
   "Footer now reads 'Free trial 19/20 left this hour' (remaining_quota decremented by one, quota_limit 20)",
   "A 'Clear conversation' text button appears in the footer; transcript auto-scrolls to the newest turn"
  ],
  "priority": "P0",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ChatPage.tsx send(), quotaText",
   "packages/web/src/components/chat/TurnView.tsx (Hit badge, pending note)",
   "packages/web/src/components/chat/util.ts answerHits/matchSample",
   "packages/node/src/api.ts POST /api/chat (remaining_quota, quota_limit 20)",
   "packages/node/src/market.ts chat() compare flow under runtime lock",
   "packages/web/src/i18n/pages/chat.ts: chat.mode.compare, chat.bubble.*, chat.hit.*, chat.quota.left_of, chat.input.in_flight, chat.input.sending"
  ]
 },
 {
  "id": "AZ-009",
  "persona": "Visitor (knowledge user)",
  "title": "Exhaust the 20-per-hour free trial and read the quota message",
  "goal": "A visitor is told clearly when free tries are used up and how to continue (wait an hour or buy).",
  "preconditions": [
   "No operator session; quota for this IP is fresh (no anonymous live test from this host in the last hour)",
   "Model server available; pixelplus-087600 is testable (body on node-a, status LISTED or SUPERSEDED — not still ANNOUNCED)",
   "Browser and curl run on the same host and use the same hostname (http://localhost) so req.ip matches (IPv4 vs IPv6 loopback would count as different visitors)",
   "Use 'Before only' view to keep each request fast"
  ],
  "steps": [
   "Open http://localhost:3402/chat/pixelplus-087600 and select 'Before only'",
   "Send the chip '종목코드 픽셀플러스' 20 times in a row (wait for each answer), watching the footer counter",
   "After the 20th answer read the page",
   "Try to type in the textarea and try one more send via curl: POST http://localhost:3402/api/chat with {\"patch_id\":\"pixelplus-087600\",\"mode\":\"base\",\"messages\":[{\"role\":\"user\",\"content\":\"hi\"}]}"
  ],
  "expected": [
   "Footer counts down 'Free trial 19/20 left this hour' … 'Free trial 1/20 left this hour'",
   "After the 20th answer (remaining_quota 0) a yellow alert appears above the grid: 'You used all free tries for this hour. Try again in an hour or buy the knowledge.' and the textarea is disabled with that same text as placeholder; chips and Send are disabled",
   "A further POST /api/chat from the same IP returns HTTP 429 with message 'quota_chat: free live-test quota exhausted for this hour — buy the patch or run your own node'; the 429 body also carries quota_reset (epoch ms = the window start plus the hour); in the UI that maps to the red turn error 'You used all free tries for this hour. Try again in an hour, or buy the knowledge and use it without limits on your own node.' with NO Retry button (send() would return at the exhausted guard without issuing a request) — in its place a link 'Buy this knowledge' to the knowledge page and the measured reset time 'Free tries reset at HH:MM'",
   "The quota is 20 requests per visitor IP in a fixed 60-minute window that starts at the visitor's first request (it resets once 3,600,000 ms have passed since the window began); an operator session is unlimited ('You are signed in as this node’s operator — unlimited tests.')"
  ],
  "priority": "P0",
  "area": "chat",
  "automation": "api",
  "evidence": [
   "packages/node/src/api.ts POST /api/chat (chatQuota(visitor, 20, 3600_000), HttpError 429)",
   "packages/node/src/market.ts chatQuota()",
   "packages/web/src/pages/ChatPage.tsx exhausted state, composerDisabled, mapChatError 429 → chat.err.quota",
   "packages/web/src/i18n/pages/chat.ts: chat.quota.left_of, chat.quota.none, chat.err.quota, chat.quota.operator"
  ]
 },
 {
  "id": "AZ-010",
  "persona": "Visitor (knowledge user)",
  "title": "Audit the public record: filters, integrity card and origin → derivative map",
  "goal": "Anyone can check who registered, verified and superseded which knowledge, and see the lineage graph.",
  "preconditions": [
   "No operator session",
   "Node-a ledger is the AIN dev chain (kind 'ain', app /apps/knowledge); all four demo items verified so the chain holds ~20 records (4 anchor, 8 attest, 3 supersede, 3 node, 2 branch) — read the live count from GET /api/info ledger.records"
  ],
  "steps": [
   "Open http://localhost:3402/ledger",
   "Read the info card and the developer row under it; click 'Open in explorer'",
   "Read the table headers and the first rows",
   "Open the select (default 'All records') and choose 'Purchase settled'",
   "Choose 'Newer version' and compare the rows with GET /api/ledger?kind=supersede",
   "Choose 'All records', scroll to 'Origin → derivative map', hover boxes and click the box 'krx-all-2761'"
  ],
  "expected": [
   "Title 'Public record' with description 'A record anyone can check: who registered, verified and bought which knowledge.'; header badge next to the logo reads 'AI Network'",
   "Info card: 'Record type' 'AIN blockchain'; 'Network' ain:local; 'Records' {n} (= ledger.records); 'Blocks recorded' (chain height, increasing); 'Connected AI Network node' http://localhost:8081; 'Integrity' 'valid · {n} checked' in green",
   "Developer row: 'Stored at' 'NODE OPERATORS & DEVELOPERS /apps/knowledge — Recorded on the AI Network — registrations as knowledge entries; …'; 'Open in explorer' opens http://localhost:8081/get_value?ref=%2Fapps%2Fknowledge (URL-encoded href; visible text unencoded) in a new tab (JSON)",
   "Table headers 'Time', 'Kind', 'What happened', 'By', 'Record ID / tx'; kind chips use 'Registered', 'Verification', 'Newer version', 'Knowledge track', 'Node'; 20 rows per page and a pager. GET /api/ledger returns the NEWEST `limit` records and cannot page further back, so when the ledger holds more than the page asked for (1,000) a line above the table reads 'Showing the most recent 1,000 of {n} records — the node returns this many at a time and cannot page further back.'; with a smaller ledger that line is absent",
   "Select options: 'All records', 'Registered', 'Verification', 'Purchase settled', 'Knowledge track', 'Node', 'Newer version', 'Subscription', 'Re-verification request'; 'Purchase settled' → 'No \"Purchase settled\" records yet.' (until a purchase has been made)",
   "'Newer version' → one row per supersede record on the WHOLE ledger (the node applies the filter, not the fetched window): e.g. 'krx-all-2761 marked as the newer version of krx-all-2761-ep6 (241,992 overlapping memory entries)'",
   "Map: 4 boxes in three columns — left krx-all-2761-ep6 and pixelplus-087600, middle krx-all-2761-ep12, right krx-all-2761 (label 'For sale · Qwen3.8-Flash-N…' — the map prints the same listing state as the chips); two grey solid 'extends' arrows (krx-all-2761 → ep12, ep12 → ep6) and three orange dashed 'supersedes' arrows; legend 'derived from origin (creator revenue share)', 'replaced by a newer version (same subject, overlapping entries)', 'origins on the left, derivatives to the right'; clicking a box opens that knowledge's detail page"
  ],
  "priority": "P0",
  "area": "ledger",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/LedgerPage.tsx (KINDS, PAGE=20, InfoCard, KnowledgeGraph layout)",
   "packages/node/src/api.ts /api/ledger, /api/ledger/verify, /api/ledger/graph",
   "packages/web/src/i18n/pages/detail.ts: detail.ledger.*, detail.kind.*, detail.rec.supersede, detail.ledger_kind.ain",
   "packages/web/src/components/ui/Header.tsx LedgerBadge 'AI Network'",
   "packages/node/src/seed.ts parents chain ep6 ← ep12 ← krx-all-2761; GET /api/ledger?limit=1000, GET /api/ledger/graph"
  ]
 },
 {
  "id": "AZ-011",
  "persona": "Visitor (knowledge user)",
  "title": "Pick an audience card and land on the right entry point (creator card = teach the model)",
  "goal": "Visitors self-identify (use / teach / operate) and each card sends them to the correct page; the creator card no longer leads to the sign-in wall but to Live test with the teach banner.",
  "preconditions": [
   "No operator session",
   "http://localhost:3402/ loaded"
  ],
  "steps": [
   "Scroll to the section titled 'Which one are you?'",
   "Read the three cards and their numbered steps",
   "Click 'Explore knowledge' on the first card",
   "Return to / and click 'Teach the model' on the second card",
   "Return to / and click the small link 'Already have a knowledge file (.npz) and run a node? Register a file →' under the second card's button",
   "Return to / and click 'Open the operator console' on the third (dark) card",
   "Back on /, read the top navigation of the landing page"
  ],
  "expected": [
   "Section sub-title: 'Ainize is used by people who use knowledge, people who make it, and people who run the network.'",
   "Card 1 title 'I want to use knowledge' with steps 'Find knowledge verified for your topic and model.' / 'Ask the same question before and after loading it and see the answer change.' / 'If you like it, pay and load it in seconds — unload any time.'",
   "Card 2 title 'I want to teach the model something' with steps 'Ask the model in Live test and correct it when it is wrong.' / 'This node trains your corrections into knowledge — no sign-in, no server of your own.' / 'Keep it private, or publish it and get paid on every sale.'; the button reads 'Teach the model'; there is no 'Register knowledge' / 'I want to sell knowledge' any more",
   "Card 3 title 'Node operators & developers' with the label 'For developers · terminal' (displayed in uppercase by CSS) and a code block 'npm install -g ainize / ainize init / ainize start'",
   "'Explore knowledge' → /explore; 'Teach the model' → /chat?teach=1 (Live test with the purple teach banner 'Wrong answer? Click \"Teach the right answer\" under any reply…' and the open lesson basket — NOT /signing); 'Register a file →' → /signing?next=%2Fnew-patch (the only remaining route to the sign-in wall, explicitly labelled for node operators); 'Open the operator console' → /signing",
   "When GET /api/info.accepts_contributions is true the landing nav shows 'Teach' next to 'Live test' (→ /chat?teach=1); when false the creator card adds 'This node is not accepting lessons right now. Live test still works.' under the button"
  ],
  "priority": "P1",
  "area": "landing",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/LandingPage.tsx (AudienceGrid; data-testid landing-creator-card / landing-teach-cta / landing-register-link / landing-nav-teach)",
   "packages/web/src/i18n/glossary.ts AUDIENCE.user/creator/operator",
   "packages/web/src/i18n/pages/public.ts: landing.audience.creator.title/s1/s2/s3/cta/operator_link/off, landing.nav.teach (spec §5.1)",
   "packages/e2e/tests/web-teach-operator.spec.ts 'AZ-011 …'"
  ]
 },
 {
  "id": "AZ-012",
  "persona": "Visitor (knowledge user)",
  "title": "Copy the one-line commands and read the How-it-works / Why Ainize story",
  "goal": "A visitor can grab the publish/use commands to the clipboard and understand the product narrative and footer links.",
  "preconditions": [
   "No operator session",
   "Browser allows clipboard write (HTTPS or localhost)"
  ],
  "steps": [
   "On http://localhost:3402/ scroll to 'One line is enough'",
   "Click 'Copy' next to the 'Use knowledge' command, then paste into a text field",
   "Click 'Copy' next to the 'Publish knowledge' command",
   "Click 'Full API & CLI reference →'",
   "Go back, scroll to 'How it works' and 'Why Ainize'",
   "Scroll to the dark footer and click 'Public record', then 'Terms'"
  ],
  "expected": [
   "'Use knowledge' card shows 'ainize use krx-all-2761'; after clicking, the button reads 'Copied' for ~1.5 s and the pasted text is exactly 'ainize use krx-all-2761'",
   "'Publish knowledge' card shows 'ainize publish ./my-knowledge.npz --name \"한국 상장사 종목코드\" --model Qwen3.8-Flash-Next --benchmark ./bench.json --price 25' and copies verbatim",
   "Caption 'You can do all of this on the web too — start with \"Explore knowledge\" and \"Live test\" above.'; the reference link opens /docs",
   "'How it works' shows 'Three steps. No retraining, no restart.' with steps 01 'Verified', 02 'Live test', 03 'Load into model'",
   "'Why Ainize' timeline shows '2019 – 2020' 'Repo → running AI service', '2026' 'Knowledge → something the model knows', 'AIN' 'The first three letters: AI Network' and tagline 'Creators get paid per sale; users only ever see verified knowledge.'",
   "Footer links: 'Terms' → /terms, 'Network' → /network, 'Public record' → /ledger, 'ain-js' (external), 'Contact us' (mailto:support@ainize.ai); copyright 'ⓒ 2026 Common Computer Inc. · Ainize'"
  ],
  "priority": "P1",
  "area": "landing",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/LandingPage.tsx ONE_LINE_PUBLISH / ONE_LINE_USE, CopyButton",
   "packages/web/src/components/ui/Misc.tsx CopyButton ('Copied')",
   "packages/web/src/i18n/pages/public.ts: landing.oneline.*, landing.how.*, landing.why.*, landing.footer.*",
   "packages/web/src/i18n/pages/common.ts: common.copy, common.copied"
  ]
 },
 {
  "id": "AZ-013",
  "persona": "Visitor (knowledge user)",
  "title": "Re-order Explore by each sort option",
  "goal": "Sorting by price, newest and size changes the list order predictably.",
  "preconditions": [
   "No operator session",
   "Demo catalog prices: krx-all-2761 25 AIN, -ep12 10 AIN, -ep6 5 AIN, pixelplus-087600 0.1 AIN"
  ],
  "steps": [
   "Open http://localhost:3402/explore and switch 'Show' to 'All versions' (the default hides the three retired ones)",
   "Open the sort select at the top-right and read its options",
   "Choose 'Price' and note the row order",
   "Choose 'Newest' and note the row order",
   "Choose 'Knowledge size' and note the row order",
   "Choose 'Most popular'"
  ],
  "expected": [
   "Select options are exactly 'Most popular', 'Newest', 'Price', 'Knowledge size' (default 'Most popular')",
   "'Price' orders ascending: pixelplus-087600 (0.1 AIN), krx-all-2761-ep6 (5 AIN), krx-all-2761-ep12 (10 AIN), krx-all-2761 (25 AIN)",
   "'Newest' orders by creation: krx-all-2761, krx-all-2761-ep12, krx-all-2761-ep6, pixelplus-087600",
   "'Knowledge size' orders by memory entries descending: krx-all-2761 (270,053) first, the two 241,992 items next, pixelplus-087600 (2,992) last",
   "Sorting is done server-side by /api/catalog (the page fetches up to 200 items and paginates 10 per page client-side); while the request is in flight the count line '4 knowledge' appends ' · updating…'; the page resets to 1 / 1 after each sort change",
   "'Most popular' ranks by status FIRST and only then by downloads: krx-all-2761 (LISTED, 55 downloads) heads the list ahead of pixelplus-087600 (SUPERSEDED, 187 downloads), and the three superseded items follow — GET /api/catalog?sort=popular returns statuses ['LISTED','SUPERSEDED','SUPERSEDED','SUPERSEDED']"
  ],
  "priority": "P1",
  "area": "explore",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ExplorePage.tsx SORTS, SelectBox, reset()",
   "packages/node/src/api.ts /api/catalog sorters (latest/popular/price/rows)",
   "packages/web/src/i18n/pages/public.ts: explore.sort.popular/latest/price/rows, explore.updating, explore.count"
  ]
 },
 {
  "id": "AZ-014",
  "persona": "Visitor (knowledge user)",
  "title": "Filter Explore by model and topic and search, including the empty state",
  "goal": "Chips and search narrow the list, and a no-match query shows a helpful empty message.",
  "preconditions": [
   "No operator session"
  ],
  "steps": [
   "Open http://localhost:3402/explore",
   "Read the 'Model', 'Topic' and 'Show' chip groups; hover the 'Topic' label",
   "Click 'All versions' in the 'Show' group",
   "Click the 'Qwen3.8-Flash-Next' chip, then click it again",
   "Click the 'krx-ticker-codes' chip",
   "Type 'pixel' into the search box (placeholder 'Search by name or description')",
   "Clear it and type 'zzz-no-match'",
   "Clear the search box"
  ],
  "expected": [
   "Chip groups: 'Model' → 'All', 'Qwen3.8-Flash-Next'; 'Topic' → 'All', 'krx-ticker-codes'; 'Show' → 'Current only' (active by default, purple border) and 'All versions'; with 'Current only' the list holds 1 row and with 'All versions' 4; Topic tooltip 'Knowledge on the same topic is scored with the same question set. (benchmark.queries)'",
   "Clicking a chip highlights it and the count stays '4 knowledge' (all demo items share that model/topic); clicking the active chip again returns to 'All'",
   "Search 'pixel' → count '1 knowledge' and only 'Pixelplus ticker code (single fact)' remains (search matches id/name/description/model/topic, case-insensitive)",
   "Search 'zzz-no-match' → count '0 knowledge', the message 'No knowledge matches. Try another model, topic or search term.' and no pagination bar",
   "Clearing the box restores '4 knowledge'"
  ],
  "priority": "P1",
  "area": "explore",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ExplorePage.tsx Filters, Chip, Search, Empty",
   "packages/node/src/api.ts /api/catalog q/model/schema filters",
   "packages/web/src/i18n/pages/public.ts: explore.filter.model, explore.filter.schema, explore.filter.schema_help, explore.filter.all, explore.search, explore.empty"
  ]
 },
 {
  "id": "AZ-015",
  "persona": "Visitor (knowledge user)",
  "title": "Read the Overview tab: model, verification questions, integrity and tracks",
  "goal": "A visitor learns which model the knowledge targets, what questions verify it, and how to check a downloaded file's integrity.",
  "preconditions": [
   "No operator session",
   "Detail page of krx-all-2761 open"
  ],
  "steps": [
   "Ensure the 'Overview' tab is active",
   "Read the 'Description' section and the accuracy line under the score bar",
   "Read the 'Target model' key/value block",
   "Read the 'Verification questions' block and the sample list",
   "Expand the details row 'For node operators & developers · Recipe (portable to other models)'",
   "In 'Integrity', click 'Copy' next to the content hash and paste somewhere",
   "Read 'Knowledge tracks'"
  ],
  "expected": [
   "Accuracy line 'Accuracy 100% on 26 of 2,761 questions checked by verifiers', and under it the baseline from the same run: 'The same run scored 1/8 before the knowledge was loaded → 26/26 after'",
   "'Target model' note 'This knowledge works only on the model below. For other models it can be rebuilt from the recipe below.'; rows 'Model' Qwen3.8-Flash-Next, 'Checkpoint' W4A16, 'Entry width' 160, 'Billing' 'pay once per download', 'License' 'Use on the identified model · no resale of raw data', 'Created' (date)",
   "'Verification questions' note 'Verifier nodes score the knowledge with these questions. They are published with the knowledge — not sealed — so anyone can re-run the same scoring and check the verdict. It also means the author knew these questions in advance; read the accuracy with that in mind.' (the old wording claimed a sealed answer key that GET /api/patches/:id serves to anonymous callers — critique 2 item 164); 'Subject' krx-ticker-codes (link to /benchmarks/krx-ticker-codes); 'facts covered' '2,761 facts'; 'Question formats' 'template, chat'; 'Side-effect limit' — since no attestation carries collateral_nat — reads 'Limit declared (≤ 0.08 nat) — not yet measured by any verifier' in the warning tone (#8a4b00) with a 'See the verification tab →' button that opens the tab where the same fact reads 'not reported'; once a verifier does report one the row reads 'Threshold set — measured by {n} of {of} verifiers'; 'Question-set hash' shown",
   "'Sample questions (26)' lists 12 items like '\"종목코드 픽셀플러스 \" → 087600' followed by '… 14 more'",
   "Recipe details show JSON with corpus_template and hyperparams",
   "'Integrity' note 'A downloaded file is genuine only if its content hash matches the value below.'; 'Content hash' 57c93463… (stable: sha256 of rows-pin.npz); 'Copy' turns into 'Copied' and the clipboard holds the 64-hex sha256; 'Public record ID' equals record_hash from GET /api/patches/krx-all-2761 (chain-specific, do not hardcode); 'File stored on this node' 'yes'",
   "'Knowledge tracks' → 'finance/KRX-latest (market=KRX, version=latest)'"
  ],
  "priority": "P1",
  "area": "patch",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/PatchPage.tsx Overview()",
   "packages/web/src/i18n/pages/detail.ts: detail.ov.*",
   "GET /api/patches/krx-all-2761 (anchor.model, benchmark, patch_sha256, record_hash, has_body, branches)"
  ]
 },
 {
  "id": "AZ-016",
  "persona": "Visitor (knowledge user)",
  "title": "Follow origins, overlap and newer-version notices in both directions (and the multi-select overlap warning in Live test)",
  "goal": "A visitor understands that krx-all-2761 replaces three older versions and that the older version page points to the newer one.",
  "preconditions": [
   "No operator session",
   "krx-all-2761 is LISTED and the supersede records exist (written by reconcileSupersedes after quorum): krx-all-2761 supersedes -ep12, -ep6, pixelplus-087600",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open the detail page of krx-all-2761 and click 'Origins & derivatives'",
   "Read the tree (Origins / This / Derived) and the 'Revenue shared with' row",
   "Read the 'overlap check' table and the blue notice under it",
   "Click the origin box 'KRX ticker codes for 2,761 listed companies — epoch 12' (id krx-all-2761-ep12)",
   "On the ep12 page read the meta line, then open its 'Origins & derivatives' tab",
   "Click the 'krx-all-2761' link inside the yellow notice",
   "Open /chat, tick 'KRX ticker codes for 2,761 listed companies (final)' and then 'Pixelplus ticker code (single fact)' in the picker 'Knowledge to load (pick up to 3)'"
  ],
  "expected": [
   "Note: 'creator revenue share — Knowledge built on other knowledge records its origins publicly, and every sale automatically shares revenue with the original creators. Creator share on this network: 30% of each sale.'",
   "'Origins' shows one box with the ep12 name and id krx-all-2761-ep12 underneath; 'Derived' shows 'no derived knowledge yet'; 'Revenue shared with' lists the node-a address (0xF7A9dE49…A1F5)",
   "'overlap check' table has 3 rows: krx-all-2761-ep12 '241,992 memory entries', krx-all-2761-ep6 '241,992 memory entries', pixelplus-087600 '2,170 memory entries'; Relation 'Same subject — contradictory or a newer version'; Status chip 'Newer version available'",
   "Blue notice: 'This knowledge replaces the older version(s): krx-all-2761-ep12, krx-all-2761-ep6, pixelplus-087600 — same subject, overlapping entries, newer registration.'",
   "ep12 page: status chip 'Newer version: krx-all-2761' (warning palette), meta line ends with '∙ Newer version: krx-all-2761' link; its Origins tab shows 'Derived' → krx-all-2761 and a yellow notice 'A newer version exists: krx-all-2761. Subscribed nodes should update.'",
   "The notice link navigates back to the krx-all-2761 page",
   "Live test picker: an info alert names both knowledges — '… overlap on 2,170 memory entries — Pixelplus ticker code (single fact), ticked last, wins.' (the same overlap the detail page's 'overlap check' table reports); the rows show 'Loads 1.' / 'Loads 2.' and the URL is /chat/krx-all-2761,pixelplus-087600 (see AZ-101 / TM-090 for the full multi-knowledge test)"
  ],
  "priority": "P1",
  "area": "patch",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/PatchPage.tsx Lineage(), Info superseded_by link",
   "packages/web/src/i18n/pages/detail.ts: detail.lin.*, detail.patch.newer_version",
   "packages/node/src/market.ts (SUPERSEDED state, reconcileSupersedes)",
   "GET /api/patches/krx-all-2761 conflicts/lineage/supersedes; GET /api/patches/krx-all-2761-ep12 superseded_by",
   "GET /api/info royalty_share 0.3",
   "packages/web/src/components/chat/KnowledgePicker.tsx (overlap alert data-testid=chat-overlap); packages/web/src/i18n/pages/chat.ts chat.picker.overlap_pair; GET /api/chat/patches overlaps[]"
  ]
 },
 {
  "id": "AZ-017",
  "persona": "Visitor (knowledge user)",
  "title": "Compare all knowledge on the same subject and hit the unknown-topic 404",
  "goal": "A visitor can compare versions scored with the same question set and gets a clear message for a topic that does not exist.",
  "preconditions": [
   "No operator session",
   "krx-all-2761 LISTED (so the stats line counts 1 verified)"
  ],
  "steps": [
   "Open http://localhost:3402/benchmarks/krx-ticker-codes",
   "Read the title, stats line and explanation",
   "Open the sort select and read its options; choose 'Knowledge size'",
   "Click 'Back to Explore'",
   "Open http://localhost:3402/benchmarks/no-such-topic"
  ],
  "expected": [
   "Title 'Knowledge on this topic krx-ticker-codes'; stats '4 knowledge · 1 current version(s) · 3 question set(s) · models: Qwen3.8-Flash-Next' — 'current version(s)' counts LISTED items; it never says '1 verified' above four cards that all passed verification",
   "Explanation 'Scores are comparable only within one question set. The list below is grouped by the question set the verifiers used — a different set is a different exam, and those numbers cannot be lined up against each other. When contents overlap, the newer verified knowledge replaces the older one (\"Newer version available\").' followed by the link 'Back to Explore'",
   "The list is grouped by benchmark_hash, newest set first, with a heading per set naming the form and the question count and showing the hash: 'Question set · template + chat · 2,761 questions' (b6beb92f, krx-all-2761 alone), 'Question set · template · 2,761 questions' (7b126cec, ep12 + ep6), 'Question set · template + natural · 8 questions' (8f1017b4, pixelplus-087600). A group of one says 'The only knowledge scored on this question set — nothing here to compare it with.'; a group of several says 'These {n} were scored on this same question set, so they can be compared with each other.'",
   "Each card names the form its questions were asked in next to the accuracy ('asked as template + chat'), so two scores from different sets can never be read as one ranking",
   "Sort options are 'Most popular', 'Newest', 'Knowledge size' (no 'Price' here)",
   "Sorting by 'Knowledge size' reorders the cards INSIDE each question set (the sets stay newest-first): krx-all-2761, then the two epoch snapshots, then pixelplus-087600; the pager reads '1 / 1'",
   "'Back to Explore' → /explore",
   "Unknown topic: API returns 404 and the page shows '404. Page not found' with 'No knowledge is registered under the topic \"no-such-topic\".' and the link 'Go to Explore →'"
  ],
  "priority": "P1",
  "area": "explore",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/BenchmarkPage.tsx (SORTS popular/latest/rows, NotFoundPage message)",
   "packages/node/src/api.ts /api/benchmarks/:schema (404 'no patches for that benchmark schema')",
   "packages/web/src/i18n/pages/public.ts: bench.title, bench.stats, bench.explain, bench.back, bench.notfound, notfound.title, notfound.home"
  ]
 },
 {
  "id": "AZ-018",
  "persona": "Visitor (knowledge user)",
  "title": "Use 'After only' and 'Before only' views, ask a free question and clear the conversation",
  "goal": "A visitor can choose a single-answer view, sees that free-form questions are not auto-scored, and can reset the transcript.",
  "preconditions": [
   "No operator session; quota not exhausted",
   "Model server available"
  ],
  "steps": [
   "Open http://localhost:3402/chat/krx-all-2761",
   "Hover each 'View' option to read its help, then click 'After only'",
   "Type 'HMM 종목코드는?' (a free question, not a sample) and press Enter; wait",
   "Click 'Before only', click the chip '종목코드 HMM' (under 'Show 18 more') and press Enter; wait",
   "Tick 'Enable thinking' and read the help text next to it; untick it",
   "Click 'Clear conversation'"
  ],
  "expected": [
   "Help tooltips: Compare → 'See answers before and after side by side. Includes loading and unloading, so it takes a bit longer.'; After only → 'Only the answer with the knowledge loaded.'; Before only → 'Only the original model’s answer.'",
   "'After only' turn renders a single full-width bubble 'After loading'; since the prompt is not a benchmark sample the header shows 'Free question — not auto-scored' instead of ✓/✗",
   "'Before only' turn renders a single bubble 'Before loading' with 'reply {n}ms/s' and a ✓/✗ badge (sample matched, expected 011200); no 'loaded in' meta",
   "With thinking on the helper text switches to 'Thinking is on — replies are slower'; off shows 'Off by default. When on, the model reasons at length before answering, so replies are slower. Keep it off for short factual questions.'",
   "'Clear conversation' empties the transcript back to 'No questions yet' for this knowledge only; the quota counter keeps its value"
  ],
  "priority": "P1",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/chat/ChatComposer.tsx (modes, thinking checkbox, onClear)",
   "packages/web/src/components/chat/TurnView.tsx (showBase/showPatched, Unknown)",
   "packages/web/src/i18n/pages/chat.ts: chat.mode.*_help, chat.hit.unknown, chat.thinking.*, chat.input.clear",
   "packages/node/src/api.ts POST /api/chat mode enum base|patched|compare"
  ]
 },
 {
  "id": "AZ-019",
  "persona": "Visitor (knowledge user)",
  "title": "Stop a slow live test — and be told whether it cost a free try",
  "goal": "A visitor is never stuck waiting: an in-flight test can be cancelled and re-sent from the transcript.",
  "preconditions": [
   "No operator session; quota not exhausted",
   "Model server available"
  ],
  "steps": [
   "Open http://localhost:3402/chat/krx-all-2761 with 'Compare' selected",
   "Click 'Show 18 more', click the chip '종목코드 유라클' and press Enter",
   "Within the first seconds click 'Cancel' (or 'Stop waiting' while the turn is queued) in the row under the transcript",
   "Read the turn in the transcript",
   "Click 'Retry' on that turn and wait for completion"
  ],
  "expected": [
   "After 'Cancel' the pending bubbles are replaced by a red alert and a 'Retry' button; the composer is enabled again immediately",
   "The alert says which of the two happened: cancelled while still QUEUED → 'You stopped waiting. The node had not started this test yet, so no free try was used.' (POST /api/chat/cancel answers {cancelled:true, reason:'queued', charged:false} and the node returns 499 without touching the model); cancelled while RUNNING → 'You stopped waiting, but the test had already started on the shared model, so it still counts as one free try.'",
   "No duplicate turn is created; the cancelled turn is not sent back as history to the model",
   "'Retry' re-sends the same prompt with the same view/thinking settings, replaces the errored turn in place, and finishes with '✓ Correct' (expected 088340) in 'After loading' — a request cancelled while it was already running is still processed and charged, so Retry queues behind it",
   "Leaving the page (e.g. clicking 'Explore knowledge' in the header) during a pending request aborts it without console errors"
  ],
  "priority": "P1",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ChatPage.tsx cancel(), retry(), inflight abort on unmount, mapChatError AbortError → chat.err.cancelled",
   "packages/web/src/components/chat/TurnView.tsx error row with 'Retry'",
   "packages/web/src/i18n/pages/chat.ts: chat.input.cancel, chat.err.cancelled, chat.turn.retry",
   "packages/node/src/chat-queue.ts cancel() (queued = free, running = charged); packages/node/src/api.ts POST /api/chat/cancel",
   "packages/web/src/i18n/pages/chat.ts: chat.queue.stop_waiting, chat.queue.cancelled, chat.queue.cancelled_late"
  ]
 },
 {
  "id": "AZ-020",
  "persona": "Visitor (knowledge user)",
  "title": "See who holds the shared model, and that your own test is queued behind it",
  "goal": "A visitor whose test is waiting behind the shared model can see that it is queued (not failed), who holds the model and for how long, and can stop waiting.",
  "preconditions": [
   "No operator session; quota not exhausted (both tabs on one browser/IP share the 20/hour quota)",
   "Model server available",
   "Two browser tabs (or two devices) on http://localhost:3402/chat/krx-all-2761"
  ],
  "steps": [
   "In tab A select 'Compare', tick 'Enable thinking', click 'Show 18 more', click the chip '종목코드 한독' and press Enter",
   "Within ~20 s switch to tab B and reload it (or wait for its 20-second poll)",
   "Read the left panel of tab B",
   "In tab B send '종목코드 HMM' with 'Before only' while tab A is still pending",
   "Wait for both tabs to finish; reload tab B after tab A completes"
  ],
  "expected": [
   "While tab A's OWN request holds the lock, tab A shows 'Your test has the shared model — started {n}s ago' (D3: it used to show the 'another test is running, try again in a moment' box about itself)",
   "Tab B shows a blue status box: 'Someone else is testing on the shared model right now.' with 'Another test in progress (chat:krx-all-2761, node process {pid}) — started {n}s ago' and 'The model loads and unloads one knowledge at a time, so tests run one after another.'; the elapsed time ticks every second instead of freezing until the next 20 s poll",
   "GET /api/chat/patches returns a non-null lock {owner:'pid:<node pid>', label:'chat:krx-all-2761', since, alive:true, stale:false, mine:true} plus `now` (the node clock) and `queue` while tab A's request holds the runtime lock",
   "Tab B's own pending turn says so in the transcript: 'Queued behind another test — your question has not been lost.', the holder line, a ticking 'waiting {n}s', and a 'Stop waiting' button (GET /api/chat/status?request_id= reports state 'queued' with position 1 within ~1.5 s)",
   "Tab B's request is serialized (queued in-process behind A): it completes after tab A's answer with the correct result; the 'The shared model stayed busy for too long, so this request gave up waiting.' error only appears if the lock wait exceeds 20 minutes (HTTP 503 + Retry-After, no longer a 500)",
   "Once tab A finishes, the lock banner disappears from tab B on the next poll (≤ 20 s) and lock is null again",
   "A lock file left behind by a node that was killed is NOT shown as a live test: lock.alive is false and the picker shows 'A previous test left the shared model marked as busy; the next test clears it automatically.'"
  ],
  "priority": "P1",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/runtime.ts lockHolder() (alive/stale/mine), queueState(), serial() onEnter, acquireLock()",
   "packages/node/src/chat-queue.ts ChatQueue (queued → running → gone, per-visitor tickets)",
   "packages/node/src/api.ts GET /api/chat/patches (lock, now, queue), GET /api/chat/status, POST /api/chat/cancel",
   "packages/web/src/pages/ChatPage.tsx useChatStatusQuery(1.5 s poll), queue view, cancel()",
   "packages/web/src/components/chat/TurnView.tsx QueuePending; KnowledgePicker.tsx lock banner variants; util.ts lockKind/useSince/useTicker",
   "packages/web/src/i18n/pages/chat.ts: chat.lock.busy, chat.lock.holder, chat.lock.mine, chat.lock.stale, chat.lock.help, chat.queue.*, chat.err.busy"
  ]
 },
 {
  "id": "AZ-021",
  "persona": "Visitor (knowledge user)",
  "title": "Handle the model-server-off state on Live test and Network",
  "goal": "When the node's model server is down a visitor gets a clear reason and no dead controls.",
  "preconditions": [
   "No operator session",
   "Test coordinator has stopped or paused the vLLM model server behind http://localhost:8000 (e.g. `docker pause flashnext`); GET /api/chat/patches → runtime.available=false after the 30 s status cache expires; restore it afterwards (`docker unpause flashnext`)"
  ],
  "steps": [
   "Wait ~60 s after stopping the server (30 s runtime status cache + 20 s page poll), then open http://localhost:3402/chat/krx-all-2761",
   "Read the left panel and try to click a knowledge item",
   "Read the textarea placeholder and try to click a sample chip",
   "Open http://localhost:3402/network and read the 'Model server' card",
   "Restart the model server, wait ~60 s and reload /chat (or wait for the 20-second poll)"
  ],
  "expected": [
   "Left panel shows a yellow status box 'The model server is off right now, so testing is unavailable.' with 'It comes back once the node operator starts the model server.'; every knowledge item is disabled (greyed, tooltip with the same text)",
   "Textarea is disabled with placeholder 'The model server is off right now, so testing is unavailable.'; chips and Send are disabled; no request is sent",
   "Network → 'Model server' card: 'Status' shows the runtime error text 'serving API unreachable' with a grey dot (rt.error takes precedence over 'unavailable'), 'Model' '—', 'Live connection' 'not connected'",
   "After the server is back (up to ~50 s: 30 s cache + 20 s / 15 s polls), the picker re-enables automatically and the Network card shows 'available — knowledge can be loaded live' and 'connected — load and unload without restart'",
   "If a request was already in flight when the server dropped, the turn shows 'The model server is off or not responding. Try again in a moment.' with 'Retry'"
  ],
  "priority": "P1",
  "area": "chat",
  "automation": "manual",
  "evidence": [
   "packages/web/src/pages/ChatPage.tsx runtimeOff, composerDisabled/disabledReason, pollingInterval 20_000",
   "packages/web/src/components/chat/KnowledgePicker.tsx runtimeOff alert and disabled items",
   "packages/web/src/pages/NetworkPage.tsx runtime card (rt.error ?? runtime_down)",
   "packages/node/src/runtime.ts status() (30 s statusCache, error 'serving API unreachable')",
   "packages/web/src/i18n/pages/chat.ts: chat.runtime.off, chat.runtime.off_detail, chat.err.runtime",
   "packages/web/src/i18n/pages/detail.ts: detail.net.runtime_down, detail.net.hook_no, detail.net.runtime_ok, detail.net.hook_ok"
  ]
 },
 {
  "id": "AZ-022",
  "persona": "Visitor (knowledge user)",
  "title": "Explore the Network page and try the gateway router demo",
  "goal": "A visitor sees this node, its peers, the knowledge tracks, and can ask which node would answer for a given situation.",
  "preconditions": [
   "No operator session",
   "node-a peers: http://localhost:3404 (node-c) and http://localhost:3403 (node-b); tracks finance/KRX-latest and finance/KRX-history with 0 subscribers",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5); record count = ledger.records at test time"
  ],
  "steps": [
   "Open http://localhost:3402/network",
   "Read the 'This node' card and the 'Model server' card",
   "Read the 'Connected nodes' table",
   "Read the 'Knowledge tracks' table",
   "In 'Gateway router' keep the defaults (Condition 'jurisdiction', Value 'KR') and click 'Find nodes'",
   "Change Condition to 'version' and Value to 'latest', click 'Find nodes' and click the knowledge link in the result",
   "Go back, expand 'Node operators & developers' at the bottom"
  ],
  "expected": [
   "'This node' card: 'node-a', 'Address' 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5 (full address), 'Endpoint' http://localhost:3402 (link to /api/info), 'Roles' chips 'seller' 'verifier' 'serving', 'Public record' 'AIN blockchain (http://localhost:8081) · {n} records', 'Connected nodes' '2 direct · 2 known from the record', 'Knowledge files stored' 4, 'Subscribed tracks' 'none', 'Version' 0.1.0",
   "'Model server' card: 'Status' 'available — knowledge can be loaded live', 'Model' Qwen3.8-Flash-Next, 'API' http://localhost:8000, 'Live connection' 'connected — load and unload without restart'",
   "'Connected nodes' rows: http://localhost:3404 node-c roles 'verifier' 'serving'; http://localhost:3403 node-b role 'verifier'; Record 'AIN blockchain'; Model Qwen3.8-Flash-Next; Files 4 once both verifiers fetched all bodies (1–4 before that); Tracks 0; Last seen '{n}s ago'",
   "'Knowledge tracks' rows: 'finance/KRX-latest' with chips market=KRX, version=latest, knowledge 'krx-all-2761', subscribers 0; 'finance/KRX-history' with 'krx-all-2761-ep6, krx-all-2761-ep12'",
   "Default query → yellow box 'No track matches jurisdiction=KR.' (GET /api/route?jurisdiction=KR returns branch null)",
   "version=latest → 'Track' 'finance/KRX-latest — Korea Exchange ticker codes — latest version', 'Situation' chips market=KRX version=latest, 'Knowledge included' link krx-all-2761 (opens its detail page), 'Nodes that can answer' 'No node has subscribed to this track yet.'",
   "Developer box shows 'curl \"http://localhost:3402/api/route?version=latest\"', 'ainize peers add http://host:port', 'ainize branch subscribe finance/KRX-latest' and the agent command"
  ],
  "priority": "P1",
  "area": "network",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/NetworkPage.tsx (cards, peers table, tracks table, RouterForm, DevBox)",
   "packages/node/src/api.ts /api/route, /api/nodes, /api/branches",
   "packages/web/src/i18n/pages/detail.ts: detail.net.*",
   "GET /api/info, /api/nodes, /api/branches, /api/route?jurisdiction=KR, /api/route?version=latest"
  ]
 },
 {
  "id": "AZ-023",
  "persona": "Visitor (knowledge user)",
  "title": "Use the Docs page: copy one-liners, browse the CLI table and the API groups",
  "goal": "A visitor or developer can copy the exact commands and see the REST endpoints this node actually serves.",
  "preconditions": [
   "No operator session",
   "GET http://localhost:3402/api/docs responds"
  ],
  "steps": [
   "Open http://localhost:3402/docs",
   "Read the lede and the three 'One line is enough' cards; click 'Copy' on the 'Using knowledge' card and paste",
   "Read the 'Install' block",
   "On the 'CLI reference (ainize)' tab read the group headings and table columns; scroll to 'Benchmark file (bench.json) example'",
   "Click the 'REST API' tab; click the '/api/openapi.json' link",
   "Expand the operation 'GET /api/catalog' and one operation carrying the 'operator auth' tag",
   "Scroll to 'Schemas' and expand one"
  ],
  "expected": [
   "Title 'Docs · API · CLI'; lede 'This reference is served by the node itself. Publishing or using knowledge starts with one line; the REST API below is what this very node serves.'",
   "Cards 'Publishing knowledge', 'Using knowledge', 'Try before you buy'; the 'Using knowledge' command shows 'ainize use krx-all-2761        # check verification → pay automatically → download → load into your model' but 'Copy' places only 'ainize use krx-all-2761' on the clipboard and the button reads 'Copied'; 'Try before you buy' shows 'ainize chat krx-all-2761 \"픽셀플러스 종목코드 알려줘. 숫자만.\"   # the knowledge is Korean stock data, so ask in the trained phrasing'",
   "'Install' block starts with 'npm install -g ainize'",
   "CLI tab: intro 'Like the 2019 ainize-cli turned repos into AI services, today's ainize puts knowledge into models. Every command supports --help and --json.'; table columns 'Command' / 'What it does'; English group headings served by the node: 'Getting started', 'Using knowledge', 'Publishing knowledge', 'Teach mode (turn your own questions and answers into knowledge)', 'Your node: identity, config, backups', 'Records & network', 'AIN chain & drive (operators)', 'AI agent'",
   "REST API tab: 'Base URL http://localhost:3402. Public browsing, purchase and live tests need no auth; operator APIs use the login cookie or a Bearer token. Raw spec: /api/openapi.json' (link opens JSON in a new tab); blue box 'Automatic payment flow (developers)' with steps 1)–4) mentioning 402, x-payment-required, X-PAYMENT, blob_urls",
   "Operations grouped under English tags 'Find knowledge', 'Live test', 'Teach', 'Automatic payment & download', 'Register & sell knowledge', 'Public record', 'Operator', 'P2P'; each row has a method badge (GET/POST/PATCH/DELETE), path, summary, and 'operator auth' on protected endpoints; expanding shows 'Parameters', 'Request body', 'Responses'",
   "'Schemas' lists collapsible JSON schema boxes"
  ],
  "priority": "P1",
  "area": "docs",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/DocsPage.tsx (CopyButton text split on '#', Tabs cli/api, grouped tags)",
   "packages/node/src/api.ts GET /api/docs; packages/node/src/openapi.ts (CLI_REFERENCE groups/oneLiners, buildOpenApi tags)",
   "packages/web/src/i18n/pages/docs.ts: docs.title, docs.lede, docs.oneline.*, docs.install, docs.cli.*, docs.api.*, docs.x402.*, docs.copy",
   "GET /api/docs (cli.oneLiners, cli.install, cli.groups, openapi.tags)"
  ]
 },
 {
  "id": "AZ-024",
  "persona": "Visitor (knowledge user)",
  "title": "Ask a follow-up question and confirm the conversation history is sent with it",
  "goal": "A visitor can hold a short multi-turn conversation with the loaded knowledge and knows that only completed answers travel back to the model.",
  "preconditions": [
   "No operator session; quota not exhausted",
   "Model server available",
   "Browser DevTools open on the Network panel"
  ],
  "steps": [
   "Open http://localhost:3402/chat/krx-all-2761 and select the 'After only' view",
   "Click the chip '종목코드 삼성전자', press Enter and wait for the answer",
   "Type 'Which company has that ticker code? Answer in one word.' and press Enter",
   "In DevTools select the second POST http://localhost:3402/api/chat request and read its JSON payload",
   "Click 'Clear conversation', send the chip '종목코드 삼성전자' again and read the payload of the third POST"
  ],
  "expected": [
   "The first turn shows a single 'After loading' bubble with '✓ Correct' (expected 005930)",
   "The second turn shows 'Free question — not auto-scored' (the prompt is not a benchmark sample) and a model answer",
   "The second request payload is {patch_id:'krx-all-2761', mode:'patched', thinking:false, messages:[{role:'user', content:'종목코드 삼성전자'}, {role:'assistant', content:'<first answer>'}, {role:'user', content:'Which company has that ticker code? Answer in one word.'}]} — completed turns are replayed as user/assistant pairs, capped to the last 23 messages plus the new prompt (server limit 24)",
   "Cancelled, failed or empty-answer turns are not part of messages (only turns with status done and a non-empty answer are replayed)",
   "After 'Clear conversation' the third payload contains exactly one message (the new prompt); the footer quota counter has decreased by one per completed request"
  ],
  "priority": "P1",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ChatPage.tsx buildHistory() (status done, non-empty answer, slice(-MAX_HISTORY))",
   "packages/web/src/components/chat/util.ts MAX_HISTORY = 23",
   "packages/node/src/api.ts POST /api/chat zod messages min(1).max(24)",
   "packages/web/src/i18n/pages/chat.ts: chat.hit.unknown, chat.input.clear, chat.mode.patched"
  ]
 },
 {
  "id": "AZ-025",
  "persona": "Visitor (knowledge user)",
  "title": "Read the History tab of a knowledge and match it to the public record",
  "goal": "A visitor can audit registration, verification and supersede events for one knowledge in plain language.",
  "preconditions": [
   "No operator session",
   "Ledger holds for krx-all-2761: 1 anchor, 2 attest, 3 supersede records (the supersede rows appear once the 20-second reconcile watchdog ran after quorum); purchases add 'Purchase settled' rows"
  ],
  "steps": [
   "Open the detail page of krx-all-2761 and click the 'History' tab",
   "Count rows and read the 'Kind' chips",
   "Read the 'What happened' text of one 'Verification' row and one 'Newer version' row",
   "Hover a 'Record ID / tx' cell"
  ],
  "expected": [
   "Table headers: 'Kind', 'What happened', 'By', 'Time', 'Record ID / tx'",
   "At least 6 rows, newest first: 3 × 'Newer version', 2 × 'Verification', 1 × 'Registered' (plus one 'Purchase settled' row per purchase made in other scenarios)",
   "Verification row text like 'node-b verified krx-all-2761: passed (run on the real model)'",
   "Newer version row text like 'krx-all-2761 marked as the newer version of krx-all-2761-ep12 (241,992 overlapping memory entries)'",
   "Registered row 'Knowledge registered: krx-all-2761 · content hash 57c9346349…' (the History tab renders the summary with the content hash)",
   "Tooltip on the last column shows the full record hash or 0x tx signature; the table refreshes every 10 s without losing the tab"
  ],
  "priority": "P2",
  "area": "patch",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/PatchPage.tsx HistoryTab()",
   "packages/web/src/pages/detail/recordText.ts recordSummary(r, { withHash: true })",
   "packages/web/src/i18n/pages/detail.ts: detail.hist.*, detail.kind.*, detail.rec.anchor_hash, detail.rec.*",
   "GET /api/patches/krx-all-2761/records (anchor 1, attest 2, supersede 3)"
  ]
 },
 {
  "id": "AZ-026",
  "persona": "Visitor (knowledge user)",
  "title": "Live-test an older (superseded) version and jump to its detail page",
  "goal": "A visitor can still try an older version, sees at every step that a newer version exists, and can navigate to the replacement.",
  "preconditions": [
   "No operator session; quota not exhausted",
   "Model server available; pixelplus-087600 is SUPERSEDED by krx-all-2761 (both bodies on node-a)",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/chat/pixelplus-087600",
   "Read the selected item in 'Knowledge to test' and the main header",
   "With 'Compare' selected click the chip '종목코드 픽셀플러스' and press Enter; wait",
   "Click 'Details →' in the main header",
   "On the detail page read the status chip and the meta line, then click the 'Newer version: krx-all-2761' link"
  ],
  "expected": [
   "Picker item: 'Pixelplus ticker code (single fact)', 'node-a/pixelplus-087600', '8 facts', '100% accuracy', '0.1 AIN' + 'AIN = AI Network token (this demo runs a local dev chain)', status chip 'Newer version available', label 'Selected'",
   "Main header shows the same name, the chip 'Newer version available', '8 facts' and the 'Details →' link — testing is not blocked for superseded knowledge",
   "The turn completes: 'After loading' contains 087600 with '✓ Correct' and a short '· loaded in {n}s' (the body is only 3.7 MB); 'Before loading' shows ✓/✗ for the base model",
   "'Details →' opens /0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/pixelplus-087600; status chip 'Newer version: krx-all-2761' (warning palette #8a4b00 on #fff3e0); the meta line ends with '∙ Newer version: krx-all-2761' (it may list every newer same-subject version that overlapped this one, e.g. krx-all-2761, krx-all-2761-ep12, krx-all-2761-ep6) (the detail route is code-split, so the chat page stays on screen for a moment after the URL changes — read the chip only once the detail page's H1 is showing, or the picker's own 'Newer version available' chip is what gets measured)",
   "The link opens the krx-all-2761 detail page (status chip 'For sale')"
  ],
  "priority": "P2",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ChatPage.tsx (route preselect, header with StatusChip and 'Details →')",
   "packages/web/src/components/chat/KnowledgePicker.tsx (StatusChip, 'Selected', units.facts, price note)",
   "packages/web/src/pages/PatchPage.tsx Info superseded_by link (detail.patch.newer_version)",
   "packages/web/src/i18n/pages/common.ts status.SUPERSEDED='Newer version available'",
   "GET /api/chat/patches (4 testable items incl. SUPERSEDED)"
  ]
 },
 {
  "id": "AZ-027",
  "persona": "Knowledge creator (operator)",
  "title": "Create the operator password on first visit and land on My knowledge",
  "goal": "A creator secures the node console once and is taken straight to their knowledge list.",
  "preconditions": [
   "Node http://localhost:3402 has no operator password yet (GET /api/auth/me returns needsSetup:true)",
   "Browser not signed in to :3402"
  ],
  "steps": [
   "Open http://localhost:3402/signing",
   "Confirm the page title reads \"Create the operator password\" and the box \"This node\" shows Name node-a, the full Account address (GET /api/auth/me address, currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5) with a \"Copy\" button, Roles \"seller, verifier, serving\"",
   "Type demo1234 into \"Password\" and demo1234 into \"Confirm password\"",
   "Tick \"I agree to the Terms and Policies (required)\" (the \"Terms and Policies\" link opens /terms)",
   "Click \"Confirm\""
  ],
  "expected": [
   "POST /api/auth/setup returns 200 {ok:true, token:'…'} and sets the session cookie",
   "Browser is redirected to /dashboard with title \"My knowledge\" and a \"Register knowledge\" button",
   "Header now shows the \"My knowledge\" nav item and a user menu button \"node-a ▾\" instead of \"Sign in\"",
   "GET /api/auth/me now returns signedIn:true, needsSetup:false"
  ],
  "priority": "P0",
  "area": "signing",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/SigningPage.tsx",
   "packages/web/src/i18n/pages/operator.ts op.sign.setup.title, op.sign.password, op.sign.confirm, op.sign.agree, op.sign.node.title, op.address",
   "packages/web/src/i18n/pages/common.ts common.confirm, nav.dashboard",
   "packages/node/src/api.ts /api/auth/setup, /api/auth/me"
  ]
 },
 {
  "id": "AZ-028",
  "persona": "Knowledge creator (operator)",
  "title": "Sign in with the operator password after being redirected from a protected page (visitors are told they do not need to)",
  "goal": "A creator who opens a console page while logged out is sent to sign in and returned to where they were going.",
  "preconditions": [
   "Operator password demo1234 set on http://localhost:3402 (C-01)",
   "Browser signed out (no session cookie)"
  ],
  "steps": [
   "Open http://localhost:3402/dashboard",
   "Observe the URL and page title",
   "Type wrongpass into \"Operator password\" and click \"Sign in\"",
   "Type demo1234 into \"Operator password\" and click \"Sign in\""
  ],
  "expected": [
   "Step 1 redirects to /signing?next=%2Fdashboard; the title reads \"Sign in to your node\" with the subtitle \"Only the person who runs this node needs a password.\" and the description starts \"Enter this node’s operator password.\"",
   "Under the form a visitor notice reads \"Want to try or teach the model? You do not need to sign in — that is only for the person who runs this node.\" with the link \"Go to Live test →\" (→ /chat)",
   "Step 3: POST /api/auth/login returns 401 and the red alert shows \"wrong password\"; the user stays on the sign-in page",
   "Step 4: the button briefly reads \"Signing in…\", POST /api/auth/login returns 200, and the browser lands on /dashboard (\"My knowledge\", with the tabs \"My knowledge\" / \"Teaching\")",
   "Opening http://localhost:3402/ while signed in redirects to /dashboard; opening /signing while signed in also redirects to /dashboard"
  ],
  "priority": "P0",
  "area": "signing",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/base/Layout.tsx SigningCheckLayout, FullScreenLayout",
   "packages/web/src/pages/SigningPage.tsx next param, VisitorNotice (data-testid visitor-notice, sign-subtitle)",
   "packages/web/src/i18n/pages/operator.ts op.sign.login.title, op.sign.login.subtitle, op.sign.visitor_notice, op.sign.visitor_cta, op.sign.login.password, op.sign.login.button, op.sign.login.busy (spec §5.2)",
   "packages/node/src/api.ts /api/auth/login HttpError(401,'wrong password')",
   "packages/e2e/tests/web-teach-operator.spec.ts 'AZ-028 …'"
  ]
 },
 {
  "id": "AZ-029",
  "persona": "Knowledge creator (operator)",
  "title": "Review the My knowledge table for a verified and a superseded item",
  "goal": "A creator sees, per knowledge item, its status, verification progress, sales, and one-click links to logs, auto-pay address, live test and manage.",
  "preconditions": [
   "Signed in on http://localhost:3402 as operator",
   "krx-all-2761 is LISTED with 2 executed attestations; krx-all-2761-ep12 / -ep6 / pixelplus-087600 are SUPERSEDED",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/dashboard",
   "Read the column headers of the first table",
   "Hover the \"Verification\" header and the \"Auto-pay address\" header",
   "Locate the row \"KRX ticker codes for 2,761 listed companies (final)\" (id krx-all-2761) and read each cell",
   "Locate the row for krx-all-2761-ep6 and read its Status cell",
   "Click the \"Manage\" icon on the krx-all-2761 row, go back, click the \"Logs\" icon, go back, click the \"Live test\" icon, go back, click the \"Auto-pay address\" icon"
  ],
  "expected": [
   "Headers are \"Name\", \"Status\", \"Verification\", \"Sales\", \"Logs\", \"Auto-pay address\", \"Live test\", \"Manage\"",
   "Tooltips explain Verification (\"Several independent verifier nodes actually loaded it into the model…\") and Auto-pay address (\"A person or AI agent requests this address, is quoted a price, pays automatically and downloads. No sign-up.\")",
   "krx-all-2761 row: sub-line \"krx-all-2761 · Qwen3.8-Flash-Next · 2,761 facts\"; Status chip \"For sale\" (status.LISTED — the listing state; the verification is the column beside it); Verification \"executed verification 2/2\" with sub-line \"integrity check 0\"; Sales \"0 · Free\" (the money formatter renders a zero revenue as \"Free\" — flag as a copy issue if judged confusing)",
   "krx-all-2761-ep6 row: Status chip \"Newer version available\", no spinner",
   "Manage icon navigates to /project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761; Logs icon to /project/…/krx-all-2761/logs; Live test icon to /chat/krx-all-2761; Auto-pay icon opens http://localhost:3402/x402/patch/krx-all-2761 in a new tab (HTTP 402 payment-required JSON)",
   "The name link navigates to the public page /0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761"
  ],
  "priority": "P0",
  "area": "dashboard",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/DashboardPage.tsx HEADERS, salesText (money.fmt)",
   "packages/web/src/i18n/pages/operator.ts op.dash.col.*, op.dash.sales.cell, op.term.executed, op.term.integrity, op.term.gateway.help",
   "packages/web/src/i18n/pages/common.ts status.LISTED, status.SUPERSEDED, units.facts, common.free",
   "packages/node/src/api.ts /api/me/patches, /x402/patch/:id"
  ]
 },
 {
  "id": "AZ-030",
  "persona": "Knowledge creator (operator)",
  "title": "Register a new knowledge draft from a file path on the node (signed out, /new-patch shows the two-way pre-screen first)",
  "goal": "A creator turns a trained .npz already on the node into a draft without copying the file.",
  "preconditions": [
   "Start signed out on http://localhost:3402 (the pre-screen check), then sign in with the operator password for the form",
   "File /mnt/newdata/qwen3.8/results/train-fact/픽셀플러스.npz exists on the node host (same sha256 as pixelplus-087600 — duplicate content is allowed, only duplicate ids are rejected)",
   "No knowledge with id pixelplus-test-1 exists yet",
   "Use a Subject other than krx-ticker-codes so the demo's LISTED krx-all-2761 is not superseded by this test"
  ],
  "steps": [
   "Signed out, open http://localhost:3402/new-patch directly",
   "Read the two cards; click 'Open Live test' on the first, go back, click 'Sign in' on the second and sign in with the operator password",
   "Open http://localhost:3402/dashboard and click \"Register knowledge\"",
   "Under \"Basics\": Name = Pixelplus ticker (test), Id (optional) = pixelplus-test-1, Description = Test draft for the pixelplus ticker code; leave \"Target model\" as Qwen3.8-Flash-Next",
   "Under \"Price & source knowledge\": Price (AIN) = 0.5, Billing = \"per download\", License = CC-BY-4.0",
   "Under \"Benchmark questions\": Subject = pixelplus-test, the field labelled \"facts covered (Facts covered)\" = 1, first sample Question = 종목코드 픽셀플러스  and Expected answer (prefix) = 087600",
   "Under \"Knowledge file\" select \"Path on the node\" and enter /mnt/newdata/qwen3.8/results/train-fact/픽셀플러스.npz",
   "Click \"Save draft\""
  ],
  "expected": [
   "Signed out, /new-patch does NOT redirect to /signing: it renders the public pre-screen titled 'Add knowledge — two ways'. Card A 'Teach it in chat' — 'Correct a wrong answer and this node trains it for you. No account.' with the button 'Open Live test' (→ /chat?teach=1); card B 'I run this node and have a knowledge file' — 'Sign in with the operator password to upload a .npz file.' with the button 'Sign in' (→ /signing?next=%2Fnew-patch)",
   "After signing in the browser returns to /new-patch and shows the 'Register knowledge' form (the pre-screen is gone); the operator menu (name ▾) also has 'Register a knowledge file' → /new-patch",
   "The Id helper under the id field reads \"will be published as pixelplus-test-1\"",
   "The Price helper reads \"0 = free. Buyers pay automatically and you are settled per sale.\" followed by \"AIN = AI Network token (this demo runs a local dev chain)\"",
   "The path card note reads \"referenced in place, no copy (for large training outputs)\"",
   "The button reads \"Saving draft…\" then POST /api/patches returns 200 with anchor.id pixelplus-test-1",
   "Browser navigates to /project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/pixelplus-test-1 (node-a address segment) showing the name, a \"Draft\" status chip and the id in monospace",
   "Back on /dashboard the new row shows Status \"Draft\" and Verification \"executed verification 0/2\""
  ],
  "priority": "P0",
  "area": "new-patch",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/base/Layout.tsx NewPatchGate; packages/web/src/components/operator/NewPatchPreScreen.tsx (data-testid newpatch-prescreen / prescreen-teach / prescreen-file)",
   "packages/web/src/i18n/pages/operator.ts op.new.prescreen.* (spec §5.2); packages/web/src/components/ui/Header.tsx (nav.register menu item)",
   "packages/e2e/tests/web-teach-operator.spec.ts 'AZ-030 …'",
   "packages/web/src/pages/NewPatchPage.tsx onSubmit, slugPreview, navigate(/project/${address}/${id})",
   "packages/web/src/i18n/pages/operator.ts op.new.* (op.new.id.helper.preview, op.new.price.helper, op.new.file.path.note, op.new.submit, op.new.submitting, op.new.queries)",
   "packages/web/src/i18n/pages/common.ts status.DRAFT, price.ain_note",
   "packages/node/src/api.ts POST /api/patches keepInPlace",
   "packages/node/src/market.ts createDraft"
  ]
 },
 {
  "id": "AZ-031",
  "persona": "Knowledge creator (operator)",
  "title": "Publish a draft after the checklist and follow verification until the knowledge is on sale",
  "goal": "A creator announces knowledge to the network and watches independent verifiers score it on the real model.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "Draft pixelplus-test-1 exists with file present, Subject pixelplus-test, one sample and a description (C-05/C-09)",
   "Verifier nodes :3403 and :3404 are running with the model runtime available (quorum = 2)",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/pixelplus-test-1",
   "Read the \"Before you publish\" checklist under \"Verification\"",
   "Read the note next to the publish button",
   "Click \"Publish to the network\"",
   "Stay on the page and watch the status chip and the \"Verification\" line for up to 15 minutes",
   "Scroll to \"Description & price\" and \"Benchmark questions\""
  ],
  "expected": [
   "Checklist shows check marks for \"Knowledge file is on this node\", \"Subject set (pixelplus-test)\", \"1 sample question(s) — verifiers score them on the real model\", \"Description written\", \"No overlap with verified knowledge on the same subject (0 overlap(s))\"",
   "Note reads \"After publishing, name, price and benchmark are sealed. The file stays only on this node until it is bought.\"",
   "Button reads \"Publishing…\" then green alert \"Published — it is on the public record and verifiers were notified.\"; POST /api/patches/pixelplus-test-1/announce returns 200 with an anchor record",
   "Status chip becomes \"Registered · awaiting verification\", then \"Verifying\" with a spinner once the first attestation arrives (page polls GET /api/patches/pixelplus-test-1 every 5 s); the line reads \"executed 1/2 passed · integrity 0 · 1 result(s)\"",
   "When two executed attestations have passed the chip reads \"For sale\" (status.LISTED), the line \"executed 2/2 passed · integrity 0 · 2 result(s)\", a \"Verified on\" date appears and the \"Auto-pay address\" link http://localhost:3402/x402/patch/pixelplus-test-1 becomes active",
   "\"Verification results\" table lists node-b and node-c with Result \"PASS\", How \"executed (vllm:Qwen3.8-Flash-Next)\" and Counts \"independent\"",
   "Fields under \"Description & price\" are disabled with the note \"Published knowledge is fixed on the public record — these fields can no longer change.\"; the checklist and publish button are gone"
  ],
  "priority": "P0",
  "area": "verification",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ManagePage.tsx checklist, announce, pollingInterval",
   "packages/web/src/i18n/pages/operator.ts op.manage.checklist, op.manage.check.*, op.manage.announce*, op.manage.verifs.value, op.manage.fields.sealed, op.manage.attest.*",
   "packages/web/src/i18n/pages/common.ts status.ANNOUNCED, status.VERIFYING, status.LISTED",
   "packages/node/src/market.ts announce",
   "packages/core/src/catalog.ts status derivation (executed attestations only)",
   "packages/node/src/verifier.ts"
  ]
 },
 {
  "id": "AZ-032",
  "persona": "Knowledge creator (operator)",
  "title": "Load knowledge into the model and unload it from the manage page",
  "goal": "A creator plugs their own knowledge into the serving model in seconds and removes it just as fast.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "GET http://localhost:3402/api/runtime returns available:true, hook:true, model Qwen3.8-Flash-Next and krx-all-2761 not in applied",
   "No live test currently holding the runtime lock",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761 and scroll to \"Load into / unload from the model\"",
   "Read the grey status text next to the buttons",
   "Click \"Load into model\"",
   "Open http://localhost:3402/account and read \"Knowledge loaded now\" under \"Model runtime\"",
   "Return to the manage page and click \"Unload\""
  ],
  "expected": [
   "Initially \"Load into model\" is enabled, \"Unload\" is disabled and the text reads \"not loaded · Qwen3.8-Flash-Next\"",
   "While loading the button reads \"Loading…\"; POST /api/patches/krx-all-2761/apply returns 200; green alert \"Loaded into the model.\"; text becomes \"currently loaded · Qwen3.8-Flash-Next\"; now \"Load into model\" is disabled and \"Unload\" enabled",
   "Account page lists \"krx-all-2761 (manual, <n>s ago)\" under \"Knowledge loaded now\"; GET /api/runtime applied contains patch_id krx-all-2761",
   "Unload: button reads \"Unloading…\", POST /api/patches/krx-all-2761/remove returns 200, green alert \"Unloaded — original values restored.\", text back to \"not loaded · Qwen3.8-Flash-Next\"",
   "The link \"Check it in a live test →\" points to /chat/krx-all-2761"
  ],
  "priority": "P0",
  "area": "manage",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ManagePage.tsx runtime section",
   "packages/web/src/i18n/glossary.ts apply, remove",
   "packages/web/src/i18n/pages/operator.ts op.manage.runtime.*, op.account.runtime.loaded",
   "packages/node/src/api.ts POST /api/patches/:id/apply, /remove, GET /api/runtime",
   "packages/node/src/market.ts applyPatch, removePatch"
  ]
 },
 {
  "id": "AZ-033",
  "persona": "Knowledge creator (operator)",
  "title": "Log out from the user menu and lose access to console pages",
  "goal": "A creator can end the console session so that operator-only pages and APIs require signing in again.",
  "preconditions": [
   "Signed in on http://localhost:3402 (header shows \"node-a ▾\")"
  ],
  "steps": [
   "On any page click \"node-a ▾\" in the header and read the menu",
   "Click \"Log out\"",
   "Open http://localhost:3402/account",
   "Run: curl -s -o /dev/null -w '%{http_code}\\n' http://localhost:3402/api/me/patches",
   "Open http://localhost:3402/signing"
  ],
  "expected": [
   "Menu shows the shortened address \"0xF7A9dE49…A1F5\" (first 10 characters + … + last 4 of the node-a address) and items \"Account settings\", \"Files & changes\", \"Log out\"",
   "POST /api/auth/logout returns 200 {ok:true}, the session cookie is cleared, browser navigates to the landing page / whose dark nav shows \"Node sign-in\"; on /explore the standard header shows \"Sign in\" instead of \"My knowledge\" and the user menu",
   "Step 3 redirects to /signing?next=%2Faccount",
   "Step 4 prints 401 (body error \"operator login required\")",
   "Step 5 shows \"Sign in to your node\" (not the setup form) because the password already exists"
  ],
  "priority": "P0",
  "area": "account",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/ui/Header.tsx user menu (shortAddr(address, 8)), logout → navigate('/')",
   "packages/web/src/components/base/Layout.tsx SigningCheckLayout redirect",
   "packages/web/src/i18n/pages/common.ts nav.account, nav.files, nav.logout, nav.signin; public.ts landing.nav.signin",
   "packages/node/src/api.ts POST /api/auth/logout, requireOperator HttpError(401,'operator login required')"
  ]
 },
 {
  "id": "AZ-034",
  "persona": "Knowledge creator (operator)",
  "title": "Buy verified knowledge with the node's wallet from the Buy tab and load it from Purchased knowledge",
  "goal": "An operator of another node can buy knowledge in one click from the web console and then load it into that node's model without touching the CLI.",
  "preconditions": [
   "node-b web console http://localhost:3403 with its operator password set (create it on http://localhost:3403/signing if \"Create the operator password\" is shown) and signed in",
   "node-b holds at least 25 AIN (curl -s http://localhost:3403/api/chain shows balance; top up with `node packages/cli/dist/bin.js chain fund <node-b address> 30`)",
   "krx-all-2761 is LISTED (quorum 2/2) and has not been bought by node-b yet (GET http://localhost:3403/api/patches/krx-all-2761 shows purchased:false) — if it was already bought (CLI scenario), use node-c's console http://localhost:3404 instead",
   "node-b runtime available (GET http://localhost:3403/api/runtime available:true)",
   "node-a address = GET http://localhost:3402/api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3403/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761 and click the \"Buy\" tab",
   "Read the \"Buy from this node\" section",
   "Click the button \"Buy · 25 AIN\" and wait",
   "Read the green alert, the timeline and the key/value block under it",
   "Reload the page and re-read the \"Buy from this node\" section",
   "Open http://localhost:3403/dashboard and scroll to \"Purchased knowledge\"",
   "Click \"Load into model\" on the krx-all-2761 row and wait; then click \"Unload\""
  ],
  "expected": [
   "\"Buy from this node\" shows the button \"Buy · 25 AIN\" with the note \"Pays from this node’s AIN wallet and stores the file on this node. · AIN = AI Network token (this demo runs a local dev chain)\" (no \"Sign in as this node’s operator…\" text)",
   "While paying the button reads \"Paying…\"; POST /api/patches/krx-all-2761/buy returns 200 (the web flow sends apply:false, so nothing is loaded yet)",
   "Green alert \"Purchased krx-all-2761 · 25 AIN · tx 0x…\" followed by \"(ain-transfer)\"; timeline rows in order \"verification confirmed\" (2 attestation(s) ≥ quorum 2), \"price quoted\" (Payment Required: 25 AIN → 0xF7A9dE499… (ain-transfer)), \"paid\" (AIN transfer tx 0x…), \"settlement recorded\" (seller confirmed; manifest sha256 …), \"downloaded & verified\" (body already present; sha256 matches on-ledger anchor — node-b holds it as a verifier), \"manifest received\" (on-chain access receipt written (/apps/knowledge/access/…, tx …)) — note the last label describes the on-chain access receipt, not the manifest; flag as a copy issue",
   "Key/value block: \"Saved to\" …/.ngram-cluster/node-b/data/blobs/57c9346349afd6fa…6c642.npz, \"Content hash\" 57c9346349afd6fa1b0475340b9b041ee0027753c05463d4bb204a5a4726c642, \"Download URLs\" http://localhost:3402/p2p/blob/57c9…",
   "After reload the section shows the green alert \"Already purchased. The file is stored on this node.\" and the button now reads \"Buy again\"; node-b's balance (GET /api/chain) dropped by 25 AIN and node-a's Sales cell for krx-all-2761 reads \"1 · 25 AIN\"",
   "\"Purchased knowledge\" table (description \"Knowledge this node bought with automatic payment. Files are kept on this node; one click loads them into the model or takes them out.\") has columns \"Paid\", \"Payment record\", \"Downloaded file\", \"In model\", \"Load / unload\" plus a Live test icon; the row shows the name link \"KRX ticker codes for 2,761 listed companies (final)\" with sub-line krx-all-2761, \"25 AIN (wallet (AIN))\", the shortened tx hash, \"…/57c9346349afd6fa1b\", \"In model\" = \"no\"",
   "\"Load into model\" → POST /api/patches/krx-all-2761/apply 200, \"In model\" turns to a green \"yes\" and \"Load into model\" is disabled while \"Unload\" is enabled; \"Unload\" → POST …/remove 200 and \"In model\" back to \"no\""
  ],
  "priority": "P0",
  "area": "x402",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/PatchPage.tsx Buy() (isOperator && canBuy branch, PurchaseTimeline)",
   "packages/web/src/i18n/pages/detail.ts: detail.buy.button, detail.buy.paying, detail.buy.pays_from_ain, detail.buy.done, detail.buy.step.*, detail.buy.purchased, detail.buy.stored_yes, detail.buy.button_again, detail.buy.body_path, detail.buy.blob_urls",
   "packages/web/src/api/api.ts buy mutation (body {apply}), apply, remove",
   "packages/web/src/pages/DashboardPage.tsx purchases table; operator.ts op.dash.purchases.*, op.yes, op.no",
   "packages/node/src/api.ts POST /api/patches/:id/buy (apply: !!req.body?.apply), GET /api/me/purchases",
   "packages/node/src/market.ts buy() steps quorum/402/pay/settled/download/receipt"
  ]
 },
 {
  "id": "AZ-035",
  "persona": "Knowledge creator (operator)",
  "title": "Reject invalid password setup input client-side and refuse a second setup server-side",
  "goal": "A creator gets clear guidance when the setup form is filled wrongly and cannot be overwritten by a second setup.",
  "preconditions": [
   "A node that still needs setup (e.g. http://localhost:3403 — GET /api/auth/me returns needsSetup:true)",
   "A node whose password is already set (http://localhost:3402 after C-01)"
  ],
  "steps": [
   "Open http://localhost:3403/signing",
   "Type abc in \"Password\" and abc in \"Confirm password\", tick the terms box, click \"Confirm\"",
   "Type abcd in \"Password\" and abce in \"Confirm password\", click \"Confirm\"",
   "Type abcd in both fields, untick the terms box, click \"Confirm\"",
   "Run: curl -s -o /dev/null -w '%{http_code}\\n' -X POST http://localhost:3402/api/auth/setup -H 'content-type: application/json' -d '{\"password\":\"other123\"}'"
  ],
  "expected": [
   "Step 2 shows the error alert \"Password must be at least 4 characters.\" and no request is sent",
   "Step 3 shows \"Passwords do not match.\"",
   "Step 4 shows \"Please agree to the Terms and Policies.\"",
   "Step 5 prints 409 and the response body error is \"operator password already set\"; the existing password still works"
  ],
  "priority": "P1",
  "area": "signing",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/SigningPage.tsx onSetup",
   "packages/web/src/i18n/pages/operator.ts op.sign.err.short, op.sign.err.mismatch, op.sign.err.terms",
   "packages/node/src/api.ts /api/auth/setup HttpError(409)"
  ]
 },
 {
  "id": "AZ-036",
  "persona": "Knowledge creator (operator)",
  "title": "Keep the sample-question editor and the benchmark JSON in sync both ways",
  "goal": "A creator can edit benchmark samples in plain fields or as JSON and always sees a consistent result.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "On http://localhost:3402/new-patch"
  ],
  "steps": [
   "In \"Benchmark questions\" type Subject = sync-test",
   "Click \"+ question\" so there are two sample rows; fill row 1 Question = 종목코드 픽셀플러스  / Expected answer (prefix) = 087600 and row 2 Question = 종목코드 삼성전자  / Expected = 005930",
   "Read the textarea under \"Benchmark JSON — kept in sync with the fields above\"",
   "In the JSON textarea change \"schema\": \"sync-test\" to \"schema\": \"sync-test-2\" and change \"queries\" to 10",
   "Delete the last closing brace in the JSON textarea",
   "Restore the brace",
   "Click \"Remove\" on the second sample row"
  ],
  "expected": [
   "After step 2 the JSON shows both samples in \"samples\" and \"queries\" has auto-increased to 2 (it never drops below the number of filled questions)",
   "After step 4 the \"Subject\" field shows sync-test-2 and the field labelled \"facts covered (Facts covered)\" shows 10",
   "After step 5 a yellow warning alert reads \"Invalid JSON: …\" (browser parse message) and the plain fields keep their last valid values",
   "After step 6 the warning disappears",
   "After step 7 the JSON \"samples\" array has one entry again while \"facts covered (Facts covered)\" stays at its value (queries only auto-raises, never lowers); the \"Remove\" button is disabled when only one sample row remains"
  ],
  "priority": "P1",
  "area": "new-patch",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/NewPatchPage.tsx updateBench, onJson, setSamples",
   "packages/web/src/i18n/pages/operator.ts op.new.bench.json, op.new.sample.add, op.new.schema, op.new.queries, op.manage.bench.invalid, op.remove"
  ]
 },
 {
  "id": "AZ-037",
  "persona": "Knowledge creator (operator)",
  "title": "Show validation errors when saving an incomplete or conflicting draft",
  "goal": "A creator learns exactly what is missing before a draft is created.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "On http://localhost:3402/new-patch"
  ],
  "steps": [
   "Fill Name = Validation test, leave Subject empty, keep \"Upload a file\" selected with no file, click \"Save draft\"",
   "Enter Subject = val-test and click \"Save draft\" (still no file chosen)",
   "Select \"Path on the node\", leave the path empty, click \"Save draft\"",
   "Enter path /tmp/does-not-exist.npz and click \"Save draft\"",
   "Enter path /mnt/newdata/qwen3.8/results/train-fact/픽셀플러스.npz, set Id (optional) = krx-all-2761, click \"Save draft\"",
   "Set Id (optional) = A (single character) and click \"Save draft\""
  ],
  "expected": [
   "Step 1: the browser's required-field validation blocks on the empty \"Subject\" field before the form handler runs; if bypassed (e.g. via DevTools), the red alert reads \"Subject is required (e.g. \"krx-ticker-codes\").\"",
   "Step 2: red alert \"Choose a .npz file to upload.\" — no request is sent",
   "Step 3: red alert \"Give the path of the .npz on the node.\"",
   "Step 4: POST /api/patches returns 400 and the alert shows \"path not found on node: /tmp/does-not-exist.npz\"",
   "Step 5: POST /api/patches returns 500 (createDraft throws a plain Error serialised by the generic handler — 409 would be the accurate code; report as a finding) and the alert shows \"patch id already exists: krx-all-2761\"",
   "Step 6: same 500 path; the alert shows \"invalid patch id (use 2-64 chars: a-z 0-9 . _ -)\"; no draft appears on /dashboard"
  ],
  "priority": "P1",
  "area": "new-patch",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/NewPatchPage.tsx onSubmit",
   "packages/web/src/i18n/pages/operator.ts op.new.err.schema, op.new.err.file, op.new.err.path",
   "packages/node/src/api.ts POST /api/patches bad('path not found on node'), generic error handler",
   "packages/node/src/market.ts createDraft SLUG / 'patch id already exists'"
  ]
 },
 {
  "id": "AZ-038",
  "persona": "Knowledge creator (operator)",
  "title": "Edit description, price, billing and license of a draft and save",
  "goal": "A creator can adjust a draft's commercial fields until it is published.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "Draft pixelplus-test-1 exists (C-05)",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/pixelplus-test-1",
   "Scroll to \"Description & price\"",
   "Change \"Description\" to Updated draft description, \"Price (AIN)\" to 1.25, \"Billing\" to \"per hour loaded\", \"Knowledge track\" to law/KR, \"License\" to MIT",
   "Click \"Save\"",
   "Reload the page and re-read the fields and the note next to Save"
  ],
  "expected": [
   "All four fields plus Billing are editable (not greyed) because the status chip is \"Draft\"",
   "The button reads \"Saving…\" then a green alert \"Saved.\" appears; PATCH /api/patches/pixelplus-test-1 returns 200 {anchor:{…}}",
   "After reload the fields keep the new values and the note reads \"Current price: 1.25 AIN\"",
   "The checklist item \"Description written\" shows a check mark"
  ],
  "priority": "P1",
  "area": "manage",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ManagePage.tsx saveFields",
   "packages/web/src/i18n/pages/operator.ts op.manage.fields.*, op.billing.per_apply_hour, op.saved, op.saving",
   "packages/web/src/i18n/pages/common.ts common.save",
   "packages/node/src/api.ts PATCH /api/patches/:id",
   "packages/node/src/market.ts updateDraft"
  ]
 },
 {
  "id": "AZ-039",
  "persona": "Knowledge creator (operator)",
  "title": "Validate and save the benchmark JSON of a draft",
  "goal": "A creator can correct the verification questions of a draft and is stopped from saving a broken benchmark.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "Draft pixelplus-test-1 exists (C-05)",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/pixelplus-test-1 and scroll to \"Benchmark questions\"",
   "Note the value after \"benchmark fingerprint\"",
   "Delete the closing brace of the JSON and click \"Save benchmark\"",
   "Restore the brace, change \"schema\" to \"\" (empty string) and click \"Save benchmark\"",
   "Set \"schema\" back to \"pixelplus-test\", add a second sample {\"prompt\":\"종목코드 삼성전자 \",\"expect\":\"005930\"} and click \"Save benchmark\""
  ],
  "expected": [
   "Step 3: red alert \"Invalid JSON: …\"; no request sent",
   "Step 4: red alert \"benchmark.schema (string) is required\"",
   "Step 5: green alert \"Benchmark saved.\"; PATCH /api/patches/pixelplus-test-1 returns 200; the \"benchmark fingerprint\" value changes; the checklist reads \"2 sample question(s) — verifiers score them on the real model\"",
   "The textarea is disabled and no \"Save benchmark\" button is shown on a published item such as krx-all-2761"
  ],
  "priority": "P1",
  "area": "manage",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ManagePage.tsx saveBench",
   "packages/web/src/i18n/pages/operator.ts op.manage.bench.title, op.manage.bench.hash, op.manage.bench.save, op.manage.bench.saved, op.manage.bench.invalid, op.manage.bench.schema_required, op.manage.check.samples"
  ]
 },
 {
  "id": "AZ-040",
  "persona": "Knowledge creator (operator)",
  "title": "Inspect the overlap check and lineage of the verified KRX knowledge (and see the same overlap warned about in Live test)",
  "goal": "A creator sees which of their other knowledge overlaps with this one and which versions it replaced.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "krx-all-2761 is LISTED and supersedes krx-all-2761-ep12, krx-all-2761-ep6 and pixelplus-087600; its declared parent is krx-all-2761-ep12 (seed lineage ep6 ← ep12 ← krx-all-2761)",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761",
   "Scroll to \"Overlap check\" and read the description and table",
   "Click the krx-all-2761-ep6 link in the table",
   "Go back and scroll to \"Source & derived knowledge\"",
   "Open http://localhost:3402/project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761-ep6 and read the same section",
   "Click the Live test icon of krx-all-2761 on /dashboard, then also tick 'Pixelplus ticker code (single fact)' in the picker"
  ],
  "expected": [
   "Description reads \"Automatically checks whether other knowledge on this node overlaps with this one. When newer knowledge on the same subject is verified, the older one is marked \"newer version available\".\"",
   "Table columns \"Knowledge\", \"Overlapping entries\", \"Same subject\", \"Status\"; rows krx-all-2761-ep12 (\"241,992 memory entries\", \"yes\" in red, chip \"Newer version available\"), krx-all-2761-ep6 (same figures), pixelplus-087600 (\"2,170 memory entries\", \"yes\")",
   "The link opens the public page /0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761-ep6",
   "On krx-all-2761: \"Built on\" = link \"KRX ticker codes for 2,761 listed companies — epoch 12\" with chip \"Newer version available\", \"Derived from this\" = \"none\", \"Replaces\" = \"krx-all-2761-ep12, krx-all-2761-ep6, pixelplus-087600\", \"Replaced by\" = \"—\"",
   "On krx-all-2761-ep6: \"Derived from this\" = \"KRX ticker codes for 2,761 listed companies — epoch 12\", \"Replaced by\" = \"krx-all-2761\" and the status chip reads \"Newer version available\"",
   "Below the section: \"When derived knowledge sells, the original creator automatically receives a share (creator revenue share).\"",
   "Live test: the picker warns 'KRX ticker codes for 2,761 listed companies (final) and Pixelplus ticker code (single fact) overlap on 2,170 memory entries — Pixelplus ticker code (single fact), ticked last, wins.' — the overlap figure matches the manage page's 'Overlap check' row for pixelplus-087600"
  ],
  "priority": "P1",
  "area": "manage",
  "automation": "manual",
  "evidence": [
   "packages/web/src/pages/ManagePage.tsx conflicts table, lineage KeyValue",
   "packages/web/src/i18n/pages/operator.ts op.manage.conflict.*, op.manage.lineage.*",
   "packages/web/src/i18n/pages/common.ts units.rows, status.SUPERSEDED",
   "packages/node/src/api.ts GET /api/patches/:id (conflicts, lineage.parents, supersedes); packages/node/src/seed.ts parents",
   "packages/web/src/components/chat/KnowledgePicker.tsx (chat.picker.overlap_pair); packages/node/src/market.ts chatOverlaps()"
  ]
 },
 {
  "id": "AZ-041",
  "persona": "Knowledge creator (operator)",
  "title": "Try Verify now on your own knowledge and be told why you cannot",
  "goal": "An operator learns that a verification of their own knowledge would never count, before spending GPU minutes on it — the badge only means other nodes ran it.",
  "preconditions": [
   "Signed in on http://localhost:3402 (node-a roles include verifier)",
   "The knowledge under test was published by node-a itself (AZ-031's visibility:test knowledge, or any id from GET /api/me/patches) — on this cluster node-a is the only seller, so every catalog entry is its own",
   "Shipped config: verifier.allowSelfAttest is false",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/<own knowledge id>",
   "Look under 'Verification' for the 'Verify now (this node)' button",
   "Read the line printed in its place",
   "Call POST /api/patches/<id>/verify with the operator token directly (what `ainize patch verify` does)",
   "Re-read the 'Verification' line and the 'Verification results' table"
  ],
  "expected": [
   "No 'Verify now (this node)' button is rendered on your own knowledge (it is offered only for knowledge published by another node)",
   "In its place: 'You cannot verify your own knowledge. Verified means other nodes ran it on a real model — an attestation by this node would not count.'",
   "POST /api/patches/<id>/verify returns 409 {\"error\":\"cannot verify your own knowledge: <id> was published by this node (verifier.allowSelfAttest is false). A self-check never counts toward the quorum — another node has to verify it.\"} and no attestation record is appended (GET /api/patches/<id> attestation count unchanged)",
   "`ainize patch verify <id>` prints the same sentence and exits non-zero",
   "The 'Verification' line still reads 'executed 2/2 passed · integrity 0 · 2 result(s)' — the numerator never exceeds the quorum, and a self-check would have been reported separately as 'n self-check(s), not counted' rather than counted"
  ],
  "priority": "P1",
  "area": "verification",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ManagePage.tsx canVerify (isMine), op.manage.self_verify",
   "packages/node/src/api.ts POST /api/patches/:id/verify → Verifier.verifyOne",
   "packages/node/src/verifier.ts verifyOne (author check before any GPU work), packages/node/src/market.ts attest (write refused)",
   "packages/core/src/catalog.ts deriveCatalog (self_checks excluded from passed/status)",
   "packages/node/test/trust.test.ts item 146"
  ]
 },
 {
  "id": "AZ-042",
  "persona": "Knowledge creator (operator)",
  "title": "Delete a draft with typed confirmation and see that published knowledge cannot be deleted",
  "goal": "A creator can discard an unpublished draft safely, while public records stay permanent.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "Draft pixelplus-upload-1 (C-08) or another DRAFT owned by node-a exists",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/pixelplus-upload-1 and scroll to \"Delete draft\"",
   "Click \"Delete\"",
   "Type pixelplus-upload into the field and observe the \"Confirm delete\" button",
   "Complete the text to pixelplus-upload-1 and click \"Confirm delete\"",
   "Open http://localhost:3402/project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761 and scroll to \"Delete draft\"",
   "Open http://localhost:3402/project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/pixelplus-upload-1 again"
  ],
  "expected": [
   "Step 1 text: \"Deleting a draft removes it from this node. Nothing was published yet, so there is no public record.\"",
   "Step 2 shows an input with placeholder \"Type pixelplus-upload-1 to proceed.\"; \"Confirm delete\" is disabled",
   "Step 3: the button stays disabled while the text does not match exactly",
   "Step 4: button reads \"Deleting…\", DELETE /api/patches/pixelplus-upload-1 returns 200 {ok:true}, browser navigates to /dashboard and the row is gone",
   "Step 5 (published): text \"Published knowledge cannot be deleted — its record is permanent. To stop serving the file from this node, use the developer command below.\" with the box \"Stop serving the file from this node:\" and the command ainize patch forget krx-all-2761; no Delete button (note for the team: bin.ts has no `patch forget` subcommand — report as a docs/CLI gap)",
   "Step 6: page shows title \"Knowledge not found\", the API error text \"patch not found\" and the link \"Back to my knowledge\""
  ],
  "priority": "P1",
  "area": "manage",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ManagePage.tsx delete section, confirmMode",
   "packages/web/src/i18n/pages/operator.ts op.manage.delete.*, op.manage.dev.forget, op.manage.notfound, op.manage.back",
   "packages/node/src/api.ts DELETE /api/patches/:id",
   "packages/node/src/market.ts deleteDraft; packages/cli/src/bin.ts (no `patch forget`)"
  ]
 },
 {
  "id": "AZ-043",
  "persona": "Knowledge creator (operator)",
  "title": "Filter node logs by level, expand details, load older events and read the public-record timeline",
  "goal": "A creator can audit everything that happened to one knowledge item on this node and on the network.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "krx-all-2761 has events (verification, live tests) and public records (anchor, attestations, supersedes — the 'replaced' entries exist only after reconcileSupersedes ran once krx-all-2761 reached 2/2)",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761/logs",
   "Read the title, the description and the panel header",
   "Click a log line ending with \"⋯\" (e.g. a \"usage\" line \"live test krx-all-2761 (compare) …\")",
   "Change the level select from \"All levels\" to \"Error\", then to \"Warning\", then to \"Info\"",
   "Click \"Load older\" and read the panel footer",
   "Scroll to \"Public-record timeline\" and read the entries",
   "Click \"Back to manage\""
  ],
  "expected": [
   "Title \"krx-all-2761 logs\"; description begins \"What happened on this node for this knowledge (draft, publish, verification, trades, load/unload), refreshed every 5 seconds.\"; header shows \"<n> line(s)\" and briefly \" · refreshing…\" every 5 s",
   "Each row shows time, level (INFO/WARN/ERROR coloured), kind and message; clicking the line expands a JSON block with fields such as mode, hit, base_ms, patched_ms; clicking again collapses it",
   "\"Error\" shows only error rows (possibly the single row \"No events for this knowledge yet.\"), \"Warning\" only warn rows, \"Info\" info+debug rows",
   "After \"Load older\" the request uses limit=300 and the footer reads \"showing up to 300 events · times in local timezone\"",
   "Timeline entries are listed oldest → newest (ledger order): kind \"registered\" with \"registered by node-a · file fingerprint 57c9346349af… · price 25 AIN\", two \"verified\" entries \"PASS by node-b (executed (vllm:Qwen3.8-Flash-Next)) · accuracy 26/26, 1/8\" / node-c, and \"replaced\" entries \"krx-all-2761 replaces krx-all-2761-ep12 (241992 shared entries)\"; each shows \"record <hash>\" only (on the AIN ledger record.sig is empty, so no \"· tx <hash>\" suffix)",
   "\"Back to manage\" navigates to /project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761"
  ],
  "priority": "P1",
  "area": "logs",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/LogsPage.tsx LEVELS filter, setLimit(l+200), describe()",
   "packages/web/src/i18n/pages/operator.ts op.logs.*",
   "packages/node/src/api.ts GET /api/patches/:id/events, GET /api/patches/:id/records (ledger order)"
  ]
 },
 {
  "id": "AZ-044",
  "persona": "Knowledge creator (operator)",
  "title": "Save display name, payout address and notification preference on the node",
  "goal": "A creator's identity and payout preferences persist on the node and show up wherever the creator is named.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "Current settings: display name node-a, notifications Everything"
  ],
  "steps": [
   "Open http://localhost:3402/account and scroll to \"Notifications & payout\"",
   "Confirm \"Save settings\" is disabled while nothing changed",
   "Change \"Display name\" to node-a demo, leave \"Payout address\" as pre-filled (it defaults to the node address 0xF7A9…; the placeholder only shows when the value is cleared), select \"Sales only — when my knowledge sells\"",
   "Click \"Save settings\"",
   "Reload the page; read the header user-menu button and the settings form",
   "Clear \"Display name\" completely and try to save",
   "Restore \"Display name\" to node-a and \"Everything — sales, verification results, new knowledge in subscribed tracks\", click \"Save settings\""
  ],
  "expected": [
   "Description reads \"These settings are stored on the node, so they are the same in every browser.\"; helper under Display name: \"Shown as the creator on knowledge pages and in the network.\"; under Payout address: \"Where sales revenue and creator revenue share arrive. Defaults to this node’s address.\"",
   "Step 4: button reads \"Saving…\", PATCH /api/me/settings returns 200 {settings:{…}}, green text \"Saved on the node.\" appears and the button is disabled again",
   "Step 5: after reload the form shows node-a demo and \"Sales only\" selected; the header button reads \"node-a demo ▾\"; GET /api/me/settings returns display_name \"node-a demo\", notifications \"sales\"",
   "Step 6: browser required validation blocks the empty name; if bypassed, PATCH /api/me/settings answers 400 {error:'invalid request', issues:[…]} (zod min(1)) and the alert shows \"invalid request: …\"",
   "Step 7: \"Saved on the node.\" again and header back to \"node-a ▾\""
  ],
  "priority": "P1",
  "area": "account",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/AccountPage.tsx onSaveSettings, dirty",
   "packages/web/src/i18n/pages/operator.ts op.account.settings.*, op.account.display_name*, op.account.payout*, op.account.notif.*",
   "packages/node/src/api.ts GET/PATCH /api/me/settings (display_name min 1 max 64)",
   "packages/node/src/market.ts settings() (payout_address defaults to node address), updateSettings (cfg.name)"
  ]
 },
 {
  "id": "AZ-045",
  "persona": "Knowledge creator (operator)",
  "title": "Read account identity, AIN wallet balance, sales and creator revenue share",
  "goal": "A creator sees how the node is identified, how verification is decided, and what they have earned.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "Node runs the AIN ledger (GET /api/info ledger.kind = ain, currency AIN, quorum 2)",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/account",
   "Read the \"Account\" key-value block",
   "Scroll to \"Wallet\" and read the balance, the note and the summary line",
   "Read the \"Sales\" and \"Creator revenue share received\" tables",
   "Click \"Set up app on chain\"",
   "Scroll to \"Node operators & developers\" and read the box"
  ],
  "expected": [
   "Account block: Name node-a; Account address 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5 with \"Copy\"; Roles \"seller, verifier, serving\"; Node endpoint http://localhost:3402; Public record \"AI Network blockchain · http://localhost:8081 · /apps/knowledge\"; Records \"<n> · chain blocks <height>\"; Verified when \"2+ independent verifier nodes pass\"; Version <semver>",
   "Wallet description: \"AIN balance of this node’s account. Purchases are AIN transfers; sales revenue and creator revenue share arrive here.\"; a large number followed by \"AIN\" (or \"—\" if the chain is unreachable) and the note \"AIN = AI Network token (this demo runs a local dev chain)\"",
   "Summary reads \"<p> purchase(s) · <s> sale(s) · <r> creator-share payment(s) received\" matching GET /api/me/wallet",
   "With no sales the tables read \"No sales yet.\" and \"None yet — it accrues when knowledge built on yours sells.\"",
   "Set up app on chain: button reads \"Setting up…\" then green alert \"Knowledge app and market rules are set on chain.\" (POST /api/chain/setup 200; safe to repeat — note says \"Needed once; safe to run again.\")",
   "Developer box shows the command ainize node retire, the ain-js note and a \"Node state (raw)\" JSON containing address, ledger, peers, counts, chain_head, settings"
  ],
  "priority": "P1",
  "area": "account",
  "automation": "manual",
  "evidence": [
   "packages/web/src/pages/AccountPage.tsx identity/wallet/dev sections",
   "packages/web/src/i18n/pages/operator.ts op.account.identity, op.account.quorum.value, op.account.wallet.*, op.account.chain.*, op.account.sales.empty, op.account.royalties.empty, op.account.dev.*",
   "packages/web/src/i18n/pages/common.ts price.ain_note",
   "packages/node/src/api.ts GET /api/me/wallet, POST /api/chain/setup, GET /api/info"
  ]
 },
 {
  "id": "AZ-046",
  "persona": "Knowledge creator (operator)",
  "title": "Remove and re-add a connected peer node",
  "goal": "A creator controls which nodes their node connects to first, and the change persists in the node config.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "Peers http://localhost:3403 and http://localhost:3404 are listed under \"Connected nodes\" (GET /api/info peers = 2)"
  ],
  "steps": [
   "Open http://localhost:3402/account and scroll to \"Connected nodes\"",
   "Read the description and the table columns",
   "Click \"Remove\" on the row http://localhost:3404 and immediately run: curl -s http://localhost:3402/api/nodes",
   "Type http://localhost:3404/ (with trailing slash) into \"Add node endpoint\" and click \"Add\"",
   "Wait ~30 s and re-read the \"Last seen\" cell of the re-added row",
   "Open http://localhost:3402/network"
  ],
  "expected": [
   "Description: \"Nodes this one connects to first; the rest are discovered automatically. See the whole network on the Network page.\" (Network is a link to /network); columns \"Endpoint\", \"Name\", \"Account address\", \"Roles\", \"Last seen\", \"Failures\" plus an unlabeled 7th column holding the Remove button",
   "Rows show node-b (roles \"verifier\") and node-c (roles \"verifier, serving\") with a relative \"Last seen\" and Failures 0",
   "Remove: DELETE /api/peers returns 200 {ok:true}; green alert \"Node removed.\"; the :3404 row disappears — but only briefly: node-c has node-a as a configured peer and calls /p2p/hello every ~4 s, and the peer exchange re-adds it, so the row re-appears automatically within seconds (auto-rediscovery is expected behaviour)",
   "Add: the trailing slash is stripped, POST /api/peers {endpoint:\"http://localhost:3404\"} returns 200; green alert \"Node added.\"; the row is present with a fresh relative time (or \"never\" until the next hello)",
   "After a while Last seen updates to a recent \"<n>s ago\"",
   "Network page lists all three nodes again"
  ],
  "priority": "P1",
  "area": "account",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/AccountPage.tsx onAddPeer (strip trailing slash), removePeer",
   "packages/web/src/i18n/pages/operator.ts op.account.peers.*, op.never, op.remove, op.add",
   "packages/node/src/api.ts POST/DELETE /api/peers (saveConfig)",
   "packages/node/src/p2p.ts round() (hello + peer exchange, gossipIntervalMs 4000)"
  ]
 },
 {
  "id": "AZ-047",
  "persona": "Knowledge creator (operator)",
  "title": "Review Files & changes: pairing hint, sync, file tree and change history",
  "goal": "A creator sees their knowledge files mirrored in one folder, learns how to connect aindrive once, and can browse file contents and history.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "Drive integration enabled on node-a but not yet paired (GET /api/drive returns configured:false, running:false, folder /home/comcom/.ngram-cluster/node-a/data/drive)"
  ],
  "steps": [
   "Click \"node-a ▾\" in the header and choose \"Files & changes\"",
   "Read the key-value block and the buttons in the title row",
   "Read the \"Connect once\" section and click \"Copy\" next to the pairing command",
   "Click \"Sync\"",
   "In \"Folder contents\" open the folder krx-all-2761 under patches and click manifest.json",
   "Try clicking the .npz entry in the same folder",
   "Click benchmark.json in patches/krx-all-2761 and read the \"Change history\" section"
  ],
  "expected": [
   "Title \"Files & changes\"; Sharing \"stopped\"; Connected \"not yet\"; Server https://aindrive.ainetwork.ai; Folder /home/comcom/.ngram-cluster/node-a/data/drive; Files <n>; \"Start sharing\" is disabled (not connected), \"Sync\" is enabled, no \"Open in aindrive ↗\" link",
   "\"Connect once\" text: \"Run the command below in a terminal on the node’s computer; a browser opens. After you sign in the folder is connected, and from then on you only press \"Start sharing\" here.\"; the box shows cd /home/comcom/.ngram-cluster/node-a/data/drive && npx aindrive login --server https://aindrive.ainetwork.ai … and the note \"The link is single-use and expires in 10 minutes. …\"; Copy flips to \"Copied\"",
   "Sync: POST /api/drive {action:\"sync\"} returns 200 and the green alert reads \"Synced — <n> file(s) rewritten.\"; the file list refreshes",
   "manifest.json: URL becomes /drive/patches/krx-all-2761/manifest.json; the right pane shows the path, a \"Copy content\" button, \"doc id — (not connected yet; no history) · 0 change(s)\" and the JSON content",
   "The .npz row is greyed and not clickable (binary, no history)",
   "benchmark.json history reads \"No history yet — it appears once the file is edited through aindrive (web editor or an AI agent).\"; before selecting a file the pane reads \"Pick a text file (json / md / jsonl) on the left to see its content and change history.\""
  ],
  "priority": "P1",
  "area": "drive",
  "automation": "manual",
  "evidence": [
   "packages/web/src/pages/DrivePage.tsx run('sync'), groups, TEXT_EXT, changes pane",
   "packages/web/src/i18n/pages/operator.ts op.drive.*",
   "packages/web/src/i18n/pages/common.ts nav.files, common.copied",
   "packages/node/src/api.ts GET /api/drive, GET /api/drive/changes, POST /api/drive"
  ]
 },
 {
  "id": "AZ-048",
  "persona": "Knowledge creator (operator)",
  "title": "Upload a .npz file from the browser and watch the fingerprint being computed",
  "goal": "A creator without shell access on the node can register knowledge by uploading the file.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "Tester has a copy of /mnt/newdata/qwen3.8/results/train-fact/픽셀플러스.npz on their own machine",
   "No knowledge with id pixelplus-upload-1 exists",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/new-patch",
   "Fill Name = Pixelplus upload test, Id (optional) = pixelplus-upload-1, Subject = pixelplus-upload, one sample Question = 종목코드 픽셀플러스  / Expected answer (prefix) = 087600",
   "Keep \"Upload a file\" selected and choose 픽셀플러스.npz in the file input",
   "Read the note under the file input",
   "Click \"Save draft\"",
   "On the resulting manage page read the \"Knowledge file\" row"
  ],
  "expected": [
   "Before choosing, the upload card note reads \"copied into this node’s storage\"; after choosing it reads \"픽셀플러스.npz · 3.9 MB\" (size/1e6, one decimal)",
   "While submitting an info alert reads \"Uploading 픽셀플러스.npz (3.9 MB) and computing the fingerprint…\"",
   "POST /api/patches (multipart) returns 200; browser navigates to /project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/pixelplus-upload-1",
   "Manage page shows \"Knowledge file\" = \"present · 3.7 MB · 2,992 memory entries · 1 facts\" (bytes() uses MiB; facts = number of filled samples) and a 64-hex \"File fingerprint\"",
   "The developer box on the form explained \".npz arrays: addrs (int64), before / after (float32 rows)…\""
  ],
  "priority": "P2",
  "area": "new-patch",
  "automation": "manual",
  "evidence": [
   "packages/web/src/pages/NewPatchPage.tsx FileInput, op.new.uploading",
   "packages/web/src/i18n/pages/operator.ts op.new.file.upload, op.new.file.upload.note, op.new.file.size, op.new.uploading, op.new.dev.npz, op.manage.file.present",
   "packages/node/src/api.ts upload.single('file')"
  ]
 },
 {
  "id": "AZ-049",
  "persona": "Knowledge creator (operator)",
  "title": "Copy the README badge snippet for the auto-pay address",
  "goal": "A creator can paste a badge into a README that links people and agents to the knowledge's automatic-payment address.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "Browser grants clipboard access to http://localhost:3402",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761 and scroll to \"README badge\"",
   "Read the read-only snippet box",
   "Click \"Copy\" and watch the button label",
   "Paste into a text editor"
  ],
  "expected": [
   "Snippet is exactly [![Ainize knowledge: krx-all-2761](http://localhost:3402/static/images/ic-certified.svg)](http://localhost:3402/x402/patch/krx-all-2761)",
   "Description reads \"Paste this badge into a README — it links straight to this knowledge’s auto-payment address so people and AI agents can buy it.\"",
   "Button label switches to \"Copied\" for about 1.5 s then back to \"Copy\"",
   "Pasted text equals the snippet; opening the badge URL returns HTTP 402 with payment requirements",
   "On a draft the \"Knowledge page\" link is greyed (disabled) and the \"Auto-pay address\" link is disabled unless status is Verified"
  ],
  "priority": "P2",
  "area": "manage",
  "automation": "manual",
  "evidence": [
   "packages/web/src/pages/ManagePage.tsx snippet useMemo, ExternalAnchor $disabled",
   "packages/web/src/components/ui/Misc.tsx CopyButton",
   "packages/web/src/i18n/pages/operator.ts op.manage.badge.title, op.manage.badge.desc",
   "packages/web/src/i18n/pages/common.ts common.copy, common.copied"
  ]
 },
 {
  "id": "AZ-050",
  "persona": "Knowledge creator (operator)",
  "title": "Check the model runtime card and ask the model directly",
  "goal": "A creator confirms the serving model this node loads knowledge into is reachable and sees what it answers right now.",
  "preconditions": [
   "Signed in on http://localhost:3402",
   "GET http://localhost:3402/api/runtime returns available:true, api http://localhost:8000, model Qwen3.8-Flash-Next, hook:true"
  ],
  "steps": [
   "Open http://localhost:3402/account and scroll to \"Model runtime\"",
   "Read the key-value block",
   "Under \"Ask the model directly\" keep the default Prompt 종목코드 픽셀플러스  and click \"Ask\"",
   "Click the \"Live test\" link in the sentence below the heading"
  ],
  "expected": [
   "Description: \"The real model this node can load knowledge into and out of. Verification scoring and live tests use it too.\"",
   "Status \"available\" in green; Model server http://localhost:8000; Model Qwen3.8-Flash-Next; Load/unload hook \"connected\"; Knowledge loaded now \"none\" (or the loaded ids with \"(manual, <n>m ago)\")",
   "Button reads \"Generating…\" then POST /api/runtime/complete returns 200 {text}; a monospace box shows the grey prompt followed by the bold completion (up to 16 tokens)",
   "Sentence reads \"See what the model answers right now. To compare before/after side by side, use Live test.\"; the link opens /chat",
   "If the runtime were down, Status would read \"unavailable — <error>\" and the \"Ask\" button would be disabled"
  ],
  "priority": "P2",
  "area": "account",
  "automation": "manual",
  "evidence": [
   "packages/web/src/pages/AccountPage.tsx runtime section, onTry",
   "packages/web/src/i18n/pages/operator.ts op.account.runtime.*, op.account.try.default_prompt",
   "packages/node/src/api.ts POST /api/runtime/complete, GET /api/runtime"
  ]
 },
 {
  "id": "AZ-051",
  "persona": "Node operator / developer",
  "title": "Log in and out as operator from the CLI (first login sets the node password) and observe the 401 guard",
  "goal": "An operator can obtain and drop a bearer session for operator-only commands without opening the browser.",
  "preconditions": [
   "Demo cluster running",
   "`curl -s http://localhost:3403/api/auth/me` returns `\"needsSetup\":true` for node-b (no operator password set yet) — otherwise the tester must know node-b's existing password",
   "Shell: `export PATH=\"$HOME/.local/node/bin:$PATH\"; cd /mnt/newdata/ainize/knowledge-marketplace; B=\"node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-b\"`",
   "node-b address = `curl -s http://localhost:3403/api/chain` (currently 0x430b3A373fa4FF685ca40d03BA455c26c0959935)"
  ],
  "steps": [
   "Run `$B wallet` before logging in",
   "Run `$B login --password demo-pass-1234`",
   "Run `curl -s http://localhost:3403/api/auth/me`",
   "Run `$B wallet`",
   "Run `$B logout`",
   "Run `$B wallet` again",
   "Run `$B login --password wrong-password`; then `echo $?`",
   "Run `NGRAM_PASSWORD=demo-pass-1234 $B login` and inspect `$HOME/.ngram-cluster/node-b/cli.json` permissions with `stat -c %a`"
  ],
  "expected": [
   "Step 1 fails with `error: operator login required — run \\`ainize login\\` first`; exit code 3",
   "Step 2 prints `✓ operator password set and logged in to http://localhost:3403 (token saved in /home/<user>/.ngram-cluster/node-b/cli.json)`",
   "Step 3 now returns `\"signedIn\":false,\"needsSetup\":false` (cookie-less curl is not signed in, but the password is set)",
   "Step 4 prints a key/value block padded to the widest key: `address             0x430b3A373fa4FF685ca40d03BA455c26c0959935`, `ledger              ain · ain:local`, `balance             <n> AIN`, then `sales`, `royalties received`, `purchases` counters",
   "Step 5 prints `✓ logged out`; cli.json no longer contains `token`",
   "Step 6 fails again with `error: operator login required — run \\`ainize login\\` first` (exit 3)",
   "Step 7 fails with `error: wrong password — run \\`ainize login\\` first` (every 401 gets the login hint) and exit code 3",
   "Step 8 prints `✓ logged in to http://localhost:3403 (token saved in …/cli.json)` (no 'operator password set and' prefix this time) and cli.json has mode 600"
  ],
  "priority": "P0",
  "area": "account",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/auth.ts (login/logout messages, NGRAM_PASSWORD)",
   "packages/cli/src/client.ts (401 → ' — run `ainize login` first', exit 3)",
   "packages/cli/src/context.ts writeState mode 0o600",
   "packages/cli/src/commands/branch.ts wallet() kv padding",
   "packages/node/src/api.ts /api/auth/me, /api/auth/setup, /api/auth/login, requireOperator"
  ]
 },
 {
  "id": "AZ-052",
  "persona": "Node operator / developer",
  "title": "Announce a public patch and watch node-b and node-c verify it on the real model until it is LISTED",
  "goal": "A creator sees independent verifiers execute the benchmark on the serving model and list the knowledge once quorum is met, with no self-attestation.",
  "preconditions": [
   "node-a operator logged in from the CLI (`A=\"node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-a\"; $A login --password <node-a password>`); `NPZ=/mnt/newdata/qwen3.8/results/train-fact/픽셀플러스.npz`; vLLM serving Qwen3.8-Flash-Next reachable at http://localhost:8000 (`$A status` shows `runtime     available · Qwen3.8-Flash-Next · hook ok`)",
   "Use a benchmark schema that no existing patch uses so that listing does not supersede the demo's krx-all-2761: `BENCH08='{\"schema\":\"o08-pixel-check\",\"queries\":1,\"format\":[\"template\"],\"samples\":[{\"prompt\":\"종목코드 픽셀플러스 \",\"expect\":\"087600\"}]}'`",
   "node-b home for logs: `B=\"node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-b\"`; verifier addresses from GET /api/nodes (currently node-b 0x430b3A37…, node-c 0x199Ff870…)",
   "Tester accepts a permanent extra public anchor on the local dev chain"
  ],
  "steps": [
   "Run `$A publish $NPZ --id o08-pixel-copy --name \"[o08 test] Pixelplus ticker copy\" --model Qwen3.8-Flash-Next --benchmark \"$BENCH08\" --price 0.1`",
   "Immediately run `curl -si http://localhost:3402/x402/patch/o08-pixel-copy | head -1` and `curl -s http://localhost:3402/x402/patch/o08-pixel-copy`",
   "Run `$B logs --follow` (node-b home, no --kind so both 'verifier' and 'verify' events show) in a second terminal",
   "Poll `$A patch get o08-pixel-copy` every 20 s for up to 5 minutes",
   "Run `$A patch ls --status LISTED`",
   "Run `$A patch get krx-all-2761 | grep -i superseded`",
   "Run `$A ledger ls --kind attest --limit 3`"
  ],
  "expected": [
   "Step 1 prints `✓ draft created: o08-pixel-copy  (2,992 rows, sha256 …)` and `✓ announced o08-pixel-copy → ledger record <hash> (verifiers will now attest; quorum lists it)`; `$A logs --kind publish --limit 2` shows `announced o08-pixel-copy (conflicts: N)`. N is every knowledge on this node whose address set overlaps the new body — 4 on a node holding only the demo bodies, and one more for each pixel copy earlier runs announced. The pre-check runs inside the node and counts private drafts (taught lessons) too, so it equals what GET /api/patches/:id/conflicts reports TO THE OPERATOR; a visitor is shown fewer, because private drafts are redacted from public overlap answers.",
   "Step 2 returns `HTTP/1.1 423 Locked` with body `{\"error\":\"patch not listed yet (verification 0/2)\"}` (or 1/2 if one verifier already finished)",
   "Step 3 shows on node-b, in order: `verifier` lines `verifying o08-pixel-copy ([o08 test] Pixelplus ticker copy)` and `benchmark o08-pixel-copy: 1/1 restarts=0`, then a `verify` line `attested o08-pixel-copy: PASS (vllm:Qwen3.8-Flash-Next)`; node-c logs the same (they run sequentially because they share the runtime lock)",
   "Step 4: status goes ANNOUNCED → VERIFYING (after the first attestation) → LISTED with `verification 2/2 passed ✓ quorum`; the attestations table has exactly two rows, verifiers node-b and node-c, `PASS  free_generation=1/1 pre_apply=…  vllm:Qwen3.8-Flash-Next`; node-a (the author) never appears as a verifier",
   "Step 5 lists both krx-all-2761 and o08-pixel-copy",
   "Step 6 prints nothing: krx-all-2761 is not superseded (different benchmark schema, so the overlap is not a supersede candidate)",
   "Step 7 shows the two new `attest` rows `o08-pixel-copy · PASS · vllm:Qwen3.8-Flash-Next` with AUTHOR node-b 0x430b3A… and node-c 0x199Ff8…"
  ],
  "priority": "P0",
  "area": "verification",
  "automation": "cli",
  "evidence": [
   "packages/node/src/verifier.ts round()/verifyOne() (allowSelfAttest false, vllm:<model>, log messages)",
   "packages/node/src/runtime.ts verify() (free_generation score, restarts_detected)",
   "packages/node/src/market.ts announce() (pending_supersede only for same_schema), reconcileSupersedes(), attest() (kind 'verify')",
   "packages/node/src/api.ts GET /x402/patch/:id 423 when !quorum_ok",
   "~/.ngram-cluster/node-a/config.json verifier.quorum=2, intervalMs=5000"
  ]
 },
 {
  "id": "AZ-053",
  "persona": "Node operator / developer",
  "title": "Use knowledge in one line: `ainize use krx-all-2761` verifies, pays in AIN, downloads and loads it; then re-run and remove",
  "goal": "A node operator can acquire and activate verified knowledge with one command and see the money and state changes on every side.",
  "preconditions": [
   "Demo cluster running on the AIN ledger; node-b operator logged in (O-02); node-b balance ≥ 25 AIN (`curl -s http://localhost:3403/api/chain` shows balance)",
   "node-a operator logged in in another shell for the seller-side check (`A=...node-a`)",
   "Shell: `B=\"node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-b\"`; note node-b's balance before: `$B wallet | grep balance`",
   "Addresses: node-a 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5, node-b 0x430b3A373fa4FF685ca40d03BA455c26c0959935 (current; read from GET /api/nodes)"
  ],
  "steps": [
   "Run `$B use krx-all-2761`",
   "Run `$B wallet`",
   "Run `$A wallet` (node-a) and `$A ledger ls --kind settle --limit 3`",
   "Run `$B patch get krx-all-2761 | head -1`",
   "Run `$B chat krx-all-2761 --mode patched \"종목코드 삼성전자\"`",
   "Run `$B use krx-all-2761` a second time",
   "Run `$B patch remove krx-all-2761`",
   "Run `$B patch ls --status LISTED`"
  ],
  "expected": [
   "Step 1 prints `✓ bought krx-all-2761 for 25 (ain-transfer)  tx 0x…` followed by `+<ms>ms`-prefixed step lines: `quorum    2 attestation(s) ≥ quorum 2`, `402       Payment Required: 25 AIN → 0xF7A9dE499… (ain-transfer)`, `pay       AIN transfer tx 0x…`, `settled   seller confirmed; manifest sha256 …`, `download  body already present; sha256 matches on-ledger anchor` (node-b already holds the body as a verifier), `receipt   on-chain access receipt written (/apps/knowledge/access/…, tx …)`, `apply     <scripts/patch.py output>`, `body: /home/<user>/.ngram-cluster/node-b/data/blobs/57c9346349afd6fa1b0475340b9b041ee0027753c05463d4bb204a5a4726c642.npz`, and finally `loaded into the model — try: ainize chat krx-all-2761 \"your question\"`",
   "Step 2 shows `balance` reduced by 25 AIN and `purchases    1`",
   "Step 3: node-a's wallet lists a `recent sales` row `krx-all-2761  25 AIN  0x430b3A37…9935  <time>`; `ledger ls --kind settle` shows `krx-all-2761 · 25 AIN · buyer 0x430b…9935`; `patch ls` SOLD column for krx-all-2761 increments by 1 on every node",
   "Step 4 header shows `LISTED  purchased  applied`",
   "Step 5 prints `after (krx-all-2761 loaded)  <n> ms · already loaded` with an answer containing 005930 and the footer `correct ✓ (benchmark)  model Qwen3.8-Flash-Next` (no apply cost: the patch is already in the model)",
   "Step 6 prints `✓ krx-all-2761 is already on this node (purchased)`, `✓ applied krx-all-2761: …` and `try it: ainize chat krx-all-2761 \"your question\"` — no second payment, node-b balance unchanged",
   "Step 7 prints `✓ removed krx-all-2761: …`; `$B patch get krx-all-2761` no longer shows `applied`; `curl -s http://localhost:3403/api/runtime` has `\"applied\":[]`",
   "Step 8 still shows krx-all-2761 LISTED (buying does not change status)"
  ],
  "priority": "P0",
  "area": "x402",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/patch.ts patchUse/patchBuy/patchApply/patchRemove output",
   "packages/cli/src/commands/chat.ts renderChat ('already loaded', 'correct ✓ (benchmark)')",
   "packages/node/src/market.ts buy() steps (quorum/402/pay/settled/download/receipt/apply), settlePayment() ain-transfer, isApplied()",
   "packages/node/src/api.ts POST /api/patches/:id/buy, GET /api/me/wallet, GET /api/runtime",
   "packages/cli/src/commands/branch.ts wallet(); packages/cli/src/commands/ledger.ts summary()"
  ]
 },
 {
  "id": "AZ-054",
  "persona": "Node operator / developer",
  "title": "Live-test knowledge from the CLI: `chat --list`, one-shot compare, `--mode`, `--thinking`, `--json`, quota footer and the interactive REPL",
  "goal": "A developer can measure before/after answers of the shared model for any knowledge held on a node, script it with JSON, and hold a multi-turn session.",
  "preconditions": [
   "Demo cluster running; runtime available; node-a home used (`A=\"node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-a\"`); node-a holds all four bodies (seller)",
   "An empty home for the anonymous run: `mkdir -p /tmp/ainize-anon`"
  ],
  "steps": [
   "Run `$A chat`",
   "Run `$A chat --list`",
   "Run `$A chat pixelplus-087600 \"종목코드 픽셀플러스\"` (node-a logged in)",
   "Run `$A chat pixelplus-087600 --mode base \"종목코드 픽셀플러스\"` and `$A chat pixelplus-087600 --mode patched \"종목코드 픽셀플러스\"`",
   "Run `$A chat pixelplus-087600 --thinking --max-tokens 400 \"픽셀플러스의 종목코드는?\"`",
   "Run `$A --json chat pixelplus-087600 \"종목코드 픽셀플러스\" | python3 -c \"import sys,json; d=json.load(sys.stdin); print(sorted(d))\"`",
   "Run `node packages/cli/dist/bin.js --home /tmp/ainize-anon --node http://localhost:3402 chat pixelplus-087600 --mode base \"종목코드 픽셀플러스\"`",
   "Run `$A chat krx-all-2761 --mode patched`; type `종목코드 삼성전자` Enter; type `/mode compare` Enter; `/help` Enter; `/reset` Enter; `/quit` Enter",
   "Run `printf '종목코드 삼성전자\\n/quit\\n' | $A chat krx-all-2761 --mode patched`",
   "Run `$A chat pixelplus-087600 \"x\" --max-tokens 5000`"
  ],
  "expected": [
   "Step 1 fails with `error: patch id required — \\`ainize chat --list\\` shows what this node can test`",
   "Step 2 prints `runtime ready  model Qwen3.8-Flash-Next  live-apply hook on` and a table `ID NAME MODEL FACTS MEMORY ROWS VERIFIED TRY` with 4 rows, each `2/2 ✓` and TRY `\"종목코드 픽셀플러스\" → 087600`, then the hint `ainize chat <ID> \"<question>\"   or   ainize chat <ID>   for an interactive session`",
   "Step 3 prints a `before (base model)  <n> ms` block, an `after (pixelplus-087600 loaded)  <n> ms · loaded in <n> ms` block whose answer contains 087600, and the footer `correct ✓ (benchmark)  model Qwen3.8-Flash-Next`; no `free live tests left` text (operator is unlimited)",
   "Step 4: `--mode base` prints only the `before (base model)` block and no correct/wrong marker; `--mode patched` prints only the `after (…)` block plus the marker (`wrong ✗ (benchmark)` would appear on a miss; `(no benchmark sample for this question)` for a free question)",
   "Step 5 shows dimmed reasoning lines prefixed `┆` inside the answer blocks before the final content",
   "Step 6 prints `['applied', 'applied_ms', 'base', 'benchmark_hit', 'benchmark_hits', 'history', 'mode', 'model', 'patch_id', 'patch_ids', 'patched', 'quota_limit', 'remaining_quota', 'was_applied']` and nothing else on stdout — `history` is `{base, patched, split}`, how many messages each column was actually sent (split:false here, a single-turn call)",
   "Step 7 (anonymous) ends with `free live tests left this hour: 19` (or one less per prior anonymous call from this IP in the hour)",
   "Step 8: banner `live test of krx-all-2761 · mode patched · /quit to exit, /help for commands`, prompt `you> ` (in `compare` mode a second dim line says 'follow-ups: each column replays only its own earlier answers — the base model is never shown the patched one', because the REPL keeps one transcript per column); the answer block `after (krx-all-2761 loaded)` contains 005930 with `correct ✓ (benchmark)`; `/mode compare` → `mode → compare`; `/help` lists `/mode base|patched|compare  (now: compare)`, `/reset   forget the transcript`, `/quit    exit`; `/reset` → `transcript cleared`; `/quit` → `bye — 1 turn(s)`; exit 0",
   "Step 9 works non-interactively (no prompt echo) and ends with `bye — 1 turn(s)`; after every call the shared table is restored (`curl -s http://localhost:3402/api/runtime` shows `\"applied\":[]`)",
   "The last step fails locally with `error: --max-tokens must be a whole number between 1 and 1024 (got 5000)` — the range the help documents is checked before the request instead of being answered with the node's zod message; exit 1"
  ],
  "priority": "P0",
  "area": "chat",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/chat.ts chatPatches/renderChat/chatRepl (strings: 'correct ✓ (benchmark)', 'free live tests left this hour', '/mode', 'bye — N turn(s)')",
   "packages/cli/src/bin.ts chat command (--list, --mode, --thinking, --max-tokens, --json)",
   "packages/node/src/api.ts POST /api/chat (remaining_quota null for operator, 20 for visitors), GET /api/chat/patches",
   "packages/node/src/market.ts chat() restore-in-finally, benchmark_hit"
  ]
 },
 {
  "id": "AZ-055",
  "persona": "Node operator / developer",
  "title": "Drive the public and operator HTTP API with curl from /api/openapi.json: catalog, detail, benchmarks, info, 401 guards, login token and settings",
  "goal": "A developer integrating without the CLI can discover every endpoint from the node's own OpenAPI document and rely on documented status codes.",
  "preconditions": [
   "Demo cluster running with krx-all-2761 LISTED and the other three SUPERSEDED; node-b password set (O-02: demo-pass-1234)",
   "Shell: `N=http://localhost:3402; NB=http://localhost:3403`",
   "node-b address = `curl -s $NB/api/chain` (currently 0x430b3A373fa4FF685ca40d03BA455c26c0959935)"
  ],
  "steps": [
   "Run `curl -s $N/api/openapi.json | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d['openapi'], d['info']['title'], d['servers'], len(d['paths']), '/x402/patch/{id}' in d['paths'], '/api/chat' in d['paths'])\"`",
   "Run `curl -s $N/api/docs | python3 -c \"import sys,json; d=json.load(sys.stdin); print(sorted(d), d['cli']['oneLiners']['use']['en'])\"`",
   "Run `curl -s \"$N/api/catalog?status=LISTED,SUPERSEDED&sort=price\" | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d['total'], d['items'][0]['anchor']['id'], 'sig' in d['items'][0]['attestations'][0], d['models'], d['schemas'])\"`",
   "Run `curl -s -o /dev/null -w '%{http_code}\\n' \"$N/api/catalog?limit=0\"` and `curl -s \"$N/api/catalog?sort=bogus\"`",
   "Run `curl -s $N/api/patches/krx-all-2761 | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d['status'], d['quorum_ok'], d['lineage']['parents'][0]['id'], d['supersedes'], d['gateway_url'], d['has_body'], d['branches'][0]['name'])\"`",
   "Run `curl -s -w ' %{http_code}\\n' $N/api/patches/does-not-exist` and `curl -s -w ' %{http_code}\\n' $N/api/benchmarks/none`",
   "Run `curl -s $N/api/benchmarks/krx-ticker-codes | python3 -c \"import sys,json; print(len(json.load(sys.stdin)['items']))\"` and `curl -s $N/api/info | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d['quorum'], d['currency'], d['counts'], d['peers'])\"`",
   "For each of `GET /api/me/wallet`, `GET /api/me/settings`, `POST /api/patches`, `POST /api/branches`, `POST /api/patches/krx-all-2761/buy`, `DELETE /api/peers` run `curl -s -w ' %{http_code}\\n' -X <METHOD> $NB<path>` without credentials",
   "Run `curl -s -w ' %{http_code}\\n' -X POST $NB/api/auth/login -H 'content-type: application/json' -d '{\"password\":\"wrong\"}'`",
   "Run `TOKEN=$(curl -s -X POST $NB/api/auth/login -H 'content-type: application/json' -d '{\"password\":\"demo-pass-1234\"}' | python3 -c \"import sys,json; print(json.load(sys.stdin)['token'])\")` then `curl -s -H \"Authorization: Bearer $TOKEN\" $NB/api/me/wallet | python3 -c \"import sys,json; print(sorted(json.load(sys.stdin)))\"`",
   "Run `curl -s -w ' %{http_code}\\n' -X POST $NB/api/auth/setup -H 'content-type: application/json' -d '{\"password\":\"another-1234\"}'`",
   "Run `curl -s -w ' %{http_code}\\n' -X PATCH $NB/api/me/settings -H \"Authorization: Bearer $TOKEN\" -H 'content-type: application/json' -d '{\"notifications\":\"weird\"}'`; then `-d '{\"notifications\":\"sales\"}'`; then `-d '{\"notifications\":\"all\"}'` to restore",
   "Run `curl -si $N/api/info | grep -i access-control-allow-origin`"
  ],
  "expected": [
   "Step 1 prints `3.1.0 Ainize node API [{'url': 'http://localhost:3402'}] 89 True True` — 89 documented paths, 55 marketplace + 34 teach-mode (23 of teach mode v1 + 11 dataset routes). The two D3 live-test queue endpoints (/api/chat/status, /api/chat/cancel) and the two health probes (/healthz, /readyz) are part of the 55.",
   "Step 2 prints `['cli', 'node', 'openapi'] Use knowledge (one line)`",
   "Step 3 prints `4 pixelplus-087600 False ['Qwen3.8-Flash-Next'] ['krx-ticker-codes']` (attestation signatures are stripped from the catalog)",
   "Step 4: both return 400 with body `{\"error\":\"invalid request\",\"issues\":[…]}`",
   "Step 5 prints `LISTED True krx-all-2761-ep12 ['krx-all-2761-ep12', 'krx-all-2761-ep6', 'pixelplus-087600'] http://localhost:3402/x402/patch/krx-all-2761 True finance/KRX-latest`",
   "Step 6 prints `{\"error\":\"patch not found\"} 404` and `{\"error\":\"no patches for that benchmark schema\"} 404`",
   "Step 7 prints `4` and `2 AIN {'patches': 4, 'listed': 1, 'verifying': 0, 'superseded': 3, 'rejected': 0} 2`",
   "Step 8: every call returns `{\"error\":\"operator login required\"} 401`",
   "Step 9 returns `{\"error\":\"wrong password\"} 401`",
   "Step 10 prints `['address', 'app', 'balance', 'height', 'kind', 'network', 'provider', 'purchases', 'records', 'royalties', 'sales', 'valid']` (200 with bearer auth; the login response also set an httpOnly `ngram_session` cookie)",
   "Step 11 returns `{\"error\":\"operator password already set\"} 409`",
   "Step 12: `weird` → 400 `invalid request`; `sales` → 200 `{\"settings\":{\"notifications\":\"sales\",\"display_name\":\"node-b\",\"payout_address\":\"0x430b3A373fa4FF685ca40d03BA455c26c0959935\"}}`; final call restores `all`",
   "Step 13 shows `access-control-allow-origin: *`"
  ],
  "priority": "P0",
  "area": "api",
  "automation": "api",
  "evidence": [
   "packages/node/src/openapi.ts buildOpenApi (openapi 3.1.0, title 'Ainize node API', servers, 50 paths), CLI_REFERENCE.oneLiners.use.en",
   "packages/node/src/api.ts /api/openapi.json, /api/docs, /api/catalog (zod, sig stripped), /api/patches/:id, /api/benchmarks/:schema, /api/info, requireOperator 401, /api/auth/login 401, /api/auth/setup 409, /api/me/settings zod enum, error handler 400 'invalid request'",
   "packages/node/src/server.ts CORS origin ?? '*'"
  ]
 },
 {
  "id": "AZ-056",
  "persona": "Node operator / developer",
  "title": "Probe the seller gateway's X-PAYMENT validation, the 423 not-listed state and the gated blob download with curl",
  "goal": "A developer building a buyer can rely on the seller's exact payload error codes, the not-yet-listed response and the download gating (token or settled buyer only).",
  "preconditions": [
   "Demo cluster running on the AIN ledger; `N=http://localhost:3402; SHA=57c9346349afd6fa1b0475340b9b041ee0027753c05463d4bb204a5a4726c642` (krx-all-2761 body)",
   "krx-all-2761 LISTED (2/2); for the 423 step an item that is still ANNOUNCED/VERIFYING (right after a fresh cluster start, or the o08-pixel-copy publish of the announce scenario)",
   "For the last step: node-b has purchased krx-all-2761 (one-line `use` scenario) and `TOKEN` for node-b is available (login via POST /api/auth/login, or `~/.ngram-cluster/node-b/cli.json`)",
   "The 402 handshake itself (headers, nonce, 409/404) and forged/replayed proofs are covered by the agent scenarios"
  ],
  "steps": [
   "Run `curl -s -w ' %{http_code}\\n' -H 'x-payment: not-base64-json' $N/x402/patch/krx-all-2761`",
   "Run `curl -s -w ' %{http_code}\\n' -H \"x-payment: $(echo -n '{\"scheme\":\"paypal\"}' | base64 -w0)\" $N/x402/patch/krx-all-2761`",
   "Run `curl -s -w ' %{http_code}\\n' -H \"x-payment: $(echo -n '{\"scheme\":\"ain-transfer\"}' | base64 -w0)\" $N/x402/patch/krx-all-2761`",
   "Run `curl -s -w ' %{http_code}\\n' -H \"x-payment: $(echo -n '{\"scheme\":\"local-credit\",\"nonce\":\"deadbeef\",\"from\":\"0x430b3A373fa4FF685ca40d03BA455c26c0959935\",\"proof\":\"00\"}' | base64 -w0)\" $N/x402/patch/krx-all-2761`",
   "While an item is still VERIFYING run `curl -si $N/x402/patch/<that id> | sed -n '1p;$p'`",
   "Run `curl -s -w ' %{http_code}\\n' -o /dev/null $N/p2p/blob/$SHA`",
   "Run `DL=$(curl -s -H \"Authorization: Bearer $TOKEN\" http://localhost:3403/api/me/purchases | python3 -c \"import sys,json; print([p for p in json.load(sys.stdin)['items'] if p['patch_id']=='krx-all-2761'][0]['manifest']['download_token'])\")` then `curl -sI \"$N/p2p/blob/$SHA?token=$DL\" | sed -n '1p;/content-type/p;/x-content-sha256/p;/content-disposition/p'`"
  ],
  "expected": [
   "Step 1 returns `{\"error\":\"missing or malformed X-PAYMENT\"} 402`",
   "Step 2 returns `{\"error\":\"unsupported scheme paypal\"} 402`",
   "Step 3 returns `{\"error\":\"ain-transfer payload needs txHash\"} 402`",
   "Step 4 returns `{\"error\":\"unknown or expired nonce\"} 402` (local-credit is not the seller's scheme on the AIN ledger, but the single-use nonce check runs first; nonces expire after 10 minutes)",
   "Step 5 returns `HTTP/1.1 423 Locked` with `{\"error\":\"patch not listed yet (verification <passed>/2)\"}` — no price quote before quorum",
   "Step 6 returns 402 (`payment required: buy the patch via /x402/patch/:id (verifiers and authors are exempt)`)",
   "Step 7 prints `HTTP/1.1 200 OK`, `content-type: application/octet-stream`, `x-content-sha256: 57c93463…6c642` (equal to $SHA) and `content-disposition: attachment; filename=\"57c93463….npz\"` — the manifest's download_token unlocks the body for 24 h; no settle record is written by any of the failing steps (`ledger ls --kind settle` count unchanged)"
  ],
  "priority": "P0",
  "area": "x402",
  "automation": "api",
  "evidence": [
   "packages/node/src/api.ts GET /x402/patch/:id (402/423 branches), GET /p2p/blob/:sha (402 message, headers)",
   "packages/node/src/market.ts settlePayment() error strings ('missing or malformed X-PAYMENT', 'unsupported scheme', 'ain-transfer payload needs txHash', 'unknown or expired nonce'), issueManifest() download_token 24h, mayDownload()",
   "packages/node/src/store.ts nonces takeNonce",
   "packages/core/src/x402.ts header names"
  ]
 },
 {
  "id": "AZ-057",
  "persona": "Node operator / developer",
  "title": "Bring up a fourth node with `ainize init`, fund it on the local AIN chain, start it detached, peer it with the demo cluster and stop it",
  "goal": "An operator can create a node identity, join the existing 3-node network and control its lifecycle from the CLI without touching the web UI.",
  "preconditions": [
   "Demo cluster running (node-a :3402, node-b :3403, node-c :3404) on the local AIN chain (docker container ngram-ain SERVING on :8081); demo patches already LISTED/SUPERSEDED",
   "Shell prepared: `export PATH=\"$HOME/.local/node/bin:$PATH\"; cd /mnt/newdata/ainize/knowledge-marketplace; H=$HOME/.ngram-o01; D=\"node packages/cli/dist/bin.js --home $H\"`",
   "$H does not exist yet"
  ],
  "steps": [
   "Run `$D init --name node-d --port 3410 --ledger ain --ain-provider http://localhost:8081 --peer http://localhost:3402 --roles verifier --public-url http://localhost:3410`",
   "Run `$D init --name node-d --port 3410` a second time (no --force)",
   "Run `$D keys show`, then `$D keys backup <file> --passphrase e2e-passphrase` twice and `$D keys import <file> --passphrase e2e-passphrase`",
   "Run `$D chain fund $ADDR 100`",
   "Run `$D start -d`",
   "Run `$D start -d` again while the node is up",
   "Wait ~10 s, then run `$D status`",
   "Run `$D peers ls`",
   "Run `curl -s http://localhost:3402/api/nodes | python3 -c \"import sys,json; print([n['name'] for n in json.load(sys.stdin)['nodes']])\"`",
   "Run `$D stop`, then `$D stop` once more",
   "Cleanup: `rm -rf $H`"
  ],
  "expected": [
   "Step 1 prints `✓ node initialised at $H/config.json` with `name node-d`, `address 0x…` (20-byte hex), `port 3410`, `ledger ain`, `roles verifier`, followed by `next: \\`ainize start\\`   (then \\`ainize login\\`, \\`ainize seed\\`)`; exit code 0",
   "Step 2 fails with ``error: config already exists at $H/config.json — change one setting with `ainize config set <key> <value>`; `--force` rewrites the file (keeping this node's identity)``; exit code 1; config.json unchanged",
   "Step 3 prints `address` and `public key` lines plus the dim hint `add --reveal to print the private key, or \\`ainize keys backup <file>\\` to save it`; no private key is shown. `keys backup` writes `{kind:\"ainize-node-key\", version:1, address, publicKey, node, created_at, cipher:{alg:\"aes-256-gcm\", kdf:\"scrypt\", …}}` with mode 0600 and no `privateKey` field, and prints `encrypted (scrypt + aes-256-gcm) — without the passphrase this file is useless, including to you`; a second backup to the same path is refused (`already exists — pick another name`), and importing the key this node already has is refused with `<addr> is already this node's identity — nothing to do` before anything is touched",
   "Step 4 prints `✓ funded 0x… with 100 AIN  tx 0x…  balance now 100 AIN`",
   "Step 5 prints `✓ node started in the background (pid N) — port 3410` and `logs: $H/node.log   stop: ainize stop`; $H/node.pid contains N",
   "Step 6 fails with `error: node already running in the background (pid N) — \\`ainize stop\\` first`; exit code 1",
   "Step 7 shows `node-d  http://localhost:3410  (pid N)`, `roles       verifier`, `ledger      ain · ain:local · http://localhost:8081 · <n> records · height <h>` (same record count as `curl http://localhost:3402/api/info`), `peers       3` (the first gossip round's peer exchange with node-a added node-b and node-c — `peers 1` is only visible in the first second), `patches     4 (1 listed)`, `quorum      2`, `currency    AIN`",
   "Step 8 lists three rows: `http://localhost:3402  node-a  0xF7A9dE49…A1F5  seller,verifier,serving  <timestamp>  0` plus http://localhost:3403 and http://localhost:3404 (shown as `(unreached)` until their first hello succeeds)",
   "Step 9 output contains `node-d` (learned through /p2p/hello and the on-chain `node` record) next to node-a, node-b, node-c",
   "Step 10 first prints `✓ stopped node (pid N)`; the second call prints `no background node running for this NGRAM_HOME`; $H/node.pid is removed; port 3410 no longer answers"
  ],
  "priority": "P1",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/bin.ts (init/start/stop/status/keys/chain fund commands)",
   "packages/cli/src/commands/init.ts (init, requireConfig, keysShow messages)",
   "packages/cli/src/commands/node.ts (start/stop/status output, node.pid)",
   "packages/cli/src/commands/chain.ts (chainFund message)",
   "packages/node/src/api.ts GET /api/nodes, POST /p2p/hello; packages/node/src/p2p.ts peer exchange",
   "packages/node/src/market.ts registerSelf()"
  ]
 },
 {
  "id": "AZ-058",
  "persona": "Node operator / developer",
  "title": "Read node events with `ainize logs` filters and confirm `ainize seed` refuses to run against a live node",
  "goal": "An operator can audit what a node did (verification, trades, p2p) from the terminal and cannot corrupt a running node's data directory by seeding.",
  "preconditions": [
   "Demo cluster running; at least one purchase or verification has happened (default seeded state has attest events)",
   "Shell: `export PATH=\"$HOME/.local/node/bin:$PATH\"; cd /mnt/newdata/ainize/knowledge-marketplace; A=\"node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-a\"; B=\"node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-b\"`"
  ],
  "steps": [
   "Run `$A seed`",
   "Run `$B logs --kind verify --limit 20`",
   "Run `$A logs --patch krx-all-2761 --limit 20`",
   "Run `$A logs --limit 3 --json`",
   "Run `$A logs --follow` in a second terminal, then in the first terminal run `curl -s -X POST http://localhost:3402/api/chat -H 'content-type: application/json' -d '{\"patch_id\":\"pixelplus-087600\",\"mode\":\"base\",\"max_tokens\":4,\"messages\":[{\"role\":\"user\",\"content\":\"hi\"}]}'`; then Ctrl+C the follower",
   "Run `$A logs --kind nosuchkind`",
   "Run `$A logs --kind patch --limit 5` from the logged-in operator home, then the same command from a home with no operator token (`--node http://localhost:3402 --home <empty home>`)",
   "Run `$A logs --level warn --limit 5` and `$A logs --kind challenge`"
  ],
  "expected": [
   "Step 1 fails with `error: a node is running at http://localhost:3402; seeding writes to its data directory — stop it first (\\`ainize stop\\`) or seed from the web console`; exit 1; catalog unchanged",
   "Step 2 prints lines formatted `<YYYY-MM-DD HH:MM:SS> info  verify    [krx-all-2761] attested krx-all-2761: PASS (vllm:Qwen3.8-Flash-Next)` (kind column padded to 9, patch id in brackets)",
   "Step 3 shows only events whose patch id is krx-all-2761 (e.g. `verifier`, `verify`, `publish`, `trade`, `usage` kinds)",
   "Step 4 prints a JSON array of at most 3 objects with keys `seq, ts, level, kind, patch_id, message, data`",
   "Step 5: within ~2 s the follower prints a new line `usage     [pixelplus-087600] live test pixelplus-087600 (base) by ip:::ffff:127.0.0.1: base only` (visitor key 'ip:' + req.ip, truncated to 24 chars); Ctrl+C exits cleanly",
   "Step 6 fails with `error: Invalid values:` / `  Argument: kind, Given: \"nosuchkind\", Choices: \"blob\", \"branch\", \"buy\", \"challenge\", \"config\", \"drive\", \"node\", \"p2p\", \"patch\", \"payout\", \"publish\", \"runtime\", \"seed\", \"settings\", \"teach\", \"trade\", \"usage\", \"verifier\", \"verify\"`; exit 1 — an unrecognised kind is never answered with an empty screen",
   "Step 7: the operator's terminal shows `patch` events including `draft created: …` lines (the CLI sends its operator token); the same command with no token prints `(no events match kind 'patch' — and you are not logged in, so teach and draft lines are hidden; run `ainize login`)` — the visitor stream drops every `draft …` line by design",
   "Step 8: `--level warn` prints only `warn` and `error` rows (the level is a floor, not an exact match); a filter that matches nothing prints `(no events match kind 'challenge')`, never a blank line"
  ],
  "priority": "P1",
  "area": "logs",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/node.ts logs()/seed()",
   "packages/node/src/api.ts GET /api/events, GET /api/patches/:id/events",
   "packages/node/src/market.ts log() kinds (verify, usage, trade, buy, publish)"
  ]
 },
 {
  "id": "AZ-059",
  "persona": "Node operator / developer",
  "title": "Add, list and remove peers on a node and watch gossip discover the other nodes",
  "goal": "An operator can wire a node into the P2P mesh and prune it, with the change persisted to config.json.",
  "preconditions": [
   "node-d from the fourth-node scenario running detached on :3410 with only node-a as seed peer (`H=$HOME/.ngram-o01; D=\"node packages/cli/dist/bin.js --home $H\"`); `$D login --password demo-pass-1234` done (sets node-d's operator password); run the steps right after start, before gossip adds the other peers",
   "Demo cluster running; node-b address from `$D nodes` (currently 0x430b3A37…9935)"
  ],
  "steps": [
   "Run `$D peers ls`",
   "Run `$D peers add localhost:3403`",
   "Run `$D peers add http://localhost:3403/`",
   "Wait ~8 s (two gossip ticks) and run `$D peers ls`",
   "Run `$D config show | grep -A3 '\"peers\"'`",
   "Run `$D nodes`",
   "Run `$D peers rm http://localhost:3403`, wait ~8 s, run `$D peers ls`",
   "Run `curl -s -o /dev/null -w '%{http_code}\\n' -X POST http://localhost:3410/api/peers -H 'content-type: application/json' -d '{\"endpoint\":\"http://localhost:3404\"}'` (no auth; any demo node such as http://localhost:3403/api/peers behaves the same)"
  ],
  "expected": [
   "Step 1 shows the seed peer row `http://localhost:3402  node-a …` (plus :3403/:3404 if gossip already ran)",
   "Step 2 fails with `error: endpoint must be an http(s) URL`",
   "Step 3 prints `✓ peer added: http://localhost:3403/` (echoed as typed; the trailing slash is stripped in the request)",
   "Step 4 lists both `http://localhost:3402 node-a` and `http://localhost:3403 node-b  0x430b3A37…9935  verifier` with a fresh LAST SEEN and FAILURES 0 (NAME is `(unreached)` only until the first hello succeeds)",
   "Step 5 shows `\"peers\": [\"http://localhost:3402\", \"http://localhost:3403\"]` persisted",
   "Step 6 prints `known nodes` (columns NAME ADDRESS ENDPOINT ROLES LEDGER BRANCHES BLOBS LAST SEEN; node-a, node-b, node-c, `node-d (self)`) and `configured peers` (ENDPOINT ADDRESS LAST SEEN FAILURES) tables",
   "Step 7 prints `✓ peer removed: http://localhost:3403`; afterwards `peers ls` no longer lists :3403 unless the peer exchange re-learns it, while :3402 remains",
   "Step 8 returns 401 (`{\"error\":\"operator login required\"}`) — peer management needs the operator session"
  ],
  "priority": "P1",
  "area": "network",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/peers.ts (peersLs/peersAdd/peersRm messages)",
   "packages/cli/src/commands/node.ts nodesTable()",
   "packages/node/src/api.ts POST/DELETE /api/peers (saveConfig), GET /api/nodes, POST /p2p/hello",
   "packages/node/src/p2p.ts gossip round (gossipIntervalMs 4000)"
  ]
 },
 {
  "id": "AZ-060",
  "persona": "Node operator / developer",
  "title": "Inspect the catalog with `patch ls`, `patch get`, `patch records` and `patch conflicts`",
  "goal": "An operator can see verification evidence, lineage, address-set overlaps and ledger history of any knowledge from the terminal.",
  "preconditions": [
   "Demo cluster running with the seeded catalog (krx-all-2761 LISTED; krx-all-2761-ep12, krx-all-2761-ep6, pixelplus-087600 SUPERSEDED)",
   "Shell: `export PATH=\"$HOME/.local/node/bin:$PATH\"; cd /mnt/newdata/ainize/knowledge-marketplace; A=\"node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-a\"`",
   "Addresses from GET /api/nodes (currently node-a 0xF7A9…A1F5, node-b 0x430b3A37…, node-c 0x199Ff870…)"
  ],
  "steps": [
   "Run `$A patch ls`",
   "Run `$A patch ls --status LISTED` and `$A patch ls --q pixel`",
   "Run `$A patch ls --sort price --json | python3 -c \"import sys,json; print([e['anchor']['id'] for e in json.load(sys.stdin)])\"`",
   "Run `$A patch get krx-all-2761`",
   "Run `$A patch get pixelplus-087600`",
   "Run `$A patch conflicts krx-all-2761`",
   "Run `$A patch records krx-all-2761`",
   "Run `$A patch get does-not-exist`"
  ],
  "expected": [
   "Step 1 prints a table with columns `ID STATUS AUTHOR MODEL ROWS SIZE PRICE ATTEST SOLD BENCHMARK` and 4 rows: `krx-all-2761 LISTED node-a 0xF7A9…A1F5 Qwen3.8-Flash-Next 270,053 331.7 MB 25 AIN 2/2 … krx-ticker-codes`, `krx-all-2761-ep12 SUPERSEDED … 10 AIN 2/2`, `krx-all-2761-ep6 SUPERSEDED … 5 AIN 2/2`, `pixelplus-087600 SUPERSEDED … 2,992 3.7 MB 0.1 AIN 2/2`",
   "Step 2: the first call lists only krx-all-2761; the second only pixelplus-087600",
   "Step 3 prints `['pixelplus-087600', 'krx-all-2761-ep6', 'krx-all-2761-ep12', 'krx-all-2761']` (ascending price)",
   "Step 4 shows the bold name + `LISTED  (yours)`, key/values `id krx-all-2761`, `price 25 AIN · per_download`, `benchmark krx-ticker-codes · 2761 queries · template/chat · collateral ≤ 0.08 nat`, `gateway http://localhost:3402/x402/patch/krx-all-2761`, `verification 2/2 passed ✓ quorum`, `body on this node yes`; an `attestations` table (VERIFIER RESULT SCORE VERIFIED ON RESTARTS COUNTS AT) with two rows (node-b, node-c) `PASS  free_generation=26/26 pre_apply=1/8  vllm:Qwen3.8-Flash-Next  0  yes` — the STAKE column is gone because no deposit was ever escrowed (critique 2 item 127), and COUNTS says whether the result counted toward the quorum (`no — self-check` for an attestation by the anchor's own author); `lineage` with `parents : krx-all-2761-ep12 (SUPERSEDED)` and `supersedes: krx-all-2761-ep12, krx-all-2761-ep6, pixelplus-087600`; an `address-set overlaps (A₁ ∩ A₂)` table; `branches` listing `finance/KRX-latest {\"market\":\"KRX\",\"version\":\"latest\"}`",
   "Step 5 shows `SUPERSEDED` and the warning line `superseded by: krx-all-2761`",
   "Step 6 prints three rows: `krx-all-2761-ep12 241,992 yes SUPERSEDED`, `krx-all-2761-ep6 241,992 yes SUPERSEDED`, `pixelplus-087600 2,170 yes SUPERSEDED`",
   "Step 7 lists ledger records of kinds `anchor` (author node-a 0xF7A9…), two `attest` (authors node-b 0x430b3A… and node-c 0x199Ff8…) and three `supersede` rows with a HASH column",
   "Step 8 fails with `error: patch not found`; exit 1"
  ],
  "priority": "P1",
  "area": "patch",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/patch.ts patchLs/patchGet/patchConflicts/patchRecords",
   "packages/node/src/api.ts GET /api/catalog, /api/patches/:id, /api/patches/:id/records, /api/patches/:id/conflicts",
   "packages/node/src/market.ts conflicts()"
  ]
 },
 {
  "id": "AZ-061",
  "persona": "Node operator / developer",
  "title": "Register a draft with `ainize publish --no-announce`, check its visibility, reject bad inputs and delete it",
  "goal": "A creator can stage knowledge locally, see overlaps before anything reaches the ledger, and clean up without leaving traces.",
  "preconditions": [
   "Demo cluster running; node-a operator logged in from the CLI (`$A login --password <node-a password>`; node-a's password is the one set on first visit to http://localhost:3402/signing, or set it now if `/api/auth/me` says needsSetup)",
   "Real body file exists on the node machine: /mnt/newdata/qwen3.8/results/train-fact/픽셀플러스.npz",
   "Shell: `export PATH=\"$HOME/.local/node/bin:$PATH\"; cd /mnt/newdata/ainize/knowledge-marketplace; A=\"node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-a\"; NPZ=/mnt/newdata/qwen3.8/results/train-fact/픽셀플러스.npz; BENCH='{\"schema\":\"krx-ticker-codes\",\"queries\":1,\"format\":[\"template\"],\"samples\":[{\"prompt\":\"종목코드 픽셀플러스 \",\"expect\":\"087600\"}]}'`"
  ],
  "steps": [
   "Run `$A publish ./missing.npz --name x --model Qwen3.8-Flash-Next --benchmark \"$BENCH\"`",
   "Run `$A publish README.md --name x --model Qwen3.8-Flash-Next --benchmark \"$BENCH\"`",
   "Run `$A publish $NPZ --name x --model Qwen3.8-Flash-Next --benchmark '{\"queries\":1}'`",
   "Run `$A publish $NPZ --id o06-draft --name \"O06 draft test\" --model Qwen3.8-Flash-Next --benchmark \"$BENCH\" --price 0.1 --no-announce`",
   "Run `$A patch ls` and `$A patch ls --drafts`",
   "Run `curl -s -o /dev/null -w '%{http_code}\\n' http://localhost:3402/api/patches/o06-draft`",
   "Run `$A patch conflicts o06-draft`",
   "Run `$A use o06-draft`",
   "Run `$A patch rm o06-draft` then `$A patch ls --drafts`"
  ],
  "expected": [
   "Step 1 fails with `error: file not found: /mnt/newdata/ainize/knowledge-marketplace/missing.npz`",
   "Step 2 fails with `error: patch body must be a .npz (addrs/before/after arrays)`",
   "Step 3 fails with `error: benchmark.schema is required (e.g. \"krx-ticker-codes\")`",
   "Step 4 prints `✓ draft created: o06-draft  (2,992 rows, sha256 62d0978ccbf0…)` followed by `announce when ready: ainize patch announce o06-draft`; no ledger record is written (`$A ledger ls --kind anchor` count unchanged)",
   "Step 5: plain `patch ls` does not contain o06-draft; `--drafts` shows `o06-draft  DRAFT  node-a … 2,992  3.7 MB  0.1 AIN  0/2`",
   "Step 6 returns 404 (drafts are invisible to non-operators)",
   "Step 7 lists `pixelplus-087600  2,992  yes  SUPERSEDED` (identical body → full overlap) and `krx-all-2761  2,170  yes  LISTED`, plus rows for krx-all-2761-ep12 and krx-all-2761-ep6 (node-a holds all four bodies; counts from live data)",
   "Step 8 fails with `error: o06-draft is DRAFT (verification 0/2) — not verified yet; try \\`ainize patch get o06-draft\\``",
   "Step 9 prints `✓ draft o06-draft deleted`; `--drafts` no longer lists it"
  ],
  "priority": "P1",
  "area": "new-patch",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/patch.ts patchPublish/patchRm/patchUse validation messages",
   "packages/cli/src/bin.ts publish one-liner (--announce default true, --no-announce)",
   "packages/node/src/api.ts POST /api/patches (path on node), GET /api/patches/:id (DRAFT → 404 for non-operator), DELETE /api/patches/:id",
   "packages/node/src/market.ts createDraft(), conflicts()"
  ]
 },
 {
  "id": "AZ-062",
  "persona": "Node operator / developer",
  "title": "Use a SUPERSEDED knowledge with `ainize use --no-apply` and get the newer-version note",
  "goal": "A buyer of a point-in-time version is warned that a newer version exists but can still download it without loading it into the model.",
  "preconditions": [
   "node-b operator logged in with ≥ 0.1 AIN (O-02); pixelplus-087600 not yet purchased by node-b (`$B patch get pixelplus-087600 | head -1` shows no `purchased`)",
   "node-b already holds the pixelplus body (verifier), so no download happens"
  ],
  "steps": [
   "Run `$B patch get pixelplus-087600 | grep -E 'SUPERSEDED|superseded by'`",
   "Run `$B use pixelplus-087600 --no-apply`",
   "Run `$B patch get pixelplus-087600 | head -1`",
   "Run `curl -s http://localhost:3403/api/runtime | python3 -c \"import sys,json; print(json.load(sys.stdin)['applied'])\"`",
   "Run `$B patch apply pixelplus-087600` then `$B patch remove pixelplus-087600`",
   "Run `curl -s 'http://localhost:3402/api/catalog?status=SUPERSEDED' | python3 -c \"import sys,json; print([(e['anchor']['id'], e['downloads']) for e in json.load(sys.stdin)['items']])\"`"
  ],
  "expected": [
   "Step 1 shows the status `SUPERSEDED` and the warning `superseded by: krx-all-2761` (it may list every newer same-subject version, e.g. `krx-all-2761, krx-all-2761-ep12, krx-all-2761-ep6` — match on the prefix)",
   "Step 2 first prints the dim note `note: a newer version exists on the same subject → krx-all-2761 … (newer version available)`, then `✓ bought pixelplus-087600 for 0.1 (ain-transfer)  tx 0x…` with steps quorum/402 (`0.1 AIN`)/pay/settled/download (`body already present; sha256 matches on-ledger anchor`)/receipt but no `apply` step, and ends with `downloaded — load with: ainize patch apply pixelplus-087600`",
   "Step 3 header shows `SUPERSEDED  purchased` (not `applied`)",
   "Step 4 prints `[]` — nothing was loaded into the shared model",
   "Step 5 prints `✓ applied pixelplus-087600: …` then `✓ removed pixelplus-087600: …`",
   "Step 6 shows pixelplus-087600 with downloads incremented by 1 — SUPERSEDED knowledge remains purchasable"
  ],
  "priority": "P1",
  "area": "x402",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/patch.ts patchUse (SUPERSEDED accepted; superseded_by note; --no-apply hint)",
   "packages/cli/src/bin.ts use command (--apply default true)",
   "packages/node/src/market.ts buy() (quorum_ok is the only gate; status not checked)"
  ]
 },
 {
  "id": "AZ-063",
  "persona": "Node operator / developer",
  "title": "Audit the shared ledger with `ledger ls`, `ledger verify`, `ledger graph` and `ledger export`, and cross-check two nodes",
  "goal": "An operator can prove that provenance records are intact and identical across nodes and take a machine-readable copy.",
  "preconditions": [
   "Demo cluster running on the AIN ledger (record count is a live value: 10 right after a fresh start, ~20 once attestations/supersedes landed)",
   "Shell: `A=\"node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-a\"; OUT=/tmp/ainize-ledger.jsonl; mkdir -p /tmp/ainize-anon`"
  ],
  "steps": [
   "Run `$A ledger ls --limit 10`",
   "Run `$A ledger ls --kind attest` and `$A ledger ls --kind supersede`",
   "Run `$A ledger verify` and `curl -s http://localhost:3402/api/ledger/verify`",
   "Run `$A ledger graph`",
   "Run `$A ledger export $OUT`; then `wc -l $OUT` and `head -1 $OUT | python3 -c \"import sys,json; r=json.load(sys.stdin); print(sorted(r)[:6], r['kind'])\"`",
   "Run `node packages/cli/dist/bin.js --node http://localhost:3403 --home /tmp/ainize-anon ledger ls --limit 1 | head -3` and compare the `records` count with node-a",
   "Run `$A ledger ls --kind bogus`, then `$A ledger ls --kind subscribe --limit 1` and `$A --node localhost:3402 status`"
  ],
  "expected": [
   "Step 1 prints `ledger   ain · ain:local · http://localhost:8081`, `records  <n>`, `height   <h>`, `head     -` and a table `AT KIND AUTHOR SUMMARY HASH`",
   "Step 2: attest rows read `krx-all-2761 · PASS · vllm:Qwen3.8-Flash-Next` (one per verifier per patch); supersede rows read `krx-all-2761 supersedes pixelplus-087600 (2170 rows)`, `krx-all-2761 supersedes krx-all-2761-ep6 (241992 rows)`, `krx-all-2761 supersedes krx-all-2761-ep12 (241992 rows)`",
   "Step 3 prints `✓ ledger valid — <n> record(s) checked (hashes, signatures, imported chain linkage)`; the API returns `{\"valid\":true,\"checked\":<n>,\"errors\":[]}` with the same n",
   "Step 4 prints `lineage (child → parent edges, royalties flow upward)` and the tree `krx-all-2761-ep6 [Qwen3.8-Flash-Next · krx-ticker-codes] SUPERSEDED` → `└─ krx-all-2761-ep12 … SUPERSEDED` → (indented one level further) `└─ krx-all-2761 … LISTED  supersedes krx-all-2761-ep12, krx-all-2761-ep6, pixelplus-087600`, plus a separate root `pixelplus-087600 … SUPERSEDED`",
   "Step 5 prints `✓ exported <n> record(s) to /tmp/ainize-ledger.jsonl` where n is the node's whole `records` count — the CLI asks how big the ledger is and requests exactly that many (it used to ask for a fixed 1,000 and printed 'exported 1000 record(s)' once the chain grew past it). Past the node's per-request maximum (5,000) it says so instead: `exported the most recent 5000 of <n> record(s) … cannot page further back`. `wc -l` equals n; each line is one JSON record whose keys are `author, body, hash, kind, parents, sig, ts` (sorted(r)[:6] → ['author','body','hash','kind','parents','sig']), oldest first",
   "Step 6: node-b reports the same `records` count and network `ain:local` — one shared chain, no divergence",
   "Step 7 fails with `error: Invalid values:` / `  Argument: kind, Given: \"bogus\", Choices: \"anchor\", \"attest\", \"settle\", \"challenge\", \"branch\", \"node\", \"supersede\", \"subscribe\"`, exit 1 — an unrecognised kind is never answered with `ledger is empty` two lines under a four-figure record count. A kind that is real but matches nothing prints `no records of kind 'subscribe' (<n> record(s) in the ledger)`. And `--node localhost:3402` — a scheme-less URL — fails with `error: --node must be a full URL — did you mean http://localhost:3402?` (exit 2) instead of reporting a healthy node as unreachable"
  ],
  "priority": "P1",
  "area": "ledger",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/ledger.ts ledgerLs/ledgerVerify/ledgerGraph/ledgerExport (exact strings)",
   "packages/node/src/api.ts GET /api/ledger, /api/ledger/verify, /api/ledger/graph",
   "Live probe: /api/ledger/verify → {valid:true, checked:<n>, errors:[]}"
  ]
 },
 {
  "id": "AZ-064",
  "persona": "Node operator / developer",
  "title": "Create a branch, add knowledge, subscribe a node and route `jurisdiction=KR` to it",
  "goal": "An operator can group knowledge into a context-tagged branch and have the gateway resolve a request context to the serving nodes that subscribe to it.",
  "preconditions": [
   "node-a operator logged in (`A=…node-a`), runtime available; branch `law/KR` does not exist yet (`$A branch ls` shows only the two seeded tracks finance/KRX-latest and finance/KRX-history)",
   "node-b operator logged in (`B=…node-b`) for the ownership check",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)",
   "Tester accepts permanent branch/subscribe records on the local dev chain"
  ],
  "steps": [
   "Run `$A route jurisdiction=KR`",
   "Run `$A branch create 'bad name!' --context jurisdiction=KR`",
   "Run `$A branch create law/KR --description \"O14 test branch\" --context jurisdiction=KR`",
   "Run `$A branch add law/KR pixelplus-087600`",
   "Run `$B branch add law/KR krx-all-2761` (node-b is not the owner)",
   "Run `$A route jurisdiction=KR`",
   "Run `$A branch subscribe law/KR`",
   "Run `$A route jurisdiction=KR` and `$A branch ls`",
   "Run `$B branch ls` (node-b view) and `$A status | grep branches`",
   "Run `$A route jurisdiction=US` and `$A route jurisdiction`",
   "Run `$A branch unsubscribe law/KR` then `$A route jurisdiction=KR`"
  ],
  "expected": [
   "Step 1 prints `no branch matches {\"jurisdiction\":\"KR\"}` (the seeded tracks only carry market/version)",
   "Step 2 fails with `error: invalid branch name`",
   "Step 3 prints `✓ branch law/KR created {\"jurisdiction\":\"KR\"} with 0 patch(es)`; `$A ledger ls --kind branch --limit 1` shows `law/KR · 0 patch(es)`",
   "Step 4 prints `✓ pixelplus-087600 added to law/KR (1 patches)`",
   "Step 5 fails with `error: only the branch owner can add patches`",
   "Step 6 prints `context {\"jurisdiction\":\"KR\"} → branch law/KR {\"jurisdiction\":\"KR\"}` followed by `no node currently subscribes to that branch`",
   "Step 7 prints `✓ subscribed law/KR  (patches acquired and applied when a runtime is available)`; `$A logs --kind runtime --limit 2` shows `applied pixelplus-087600: …` (node-a authored it, so no purchase was needed)",
   "Step 8: route now shows a table `SERVING NODE node-a  ENDPOINT http://localhost:3402  MODEL Qwen3.8-Flash-Next  ADDRESS 0xF7A9dE49…A1F5`; `branch ls` shows three rows including `law/KR ✓` with CONTEXT `jurisdiction=KR`, PATCHES `pixelplus-087600`, SUBSCRIBERS `node-a`, and the legend `✓ = this node subscribes`",
   "Step 9: node-b's `branch ls` lists law/KR with SUBSCRIBERS `node-a` (without the ✓); node-a `status` prints `branches    law/KR`",
   "Step 10: `jurisdiction=US` → `no branch matches {\"jurisdiction\":\"US\"}`; bare `jurisdiction` → `error: context must be key=value, got \"jurisdiction\"`",
   "Step 11 prints `✓ unsubscribed law/KR`, pixelplus-087600 is removed from the runtime (`/api/runtime` applied is `[]`), and route again says `no node currently subscribes to that branch`"
  ],
  "priority": "P1",
  "area": "network",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/branch.ts branchCreate/branchAdd/branchSubscribe/route/parseContext strings",
   "packages/node/src/market.ts createBranch() name regex, addToBranch() owner check, subscribe() apply/remove, route()",
   "packages/node/src/api.ts /api/branches, /api/route, /api/branches/:name/subscribe"
  ]
 },
 {
  "id": "AZ-065",
  "persona": "Node operator / developer",
  "title": "Operate the local AIN chain from the CLI: `chain status`, `chain up`, `chain fund`, `chain setup` and `wallet`",
  "goal": "An operator can confirm the payment rail is healthy, top up accounts on the dev chain and see balances without leaving the terminal.",
  "preconditions": [
   "Docker container ngram-ain running and SERVING on :8081; demo cluster on the AIN ledger",
   "node-a operator logged in (`A=…node-a`)",
   "Addresses are live values: node-b = `curl -s http://localhost:3403/api/chain` address (currently 0x430b3A373fa4FF685ca40d03BA455c26c0959935), app admin = node-a's address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5); set `NB=<node-b address>`"
  ],
  "steps": [
   "Run `$A chain status`",
   "Run `$A chain up`",
   "Note `curl -s http://localhost:3403/api/chain`, then run `$A chain fund $NB 10`",
   "Run `$A chain fund notanaddress`",
   "Run `$A chain fund $NB 1 --provider http://example.com:8081`",
   "Run `$A chain setup`",
   "Run `$A wallet` and `curl -s http://localhost:3402/api/chain`",
   "Run `$A chain status --provider http://localhost:9`"
  ],
  "expected": [
   "Step 1 prints `provider    http://localhost:8081`, `reachable   yes`, `state       SERVING`, `health      true`, `validator   0x00ADEc28B6a845a085e03591bE7550dd68673C1C`, `last block  <n>`, `container   ngram-ain: running`",
   "Step 2 prints `✓ a local AIN chain is already SERVING on :8081 (validator 0x00ADEc28B6a845a085e03591bE7550dd68673C1C, block <n>) — nothing to do` and does not start a second container",
   "Step 3 prints `✓ funded <node-b full address> with 10 AIN  tx 0x…  balance now <previous+10> AIN`; `/api/chain` on :3403 reports the new balance",
   "Step 4 fails with `error: address must be a 0x-prefixed 20-byte hex address`",
   "Step 5 fails with `error: refusing to use the genesis key against a non-local chain (http://example.com:8081)`",
   "Step 6 prints `✓ knowledge app already exists (admin <node-a full address>) — rules refreshed if we are admin` (the cluster created /apps/knowledge at boot)",
   "Step 7: wallet shows `ledger      ain · ain:local`, `balance     <n> AIN`, `sales <count>`, `royalties received <count>`, `purchases <count>`; the API returns `{\"kind\":\"ain\",\"network\":\"ain:local\",\"provider\":\"http://localhost:8081\",\"app\":\"/apps/knowledge\",\"height\":…,\"records\":…,\"valid\":true,\"address\":\"0xF7A9dE49…\",\"balance\":<n>}`",
   "Step 8 prints `reachable   no`, `state       -`, `health      -` for the unreachable provider while still reporting `container   ngram-ain: running`"
  ],
  "priority": "P1",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/chain.ts chainStatus/chainUp/chainFund/chainSetup (assertLocal, GENESIS validator)",
   "packages/cli/src/commands/branch.ts wallet()",
   "packages/node/src/api.ts GET /api/chain, GET /api/me/wallet, POST /api/chain/setup",
   "scripts/cluster.mjs setupApp at boot"
  ]
 },
 {
  "id": "AZ-066",
  "persona": "Node operator / developer",
  "title": "Exhaust the anonymous live-test quota (20/hour per IP) via POST /api/chat and confirm operators are unmetered and failed calls are not charged",
  "goal": "An operator can verify that free trials are capped per visitor while a logged-in operator and error responses do not consume tries.",
  "preconditions": [
   "Demo cluster running; no other anonymous live test from this machine's IP in the last hour (otherwise the counter starts lower)",
   "Operator bearer TOKEN for node-a (`curl -X POST http://localhost:3402/api/auth/login …` with node-a's password)",
   "Tester accepts that anonymous live tests from this IP (including the web UI's Live test page) are blocked on node-a for up to one hour afterwards",
   "`N=http://localhost:3402; BODY='{\"patch_id\":\"pixelplus-087600\",\"mode\":\"base\",\"max_tokens\":4,\"messages\":[{\"role\":\"user\",\"content\":\"hi\"}]}'`"
  ],
  "steps": [
   "Run `curl -s -w ' %{http_code}\\n' -X POST $N/api/chat -H 'content-type: application/json' -d '{\"patch_id\":\"pixelplus-087600\",\"messages\":[]}'`",
   "Run `curl -s -w ' %{http_code}\\n' -X POST $N/api/chat -H 'content-type: application/json' -d '{\"patch_id\":\"no-such-patch\",\"messages\":[{\"role\":\"user\",\"content\":\"hi\"}]}'`",
   "Run `curl -s -X POST $N/api/chat -H 'content-type: application/json' -d \"$BODY\" | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d['remaining_quota'], d['quota_limit'], d['patched'], d['base'] is not None)\"`",
   "Run `for i in $(seq 1 19); do curl -s -X POST $N/api/chat -H 'content-type: application/json' -d \"$BODY\" | python3 -c \"import sys,json; print(json.load(sys.stdin)['remaining_quota'])\"; done`",
   "Run `curl -s -w ' %{http_code}\\n' -X POST $N/api/chat -H 'content-type: application/json' -d \"$BODY\"`",
   "Run `curl -s -X POST $N/api/chat -H 'content-type: application/json' -H \"Authorization: Bearer $TOKEN\" -d \"$BODY\" | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d['remaining_quota'], d['quota_limit'])\"`",
   "Run `curl -s -w ' %{http_code}\\n' -X POST http://localhost:3403/api/chat -H 'content-type: application/json' -d \"$BODY\" | tail -c 40`",
   "Run `node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-a logs --kind usage --limit 3`"
  ],
  "expected": [
   "Step 1 returns 400 `{\"error\":\"invalid request\",\"issues\":[…]}` (messages must have ≥ 1 entry)",
   "Step 2 returns `{\"error\":\"patch not found\"}` with status 500 (a thrown market error, not an HttpError — note: 404 would be the accurate code; report as a finding)",
   "Step 3 prints `19 20 None True` — the first successful anonymous call leaves 19, base-only mode returns no `patched` answer; steps 1–2 did not consume a try",
   "Step 4 prints 18, 17, … down to 0",
   "Step 5 returns `{\"error\":\"quota_chat: free live-test quota exhausted for this hour — buy the patch or run your own node\"} 429`",
   "Step 6 prints `None None` — the operator session is unmetered even from the same IP",
   "Step 7 returns 200 on node-b (quota is per node process, in memory) with `\"quota_limit\":20`",
   "Step 8 shows `usage     [pixelplus-087600] live test pixelplus-087600 (base) by ip:… : base only` entries",
   "The same quota applies per POST regardless of whether the body uses `patch_id` or `patch_ids` (AZ-101)"
  ],
  "priority": "P1",
  "area": "api",
  "automation": "api",
  "evidence": [
   "packages/node/src/api.ts POST /api/chat (zod min(1) messages, chatQuota check without consume → 429 text, remaining_quota/quota_limit, visitor ip: vs operator:)",
   "packages/node/src/market.ts chatQuota() (limit 20, 1 h window, in-memory Map), chat() 'patch not found' throw → 500 via generic handler",
   "packages/node/src/openapi.ts /api/chat 429 description"
  ]
 },
 {
  "id": "AZ-067",
  "persona": "Node operator / developer",
  "title": "Restart the demo cluster with scripts/cluster-restart.sh and confirm data survives, peers re-gossip and the agent buyer still completes a 402 purchase",
  "goal": "An operator can bounce all three nodes safely and prove the marketplace (ledger, catalog, verification state, autonomous agent flow) comes back intact.",
  "preconditions": [
   "Demo cluster running; `export PATH=\"$HOME/.local/node/bin:$PATH\"; cd /mnt/newdata/ainize/knowledge-marketplace`",
   "Record before: `curl -s http://localhost:3402/api/info | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d['ledger']['records'], d['counts'])\"`",
   "Nobody else is using the demo during the ~1 minute restart"
  ],
  "steps": [
   "Run `scripts/cluster-restart.sh` (without --fresh)",
   "Run `tail -f ~/.ngram-cluster/cluster.log` until the line `[cluster] web UI → http://localhost:3402   (B: 3403, C: 3404; homes under /home/<user>/.ngram-cluster; ledger=ain)` appears, then Ctrl+C",
   "Run `for p in 3402 3403 3404; do curl -s -o /dev/null -w \"$p %{http_code}\\n\" http://localhost:$p/api/info; done`",
   "Re-run the 'before' command and `node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-a patch ls`",
   "Wait ~10 s and run `node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-a peers ls`",
   "Run `node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-a status`",
   "Run `cat ~/.ngram-cluster/nodes.pid | wc -l` and `cat ~/.ngram-cluster/supervisor.pid`",
   "Run `node packages/agent/dist/bin.js keys` and fund it: `node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-a chain fund <agent address> 5`",
   "Run `node packages/agent/dist/bin.js catalog`",
   "Run `node packages/agent/dist/bin.js run --market http://localhost:3402 --patch pixelplus-087600 --pay ain-transfer --json | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d['success'], d['patch_id'], d['scheme'], d['already_known'], d['restored'], d['steps'][-1])\"`"
  ],
  "expected": [
   "Step 1 prints one or more `stopped <pid>` lines followed by `cluster starting (log: /home/<user>/.ngram-cluster/cluster.log)`; exit 0",
   "Step 2: the log shows `[node-a] … [node-b] … [node-c] …` boot lines and the web UI line; a single `retrying in 3 s` line is acceptable (EADDRINUSE while the old port is released — the supervisor only respawns a child that exits within 15 s of start)",
   "Step 3 prints `3402 200`, `3403 200`, `3404 200` within ~30 s",
   "Step 4: `records` and `counts` are unchanged (`{'patches': 4, 'listed': 1, 'verifying': 0, 'superseded': 3, 'rejected': 0}` plus anything added by other scenarios); `patch ls` shows the same 4+ rows with identical statuses and ATTEST `2/2` — nothing was re-seeded (`~/.ngram-cluster/node-a/.seeded` marker respected)",
   "Step 5 shows node-b and node-c rows with a fresh LAST SEEN and FAILURES `0`",
   "Step 6 shows `runtime     available · Qwen3.8-Flash-Next · hook ok` and `peers       2` (the serving model was not restarted by the script)",
   "Step 7: nodes.pid has 3 lines; supervisor.pid holds the pid of `node scripts/cluster.mjs`",
   "Step 8 prints `address     0x…`, `publicKey   …`, `home        /home/<user>/.ngram-agent`; funding prints `✓ funded 0x… with 5 AIN …`",
   "Step 9 prints `krx-all-2761             LISTED       270053 rows  25 AIN  attest 2/2  KRX ticker codes for 2,761 listed companies (final)` (LISTED only by default)",
   "Step 10: since pixelplus-087600 is SUPERSEDED and `--patch` does not follow supersede marks, the agent refuses: stderr `agent failed: patch pixelplus-087600 is SUPERSEDED, not LISTED — refusing to buy` and exit 1 — so use `--patch krx-all-2761` (needs ≥ 25 AIN) for the success check: `True krx-all-2761 ain-transfer False True result: SUCCESS — the 402 purchase loop completed` (if the shared model already answers 087600, `already_known` is `True` and `patch_id` is None — still `success` True); node-a's `ledger ls --kind settle --limit 1` shows the agent's address as buyer"
  ],
  "priority": "P1",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "scripts/cluster-restart.sh (stop_matching, port wait, log path message)",
   "scripts/cluster.mjs (defs, .seeded marker, nodes.pid/supervisor.pid, respawn only within 15 s, web UI line)",
   "packages/agent/src/bin.ts (keys/catalog/run options, exit codes)",
   "packages/agent/src/agent.ts pickPatch (followLatest false for --patch), 'is SUPERSEDED, not LISTED — refusing to buy', 'result: SUCCESS — the 402 purchase loop completed', already_known",
   "packages/node/src/api.ts /api/info, /api/nodes"
  ]
 },
 {
  "id": "AZ-068",
  "persona": "Node operator / developer",
  "title": "Publish a hidden test listing with `ainize publish --test` and confirm it stays out of public catalogs and counts",
  "goal": "A developer can exercise the publish/announce/verify path on the shared chain without polluting what visitors see.",
  "preconditions": [
   "Same shell setup and node-a login as the draft scenario ($A, $NPZ, $BENCH)",
   "Tester accepts that one hidden anchor record (and its attestations) is written to the local dev chain (a `chain down` + `scripts/cluster-restart.sh --fresh` resets everything)"
  ],
  "steps": [
   "Run `$A publish $NPZ --id o07-hidden --name \"O07 hidden test\" --model Qwen3.8-Flash-Next --benchmark \"$BENCH\" --price 0.1 --test`",
   "Run `$A patch ls --status ANNOUNCED,VERIFYING,LISTED` and `curl -s http://localhost:3402/api/catalog | grep -c o07-hidden`",
   "Run `curl -s -o /dev/null -w '%{http_code}\\n' http://localhost:3402/api/patches/o07-hidden` and the same against :3403",
   "Run `$A ledger ls --kind anchor --limit 5`",
   "Wait 2 minutes, then run `$B logs --kind verify --limit 30 | grep o07-hidden` on node-b (`B=\"node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-b\"`) and `$A patch get o07-hidden | head -1`",
   "Run `curl -s http://localhost:3402/api/info | python3 -c \"import sys,json; print(json.load(sys.stdin)['counts'])\"` and `curl -s http://localhost:3402/api/chat/patches | grep -c o07-hidden`"
  ],
  "expected": [
   "Step 1 prints `✓ draft created: o07-hidden  (2,992 rows, …)` and then `✓ announced o07-hidden → ledger record <hash> (verifiers will now attest; quorum lists it)`",
   "Step 2: `patch ls` output does not contain o07-hidden; grep count is 0 (test-visibility anchors are filtered from /api/catalog on every node unless `includeTestAnchors` is set)",
   "Step 3 returns 200 on both nodes — direct lookup works by design (hidden from listings, not from direct access)",
   "Step 4 shows the raw ledger record `anchor … o07-hidden · Qwen3.8-Flash-Next · 2992 rows` — the record exists on the shared chain even though catalogs hide it",
   "Step 5: verifiers DO attest hidden anchors: node-b logs `attested o07-hidden: PASS (vllm:Qwen3.8-Flash-Next)` and `patch get` shows `LISTED` — while `patch ls` / `/api/catalog` still omit it",
   "Step 6 still shows `{'patches': 4, 'listed': 1, 'verifying': 0, 'superseded': 3, 'rejected': 0}` and the Live test list count is 0 — hidden anchors never reach visitors"
  ],
  "priority": "P2",
  "area": "verification",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/patch.ts patchPublish (visibility: 'test')",
   "packages/node/src/market.ts catalog() filter on anchor.visibility !== 'test' || cfg.includeTestAnchors; entry() uses catalogAll()",
   "packages/core/src/types.ts includeTestAnchors",
   "packages/node/src/verifier.ts round() iterates catalogAll() (verifies test-visibility anchors too)",
   "packages/node/src/api.ts GET /api/ledger (unfiltered), GET /api/info counts, GET /api/chat/patches"
  ]
 },
 {
  "id": "AZ-069",
  "persona": "Node operator / developer",
  "title": "Show that a verifier whose serving API is down keeps retrying for 15 minutes instead of attesting hash-only",
  "goal": "A node operator can trust that a serving restart on a verifier delays the attestation rather than producing a degraded 'hash-only' vote on a runtime-capable node.",
  "preconditions": [
   "node-d from the fourth-node scenario stopped; then re-pointed to a dead serving API: `$D config set runtime.api http://localhost:8999` (repo stays /mnt/newdata/qwen3.8) and `$D config set roles verifier`; `$D start -d`",
   "A patch is currently ANNOUNCED or VERIFYING on the network — run the announce scenario's publish step (or publish another public patch with a unique schema) right after node-d is up",
   "Demo cluster running"
  ],
  "steps": [
   "Run `$D status`",
   "Run `$D logs --limit 20` within the first minute after the announce",
   "Wait 60 s and run `$D logs --kind verifier --limit 5` again",
   "Run `$D config set runtime.api http://localhost:8000`, then `$D stop && $D start -d`",
   "Wait ~60 s (the shared lock may be held by node-b/node-c) and run `$D logs --kind verify --limit 5`",
   "Run `node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-a patch get <announced id>`"
  ],
  "expected": [
   "Step 1 shows `runtime     unavailable (serving API unreachable)`",
   "Step 2 shows an info `verifier` line `verifying <id> (<name>)`, a `blob` line for the body fetch, then a `warn verifier` line `verify <id> failed: runtime unavailable (serving API unreachable) — waiting up to 15 min before hash-only fallback` (repo present + real model + benchmark samples ⇒ the node is expected to have a runtime, so it waits)",
   "Step 3 shows the same warning repeated every ~5 s; the minute count is rounded (`waiting up to 15 min` for the first ~30 s, `14 min` after ~1 minute); no `attest` record from node-d exists (`patch records <id>` has no author = node-d)",
   "Step 4 prints `✓ runtime.api = \"http://localhost:8000\"  (the node reads config.json when it starts)` on stdout and, because node-d is still running at that moment, the warning `! the node in $H is running (pid N) and keeps using the value it started with — restart it to apply this (`ainize stop` then `ainize start -d`)` on stderr; then `✓ stopped node` / `✓ node started in the background`",
   "Step 5 shows `attested <id>: PASS (vllm:Qwen3.8-Flash-Next)` — the pending patch was verified for real once the runtime came back, never as hash-only",
   "Step 6 shows node-d's attestation row with `VERIFIED ON vllm:Qwen3.8-Flash-Next` (third attestation; status LISTED) and the verification line `2/2 passed ✓ quorum (+1 more independent attestation)` — the numerator is clamped to the quorum, so a third verifier can never make it read `3/2` (critique 2 item 146). If node-d had instead written `hash-only` with `benchmark: not executed …` within the first 15 minutes, the grace period is broken — report it"
  ],
  "priority": "P2",
  "area": "verification",
  "automation": "manual",
  "evidence": [
   "packages/node/src/verifier.ts RUNTIME_GRACE_MS = 15 min, graceLeft(), 'waiting up to N min before hash-only fallback' message (Math.round)",
   "packages/node/src/runtime.ts status() error 'serving API unreachable'",
   "packages/cli/src/commands/init.ts configSet message"
  ]
 },
 {
  "id": "AZ-070",
  "persona": "Node operator / developer",
  "title": "Check the aindrive mirror: `drive status --files`, `drive sync`, `drive up` before pairing, and the changes API guard",
  "goal": "An operator can see which node files are mirrored for sharing, refresh the mirror, and gets a clear pairing hint instead of a silent failure.",
  "preconditions": [
   "Demo cluster running; node-a operator logged in (`A=…node-a`); node-a drive not paired (`curl -s http://localhost:3402/api/drive` shows `\"configured\":false`)"
  ],
  "steps": [
   "Run `$A drive status`",
   "Run `$A drive status --files | head -12`",
   "Run `$A drive sync`",
   "Run `$A drive up`",
   "Run `$A drive stop`",
   "Run `curl -s 'http://localhost:3402/api/drive/changes?path=ledger/records.jsonl' | head -c 300` and `curl -s -o /dev/null -w '%{http_code}\\n' 'http://localhost:3402/api/drive/changes?path=../config.json'`",
   "Run `curl -s -o /dev/null -w '%{http_code}\\n' -X POST http://localhost:3402/api/drive -H 'content-type: application/json' -d '{\"action\":\"up\"}'` (no auth)",
   "Run `$A drive login --no-open` and press Ctrl+C once the link is printed"
  ],
  "expected": [
   "Step 1 prints `folder    /home/<user>/.ngram-cluster/node-a/data/drive`, `paired    no — run \\`ainize drive login\\``, `agent     stopped`, `server    https://aindrive.ainetwork.ai`, `drive id  -`, `url       -`, `files     <n>`",
   "Step 2 adds a `PATH SIZE MODIFIED` table containing `branches/finance__KRX-latest.json`, `branches/finance__KRX-history.json`, `ledger/records.jsonl` and patch folders",
   "Step 3 prints `✓ drive folder synced (<n> file(s) written)`",
   "Step 4 prints `! drive not paired yet — run: cd /home/<user>/.ngram-cluster/node-a/data/drive && npx aindrive login --server https://aindrive.ainetwork.ai   # one-time browser pairing, then: ainize drive up`",
   "Step 5 prints `✓ not running`",
   "Step 6: the first call returns JSON for the mirrored ledger file; the path-traversal call returns 400 (`{\"error\":\"path must be relative to the drive folder\"}`)",
   "Step 7 returns 401",
   "Step 8 prints the `aindrive pairing` header with `folder :` and `server :` lines and the dim instructions `A browser sign-in link will be printed — open it, click Authorize, and this folder becomes a drive.` / `After pairing, Ctrl+C here and run \\`ainize drive up\\` to serve it in the background.` (or `error: aindrive CLI not installed (npm install aindrive)` if the package is missing)"
  ],
  "priority": "P2",
  "area": "drive",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/drive.ts driveStatus/driveAction/driveLogin strings",
   "packages/node/src/drive.ts up()/stop() messages, login_hint",
   "packages/node/src/api.ts /api/drive, /api/drive/changes path guard"
  ]
 },
 {
  "id": "AZ-071",
  "persona": "AI agent / automation",
  "title": "Run the autonomous buyer end to end: detect the gap, pay 25 AIN via 402, verify the hash, load and restore",
  "goal": "An AI agent closes its own knowledge gap without a human: it notices a wrong answer, buys the verified knowledge with automatic payment, checks integrity and loads it into the running model.",
  "preconditions": [
   "Demo cluster running on the AIN ledger; vLLM at http://localhost:8000 with the live-apply hook; krx-all-2761 LISTED (2/2) and NOT currently loaded in the shared model (GET http://localhost:3402/api/runtime applied = [])",
   "Node 24 on PATH: `export PATH=\"$HOME/.local/node/bin:$PATH\"; cd /mnt/newdata/ainize/knowledge-marketplace`",
   "Agent identity funded with ≥ 50 AIN: `ADDR=$(node packages/agent/dist/bin.js keys --json | python3 -c \"import sys,json; print(json.load(sys.stdin)['address'])\")`; `node packages/cli/dist/bin.js chain fund $ADDR 60`",
   "Record before: settle count `curl -s 'http://localhost:3402/api/ledger?kind=settle&limit=1000' | python3 -c \"import sys,json; print(len(json.load(sys.stdin)['records']))\"`, downloads from GET /api/catalog for krx-all-2761, and node-a's balance from GET http://localhost:3402/api/chain",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Run `node packages/agent/dist/bin.js run --market http://localhost:3402 --patch krx-all-2761` (defaults: question '픽셀플러스 종목코드 알려줘', prompt '종목코드 픽셀플러스 ', expect 087600, pay auto) and read the log; `echo $?`",
   "Re-read the settle count, krx-all-2761 downloads and node-a's balance",
   "Run the same command again with `--json` and read the JSON",
   "Run `ls -l ~/.ngram-agent/patches/` and `curl -s http://localhost:3402/api/runtime | python3 -c \"import sys,json; print(json.load(sys.stdin)['applied'])\"`"
  ],
  "expected": [
   "Step 1 log, in order: `[0] agent 0x…  market http://localhost:3402`; `[1] question: \"픽셀플러스 종목코드 알려줘\"`; `    current answer: \"…\"  → wrong/unknown — knowledge purchase needed`; `[2] searching the catalog (ledger anchors + verification quorum)`; `    candidate: krx-all-2761  347.8 MB  270053 rows  price 25 AIN  quorum met by 2 verifier(s) (vllm:Qwen3.8-Flash-Next, vllm:Qwen3.8-Flash-Next)` (size uses 1e6 bytes per MB, hence 347.8 MB vs the UI's 331.7 MB); `[3] requesting the resource → GET http://localhost:3402/x402/patch/krx-all-2761`; `    402 Payment Required: 25 AIN → 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5  (ain-transfer, nonce <24 hex>)`; `[4] paying (AIN transfer on chain) and retrying with the proof`; `    settled: tx 0x…  {\"settled\":true,\"tx\":\"0x…\",\"royalty\":{\"0xF7A9dE49902C95661AC6556D631e2B60a081A1F5\":\"25\"}}`; `[5] manifest sha256 <16 hex>… matches · body sha256 57c9346349afd6fa… (same as the on-ledger anchor)`; `    received 347.8 MB from http://localhost:3402/p2p/blob/57c9346349afd6fa…`; `    sha256 == on-ledger anchor hash — integrity holds no matter which peer served it`; `[6] loading into the running model (no restart)`; `    <scripts/patch.py apply output>`; `    answer with knowledge: \"087600…\"  → correct`; `    restored (subscription ended): <patch.py remove output>`; `result: SUCCESS — the 402 purchase loop completed`; exit code 0",
   "Step 2: settle count +1 (buyer = agent address, amount '25', scheme 'ain-transfer'), krx-all-2761 downloads +1, node-a balance +25 AIN, agent balance −25 AIN",
   "Step 3: the second run pays again (25 AIN) but prints `    body already present — download skipped` in step [5]; the JSON has identity=<agent address>, before (wrong answer), after starting with 087600, already_known=false, patch_id='krx-all-2761', scheme='ain-transfer', tx_hash '0x…', amount='25', sha256='57c9346349afd6fa1b0475340b9b041ee0027753c05463d4bb204a5a4726c642', path '/home/<user>/.ngram-agent/patches/57c9…npz', applied=true, restored=true, success=true and the steps array; nothing else on stdout; exit 0",
   "Step 4: the cached body 57c9346349afd6fa…6c642.npz (347,829,014 bytes) is in the agent home; the node's applied list is [] (the agent applies via scripts/patch.py directly and restores the shared table afterwards)"
  ],
  "priority": "P0",
  "area": "agent",
  "automation": "cli",
  "evidence": [
   "packages/agent/src/agent.ts runAgent() step strings ([0]–[6], 'wrong/unknown — knowledge purchase needed', 'candidate:', 'settled: tx', 'received … from', 'body already present — download skipped', 'answer with knowledge:', 'restored (subscription ended):', 'result: SUCCESS — the 402 purchase loop completed')",
   "packages/agent/src/bin.ts run defaults (question/prompt/expect/pay/repo), --json output, exit codes",
   "packages/node/src/api.ts GET /x402/patch/:id (402 → X-PAYMENT → 200 manifest + x-payment-response), GET /p2p/blob/:sha",
   "packages/node/src/market.ts settlePayment() (settle record, royalty to the single author), issueManifest()",
   "packages/core/src/catalog.ts royaltySplit()"
  ]
 },
 {
  "id": "AZ-072",
  "persona": "AI agent / automation",
  "title": "Verify the x402 402 challenge contract on the seller gateway (header, body, CORS exposure, non-seller and unknown ids)",
  "goal": "Any HTTP client (ain-js compatible) can discover price, payee and nonce for a knowledge patch from the 402 response alone.",
  "preconditions": [
   "Seller node http://localhost:3402 up; krx-all-2761 LISTED (author = node-a; payTo = GET /api/info node.address, currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)",
   "curl and python3 available"
  ],
  "steps": [
   "`curl -s -D - -o /tmp/x402.json http://localhost:3402/x402/patch/krx-all-2761` and read status line and headers",
   "Decode the header: `curl -s -D - -o /dev/null http://localhost:3402/x402/patch/krx-all-2761 | grep -i '^x-payment-required:' | cut -d' ' -f2 | base64 -d`",
   "Compare with body: `python3 -c \"import json;d=json.load(open('/tmp/x402.json'));print(d['x402Version'],d['error'],d['requirements']==d['accepts'],d['requirements'][0])\"`",
   "Repeat step 1 twice more and compare the `nonce` and `expires_at` values",
   "`curl -s -i http://localhost:3403/x402/patch/krx-all-2761` (verifier node, not the seller)",
   "`curl -s -i http://localhost:3402/x402/patch/does-not-exist`"
  ],
  "expected": [
   "Status 'HTTP/1.1 402 Payment Required'; headers include 'www-authenticate: x402', 'x-payment-required: <base64>', 'access-control-expose-headers: x-payment-required, x-payment-tx-hash, x-payment-currency, x-payment-response, x-content-sha256' and 'access-control-allow-headers: content-type, authorization, x-payment, x-ngram-auth, x-ngram-buyer'",
   "Decoded header is a JSON array with exactly one requirement: {\"scheme\":\"ain-transfer\",\"network\":\"ain:local\",\"asset\":\"AIN\",\"payTo\":\"0xF7A9dE49902C95661AC6556D631e2B60a081A1F5\",\"maxAmountRequired\":\"25\",\"resource\":\"/x402/patch/krx-all-2761\",\"description\":\"Knowledge patch krx-all-2761 (270053 rows, Qwen3.8-Flash-Next)\",\"nonce\":\"<24 hex>\",\"expires_at\":<ms epoch>}",
   "Body prints '1 payment required True' and body.requirements[0] equals the decoded header entry; expires_at is about 10 minutes (600000 ms) after the request",
   "Each 402 issues a fresh nonce (three different 24-hex nonces)",
   "Non-seller node answers 409 {\"error\":\"not sold here; gateway is http://localhost:3402/x402/patch/krx-all-2761\"}",
   "Unknown id answers 404 {\"error\":\"patch not found\"}"
  ],
  "priority": "P0",
  "area": "x402",
  "automation": "api",
  "evidence": [
   "packages/core/src/x402.ts (X402_HEADER_REQUIRED='x-payment-required', encodeRequirements base64, newNonce 12 bytes)",
   "packages/node/src/api.ts GET /x402/patch/:id (402 branch, 409 'not sold here', 404 'patch not found'); packages/node/src/server.ts CORS expose/allow headers",
   "packages/node/src/market.ts requirementsFor() (10-minute nonce TTL, asset 'AIN' when ledger.kind==='ain')",
   "packages/web/src/i18n/pages/docs.ts 'docs.x402.steps'"
  ]
 },
 {
  "id": "AZ-073",
  "persona": "AI agent / automation",
  "title": "Verify the settled 200 response contract and its ledger/event side effects after an ain-transfer payment",
  "goal": "After paying, an agent receives a tamper-evident manifest plus receipts (tx hash, royalty split) that match what the seller recorded on the ledger.",
  "preconditions": [
   "The end-to-end agent run completed once (a fresh settle record exists for krx-all-2761 with buyer = agent address)",
   "Agent address known from `node packages/agent/dist/bin.js keys`; node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "From the agent log copy the '    settled: tx 0x…' hash and the echoed x-payment-response JSON",
   "GET http://localhost:3402/api/ledger?kind=settle&limit=1 and read records[0].body",
   "GET http://localhost:3402/api/events?kind=trade&limit=1",
   "GET http://localhost:3402/api/patches/krx-all-2761/records and count kind='settle' records whose body.buyer is the agent address",
   "Check on-chain: `curl -s -X POST http://localhost:8081/json-rpc -H 'content-type: application/json' -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"ain_get\",\"params\":{\"protoVer\":\"1.0.0\",\"type\":\"GET_VALUE\",\"ref\":\"/apps/knowledge/market/settlements/krx-all-2761\"}}'`",
   "Check the blob gate without credentials: `curl -s -i http://localhost:3402/p2p/blob/57c9346349afd6fa1b0475340b9b041ee0027753c05463d4bb204a5a4726c642 | head -5`"
  ],
  "expected": [
   "x-payment-response JSON has settled=true, tx equal to the logged tx hash, and royalty {\"0xF7A9dE49902C95661AC6556D631e2B60a081A1F5\":\"25\"} (single-author lineage → whole price to seller)",
   "Newest settle body: patch_id='krx-all-2761', seller=<node-a address>, buyer=<agent address>, amount='25', currency='AIN', scheme='ain-transfer', tx_hash=<same tx>, billing='per_download', royalty as above",
   "Newest trade event message: 'sold krx-all-2761 to 0x<first 10 of agent>… for 25 AIN (ain-transfer)' with data.tx = the tx hash; GET /api/events?kind=royalty returns no new event (no royalty leaves the seller for same-author lineage)",
   "/api/patches/krx-all-2761/records contains the settle record for the agent",
   "On-chain /apps/knowledge/market/settlements/krx-all-2761 has a child keyed by keyOf(tx_hash) whose body carries the same fields (seller, buyer, amount '25', scheme 'ain-transfer') — compare the body, not the raw hash key",
   "Unauthenticated blob GET answers 402 {\"error\":\"payment required: buy the patch via /x402/patch/:id (verifiers and authors are exempt)\"} — the body is only served with the manifest's download_token or a signed x-ngram-auth header from a settled buyer (which the agent used in its step [5])"
  ],
  "priority": "P0",
  "area": "x402",
  "automation": "api",
  "evidence": [
   "packages/node/src/api.ts GET /x402/patch/:id success branch (x-payment-tx-hash, x-payment-currency, x-payment-response, x-content-sha256), GET /p2p/blob/:sha (402 message), GET /api/ledger, /api/events, /api/patches/:id/records",
   "packages/node/src/market.ts settlePayment() (settle record, 'trade' event, royalty transfer skipped when addr === this.address), mayDownload()",
   "packages/core/src/catalog.ts royaltySplit()",
   "packages/core/src/ain-ledger.ts settle → /apps/knowledge/market/settlements/$id/$keyOf(tx)"
  ]
 },
 {
  "id": "AZ-074",
  "persona": "AI agent / automation",
  "title": "Refuse to pay when the agent's AIN balance is below the price, then succeed after funding",
  "goal": "An unfunded agent fails fast with an actionable message and never creates a half-completed purchase.",
  "preconditions": [
   "Cluster and chain up; Node 24 on PATH; krx-all-2761 LISTED and not loaded in the shared model",
   "A fresh, empty agent home directory path chosen, e.g. /tmp/claude-agent-poor (must not exist yet)"
  ],
  "steps": [
   "`node packages/agent/dist/bin.js keys --home /tmp/claude-agent-poor` (creates a new identity with 0 AIN); note the address",
   "Count settle records: GET http://localhost:3402/api/ledger?kind=settle&limit=1000",
   "`node packages/agent/dist/bin.js run --market http://localhost:3402 --home /tmp/claude-agent-poor --patch krx-all-2761`; note stderr and `echo $?`",
   "Re-count settle records",
   "Fund the identity: `node packages/cli/dist/bin.js chain fund <new address> 30` (local genesis transfer)",
   "Re-run the same `run` command from step 3"
  ],
  "expected": [
   "Step 1 prints 'address     0x…', 'publicKey   …', 'home        /tmp/claude-agent-poor'; file /tmp/claude-agent-poor/identity.json is created with mode 0600",
   "Step 3: the agent reaches step [3] (prints '    402 Payment Required: 25 AIN → …') and '[4] paying (AIN transfer on chain) and retrying with the proof', then fails with stderr 'agent failed: agent 0x<new> holds 0 AIN < price 25 (fund it: ngram chain fund 0x<new>)'; exit code 1; no AIN transfer is sent and no X-PAYMENT retry happens",
   "Settle count unchanged after the failed run (the seller only records a settlement on a valid X-PAYMENT)",
   "`chain fund` prints '✓ funded 0x<new> with 30 AIN  tx 0x…  balance now 30 AIN'",
   "Step 6 succeeds: '    settled: tx 0x…', 'result: SUCCESS — the 402 purchase loop completed', exit code 0; the new address's balance is 5 AIN afterwards (gas-free local chain)"
  ],
  "priority": "P0",
  "area": "agent",
  "automation": "cli",
  "evidence": [
   "packages/agent/src/agent.ts payFor() ain-transfer branch (balance check message)",
   "packages/agent/src/identity.ts loadIdentity() (creates identity.json, mode 0o600)",
   "packages/cli/src/commands/chain.ts chainFund() output",
   "packages/node/src/market.ts settlePayment()"
  ]
 },
 {
  "id": "AZ-075",
  "persona": "AI agent / automation",
  "title": "Reject forged X-PAYMENT proofs: unknown tx hash and a real transfer that did not go to the seller",
  "goal": "A seller never releases the manifest for a payment that cannot be verified on the AIN chain as a transfer to the seller.",
  "preconditions": [
   "Seller node http://localhost:3402 and chain http://localhost:8081 up; krx-all-2761 LISTED; node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)",
   "A real tx hash of a transfer whose recipient is NOT the seller: pick any non-seller address OTHER (e.g. node-b's from `curl -s http://localhost:3403/api/chain`, or the agent's `keys` output), run `node packages/cli/dist/bin.js chain fund $OTHER 1` and copy the printed 'tx 0x…' (genesis → OTHER)",
   "Note the newest settle record: GET http://localhost:3402/api/ledger?kind=settle&limit=1"
  ],
  "steps": [
   "Build a forged payload: `P=$(printf '{\"scheme\":\"ain-transfer\",\"network\":\"ain:local\",\"txHash\":\"0x%064d\",\"from\":\"%s\",\"to\":\"0xF7A9dE49902C95661AC6556D631e2B60a081A1F5\",\"amount\":\"25\",\"nonce\":\"deadbeef\"}' 1 $OTHER | base64 -w0)`",
   "`time curl -s -i -H \"x-payment: $P\" http://localhost:3402/x402/patch/krx-all-2761`",
   "Build a wrong-recipient payload with the chain-fund tx hash: `P2=$(printf '{\"scheme\":\"ain-transfer\",\"network\":\"ain:local\",\"txHash\":\"<fund tx hash>\",\"from\":\"0x00ADEc28B6a845a085e03591bE7550dd68673C1C\",\"to\":\"0xF7A9dE49902C95661AC6556D631e2B60a081A1F5\",\"amount\":\"25\",\"nonce\":\"deadbeef\"}' | base64 -w0)` then `curl -s -i -H \"x-payment: $P2\" http://localhost:3402/x402/patch/krx-all-2761`",
   "Send garbage: `curl -s -i -H 'x-payment: %%%not-base64%%%' http://localhost:3402/x402/patch/krx-all-2761`",
   "Send an unsupported scheme: `curl -s -i -H \"x-payment: $(echo -n '{\"scheme\":\"paypal\"}' | base64 -w0)\" http://localhost:3402/x402/patch/krx-all-2761`",
   "GET http://localhost:3402/api/ledger?kind=settle&limit=1 and compare with the record seen before the test"
  ],
  "expected": [
   "Step 2: HTTP 402 {\"error\":\"transfer not found / not executed\"} after roughly 6 seconds (the seller retries the chain lookup 5 times with 1.2 s pauses); no x-payment-tx-hash header",
   "Step 3: HTTP 402 {\"error\":\"transfer recipient <OTHER address> is not the seller\"} (recipient parsed from the on-chain /transfer/<from>/<to>/<key>/value ref)",
   "Step 4: HTTP 402 {\"error\":\"missing or malformed X-PAYMENT\"}",
   "Step 5: HTTP 402 {\"error\":\"unsupported scheme paypal\"}",
   "No new settle record, no new 'trade' event, and krx-all-2761 `downloads` unchanged"
  ],
  "priority": "P0",
  "area": "x402",
  "automation": "api",
  "evidence": [
   "packages/node/src/market.ts settlePayment() ain-transfer branch (verifyTransfer retry loop, recipient and value checks, 'unsupported scheme')",
   "packages/core/src/ain-ledger.ts verifyTransfer() (regex /transfer/from/to/key/value, is_executed)",
   "packages/core/src/x402.ts decodePayload()",
   "packages/node/src/api.ts GET /x402/patch/:id → HttpError(402, out.error)"
  ]
 },
 {
  "id": "AZ-076",
  "persona": "AI agent / automation",
  "title": "Reject a replayed X-PAYMENT (payment already used) and ignore stale nonces in the ain-transfer scheme",
  "goal": "A tx hash that already bought a manifest cannot be replayed to obtain another manifest or a second download token.",
  "preconditions": [
   "At least one settle record with scheme 'ain-transfer' AND seller = node-a exists (from the end-to-end agent run or the one-line `use` scenario); take patch_id, tx_hash and buyer from step 1 — never from older notes, the chain is re-seeded on fresh restarts. The settle ledger is shared by every node in the cluster, so the NEWEST ain-transfer record is not necessarily node-a's: replaying a sale another node made is a different scenario (node-a answers 409 'not sold here' for a patch it does not sell)",
   "Seller node http://localhost:3402 up; node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Fetch an existing settlement NODE-A SOLD: `curl -s 'http://localhost:3402/api/ledger?kind=settle&limit=1000' | python3 -c 'import json,sys;A=\"0xF7A9dE49902C95661AC6556D631e2B60a081A1F5\";b=next(r[\"body\"] for r in json.load(sys.stdin)[\"records\"] if r[\"body\"][\"scheme\"]==\"ain-transfer\" and r[\"body\"][\"seller\"]==A);print(b[\"patch_id\"],b[\"tx_hash\"],b[\"buyer\"])'`",
   "Build a replay payload with that tx hash: `R=$(printf '{\"scheme\":\"ain-transfer\",\"network\":\"ain:local\",\"txHash\":\"<tx_hash>\",\"from\":\"<buyer>\",\"to\":\"0xF7A9dE49902C95661AC6556D631e2B60a081A1F5\",\"amount\":\"25\",\"nonce\":\"000000000000000000000000\"}' | base64 -w0)`",
   "`curl -s -i -H \"x-payment: $R\" http://localhost:3402/x402/patch/<patch_id>` (same patch the tx paid for)",
   "`curl -s -i -H \"x-payment: $R\" http://localhost:3402/x402/patch/krx-all-2761` (different patch, same tx; use krx-all-2761-ep12 if the tx already belonged to krx-all-2761)",
   "Compare settle count and the target patch's downloads before/after"
  ],
  "expected": [
   "Step 3: HTTP 402 {\"error\":\"payment already used\"} — the seller's payments_seen table keys on tx_hash and is checked before the transfer is verified, so nonce and amount values are irrelevant for ain-transfer",
   "Step 4: HTTP 402 {\"error\":\"payment already used\"} — a tx cannot be re-used to buy a different patch either",
   "No new settle record, no new download token, downloads unchanged",
   "Note for the local-credit scheme (not offered on this AIN demo): the nonce is single-use and a second use answers 402 {\"error\":\"unknown or expired nonce\"} (store.takeNonce marks used=1; nonces expire after 10 minutes)"
  ],
  "priority": "P0",
  "area": "x402",
  "automation": "api",
  "evidence": [
   "packages/node/src/market.ts settlePayment() (store.paymentSeen(txHash) → 'payment already used'; local-credit takeNonce → 'unknown or expired nonce')",
   "packages/node/src/store.ts (payments_seen table, nonces table takeNonce)",
   "packages/node/src/api.ts GET /x402/patch/:id"
  ]
 },
 {
  "id": "AZ-077",
  "persona": "AI agent / automation",
  "title": "Follow supersede marks on a keyword search, and refuse an explicitly requested superseded id",
  "goal": "An agent searching by question is steered to the newest verified version, while an explicit request for an outdated version is refused rather than silently paid for.",
  "preconditions": [
   "Cluster up; krx-all-2761 LISTED, pixelplus-087600 / krx-all-2761-ep6 SUPERSEDED (agent `catalog --status SUPERSEDED` shows attest 2/2 for them); krx-all-2761 not loaded in the shared model",
   "Node 24 on PATH; agent identity funded with ≥ 25 AIN (`node packages/cli/dist/bin.js chain fund <agent address> 30`)",
   "Note the settle count before: GET http://localhost:3402/api/ledger?kind=settle&limit=1000"
  ],
  "steps": [
   "`node packages/agent/dist/bin.js run --market http://localhost:3402 --question \"Pixelplus ticker code\" --json` (no --patch: keyword search over id/name/description/schema/topic) and read the JSON",
   "Re-run the same command without `--json` and read the step [2] lines",
   "`node packages/agent/dist/bin.js run --market http://localhost:3402 --patch krx-all-2761-ep6`; capture stderr and `echo $?`",
   "`node packages/agent/dist/bin.js run --market http://localhost:3402 --patch krx-all-2761-ep6 --follow-latest`; capture stderr",
   "`node packages/agent/dist/bin.js run --market http://localhost:3402 --question \"quantum chromodynamics lattice\" --patch \"\"`; capture stderr",
   "Compare the settle count"
  ],
  "expected": [
   "Step 1: the keyword search matches 'Pixelplus ticker code (single fact)' (pixelplus-087600, SUPERSEDED), follows its supersede mark and buys the newest LISTED version instead: JSON patch_id='krx-all-2761', amount='25', scheme='ain-transfer', success=true, after starting with 087600",
   "Step 2 shows the log line '    pixelplus-087600 is superseded by krx-all-2761 (newer patch on the same benchmark) → switching' between '[2] searching the catalog (ledger anchors + verification quorum)' and '    candidate: krx-all-2761 …' (a second 25 AIN purchase is made; '    body already present — download skipped')",
   "Step 3: with an explicit --patch the id is honoured as-is; after step [2] the run stops with stderr 'agent failed: patch krx-all-2761-ep6 is SUPERSEDED, not LISTED — refusing to buy'; exit 1; no payment, no settle record",
   "Step 4: same refusal as step 3 — the `--follow-latest` flag is accepted by yargs but bin.ts does not forward it into AgentOptions (dead flag) — report as a defect (expected per the option's help text: switch to krx-all-2761)",
   "Step 5: no catalog entry matches; stderr 'agent failed: no listed patch matches \"quantum chromodynamics lattice\"'; exit 1",
   "Settle count increased by exactly 2 (steps 1 and 2 only)"
  ],
  "priority": "P1",
  "area": "agent",
  "automation": "cli",
  "evidence": [
   "packages/agent/src/agent.ts pickPatch() (keyword score over id/name/description/schema/topic_path, resolveSupersedes log line), runAgent() 'is SUPERSEDED, not LISTED — refusing to buy', 'no listed patch matches'",
   "packages/agent/src/bin.ts run options (--question, --patch, --follow-latest declared but not passed into opts)",
   "packages/node/src/api.ts GET /api/catalog?status=LISTED,SUPERSEDED (superseded_by)"
  ]
 },
 {
  "id": "AZ-078",
  "persona": "AI agent / automation",
  "title": "Skip the purchase when the model already answers correctly, and check the --max-price budget guard",
  "goal": "An agent never pays for knowledge the running model already has, and an operator can see whether the budget flag actually protects the wallet.",
  "preconditions": [
   "Cluster up; vLLM at http://localhost:8000; node-a operator logged in from the CLI (`A=\"node packages/cli/dist/bin.js --home $HOME/.ngram-cluster/node-a\"`); krx-all-2761 LISTED and not loaded (GET /api/runtime applied = [])",
   "Node 24 on PATH; a fresh unfunded agent home path e.g. /tmp/claude-agent-budget (must not exist yet)",
   "Note the settle count before: GET http://localhost:3402/api/ledger?kind=settle&limit=1000"
  ],
  "steps": [
   "Run `$A patch apply krx-all-2761` (node-a authored it, so the body is local and no purchase is needed) and confirm `curl -s http://localhost:3402/api/runtime` lists krx-all-2761 in applied",
   "Run `node packages/agent/dist/bin.js run --market http://localhost:3402 --patch krx-all-2761 --json` (default prompt '종목코드 픽셀플러스 ', expect 087600) and `echo $?`",
   "Run `$A patch remove krx-all-2761` and confirm applied is [] again",
   "Run `node packages/agent/dist/bin.js run --market http://localhost:3402 --home /tmp/claude-agent-budget --patch krx-all-2761 --max-price 10`; capture the log, stderr and `echo $?`",
   "Compare the settle count"
  ],
  "expected": [
   "Step 1 prints `✓ applied krx-all-2761: …`; /api/runtime applied contains {patch_id:'krx-all-2761', …}",
   "Step 2: the agent stops after the knowledge check — log (visible without --json) '    current answer: \"087600…\"  → correct — nothing to buy'; JSON already_known=true, success=true, patch_id=null, scheme=null, tx_hash=null, applied=false, steps has only the [0]/[1] lines; exit 0; no 402 request is made and node-a's `logs --kind buy` shows nothing new",
   "Step 3 prints `✓ removed krx-all-2761: …`",
   "Step 4: the budget is NOT enforced — bin.ts does not forward `--max-price` into AgentOptions, so instead of 'price 25 AIN exceeds --max-price 10 — refusing to buy (use --max-price to raise the budget)' the agent proceeds to step [3]/[4] and only fails because the fresh identity is unfunded: stderr 'agent failed: agent 0x… holds 0 AIN < price 25 (fund it: ngram chain fund 0x…)', exit 1 — report the ignored --max-price flag as a defect (the unfunded home keeps the test safe)",
   "Settle count unchanged after both runs"
  ],
  "priority": "P1",
  "area": "agent",
  "automation": "cli",
  "evidence": [
   "packages/agent/src/agent.ts runAgent() step [1] ('correct — nothing to buy', already_known/success early return), maxPrice check text, payFor() balance check",
   "packages/agent/src/bin.ts run handler (opts object omits maxPrice and followLatest)",
   "packages/cli/src/commands/patch.ts patchApply/patchRemove",
   "packages/node/src/api.ts POST /api/patches/:id/apply, GET /api/runtime"
  ]
 },
 {
  "id": "AZ-079",
  "persona": "AI agent / automation",
  "title": "Refuse to buy when the seller offers no payment scheme the agent is allowed to use (--pay local-credit on an AIN node)",
  "goal": "A policy-restricted agent never pays with a scheme its operator did not authorize.",
  "preconditions": [
   "Seller node http://localhost:3402 runs the AIN ledger (GET /api/info → ledger.kind='ain', currency 'AIN'); its 402 only offers scheme 'ain-transfer'",
   "Node 24 on PATH; krx-all-2761 LISTED and not loaded in the shared model (so the default knowledge check fails and the agent proceeds); agent balance ≥ 25 AIN for step 2"
  ],
  "steps": [
   "`node packages/agent/dist/bin.js run --market http://localhost:3402 --patch krx-all-2761 --pay local-credit`; capture stderr and exit code",
   "`node packages/agent/dist/bin.js run --market http://localhost:3402 --patch krx-all-2761 --pay ain-transfer --json` (explicit allowed scheme)",
   "`node packages/agent/dist/bin.js run --market http://localhost:3402 --patch krx-all-2761 --pay paypal`"
  ],
  "expected": [
   "Step 1: after '[3] requesting the resource → GET http://localhost:3402/x402/patch/krx-all-2761' the agent exits with stderr 'agent failed: 402 without a usable payment requirement (offered: ain-transfer)'; exit code 1; no settle record is added",
   "Step 2: purchase completes with JSON scheme='ain-transfer', amount='25', success=true",
   "Step 3: yargs rejects the option before any network call: 'Invalid values:' / 'Argument: pay, Given: \"paypal\", Choices: \"auto\", \"local-credit\", \"ain-transfer\"'; exit code 1"
  ],
  "priority": "P1",
  "area": "agent",
  "automation": "cli",
  "evidence": [
   "packages/agent/src/agent.ts runAgent() requirement selection (`want` / '402 without a usable payment requirement')",
   "packages/agent/src/bin.ts run option `pay` choices",
   "packages/node/src/market.ts requirementsFor() (single scheme per ledger kind)"
  ]
 },
 {
  "id": "AZ-080",
  "persona": "AI agent / automation",
  "title": "Detect a tampered or corrupted patch body by sha256 before applying it to the model",
  "goal": "An agent never loads knowledge whose bytes differ from the hash anchored on the ledger, even if the file came from its own cache or an arbitrary peer.",
  "preconditions": [
   "Cluster and serving API up; Node 24 on PATH; agent identity funded (≥ 50 AIN for two purchases) — the sha check happens after payment",
   "A scratch agent home, e.g. /tmp/claude-agent-tamper, seeded with the default identity so funds are available: `mkdir -p /tmp/claude-agent-tamper/patches && cp ~/.ngram-agent/identity.json /tmp/claude-agent-tamper/`",
   "krx-all-2761 not loaded in the shared model"
  ],
  "steps": [
   "Plant a corrupt cached body under the expected name: `head -c 1048576 /dev/urandom > /tmp/claude-agent-tamper/patches/57c9346349afd6fa1b0475340b9b041ee0027753c05463d4bb204a5a4726c642.npz`",
   "`node packages/agent/dist/bin.js run --market http://localhost:3402 --home /tmp/claude-agent-tamper --patch krx-all-2761`; capture the log, stderr and exit code",
   "Delete the corrupt file and re-run the same command"
  ],
  "expected": [
   "Step 2 log reaches '[5] manifest sha256 <16 hex>… matches · body sha256 57c9346349afd6fa… (same as the on-ledger anchor)' then '    body already present — download skipped', then aborts with stderr 'agent failed: sha256 mismatch after download: <sha256 of the random file>'; exit code 1",
   "No '[6] loading into the running model (no restart)' or '    answer with knowledge:' line is printed — the corrupt body never reaches scripts/patch.py (the node's own /api/runtime applied list is [] regardless, since the agent applies directly)",
   "The manifest itself passed its own check (x-content-sha256 equals sha256 of the response text and manifest.patch_sha256 equals the on-ledger anchor); only the body failed — the 25 AIN payment for this run was still settled",
   "Step 3: the agent downloads the real body ('    received 347.8 MB from http://localhost:3402/p2p/blob/57c9346349afd6fa…'), prints '    sha256 == on-ledger anchor hash — integrity holds no matter which peer served it' and completes with exit 0"
  ],
  "priority": "P1",
  "area": "agent",
  "automation": "cli",
  "evidence": [
   "packages/agent/src/agent.ts runAgent() step [5] (manifest hash mismatch / 'manifest sha256 differs from the on-ledger anchor' / existsSync skip / sha256File → 'sha256 mismatch after download')",
   "packages/node/src/api.ts GET /x402/patch/:id sets x-content-sha256 = sha256Hex(manifest text)",
   "packages/node/src/market.ts issueManifest() (patch_sha256 from the anchor)"
  ]
 },
 {
  "id": "AZ-081",
  "persona": "AI agent / automation",
  "title": "Write and read back the on-chain access receipt after a node-side purchase (ainize use / POST buy)",
  "goal": "A buyer's entitlement is provable on the AIN chain at the path ain-js hasAccess() reads, independent of the seller's database.",
  "preconditions": [
   "node-c (http://localhost:3404; address from `curl -s http://localhost:3404/api/chain`, currently 0x199Ff87086b81415Cd1402217FFBA3135A51bce9) has an operator session in ~/.ngram-cluster/node-c/cli.json (or log in: `NGRAM_HOME=$HOME/.ngram-cluster/node-c node packages/cli/dist/bin.js login --password <node-c operator password>`) and holds ≥ 25 AIN",
   "krx-all-2761 LISTED and not yet purchased by node-c: GET http://localhost:3404/api/patches/krx-all-2761 shows purchased=false (has_body may already be true because node-c is a verifier)",
   "entry_id = GET http://localhost:3402/api/patches/krx-all-2761 anchor.entry_id; node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5); Node 24 on PATH"
  ],
  "steps": [
   "`NGRAM_HOME=$HOME/.ngram-cluster/node-c node packages/cli/dist/bin.js use krx-all-2761 --no-apply` (one-line consumer path; equivalent API: POST http://localhost:3404/api/patches/krx-all-2761/buy with body {\"apply\":false} and the operator cookie/Bearer token)",
   "Read the printed step list and the tx hash",
   "Read the receipt on-chain: `curl -s -X POST http://localhost:8081/json-rpc -H 'content-type: application/json' -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"ain_get\",\"params\":{\"protoVer\":\"1.0.0\",\"type\":\"GET_VALUE\",\"ref\":\"/apps/knowledge/access/<node-c address>\"}}'`",
   "`NGRAM_HOME=$HOME/.ngram-cluster/node-c node packages/cli/dist/bin.js wallet`",
   "Repeat step 1 once more",
   "For comparison, after an `ainize-agent run` query the same ref for the agent address (`/apps/knowledge/access/<agent address>`)"
  ],
  "expected": [
   "Step 1 prints '✓ bought krx-all-2761 for 25 (ain-transfer)  tx 0x…' followed by timed steps: 'quorum     2 attestation(s) ≥ quorum 2', '402       Payment Required: 25 AIN → 0xF7A9dE499… (ain-transfer)', 'pay       AIN transfer tx 0x…', 'settled   seller confirmed; manifest sha256 …', 'download  body already present; sha256 matches on-ledger anchor' (or '… MB from http://localhost:3402; sha256 matches on-ledger anchor'), 'receipt   on-chain access receipt written (/apps/knowledge/access/…, tx 0x…)', then '  body: /home/comcom/.ngram-cluster/node-c/data/blobs/57c93463….npz' and '✓ downloaded — load with: ainize patch apply krx-all-2761'",
   "Step 3 returns a child keyed '<node-a address>_finance|krx_<entry_id>' with {seller:'<node-a address>', topic_path:'finance/krx', entry_id:'<entry_id>', amount:'25', currency:'AIN', tx_hash:<same tx>, accessed_at:<ms>} (node-c's access path holds only the receipts node-c itself wrote)",
   "Step 4 wallet shows 'ledger  ain · ain:local', 'balance <previous − 25> AIN', 'purchases' incremented by 1",
   "Step 5 prints '✓ krx-all-2761 is already on this node (purchased)' and 'try it: ainize chat krx-all-2761 \"your question\"' — no second payment, no new receipt",
   "Step 6: the standalone ainize-agent does NOT write an access receipt (no key under /apps/knowledge/access/<agent address>); its entitlement is only the seller's settle record — expected per current code (recordAccess is called only from Market.buy)"
  ],
  "priority": "P1",
  "area": "x402",
  "automation": "cli",
  "evidence": [
   "packages/node/src/market.ts buy() (steps quorum/402/pay/settled/download/receipt; ledger.recordAccess only for AinLedger + ain-transfer)",
   "packages/core/src/ain-ledger.ts recordAccess()/hasAccess() (/apps/knowledge/access/$buyer/{owner}_{topicKey}_{entryId})",
   "packages/cli/src/commands/patch.ts patchUse(), patchBuy() output; packages/cli/src/commands/branch.ts wallet()",
   "packages/node/src/api.ts POST /api/patches/:id/buy, GET /api/me/wallet",
   "packages/agent/src/agent.ts (no recordAccess call)"
  ]
 },
 {
  "id": "AZ-082",
  "persona": "AI agent / automation",
  "title": "Split the price along lineage when the source knowledge has a different author (royalty share 0.3)",
  "goal": "When an agent buys derived knowledge, the original knowledge creator is paid automatically on-chain without the buyer doing anything extra.",
  "preconditions": [
   "GET http://localhost:3402/api/info shows royalty_share=0.3; quorum=2; addresses from GET /api/nodes (currently node-a 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5, node-c 0x199Ff87086b81415Cd1402217FFBA3135A51bce9)",
   "Fixture: node-c (roles verifier+serving, seller of its own patches via http://localhost:3404/x402/patch/<id>) publishes a derived patch whose parent is authored by node-a. Log node-c in first (`NGRAM_HOME=$HOME/.ngram-cluster/node-c node packages/cli/dist/bin.js login --password <pw>`), then: `NGRAM_HOME=$HOME/.ngram-cluster/node-c node packages/cli/dist/bin.js patch publish /mnt/newdata/qwen3.8/results/train-fact/픽셀플러스.npz --id qa-royalty-child --name 'QA royalty child' --model Qwen3.8-Flash-Next --benchmark '{\"schema\":\"qa-royalty-child\",\"queries\":1,\"format\":[\"template\"],\"samples\":[{\"prompt\":\"종목코드 픽셀플러스 \",\"expect\":\"087600\"}]}' --price 10 --parents krx-all-2761 --topic finance/krx --announce` (distinct schema so it never supersedes krx-all-2761; duplicate sha256 is allowed); wait until node-a and node-b verifiers attest (GET http://localhost:3404/api/patches/qa-royalty-child → status LISTED, passed 2/2; allow several minutes)",
   "Agent identity funded with ≥ 10 AIN; Node 24 on PATH; no patch loaded in the shared model (otherwise the agent returns already_known without buying)",
   "Record node-a's balance: GET http://localhost:3402/api/chain → balance"
  ],
  "steps": [
   "`curl -s http://localhost:3404/x402/patch/qa-royalty-child` and read requirements[0].payTo and maxAmountRequired",
   "`node packages/agent/dist/bin.js run --market http://localhost:3404 --patch qa-royalty-child --expect 087600 --json`",
   "GET http://localhost:3404/api/ledger?kind=settle&limit=1 and read body.royalty",
   "GET http://localhost:3404/api/events?kind=royalty&limit=1",
   "GET http://localhost:3402/api/chain (node-a balance) and `NGRAM_HOME=$HOME/.ngram-cluster/node-a node packages/cli/dist/bin.js wallet`",
   "Contrast: GET http://localhost:3402/api/ledger?kind=settle and look at a krx-all-2761 settlement's royalty"
  ],
  "expected": [
   "Step 1: payTo=<node-c address> (node-c is the seller of its own patch), maxAmountRequired='10', asset 'AIN'",
   "Step 2: agent pays 10 AIN to node-c; the non-JSON log would read '    settled: tx 0x…  {\"settled\":true,\"tx\":\"0x…\",\"royalty\":{\"<node-c address>\":\"7\",\"<node-a address>\":\"3\"}}'; JSON success=true, amount='10'",
   "Step 3 settle body: seller=<node-c>, buyer=<agent>, amount='10', royalty {node-c:'7', node-a:'3'} (pool = 10 × 0.3 = 3 split across 1 ancestor author; lineage walk follows anchor.parents)",
   "Step 4: event kind 'royalty' with message 'paid 3 AIN royalty to 0xF7A9dE499… (<tx prefix>)' on patch qa-royalty-child (best-effort on-chain transfer executed by the seller node)",
   "Step 5: node-a's AIN balance increased by 3; node-a wallet lists the row under 'royalties received' with PATCH qa-royalty-child, AMOUNT 3 (API /api/me/wallet royalties[] has {patch_id:'qa-royalty-child', amount:'3'})",
   "Step 6: krx-all-2761 settlements carry royalty {node-a:'25'} only — parent krx-all-2761-ep12 has the same author, so the ancestor share folds back into the seller and no 'royalty' event is emitted"
  ],
  "priority": "P1",
  "area": "x402",
  "automation": "api",
  "evidence": [
   "packages/core/src/catalog.ts royaltySplit() (ancestor authors via anchor.parents, pool = amount × share)",
   "packages/node/src/market.ts settlePayment() (royalty map in settlement, ledger.transfer per ancestor, 'royalty' event), announce() gateway_url = this node",
   "packages/node/src/api.ts GET /api/me/wallet (royalties = settlements where seller !== me and royalty[me]), GET /api/info royalty_share",
   "packages/cli/src/commands/patch.ts patchPublish (--parents, --announce); packages/cli/src/commands/branch.ts wallet() 'royalties received'"
  ]
 },
 {
  "id": "AZ-083",
  "persona": "AI agent / automation",
  "title": "Meter live-test hits through POST /api/chat and read them back as usage events with a per-visitor quota",
  "goal": "Every patched answer served to an automated caller is recorded as a billable 'hit' with latency data, and anonymous callers are capped at 20 per hour.",
  "preconditions": [
   "Seller node http://localhost:3402 holds the body of krx-all-2761 and its runtime is available (GET /api/chat/patches lists krx-all-2761; runtime.available=true)",
   "Caller is NOT logged in as operator (no ngram_session cookie) so the visitor key is 'ip:<addr>'; no anonymous live test from this IP in the last hour",
   "Note the latest usage event seq: GET http://localhost:3402/api/events?kind=usage&limit=1"
  ],
  "steps": [
   "`curl -s -X POST http://localhost:3402/api/chat -H 'content-type: application/json' -d '{\"patch_id\":\"krx-all-2761\",\"mode\":\"compare\",\"messages\":[{\"role\":\"user\",\"content\":\"종목코드 픽셀플러스 \"}],\"max_tokens\":16}'`",
   "GET http://localhost:3402/api/events?kind=usage&limit=1 and GET http://localhost:3402/api/patches/krx-all-2761/events?limit=1",
   "`curl -s -X POST http://localhost:3402/api/chat -H 'content-type: application/json' -d '{\"patch_id\":\"krx-all-2761\",\"mode\":\"base\",\"messages\":[{\"role\":\"user\",\"content\":\"What is the ticker of Pixelplus?\"}]}'` then re-read the newest usage event",
   "Repeat step 1 in a loop until remaining_quota reaches 0, then send one more request (at most 21 requests in the same hour from the same IP; the base-only call of step 3 also consumed one try)",
   "`curl -s -X POST http://localhost:3402/api/chat -H 'content-type: application/json' -d '{\"patch_id\":\"krx-all-2761\",\"mode\":\"compare\",\"messages\":[]}'`"
  ],
  "expected": [
   "Step 1: HTTP 200 JSON with patch_id='krx-all-2761', mode='compare', base.content (pre-patch answer), patched.content containing '087600', benchmark_hit=true (prompt matches a benchmark sample; expected 087600), applied_ms > 0, was_applied=false, model='Qwen3.8-Flash-Next', quota_limit=20 and remaining_quota one less than before",
   "Step 2: newest event has kind='usage', patch_id='krx-all-2761', message 'live test krx-all-2761 (compare) by ip:<addr>: patched hit=true', data {visitor:'ip:<addr>', mode:'compare', hit:true, base_ms, patched_ms, applied_ms}; the same event appears under /api/patches/krx-all-2761/events",
   "Step 3: mode 'base' returns patched=null and benchmark_hit=null; the usage event message ends with 'base only' (no hit is metered when no patched answer was served); a free-form question in compare mode yields hit=null ('not auto-scored')",
   "Step 4: when the 20-per-hour window is exhausted the request answers HTTP 429 {\"error\":\"quota_chat: free live-test quota exhausted for this hour — buy the patch or run your own node\",\"quota_reset\":<epoch ms>} — quota_reset is the measured end of this visitor's window (window start + 3,600,000 ms), which is what the live-test page prints in place of a Retry button that could do nothing — and no usage event is written for it (the quota is checked before the runtime runs)",
   "Step 5: HTTP 400 {\"error\":\"invalid request\",\"issues\":[…]} (messages must have 1–24 items); no usage event",
   "`patch_id` stays valid for a single knowledge; POST /api/chat also accepts `patch_ids` (1–3 ids, exactly one of the two fields) and then returns `patch_ids`, `applied[]` and one usage event per knowledge — see AZ-101"
  ],
  "priority": "P1",
  "area": "api",
  "automation": "api",
  "evidence": [
   "packages/node/src/api.ts POST /api/chat (chatQuota check, 429 message, remaining_quota/quota_limit), GET /api/events, GET /api/patches/:id/events, zod schema",
   "packages/node/src/market.ts chat() ('usage' event with visitor/mode/hit/base_ms/patched_ms/applied_ms), chatQuota() (20 per 3600000 ms)",
   "packages/web/src/i18n/pages/chat.ts 'chat.dev.body' ('every patched answer is metered as a usage hit'), 'chat.hit.unknown'"
  ]
 },
 {
  "id": "AZ-084",
  "persona": "AI agent / automation",
  "title": "Inspect the agent's identity, catalog view and credit balance with the keys / catalog / balance subcommands",
  "goal": "An operator scripting the agent can discover its address to fund, see which knowledge is buyable, and machine-read everything as JSON.",
  "preconditions": [
   "Cluster up with the demo catalog fully verified (krx-all-2761 LISTED, three SUPERSEDED with attest 2/2); Node 24 on PATH (`export PATH=$HOME/.local/node/bin:$PATH`); working directory /mnt/newdata/ainize/knowledge-marketplace",
   "Default agent home ~/.ngram-agent exists (created automatically on first use)"
  ],
  "steps": [
   "`node packages/agent/dist/bin.js keys` and `node packages/agent/dist/bin.js keys --reveal --json`; `ls -l ~/.ngram-agent/identity.json`",
   "`NGRAM_AGENT_HOME=/tmp/claude-agent-env node packages/agent/dist/bin.js keys`",
   "`node packages/agent/dist/bin.js catalog --market http://localhost:3402` then `… catalog --market http://localhost:3402 --status LISTED,SUPERSEDED --json | python3 -c 'import json,sys;print([(e[\"anchor\"][\"id\"],e[\"status\"],e[\"superseded_by\"],e[\"quorum_ok\"]) for e in json.load(sys.stdin)])'`",
   "`node packages/agent/dist/bin.js catalog --market http://localhost:3402 --status REJECTED`",
   "`node packages/agent/dist/bin.js balance --market http://localhost:3402` and `… balance --market http://localhost:3402 --json`; `… balance --market http://localhost:3402 --initial 5`",
   "`node packages/agent/dist/bin.js balance --market http://localhost:9999`",
   "`node packages/agent/dist/bin.js` (no command) and `node packages/agent/dist/bin.js --help`"
  ],
  "expected": [
   "keys prints three rows 'address     0x<40 hex>', 'publicKey   <128 hex>', 'home        /home/comcom/.ngram-agent'; --reveal --json adds \"privateKey\"; identity.json has mode -rw------- (0600)",
   "With NGRAM_AGENT_HOME set, keys reports home '/tmp/claude-agent-env' and a different, newly generated address (the env var overrides ~/.ngram-agent; --home overrides both)",
   "Default catalog prints exactly the LISTED rows: 'krx-all-2761             LISTED       270053 rows  25 AIN  attest 2/2  KRX ticker codes for 2,761 listed companies (final)'; the JSON query lists krx-all-2761 (LISTED, [], True) plus pixelplus-087600, krx-all-2761-ep12, krx-all-2761-ep6 (SUPERSEDED, ['krx-all-2761', …], True)",
   "`--status REJECTED` prints '(no patches)' and exits 0",
   "balance prints '<address>  100 CREDIT (initial credit 100 from http://localhost:3402/api/info; CREDIT = local dev credit of this node, not AIN)'; JSON has {address, balance:100, currency:'CREDIT', initial_credit:100, initial_credit_source:'http://localhost:3402/api/info'}; with --initial 5 the source is 'override' and balance 5. Purchases on this AIN demo are ain-transfer, so they do not reduce CREDIT — AIN funds must be read from the chain (ain_getBalance) or the node's /api/chain",
   "Unreachable market: stderr 'balance failed: info request failed: …' (or fetch failed), exit code 1",
   "No command: 'Specify a command. Try `ainize-agent --help`.' with exit 1; --help lists commands run, catalog, balance, keys and global options --market (default http://localhost:3402), --home, --json"
  ],
  "priority": "P2",
  "area": "agent",
  "automation": "cli",
  "evidence": [
   "packages/agent/src/bin.ts (commands keys/catalog/balance, PROG name, demandCommand message, catalog row format)",
   "packages/agent/src/identity.ts agentHome() (NGRAM_AGENT_HOME), loadIdentity() (0o600)",
   "packages/agent/src/agent.ts fetchCatalog(), creditBalance(), fetchInitialCredit()",
   "packages/node/src/api.ts GET /api/info (initial_credit), GET /api/catalog status filter"
  ]
 },
 {
  "id": "AZ-085",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Show a plain error when the node API is unreachable on every public page",
  "goal": "A visitor who loses the connection to the node sees an explicit error instead of a blank page, a permanent spinner or a misleading empty state.",
  "preconditions": [
   "Demo cluster running (scripts/cluster.mjs): web UI at http://localhost:3402",
   "Chrome with DevTools available",
   "UI language English (default)"
  ],
  "steps": [
   "Open http://localhost:3402/chat/krx-all-2761 and wait until the knowledge picker shows the 4 items",
   "Open DevTools > Network and tick 'Offline'",
   "Type 'hello' in the box labelled 'Type a question and press Enter (Shift+Enter for a new line)' and press Enter",
   "Untick 'Offline' and click the 'Retry' button under the failed turn (stay on the page — the transcript lives in component state)",
   "Tick 'Offline' again and click the header link 'Explore knowledge'",
   "Still offline, click the header link 'Public record', then 'Network'; then untick 'Offline'"
  ],
  "expected": [
   "Step 3: the turn shows a red alert (role=alert) with exactly 'Could not reach the node. Check your connection and try again.' and a 'Retry' button; no raw 'TypeError' text",
   "Step 4: 'Retry' resends the same prompt and the turn completes with 'Before loading' / 'After loading' bubbles",
   "Step 5: /explore renders the red alert 'Something went wrong: TypeError: Failed to fetch' (common.error with the transport message); no spinner is left running",
   "Step 6: /ledger shows the empty box 'No records yet.'; /network renders the cached 'This node' / 'Model server' cards (RTK Query keeps the last /api/info data from the header's poll), the empty 'No connected nodes yet…' state and a spinner under 'Knowledge tracks' — LedgerPage and NetworkPage have no error branch; log as a defect if the team expects an explicit error message there; the header badge 'AI Network' never disappears"
  ],
  "priority": "P0",
  "area": "error",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ChatPage.tsx mapChatError (FETCH_ERROR -> chat.err.network), transcript in component state",
   "packages/web/src/i18n/pages/chat.ts chat.err.network, chat.turn.retry",
   "packages/web/src/i18n/pages/common.ts common.error",
   "packages/web/src/api/api.ts errorMessage",
   "packages/web/src/pages/ExplorePage.tsx (error branch)",
   "packages/web/src/pages/LedgerPage.tsx (no error branch)",
   "packages/web/src/pages/NetworkPage.tsx (spinner while !info; shared /api/info cache)"
  ]
 },
 {
  "id": "AZ-086",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Refuse to downgrade to an integrity-only attestation during the 15-minute runtime grace period",
  "goal": "A temporary vLLM outage never produces a hash-only attestation that would dilute the 'Verified on the real model' count.",
  "preconditions": [
   "docker container 'flashnext' (vLLM on :8000) is paused (`docker pause flashnext`) and 60 s have passed (Runtime.status() is cached 30 s); unpause it afterwards",
   "Operator password known for node-a (create it on http://localhost:3402/signing if 'Create the operator password' is shown)",
   "krx-all-2761 currently shows 'Verified on the real model 2/2' and '0' integrity-only checks; node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Run: curl -s -c /tmp/ainize.cookies -H 'content-type: application/json' -d '{\"password\":\"<operator password>\"}' http://localhost:3402/api/auth/login",
   "Run: curl -s -i -b /tmp/ainize.cookies -X POST http://localhost:3402/api/patches/krx-all-2761/verify",
   "Open http://localhost:3402/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761 and click the tab 'Verification'",
   "Run: curl -s 'http://localhost:3402/api/events?limit=5&kind=verifier' | head -c 600",
   "Run: docker unpause flashnext"
  ],
  "expected": [
   "Step 1: HTTP 200 {\"ok\":true,\"token\":...} and a Set-Cookie ngram_session",
   "Step 2: HTTP 500 with body exactly {\"error\":\"runtime unavailable (serving API unreachable) — waiting up to 15 min before hash-only fallback\"}; no attestation is returned (the variant 'serving API unreachable (retrying for 15 more min before hash-only fallback)' only occurs when the runtime was reachable but the benchmark run itself failed)",
   "Step 3: summary still reads 'Run on the real model 2/2' and 'Integrity only 0'; the table still has exactly two rows (node-b, node-c) with Method 'run on the real model' and Accuracy '26/26'; no new row with 'integrity only'",
   "Step 4: the newest verifier event is the info line 'verifying krx-all-2761 (KRX ticker codes for 2,761 listed companies (final))'; no attest record was appended to the public record (Public record 'Records' count unchanged)"
  ],
  "priority": "P0",
  "area": "verification",
  "automation": "api",
  "evidence": [
   "packages/node/src/verifier.ts RUNTIME_GRACE_MS, verifyOne (graceLeft branches)",
   "packages/node/src/runtime.ts status() (30 s cache, 'serving API unreachable')",
   "packages/node/src/api.ts POST /api/patches/:id/verify, POST /api/auth/login",
   "packages/web/src/pages/PatchPage.tsx Verification (SummaryRow, isExecuted)",
   "packages/web/src/i18n/pages/detail.ts detail.ver.summary_executed, detail.ver.summary_integrity, detail.how.executed"
  ]
 },
 {
  "id": "AZ-087",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Switch the whole UI between English and Korean and keep the choice across reloads and pages",
  "goal": "A Korean-speaking visitor flips one toggle and every label, status, help text and error follows, and the choice survives navigation and refresh.",
  "preconditions": [
   "Demo cluster running",
   "Fresh browser profile or localStorage key 'ainize.locale' removed (DevTools > Application)"
  ],
  "steps": [
   "Open http://localhost:3402/explore and confirm English defaults; note the header language button label",
   "Click the header button '한국어'",
   "Read the page title, sort button, filter labels, count line, the first list item's status chip and meta line, and the header nav",
   "Reload the page; then open http://localhost:3402/ (landing), http://localhost:3402/chat/krx-all-2761 and http://localhost:3402/ledger",
   "In DevTools console run: localStorage.getItem('ainize.locale')",
   "On /chat send the chip '종목코드 픽셀플러스' and read the bubble meta; then click the header 'English' button and reload",
   "With the UI in Korean, read the browser tab title and document.documentElement.lang on /explore, /, /chat/krx-all-2761, /ledger and an unknown URL; on the Korean landing check that the h1 and both hero pills actually render glyphs"
  ],
  "expected": [
   "Step 1: title 'Explore knowledge', sort 'Most popular', filters 'Model' / 'Topic' / 'Show' / 'All', search placeholder 'Search by name or description', count '1 knowledge' (Explore opens on 'Current only'; 'All versions' shows all 4), nav 'Explore knowledge · Live test · Network · Public record · Docs & API · Sign in', language button '한국어' (it names the other language); document.title is 'Explore knowledge · Ainize' and <html lang> is 'en'",
   "Step 3: title '지식 둘러보기', sort '인기순', filters '대상 모델' / '주제' / '표시' / '전체', placeholder '지식 이름·설명 검색', count '지식 1개' (and '지식 4개' with '모든 버전'), chips '검증 완료' (검증 배지) and '판매 중' (목록 상태, status.LISTED) and, with '모든 버전' on, '최신 버전: krx-all-2761', meta '만든 사람: node-a · 대상 모델: Qwen3.8-Flash-Next · 주제: krx-ticker-codes', nav '지식 둘러보기 · 라이브 테스트 · 네트워크 · 공개 기록 · 문서·API · 로그인', button now 'English'; document.title is '지식 둘러보기 · Ainize' and <html lang> is 'ko' (both follow the toggle with no reload); identifiers, knowledge names/descriptions (server data), model name, 'AI Network' badge and 'AIN' stay untranslated by design; no raw keys like 'explore.title' appear",
   "Step 4: Korean persists after reload and on every route (landing hero '지식을 AI에 끼우다', chat title '라이브 테스트', ledger title '공개 기록'); the landing page's dark nav has its own toggle that switches back too; each route names itself in the tab ('지식을 AI에 끼우다 · Ainize', '라이브 테스트 · Ainize', '공개 기록 · Ainize', 'KRX ticker codes for 2,761 listed companies (final) · Ainize', '404. 페이지를 찾을 수 없습니다 · Ainize')",
   "Step 5: returns 'ko'",
   "Step 6: bubble meta uses Korean units ('응답 536ms', '넣는 데 2.9초', '정답'); after 'English' + reload the UI is English again and localStorage holds 'en'",
   "The Korean landing page renders every heading and both hero CTA labels even on a host with no system Hangul font (fc-list :lang=ko empty): theme.font.display and theme.font.mono both end in 'Apple SD Gothic Neo', 'Noto Sans KR', the webfont index.html already downloads — h1 '지식을 AI에 끼우다' has a non-zero width and its computed font-family contains 'Noto Sans KR'"
  ],
  "priority": "P0",
  "area": "i18n",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/i18n/index.ts LocaleProvider (KEY 'ainize.locale', default 'en')",
   "packages/web/src/i18n/pages/common.ts common.locale, nav.*",
   "packages/web/src/i18n/pages/public.ts explore.*, landing.hero.title",
   "packages/web/src/i18n/pages/chat.ts chat.bubble.latency, chat.bubble.applied, chat.hit.yes",
   "packages/web/src/components/chat/util.ts fmtMs (ko '초')",
   "packages/web/src/components/ui/Header.tsx LocaleButton; packages/web/src/pages/LandingPage.tsx LocaleButton"
  ]
 },
 {
  "id": "AZ-088",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Keep the operator signed in across refresh and new tabs via the session cookie, and sign out cleanly",
  "goal": "An operator does not have to re-enter the node password on every reload, and logging out really revokes access to operator pages.",
  "preconditions": [
   "Demo cluster running; node-a at http://localhost:3402",
   "If /signing shows 'Create the operator password', create one first (min 4 chars, tick 'I agree to the Terms and Policies (required)')"
  ],
  "steps": [
   "Open http://localhost:3402/signing, enter the operator password under 'Sign in to your node' and click 'Sign in'",
   "Press F5 on the resulting page; then open a new tab at http://localhost:3402/account",
   "In DevTools > Application > Cookies inspect 'ngram_session'; in the console run document.cookie",
   "Open http://localhost:3402/ in the signed-in tab",
   "Open the header menu 'node-a ▾' and click 'Log out'; then open http://localhost:3402/dashboard",
   "Sign in again from that redirect"
  ],
  "expected": [
   "Step 1: redirected to /dashboard with title 'My knowledge'; header shows 'My knowledge' and the menu button 'node-a ▾' instead of 'Sign in'",
   "Step 2: after refresh the dashboard shows a brief spinner then renders (no bounce to /signing); the new tab renders 'Account settings' directly",
   "Step 3: cookie ngram_session is HttpOnly, SameSite=Lax, expires in about 30 days; document.cookie does not contain it",
   "Step 4: the landing page immediately redirects a signed-in operator to /dashboard",
   "Step 5: after 'Log out' the browser lands on '/' (landing, no redirect loop) and /dashboard now redirects to /signing?next=%2Fdashboard; GET /api/auth/me returns signedIn false",
   "Step 6: signing in returns to /dashboard (the next parameter is honoured)"
  ],
  "priority": "P0",
  "area": "signing",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/api.ts SESSION_COOKIE, /api/auth/login (httpOnly, sameSite lax, 30 d), /api/auth/logout, /api/auth/me",
   "packages/web/src/auth/AuthContext.tsx (useMeQuery)",
   "packages/web/src/components/base/Layout.tsx SigningCheckLayout redirect, FullScreenLayout redirect",
   "packages/web/src/pages/SigningPage.tsx next param",
   "packages/web/src/components/ui/Header.tsx logout -> navigate('/')",
   "packages/web/src/i18n/pages/operator.ts op.sign.login.title, op.sign.login.button; common.ts nav.dashboard, nav.logout"
  ]
 },
 {
  "id": "AZ-089",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Recover automatically after the node process restarts under an open Live test tab",
  "goal": "When the seller node dies and comes back, an open browser tab reports the outage in plain language and heals itself without a manual reload.",
  "preconditions": [
   "Demo cluster running via scripts/cluster.mjs (note: the supervisor only respawns a child that exits within 15 s of being started — a SIGTERM to the long-running node-a is NOT respawned, so the tester restarts it by hand)",
   "Shell access on the demo machine; `export PATH=\"$HOME/.local/node/bin:$PATH\"; cd /mnt/newdata/ainize/knowledge-marketplace`",
   "http://localhost:3402/chat/krx-all-2761 open in a browser, picker loaded, no operator sign-in"
  ],
  "steps": [
   "In a shell run: ss -ltnp 'sport = :3402' and note the pid of packages/node/dist/bin.js",
   "Run: kill <pid>  (SIGTERM)",
   "Immediately in the browser click the sample chip '종목코드 픽셀플러스' and press Enter",
   "After ~10 s restart node-a: `NGRAM_HOME=$HOME/.ngram-cluster/node-a NGRAM_PORT=3402 setsid nohup node packages/node/dist/bin.js >> ~/.ngram-cluster/node-a/node.log 2>&1 &` (or bounce all three nodes with scripts/cluster-restart.sh)",
   "Wait 30 s without reloading; run: curl -s http://localhost:3402/api/info | head -c 200",
   "Click 'Retry' under the failed turn",
   "Run: curl -s http://localhost:3402/api/runtime | python3 -c \"import sys,json;print(json.load(sys.stdin)['applied'])\""
  ],
  "expected": [
   "Step 3: while the port is closed the turn fails with 'Could not reach the node. Check your connection and try again.' and a 'Retry' button (connection refused maps to FETCH_ERROR)",
   "Step 5: /api/info answers 200 again with node.name 'node-a'; the chat page's 20 s poll of /api/chat/patches repopulates 'Knowledge to test' without a reload; header badge 'AI Network' stays",
   "Step 6: Retry succeeds: 'Before loading' and 'After loading' bubbles appear, the patched bubble shows 'loaded in N.Ns' and '✓ Correct' with expected 087600",
   "Step 7: applied is [] (the shared table was restored; a stale lock from the killed process is broken by the liveness check in acquireLock)"
  ],
  "priority": "P1",
  "area": "error",
  "automation": "manual",
  "evidence": [
   "scripts/cluster.mjs spawnNode (respawn only when the child exited within 15 s of start)",
   "packages/web/src/pages/ChatPage.tsx pollingInterval 20_000, mapChatError",
   "packages/web/src/i18n/pages/chat.ts chat.err.network, chat.bubble.applied, chat.hit.yes",
   "packages/node/src/runtime.ts acquireLock (holderAlive check)",
   "packages/node/src/api.ts GET /api/runtime"
  ]
 },
 {
  "id": "AZ-090",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Reflect the model-server outage consistently on Network, Manage and My knowledge",
  "goal": "Operators and visitors see the same truthful runtime status everywhere, and load/unload controls are disabled instead of failing.",
  "preconditions": [
   "docker container 'flashnext' is paused (`docker pause flashnext`) and at least 60 s have passed",
   "Operator password already created on http://localhost:3402/signing and the operator is signed in on 3402",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/network and read the card 'Model server'",
   "Open http://localhost:3402/project/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761 and scroll to the section 'Load into / unload from the model'",
   "Open http://localhost:3402/dashboard and read the status line under the 'Purchased knowledge' table",
   "Run: docker unpause flashnext; wait 60 s; reload /network"
  ],
  "expected": [
   "Step 1: 'Status' shows a grey dot and the raw reason 'serving API unreachable' (rt.error replaces 'available — knowledge can be loaded live'); 'Live connection' shows 'not connected'; 'Model' shows '—'",
   "Step 2: both buttons 'Load into model' and 'Unload' are disabled; the status text reads 'not loaded · Model runtime unavailable: serving API unreachable — load/unload is disabled for now.'",
   "Step 3: the dashboard shows 'Model runtime unavailable: serving API unreachable — load/unload is disabled for now.'; on node-a the purchases table is usually empty ('No purchases yet — pick verified knowledge in Explore.') — if node-a has purchases, their per-row 'Load into model' / 'Unload' buttons are disabled",
   "Step 4: 'Status' returns to a green dot with 'available — knowledge can be loaded live', 'Model' = 'Qwen3.8-Flash-Next', 'Live connection' = 'connected — load and unload without restart'"
  ],
  "priority": "P1",
  "area": "network",
  "automation": "manual",
  "evidence": [
   "packages/web/src/pages/NetworkPage.tsx runtime card (rt.error ?? runtime_down)",
   "packages/web/src/i18n/pages/detail.ts detail.net.runtime_ok, detail.net.runtime_down, detail.net.hook_ok, detail.net.hook_no",
   "packages/web/src/pages/ManagePage.tsx runtime section (disabled buttons + op.runtime.unavailable)",
   "packages/web/src/pages/DashboardPage.tsx purchases table status line",
   "packages/web/src/i18n/pages/operator.ts op.runtime.unavailable, op.manage.runtime.not_loaded, op.dash.purchases.empty"
  ]
 },
 {
  "id": "AZ-091",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Show honest loading states while a 331.7 MB knowledge is loaded, and allow cancelling",
  "goal": "A visitor waiting tens of seconds for a compare test always knows what is happening and can bail out.",
  "preconditions": [
   "Demo cluster running with vLLM available",
   "http://localhost:3402/chat/krx-all-2761 open (krx-all-2761 is 347,829,014 bytes = 'Size 331.7 MB' in Explore)",
   "No operator sign-in (visitor quota applies)"
  ],
  "steps": [
   "Note the footer text under the composer (e.g. 'Free tries are limited per hour. No sign-in needed.' or 'Free trial N/20 left this hour')",
   "Select 'Compare', click the chip '종목코드 픽셀플러스', press Enter and observe the UI during the request",
   "After the answer arrives, read the meta line of the 'After loading' bubble",
   "Send the same prompt again and click 'Cancel' / 'Stop waiting' in the strip above the composer within 2 s",
   "Read the footer quota text, click 'Retry' under the cancelled turn and read the footer again after it completes"
  ],
  "expected": [
   "Step 2: user bubble 'You' appears; both bubbles show three pulsing dots (aria-label 'Generating…') and the note 'Includes loading and unloading — this can take tens of seconds.'; the Send button shows a spinner with 'Waiting for the answer…'; a strip (role=status) reads 'Waiting for the answer — you can cancel if it takes too long.' with a 'Cancel' button; the 'View' radios, 'Enable thinking' and the textarea are disabled; the spinner does not block the picker",
   "Step 3: the patched bubble header shows 'After loading · reply NNNms · loaded in N.Ns' (applied_ms around 2-4 s for this file) and '✓ Correct'; the base bubble shows 'Before loading · reply N.Ns'",
   "Step 4: the turn switches to a red alert naming which of the two things happened — 'You stopped waiting. The node had not started this test yet, so no free try was used.' while it was still queued, or 'You stopped waiting, but the test had already started on the shared model, so it still counts as one free try.' once the node had taken the shared lock — with a 'Retry' button; the composer unlocks immediately. The pre-D3 wording 'Request cancelled.' must no longer appear: a wait is not a rejection, and the visitor is told whether it cost them anything",
   "Step 5: the quota text is unchanged at cancel time either way (no response arrived). What the retry then costs follows the message, and which message appears is a genuine race against the shared lock: cancelled while still QUEUED nothing was ever sent to the model, so only the retry is charged and the footer drops by one; cancelled once it was already RUNNING the node finishes the work and charges it, so cancel + retry drop the footer by two (e.g. 19/20 to 17/20). The 'a cancelled try is always charged' finding this scenario used to record is fixed for the queued case (POST /api/chat answers HTTP 499 and consumes nothing)"
  ],
  "priority": "P1",
  "area": "perf",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/chat/TurnView.tsx pending Dots / PendingNote / QueuePending",
   "packages/web/src/pages/ChatPage.tsx CancelRow, cancel(), inflight abort, quota handling",
   "packages/web/src/components/chat/ChatComposer.tsx loading / locked",
   "packages/node/src/chat-queue.ts ChatQueue.cancel() (free while queued, honest once running) + ChatCancelledError (HTTP 499)",
   "packages/node/src/api.ts POST /api/chat (quota checked without consuming, consumed only after market.chat resolves), POST /api/chat/cancel",
   "packages/web/src/i18n/pages/chat.ts chat.queue.cancelled, chat.queue.cancelled_late, chat.bubble.compare_pending, chat.input.sending, chat.bubble.applied",
   "packages/web/src/utils/format.ts bytes()"
  ]
 },
 {
  "id": "AZ-092",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Keep every page usable at 360 px width without horizontal page scrolling, and the header free of collisions at desktop widths",
  "goal": "A visitor on a small phone can reach all navigation items, read lists and scroll wide tables inside their container.",
  "preconditions": [
   "Demo cluster running",
   "Chrome DevTools device toolbar set to 360 x 740, English UI",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/ and inspect the top navigation ('Explore knowledge', 'Live test', 'Node sign-in', language button)",
   "Open http://localhost:3402/explore; check the header row (logo, 'AI Network' badge, 'Explore knowledge', 'Live test', 'Network', 'Public record', 'Docs & API', 'Sign in', '한국어') and the list items",
   "Open http://localhost:3402/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761, check the stats strip, then open the tab 'Verification' and swipe the table sideways",
   "Open http://localhost:3402/chat/krx-all-2761, send the chip '종목코드 픽셀플러스'",
   "Open http://localhost:3402/ledger and scroll to 'Origin → derivative map'",
   "On each page run in the console: document.documentElement.scrollWidth <= window.innerWidth",
   "Set the viewport to 1440, 1280, 1024 and 960 px on /explore and measure the 'AI Network' ledger badge against the first nav link"
  ],
  "expected": [
   "Step 1: the landing nav wraps onto a second line (flex-wrap) and all four controls are tappable",
   "Step 2: all header items are reachable and the console check is true - NOTE the inner Header nav uses white-space: nowrap without flex-wrap (Header.tsx), so overflow at 360 px is a likely defect: record actual behaviour; Explore items switch to 16 px padding and a 40 px icon; the right-hand price column (e.g. '25 AIN') is hidden below 600 px by design",
   "Step 3: the seven stats ('Purchases', 'accuracy', 'Memory entries', 'Facts', 'Size', 'Price', 'Revenue') wrap; the verification table scrolls horizontally inside its wrapper while the page itself does not",
   "Step 4: the picker stacks above the transcript (single column below 960 px) and 'Before loading' / 'After loading' bubbles stack vertically below 600 px; the textarea and 'Send' stay on one row",
   "Step 5: the SVG map scrolls inside its box (overflow-x auto) and the legend wraps",
   "Step 6: true on every page (no body-level horizontal scroll)",
   "Step 7: with the webfonts in place, at 1440, 1280, 1024 and 960 px the 'AI Network' ledger badge and 'Explore knowledge' do not intersect, the logo and the nav share one row and the bar is 81 px tall, and the nav's items fit the width left beside the logo (Home is flex: 0 0 auto, Nav flex: 1 1 auto and the nav items carry 10 px of horizontal padding). While the fallback face is still showing, or below ~900 px, the bar wraps onto a second row instead of overlapping."
  ],
  "priority": "P1",
  "area": "a11y",
  "automation": "manual",
  "evidence": [
   "packages/web/src/components/ui/Header.tsx Nav / NavItem (nowrap, sm breakpoint padding)",
   "packages/web/src/pages/LandingPage.tsx NavLinks flex-wrap",
   "packages/web/src/components/public/PatchListItem.tsx PriceCol display:none below sm, Icon 40px",
   "packages/web/src/components/ui/Table.tsx TableWrapper overflow-x auto",
   "packages/web/src/pages/ChatPage.tsx Grid md breakpoint; packages/web/src/components/chat/TurnView.tsx Pair sm breakpoint",
   "packages/web/src/pages/LedgerPage.tsx GraphBox overflow-x auto",
   "packages/web/src/theme/theme.ts breakpoint sm 600 / md 960"
  ]
 },
 {
  "id": "AZ-093",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Operate the Live test and sign-in entirely from the keyboard with visible focus",
  "goal": "A keyboard-only user can pick knowledge, send a question with Enter, insert a newline with Shift+Enter and sign in, always seeing where focus is.",
  "preconditions": [
   "Demo cluster running with vLLM available",
   "Operator password already created for node-a",
   "Desktop browser, English UI, mouse untouched during the test"
  ],
  "steps": [
   "Open http://localhost:3402/chat/krx-all-2761 and press Tab repeatedly from the address bar, noting the focus order and the focus ring",
   "Tab to the picker item 'KRX ticker codes for 2,761 listed companies — epoch 12' (krx-all-2761-ep12) and press Enter",
   "Tab to the 'View' group and press Enter on 'After only'; Tab to 'Enable thinking' and press Space twice",
   "Tab into the textarea, type '종목코드', press Shift+Enter, type '픽셀플러스', then press Enter",
   "While the answer is pending press Shift+Tab from the textarea and observe; after the answer, Tab to 'Clear conversation' and press Enter",
   "Open http://localhost:3402/signing, type a wrong password in the focused 'Operator password' field and press Enter; then type the right password and press Enter"
  ],
  "expected": [
   "Step 1: order is 'Ainize home' link → 'Explore knowledge' → 'Live test' → 'Network' → 'Public record' → 'Docs & API' → 'Sign in' → language button → picker items → 'Details →' → sample chips → 'Show N more' → 'Compare' / 'After only' / 'Before only' → 'Enable thinking' → textarea (→ 'Send' only once text is typed; the button is disabled while the textarea is empty); every focused control shows a 2 px purple (#8b3eeb) outline with 2 px offset; the textarea shows a purple border plus pale ring instead",
   "Step 2: the URL becomes /chat/krx-all-2761-ep12, the item shows 'Selected' and the header changes to that knowledge's name with status 'Newer version available'",
   "Step 3: 'After only' becomes aria-checked=true; the checkbox toggles and the helper flips between 'Thinking is on — replies are slower' and the default help sentence",
   "Step 4: Shift+Enter inserts a line break (textarea grows, max 160 px) and Enter sends; a single 'After loading' bubble appears; the composer clears; no submit happens while an IME composition is active",
   "Step 5: during the request the textarea and Send are disabled and Shift+Tab reaches 'Cancel' (the cancel strip sits above the composer in DOM order); 'Clear conversation' empties the transcript back to 'No questions yet' and then disappears",
   "Step 6: the password field is auto-focused; wrong password shows the red alert 'wrong password' (server text); the right one signs in and lands on /dashboard ('My knowledge')"
  ],
  "priority": "P1",
  "area": "a11y",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/styles/GlobalStyle.ts :focus-visible outline",
   "packages/web/src/components/chat/ChatComposer.tsx onKey (Enter / Shift+Enter / isComposing), Box :focus style, Send disabled={locked || !text.trim()}",
   "packages/web/src/components/chat/KnowledgePicker.tsx Item buttons aria-pressed",
   "packages/web/src/pages/ChatPage.tsx CancelRow before ChatComposer, clear()",
   "packages/web/src/pages/SigningPage.tsx form onSubmit, autoFocus",
   "packages/node/src/api.ts POST /api/auth/login 'wrong password'",
   "packages/web/src/i18n/pages/chat.ts chat.thinking.slow, chat.input.clear, chat.empty.title"
  ]
 },
 {
  "id": "AZ-094",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Expose meaningful roles and accessible names to screen readers on the core pages",
  "goal": "A screen-reader user hears what each control is (home link, language toggle, menu, status alerts, tabs, live region) rather than unlabeled buttons and images.",
  "preconditions": [
   "Demo cluster running",
   "Operator signed in on http://localhost:3402 for the menu check",
   "Chrome DevTools Accessibility pane or an axe browser extension",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/explore; inspect the header logo link, the language button, the sort dropdown, the operator menu button and its popup",
   "Open http://localhost:3402/chat/krx-all-2761 (as visitor in a private window); inspect the left panel, the picker items, the 'View' group, the textarea, the transcript container, and trigger an error by going DevTools-offline and sending 'x'",
   "Open http://localhost:3402/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761 and inspect the tab strip and the status chip",
   "Open http://localhost:3402/ledger and inspect the SVG map and the pagination buttons",
   "Run axe on each page and note serious/critical violations"
  ],
  "expected": [
   "Step 1: logo link name 'Ainize home' (img alt 'Ainize'); language button aria-label 'language' with visible text '한국어'; sort button aria-haspopup='listbox' with role=listbox options carrying aria-selected; operator button 'node-a ▾' has aria-haspopup='menu' and aria-expanded toggling, popup role=menu with role=menuitem 'Account settings', 'Files & changes', 'Log out'",
   "Step 2: <aside aria-label='Knowledge to test'>; items are buttons with aria-pressed; radiogroup aria-label 'View' with role=radio aria-checked; textarea aria-label 'Type a question and press Enter (Shift+Enter for a new line)'; the transcript <section> has aria-live='polite'; the spinner has role=status aria-label 'loading'; the error alert has role=alert and reads 'Could not reach the node. Check your connection and try again.'; the runtime/lock/quota boxes use role=status",
   "Step 3: role=tablist with five role=tab ('Overview', 'Verification', 'Origins & derivatives', 'Buy', 'History') exposing aria-selected; the status chip 'For sale' has a title tooltip 'On sale as the current version for this topic. Whether it passed verification is what the \"Verified\" badge beside it says.' (note: tabs have no arrow-key navigation - record as P2 gap)",
   "Step 4: svg role=img aria-label 'knowledge origin and derivative map'; pagination arrows have aria-label 'previous' / 'next' and text buttons 'First' / 'Last'",
   "Step 5: no critical axe violations; document any 'button-name' or 'color-contrast' findings with element and page"
  ],
  "priority": "P1",
  "area": "a11y",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/ui/Header.tsx aria-label 'Ainize home', 'language', aria-haspopup, role menu/menuitem",
   "packages/web/src/components/ui/Misc.tsx Spinner role=status, SelectBox listbox, Tabs role=tablist/tab, Pagination aria-labels, StatusChip title",
   "packages/web/src/components/chat/KnowledgePicker.tsx aside aria-label, Item aria-pressed, Alert role=status",
   "packages/web/src/components/chat/ChatComposer.tsx radiogroup aria-label, textarea aria-label",
   "packages/web/src/pages/ChatPage.tsx Main aria-live=polite",
   "packages/web/src/components/chat/TurnView.tsx Alert role=alert, aria-busy",
   "packages/web/src/pages/LedgerPage.tsx svg role=img aria-label (detail.ledger.graph_aria)"
  ]
 },
 {
  "id": "AZ-095",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Format large numbers, sizes and prices consistently (270,053 entries, 331.7 MB, 25 AIN)",
  "goal": "Buyers can read knowledge size, fact counts and prices at a glance with thousands separators and units, in both languages.",
  "preconditions": [
   "Demo cluster running; krx-all-2761 has rows 270053, size_bytes 347829014, queries 2761, price 25 AIN; pixelplus-087600 has rows 2992, size 3854446, queries 8, price 0.1 AIN",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5); the ledger record count is a live value (GET /api/ledger info.records)"
  ],
  "steps": [
   "Open http://localhost:3402/explore, switch 'Show' to 'All versions' and read the meta lines and price column of 'krx-all-2761' and 'pixelplus-087600'",
   "Open http://localhost:3402/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761 and read the stats strip and the 'Overview' description line",
   "Open the 'Buy' tab and read 'Price'",
   "Open http://localhost:3402/chat/krx-all-2761 and read the picker item and the header facts",
   "Open http://localhost:3402/ledger and read 'Records' and 'Blocks recorded'",
   "Switch to Korean with '한국어' and re-read the Explore meta line and picker price"
  ],
  "expected": [
   "Step 1: krx-all-2761 shows '2,761 facts · 270,053 memory entries · Size 331.7 MB · 0 downloads' with price '25 AIN' and note 'AIN = AI Network token (this demo runs a local dev chain)'; pixelplus-087600 shows '8 facts · 2,992 memory entries · Size 3.7 MB · <n> downloads' and price '0.1 AIN' (after exactly one purchase the text reads the ungrammatical '1 downloads' - record as P2 copy defect)",
   "Step 2: stats read '0 Purchases', the accuracy stat as the measured pair '1/8 → 26/26' (score.pre_apply → score.free_generation) with the note '100% after loading', '270,053 Memory entries', '2,761 Facts', '331.7 MB Size', '25 AIN Price'; 'Revenue' shows 'Free' for a zero revenue (priceLabel maps 0 to 'Free') - record as defect, expected '0 AIN'; description line 'Accuracy 100% on 26 of 2,761 questions checked by verifiers'",
   "Step 3: 'Price' reads '25 AIN · pay once per download' with the AIN note",
   "Step 4: picker item shows '2,761 facts', '100% accuracy', '25 AIN'; header shows '2,761 facts'",
   "Step 5: 'Records' shows the live count (e.g. '20') and 'Blocks recorded' a thousands-separated block height such as '2,501'",
   "Step 6: Korean keeps the en-US digit grouping: '기억 항목 270,053개', '사실 2,761건', picker price '25 AIN'"
  ],
  "priority": "P1",
  "area": "i18n",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/utils/format.ts num(), bytes(), price()",
   "packages/web/src/components/public/PatchListItem.tsx usePriceLabel, meta lines",
   "packages/web/src/pages/PatchPage.tsx Stats (revenue via f.priceLabel), detail.ov.accuracy_line",
   "packages/web/src/pages/detail/recordText.ts priceLabel (0 -> common.free)",
   "packages/web/src/components/chat/KnowledgePicker.tsx priceLabel (ko-KR / en-US)",
   "packages/web/src/i18n/pages/common.ts units.rows, units.facts, price.ain_note; public.ts item.downloads, item.size, item.price_ain; detail.ts detail.ledger.height"
  ]
 },
 {
  "id": "AZ-096",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Read the Terms page and reach the 404 pages from bad URLs",
  "goal": "Legal text is reachable from every footer and wrong addresses end in a clear, recoverable page.",
  "preconditions": [
   "No operator session",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/explore and click 'Terms and Policies' in the footer",
   "Read the headings and the contact section",
   "Open http://localhost:3402/this-page-does-not-exist",
   "Click 'Go to Explore →'",
   "Open http://localhost:3402/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/no-such-knowledge and run: curl -s -i http://localhost:3402/api/patches/no-such-knowledge | head -1"
  ],
  "expected": [
   "Terms page title 'Terms and Policies', 'Last updated: August 31, 2026', sections '1. What Ainize is', '2. Terms of use' (2.1 Node identity … 2.6 No warranty, with the 'Prohibited use' bullet list), '3. Privacy policy' (3.1–3.4), '4. Contact' with 'Questions about these terms:' and a mailto link support@ainize.ai",
   "Single-segment unknown path: page '404. Page not found' with 'Either something went wrong or the page doesn't exist anymore.' and the link 'Go to Explore →' (navigates to /explore); the header and footer remain visible; the server still answers HTTP 200 for the SPA shell",
   "Two-segment unknown knowledge: same 404 layout with the message 'This node does not know the knowledge \"no-such-knowledge\". It may not have propagated yet, or the address is wrong.'; the API answers HTTP/1.1 404 {\"error\":\"patch not found\"}"
  ],
  "priority": "P2",
  "area": "error",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/App.tsx routes '/terms', '/:author/:patchId', '*'",
   "packages/web/src/pages/TermsPage.tsx; packages/web/src/pages/NotFoundPage.tsx; packages/web/src/pages/PatchPage.tsx (NotFoundPage message)",
   "packages/web/src/components/ui/Footer.tsx ('Terms and Policies')",
   "packages/web/src/i18n/pages/public.ts: terms.*, notfound.title, notfound.desc, notfound.home",
   "packages/web/src/i18n/pages/detail.ts: detail.patch.not_found",
   "packages/node/src/api.ts GET /api/patches/:id notFound('patch not found'); packages/node/src/server.ts SPA catch-all"
  ]
 },
 {
  "id": "AZ-097",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Show helpful empty states when a filter, search or section has nothing to display",
  "goal": "A visitor who filters everything away understands that nothing matched (not that the site is broken) and how to widen the search.",
  "preconditions": [
   "Demo cluster running (4 knowledge items; two seeded tracks finance/KRX-latest and finance/KRX-history; no 'challenge' record on the ledger); English UI",
   "Record count = the 'Records' cell of the Public record info card at test time"
  ],
  "steps": [
   "Open http://localhost:3402/explore and type 'zzzz' into 'Search by name or description'",
   "Clear the search and click the topic chip 'krx-ticker-codes' then the model chip 'Qwen3.8-Flash-Next'",
   "Open http://localhost:3402/ledger, open the kind dropdown 'All records' and choose 'Re-verification request'",
   "Open http://localhost:3402/network; in 'Gateway router' leave 'jurisdiction' / 'KR' and click 'Find nodes'",
   "Open http://localhost:3402/chat/krx-all-2761 in a fresh tab and read the transcript area"
  ],
  "expected": [
   "Step 1: count line '0 knowledge' and a dashed box 'No knowledge matches. Try another model, topic or search term.'; no spinner; pagination hidden",
   "Step 2: the list returns with '1 knowledge' (Explore's default 'Current only') and the pagination shows '1 / 1'; 'All versions' brings it to '4 knowledge'",
   "Step 3: the table is replaced by 'No \"Re-verification request\" records yet.'; switching back to 'All records' restores all rows (the live record count)",
   "Step 4: the 'Knowledge tracks' table lists the two seeded tracks (so 'No tracks yet.' is not shown); after 'Find nodes' a warning 'No track matches jurisdiction=KR.' appears (valid until a law/KR track is created in the branch scenario)",
   "Step 5: the empty transcript reads 'No questions yet' followed by 'Click a sample question below or type your own. You get two answers side by side: before and after loading the knowledge.'"
  ],
  "priority": "P2",
  "area": "explore",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ExplorePage.tsx Empty / explore.count",
   "packages/web/src/i18n/pages/public.ts explore.empty, explore.count",
   "packages/web/src/pages/LedgerPage.tsx detail.ledger.empty_kind; detail.ts detail.kind.challenge",
   "packages/web/src/pages/NetworkPage.tsx detail.net.tracks_empty, detail.net.no_match",
   "packages/web/src/pages/ChatPage.tsx EmptyState chat.empty.title / chat.empty.body"
  ]
 },
 {
  "id": "AZ-098",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Display relative times ('5m ago') with an absolute-time tooltip that keeps ticking",
  "goal": "Visitors can see how fresh a verification or record is at a glance and still get the exact timestamp when they need it.",
  "preconditions": [
   "Demo cluster running; English UI; system clock of the browser and node in sync",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761 and read the grey line under the status chip",
   "Open the 'Verification' tab, read the 'Time' column and hover a value",
   "Open http://localhost:3402/ledger, read the 'Time' column of the first rows and hover one; note a value ending in 'm ago' and wait 70 s",
   "Open http://localhost:3402/network and read 'Last seen' for the two peers",
   "Switch to Korean and re-read the ledger 'Time' column"
  ],
  "expected": [
   "Step 1: 'By node-a · target model Qwen3.8-Flash-Next · verified Nh ago' (or 'Nm ago' / 'Nd ago' depending on age; units s / m / h / d / mo / y)",
   "Step 2: values like '3h ago'; tooltip shows the absolute time in the form 'Aug. 31 2026, 14:02:11 +09:00'",
   "Step 3: newest record first (whatever kind was written last — a 'Verification' or 'Newer version' row, or 'Purchase settled' after a purchase), times like '12m ago'; the tooltip has the same absolute format; after the 10 s poll cycle the minute value has increased by 1 (the page re-renders on each poll, no manual reload needed)",
   "Step 4: 'Last seen' shows values such as '14s ago' that refresh every 15 s",
   "Step 5: Korean units '12분 전' / '3시간 전' with the same absolute tooltip"
  ],
  "priority": "P2",
  "area": "ledger",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/detail/recordText.ts ago()",
   "packages/web/src/i18n/pages/detail.ts detail.ago.s/m/h/d/mo/y, detail.patch.meta, detail.patch.listed_when",
   "packages/web/src/utils/format.ts dateTime()",
   "packages/web/src/pages/PatchPage.tsx Verification Time cell title=dateTime",
   "packages/web/src/pages/LedgerPage.tsx Time cell, pollingInterval 10_000",
   "packages/web/src/pages/NetworkPage.tsx last_seen, pollingInterval 15_000"
  ]
 },
 {
  "id": "AZ-099",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Verify what happens to scroll position and filters on browser Back from a detail page",
  "goal": "A visitor browsing a long list returns to where they were (or at least to a predictable, non-broken state) after opening a knowledge and pressing Back.",
  "preconditions": [
   "Demo cluster running; desktop browser; English UI",
   "Public record page holds enough records plus the map below to require scrolling (~20 records once the demo is fully verified)"
  ],
  "steps": [
   "Open http://localhost:3402/ledger, choose kind 'Verification' in the dropdown, scroll to the bottom section 'Origin → derivative map'",
   "Click the box 'krx-all-2761' in the map",
   "On the detail page click the tab 'History', scroll down, then press the browser Back button",
   "Note the scroll position and the kind dropdown; press Forward",
   "Repeat from http://localhost:3402/explore with 'Show' set to 'All versions': scroll to the last item 'Pixelplus ticker code (single fact)' (pixelplus-087600 under the default 'Most popular' sort), open it, press Back"
  ],
  "expected": [
   "Step 2: navigates to /<node-a address>/krx-all-2761 scrolled to the top (ScrollToTop runs on every pathname change)",
   "Step 3-4: /ledger reopens scrolled to the top with the dropdown reset to 'All records' (page state is component-local and ScrollToTop overrides browser restoration); Forward reopens the detail on the 'Overview' tab, not 'History' - record whether the team accepts this or wants position/filter restoration (P2 UX finding); no blank page, no duplicate fetch errors",
   "Step 5: /explore reopens at the top with sort 'Most popular', 'Show' back at its 'Current only' default and page '1 / 1'; with 'All versions' re-selected the list renders the same 4 items without a full spinner if the RTK cache is warm (only '· updating…' may flash)"
  ],
  "priority": "P2",
  "area": "ledger",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/base/Layout.tsx ScrollToTop (window.scrollTo(0,0) on pathname)",
   "packages/web/src/pages/LedgerPage.tsx kind/page useState, KnowledgeGraph Link",
   "packages/web/src/pages/PatchPage.tsx tab useState('overview')",
   "packages/web/src/pages/ExplorePage.tsx sort/page useState; explore.updating"
  ]
 },
 {
  "id": "AZ-100",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "Degrade gracefully when clipboard copy is unavailable or denied",
  "goal": "A visitor copying a content hash or node address gets 'Copied' feedback on success and no crash (ideally a visible failure) when the browser blocks the clipboard.",
  "preconditions": [
   "Demo cluster running; Chrome; English UI",
   "Optionally the machine's LAN IP to open the UI over an insecure (non-localhost) origin",
   "node-a address = GET /api/info node.address (currently 0xF7A9dE49902C95661AC6556D631e2B60a081A1F5)"
  ],
  "steps": [
   "Open http://localhost:3402/0xF7A9dE49902C95661AC6556D631e2B60a081A1F5/krx-all-2761, scroll to 'Integrity' and click 'Copy' next to 'Content hash'",
   "Paste into the search box on /explore to check the clipboard content, then clear it",
   "Back on the detail page open DevTools console and run: Object.defineProperty(navigator, 'clipboard', { value: undefined, configurable: true })",
   "Click 'Copy' again and watch the button label and the console",
   "Open http://localhost:3402/signing and click 'Copy' next to 'Account address' in the 'This node' box, with the same override applied",
   "Optional: open http://<LAN-IP>:3402/... (insecure context) and click 'Copy'"
  ],
  "expected": [
   "Step 1: the button label switches to 'Copied' for about 1.5 s and returns to 'Copy'",
   "Step 2: the pasted text is the 64-hex sha256 starting 57c9346349afd6fa",
   "Step 4: the click is swallowed silently: label stays 'Copy', no 'Copied', no uncaught exception in the console, the page keeps working (error is caught in CopyButton) - record the missing failure feedback as a P2 UX finding",
   "Step 5: same behaviour for the address copy on the sign-in page",
   "Step 6: in an insecure context navigator.clipboard is undefined, so the same silent no-op occurs; the rest of the page is unaffected"
  ],
  "priority": "P2",
  "area": "error",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/ui/Misc.tsx CopyButton (try/catch around navigator.clipboard.writeText, 1500 ms reset)",
   "packages/web/src/i18n/pages/common.ts common.copy, common.copied, common.copy_clipboard",
   "packages/web/src/pages/PatchPage.tsx Integrity section CopyButton",
   "packages/web/src/pages/SigningPage.tsx NodeBox CopyButton (op.address)"
  ]
 },
 {
  "id": "TM-090",
  "persona": "Teach mode (visitor)",
  "title": "Load up to three knowledges together in one live test, see the overlap warning, and meter one usage event per knowledge",
  "goal": "A visitor can tick several knowledges (max 3) in the Live test picker; they load in tick order (the last one wins on overlapping memory entries), the picker warns about the overlap, and the node bills one usage event per knowledge.",
  "preconditions": [
   "A teach-mode node (this branch) whose GET /api/chat/patches lists at least two knowledges that share memory entries (`overlaps[]` non-empty; on the demo data `krx-all-2761` ∩ `pixelplus-087600` = 2,170 rows) and `runtime.available=true`",
   "Caller is NOT signed in as operator (visitor quota applies)",
   "Note the newest usage event: GET /api/events?kind=usage&limit=1"
  ],
  "steps": [
   "Open `/chat`. The picker is titled \"Knowledge to load (pick up to 3)\" with the help line \"They load in the order you tick them. If two overlap, the one ticked last wins.\"",
   "Tick `KRX ticker codes for 2,761 listed companies (final)` then `Pixelplus ticker code (single fact)`; observe the order badges (1, 2), the counter \"2/3 selected\" and the address bar (`/chat/krx-all-2761,pixelplus-087600`)",
   "Tick a third knowledge, then hover a fourth: the remaining checkboxes are disabled with the tooltip \"Up to 3 — untick one first.\"",
   "Untick the third one, send the sample question `픽셀플러스 종목코드 알려줘. 숫자만.` in Compare mode",
   "`curl -s -X POST <node>/api/chat -H 'content-type: application/json' -d '{\"patch_ids\":[\"krx-all-2761\",\"pixelplus-087600\"],\"mode\":\"compare\",\"messages\":[{\"role\":\"user\",\"content\":\"종목코드 픽셀플러스 \"}],\"max_tokens\":16}'`",
   "GET /api/events?kind=usage&limit=2",
   "`curl -s -X POST <node>/api/chat -H 'content-type: application/json' -d '{\"patch_id\":\"krx-all-2761\",\"patch_ids\":[\"pixelplus-087600\"],\"mode\":\"base\",\"messages\":[{\"role\":\"user\",\"content\":\"x\"}]}'` and the same with four ids in `patch_ids`"
  ],
  "expected": [
   "Step 2: an info alert \"KRX ticker codes … and Pixelplus ticker code … overlap on 2,170 memory entries — Pixelplus ticker code …, ticked last, wins.\" (`chat.picker.overlap_pair`); both rows show \"Loads 1.\" / \"Loads 2.\"; the transcript header reads \"2 knowledges loaded together\" with the two names numbered",
   "Step 4: the \"After loading 2\" bubble lists both knowledges with their load times (\"krx-all-2761: loaded in …\", \"pixelplus-087600: loaded in …\") and the per-knowledge ✓/✗ chips; the answer contains 087600",
   "Step 5: HTTP 200 with `patch_id='krx-all-2761'`, `patch_ids=['krx-all-2761','pixelplus-087600']`, `applied=[{patch_id,applied_ms,was_applied}×2]`, `applied_ms` = the sum, `benchmark_hits` keyed by both ids, `benchmark_hit` = OR of them, `base`/`patched` as before; the lock label seen by other clients during the call is `chat:krx-all-2761+pixelplus-087600`",
   "Step 6: two new usage events, one per patch_id, each with `data.patch_ids` = both ids and `data.position` 1/2; the node's shared table is back to its previous state afterwards (GET /api/runtime `applied` unchanged)",
   "Step 7: HTTP 400 for both (exactly one of `patch_id` / `patch_ids`; at most 3 ids)"
  ],
  "priority": "P1",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/market.ts chat() (patchIds[], exclusive('chat:<id1>+<id2>'), applyRaw in list order, restore in reverse, one usage event per patch), chatOverlaps()",
   "packages/node/src/api.ts POST /api/chat (patch_id | patch_ids refine), GET /api/chat/patches (applied[], overlaps[])",
   "packages/web/src/components/chat/KnowledgePicker.tsx (checkboxes, order badges, overlap alert data-testid=chat-overlap)",
   "packages/web/src/i18n/pages/chat.ts chat.picker.multi_title, chat.picker.multi_help, chat.picker.overlap_pair, chat.picker.max, chat.head.multi, chat.bubble.patched_multi",
   "packages/cli/src/commands/chat.ts parsePatchIds(), renderChat() (per-knowledge load order and markers); ainize chat --patch a,b"
  ]
 },
 {
  "id": "TM-091",
  "persona": "Teach mode (visitor)",
  "title": "Show the contamination banner when the operator keeps knowledge loaded for everyone",
  "goal": "A visitor understands that \"Before loading\" is not the pristine base model when the operator has pinned knowledge into the serving model.",
  "preconditions": [
   "Teach-mode node with `runtime.available=true`; operator credentials for it",
   "GET /api/chat/patches returns `applied: []`"
  ],
  "steps": [
   "Open `/chat` as a visitor: no orange banner above the list",
   "As operator: `POST /api/patches/pixelplus-087600/apply` (or Manage → Load into model)",
   "Reload `/chat` as a visitor (or wait for the 20 s poll)",
   "Tick `Pixelplus ticker code (single fact)` and send `픽셀플러스 종목코드 알려줘. 숫자만.` in Compare mode",
   "As operator: `POST /api/patches/pixelplus-087600/remove`"
  ],
  "expected": [
   "Step 2: GET /api/chat/patches → `applied: ['pixelplus-087600']`",
   "Step 3: warning banner \"This node also has Pixelplus ticker code (single fact) loaded for everyone, so \"Before loading\" already includes it.\" (`chat.picker.contaminated`, data-testid=chat-contaminated) and an \"Always loaded\" chip on that row",
   "Step 4: the response has `applied[0].was_applied=true`; in Compare mode the node still removes it for the \"Before\" answer and puts it back afterwards (GET /api/runtime `applied` still lists it); the bubble shows \"was already loaded\" when the After answer needed no re-load (After-only mode)",
   "Step 5: the banner disappears on the next poll"
  ],
  "priority": "P1",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/market.ts pinnedPatchIds(); chat() restore step re-asserts pinned knowledge after an overlapping removal",
   "packages/web/src/components/chat/KnowledgePicker.tsx (applied → Alert $tone=warning, Pinned chip)",
   "packages/web/src/i18n/pages/chat.ts chat.picker.contaminated, chat.picker.always_loaded, chat.bubble.already_applied"
  ]
 },
 {
  "id": "AZ-101",
  "persona": "Teach mode (visitor)",
  "title": "Load three knowledges together; overlap warning appears; applied[] has three entries; one usage event per knowledge",
  "goal": "A visitor ticks up to three knowledges in Live test; they load in tick order (last wins on overlap), the picker warns about the overlap, and the node meters one usage event per knowledge (spec §13.2 AZ-090).",
  "preconditions": [
   "Teach-mode node whose GET /api/chat/patches lists at least three testable knowledges, two of them overlapping (demo data: krx-all-2761 ∩ pixelplus-087600 = 2,170 rows) and runtime.available=true",
   "Not signed in as operator"
  ],
  "steps": [
   "Open /chat; tick krx-all-2761, pixelplus-087600 and a third knowledge in that order",
   "Read the picker (order badges, counter, overlap alert) and the URL",
   "Send '픽셀플러스 종목코드 알려줘. 숫자만.' in Compare mode",
   "GET /api/events?kind=usage&limit=3",
   "POST /api/chat with both patch_id and patch_ids, then with four ids"
  ],
  "expected": [
   "Order badges 1/2/3, counter '3/3 selected', remaining checkboxes disabled with 'Up to 3 — untick one first.'; URL /chat/krx-all-2761,pixelplus-087600,<third>",
   "Overlap alert names krx-all-2761 and pixelplus-087600 with '2,170 memory entries' and says the one ticked last wins",
   "'After loading 3' bubble lists three load times and per-knowledge ✓/✗ chips; the answer contains 087600; POST /api/chat response has patch_ids (3), applied[] (3 entries with applied_ms/was_applied), applied_ms = sum, benchmark_hits per id",
   "Three new usage events, one per patch_id, each with data.patch_ids and data.position 1..3; GET /api/runtime applied unchanged afterwards",
   "400 for both malformed bodies (exactly one of patch_id / patch_ids; at most 3 ids)"
  ],
  "priority": "P1",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "Detailed two-knowledge walkthrough: TM-090 (same scenario, filed by PR-3)",
   "packages/node/src/market.ts chat(), chatOverlaps(); packages/node/src/api.ts POST /api/chat (patch_id | patch_ids refine)",
   "packages/web/src/components/chat/KnowledgePicker.tsx; packages/web/src/i18n/pages/chat.ts chat.picker.*",
   "packages/e2e/tests/web-chat-multi.spec.ts 'TM-090 …'"
  ]
 },
 {
  "id": "AZ-102",
  "persona": "Teach mode (visitor)",
  "title": "Contamination banner when the operator has knowledge persistently applied",
  "goal": "A visitor understands that \"Before loading\" already includes knowledge the operator keeps loaded for everyone (spec §13.2 AZ-091).",
  "preconditions": [
   "Teach-mode node with runtime.available=true; operator credentials",
   "GET /api/chat/patches returns applied: []"
  ],
  "steps": [
   "As operator: POST /api/patches/pixelplus-087600/apply",
   "Reload /chat as a visitor",
   "Tick pixelplus-087600 and send the sample question in Compare mode",
   "As operator: POST /api/patches/pixelplus-087600/remove"
  ],
  "expected": [
   "GET /api/chat/patches → applied: ['pixelplus-087600']",
   "Warning banner 'This node also has Pixelplus ticker code (single fact) loaded for everyone, so \"Before loading\" already includes it.' (data-testid=chat-contaminated) and an 'Always loaded' chip on that row",
   "Response applied[0].was_applied=true; the node removes it for the Before answer and re-applies it afterwards (GET /api/runtime still lists it)",
   "The banner disappears on the next 20 s poll after the remove"
  ],
  "priority": "P1",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "Detailed walkthrough: TM-091",
   "packages/node/src/market.ts pinnedPatchIds(); packages/web/src/components/chat/KnowledgePicker.tsx (applied → warning, Pinned chip)",
   "packages/e2e/tests/web-chat-multi.spec.ts 'TM-091 …'"
  ]
 },
 {
  "id": "AZ-103",
  "persona": "Teach mode (visitor)",
  "title": "Teach drawer from a wrong answer → basket persists across reload",
  "goal": "A visitor corrects a wrong reply from Live test and the correction stays in the lesson basket after a reload (spec §13.2 AZ-092, §5.4, §5.5).",
  "preconditions": [
   "Teach-mode node with `teach.enabled: true`, `backend: 'stub'` and `stubOffline: true` (dev node :3412: `node packages/cli/dist/bin.js --home ~/.ngram-teach/node-t start`, operator password teach-pass) — no GPU job is ever started",
   "runtime.available=true for the one completion (AZ-103 waits for the serving model)"
  ],
  "steps": [
   "Open /chat/pixelplus-087600?teach=1 as a visitor",
   "Choose 'Before only', ask '픽셀플러스 (<tag>) 종목코드는?' and click 'Teach the right answer' under the reply",
   "Click 'Add to lesson' with an empty answer, then type 087600, an alternative phrasing, and add",
   "Add a second correction whose question already contains the answer (e.g. '종목코드 087600은 픽셀플러스인가요?' → 픽셀플러스)",
   "Reload the page"
  ],
  "expected": [
   "Banner 'Wrong answer? Click \"Teach the right answer\" under any reply and the model learns it. No account needed.'; header item 'Teach'; basket 'Your lesson (0 of 8)' / 'No corrections yet.'; policy line 'Teaching on this node: open'; 'Train this lesson' disabled",
   "Drawer 'Teach the right answer' with the question prefilled, 'The model said' read-only; empty answer → alert 'Please type the right answer.'",
   "Basket shows 'Your lesson (1 of 8)' then '(2 of 8)' with question + answer per item; 'Train this lesson' enabled",
   "After reload the basket still shows 2 corrections (localStorage key ainize.teach.basket.<stack>)"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/chat/TeachDrawer.tsx, LessonBasket.tsx; packages/web/src/lib/teachStore.ts",
   "packages/web/src/i18n/pages/teach.ts chat.banner.teach, teach.drawer.*, teach.basket.*",
   "packages/e2e/tests/web-teach.spec.ts 'AZ-103 …' (formerly titled AZ-101 in PR-6)"
  ]
 },
 {
  "id": "AZ-104",
  "persona": "Teach mode (visitor)",
  "title": "First train → Who gets the credit? sheet → key in localStorage → backup download → restore in a fresh browser",
  "goal": "The first \"Train this lesson\" creates a browser teaching key, offers a backup, and the backup restores the same lessons elsewhere (spec §13.2 AZ-093, §5.6, §5.11).",
  "preconditions": [
   "Teach-mode node with `teach.enabled: true`, `backend: 'stub'` and `stubOffline: true` (dev node :3412: `node packages/cli/dist/bin.js --home ~/.ngram-teach/node-t start`, operator password teach-pass) — no GPU job is ever started",
   "A basket with at least one correction (AZ-103); no ainize.teacher.key in localStorage"
  ],
  "steps": [
   "Click 'Train this lesson'",
   "Read the sheet, type a display name, click 'Download key backup', then 'Continue'",
   "Later, in a fresh browser: open /chat?mine=1, paste the backup JSON into 'Restore key' and click 'Use this key'"
  ],
  "expected": [
   "Sheet 'Who gets the credit?' with 'Ainize never sees the private key.' and a 0x… address; localStorage.ainize.teacher.key = {address, privateKey (64 hex)} matching the displayed address",
   "Download named ainize-teaching-key-<8hex>.json containing privateKey, address and the name; after Continue the pre-flight sheet opens and the basket reads 'Teaching as <name>'",
   "Fresh browser: 'This browser has no teaching key yet.' → after restore 'This browser · 0x1234…abcd' and the lessons of that key are listed"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/lib/teacherKey.ts (@noble secp256k1, ain-util-compatible); packages/web/test/teacherKey.test.ts (cross-library test)",
   "packages/web/src/components/chat/CreditSheet.tsx, MyKnowledgePanel.tsx",
   "packages/e2e/tests/web-teach.spec.ts 'AZ-104 …' and 'AZ-104 (restore) …' (formerly AZ-102 / AZ-109)"
  ]
 },
 {
  "id": "AZ-105",
  "persona": "Teach mode (visitor)",
  "title": "Pre-flight: already-correct fact skipped; all-correct → \"Nothing to teach\"",
  "goal": "Before training, the node checks each correction against the model and refuses to train what it already answers correctly (spec §13.2 AZ-094, §5.7).",
  "preconditions": [
   "Teach-mode node with `teach.enabled: true`, `backend: 'stub'` and `stubOffline: true` (dev node :3412: `node packages/cli/dist/bin.js --home ~/.ngram-teach/node-t start`, operator password teach-pass) — no GPU job is ever started",
   "Basket with one wrong fact and one the model already knows (AZ-103)"
  ],
  "steps": [
   "Continue from the credit sheet into the pre-flight",
   "Read both rows and the quota line, click 'Queue training (1 corrections)'",
   "Repeat with a basket containing only already-known facts"
  ],
  "expected": [
   "'Checking what the model already knows'; row 1 data-status will_train 'Wrong today — will train' with 'Model said: …'; row 2 already_known 'Already correct — skipped'",
   "Quota line 'N of <jobs_per_key_per_day> lessons left today for this key'; after queueing the URL gains ?lesson=<uuid>, the basket is empty again and ainize.teach.jobs mirrors the job id",
   "All known → 'Nothing to teach' and no job is created (POST /api/teach/preflight trainable=0)"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/api.ts POST /api/teach/preflight (costs one chat-quota unit); packages/node/src/teach.ts preflight",
   "packages/web/src/components/chat/PreflightList.tsx; packages/web/src/i18n/pages/teach.ts teach.preflight.*",
   "packages/e2e/tests/web-teach.spec.ts 'AZ-105 …' (formerly AZ-103)"
  ]
 },
 {
  "id": "AZ-106",
  "persona": "Teach mode (visitor)",
  "title": "Job lifecycle with backend 'stub': QUEUED → TRAINING → CHECKING → READY; events kind teach; card copy per state",
  "goal": "The lesson card follows the worker state machine and ends READY with the check lines and per-correction table (spec §13.2 AZ-095, §5.8, §8.3).",
  "preconditions": [
   "Teach-mode node with `teach.enabled: true`, `backend: 'stub'` and `stubOffline: true` (dev node :3412: `node packages/cli/dist/bin.js --home ~/.ngram-teach/node-t start`, operator password teach-pass) — no GPU job is ever started",
   "A queued lesson (AZ-105)"
  ],
  "steps": [
   "Watch the lesson card at the top of the transcript (5 s polling)",
   "When READY read the card, the check lines and the Before/After table",
   "GET /api/teach/jobs/<id> anonymously and GET /api/events?kind=teach&limit=50"
  ],
  "expected": [
   "Card 'Your lesson: …' with data-status moving QUEUED → PREFLIGHT → TRAINING → EXPORTED → CHECKING → READY (stub: seconds)",
   "READY on this offline stub: 'It learned it — 2 of 2 answers correct.' (no 'in the live model' claim — nothing was measured) with the notice 'Demo node — these checks were simulated, not measured in a live model.', 'Unrelated questions unchanged: 12/12', 'Other phrasing answered correctly: 1/1', 'Unsaved lessons are deleted after 7 days.'; buttons Try it now / Publish / Keep it private; no 'publish gated' notice",
   "On a node whose trainer is a stub but whose model server is live (:3402, simulated_checks false) the same card says 'It learned it — 2 of 2 answers correct.' plus 'Demo node — no real training happened; the checks were measured in the live model.' — the fake TRAINING and the simulated CHECKS are two different admissions and neither is told as the other; only a gradient node gets the bare '… correct in the live model.' with no demo notice",
   "Anonymous body is {id, status, position?, eta_s?} only; teach events include 'training started (stub)' and 'READY: …' with data.job_id"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach.ts TeachWorker (state machine, stub backend, CHECKING gates); packages/node/test/teach.test.ts",
   "packages/web/src/components/chat/LessonCard.tsx; teach.card.*",
   "packages/e2e/tests/web-teach.spec.ts 'AZ-106 …' (formerly AZ-104)"
  ]
 },
 {
  "id": "AZ-107",
  "persona": "Teach mode (visitor)",
  "title": "Try it now on a READY lesson: /api/chat with the draft id returns before/after",
  "goal": "The visitor can load the private draft into a live test before deciding what to do with it (spec §13.2 AZ-096).",
  "preconditions": [
   "Teach-mode node with `teach.enabled: true`, `backend: 'stub'` and `stubOffline: true` (dev node :3412: `node packages/cli/dist/bin.js --home ~/.ngram-teach/node-t start`, operator password teach-pass) — no GPU job is ever started",
   "A READY lesson owned by this browser key; runtime.available=true for the completion"
  ],
  "steps": [
   "Click 'Try it now' on the card",
   "Read the picker and header",
   "Send the taught question in 'After only' mode",
   "GET /api/chat/patches without the x-ngram-auth header"
  ],
  "expected": [
   "URL becomes /chat/pixelplus-087600,taught-<slug>-<hex>?lesson=<id>; the draft is listed under 'Your lessons' and the header says '2 knowledges loaded together'",
   "The reply 'After loading 2' contains 087600 (the 픽셀플러스 fixture really teaches the fact)",
   "The draft id is NOT in the anonymous picker list (private, visibility test); it only appears in lessons[] for the owner's signed request"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach.ts lessonsFor(); packages/node/src/api.ts GET /api/chat/patches lessons[] (signed)",
   "packages/web/src/pages/ChatPage.tsx (Try it now → refetch chat/patches, add draft to the route)",
   "packages/e2e/tests/web-teach.spec.ts 'AZ-107 …' (formerly AZ-105)"
  ]
 },
 {
  "id": "AZ-108",
  "persona": "Teach mode (visitor)",
  "title": "Locality gate: a lesson that changes a locality prompt → publish disabled with the gated copy; save still works",
  "goal": "A lesson with side effects on unrelated prompts can be kept privately but never published (spec §13.2 AZ-097, §12).",
  "preconditions": [
   "Teach-mode node with `teach.enabled: true`, `backend: 'stub'` and `stubOffline: true` (dev node :3412: `node packages/cli/dist/bin.js --home ~/.ngram-teach/node-t start`, operator password teach-pass) — no GPU job is ever started (the offline stub fails the locality gate for any fact containing the marker LOCALITY_FAIL)"
  ],
  "steps": [
   "Teach a fact whose answer contains 'LOCALITY_FAIL' and train it",
   "Read the READY card",
   "Click 'Keep it private' and download the file; then POST /api/teach/jobs/<id>/publish"
  ],
  "expected": [
   "checks.locality.ok=false (same < minSame of 12), checks.ok=false; card shows the gated copy ('… cannot be published because it failed a side-effect check') with the Publish button disabled",
   "Keep it private still works (npz link, sha256); POST …/publish → 409 checks_failed"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/src/teach.ts CHECKING (locality prompts, minSame 11/12), stubOffline LOCALITY_FAIL marker; packages/node/test/teach.test.ts 'locality gate'",
   "packages/web/src/components/chat/LessonCard.tsx publish-gated"
  ]
 },
 {
  "id": "AZ-109",
  "persona": "Teach mode (visitor)",
  "title": "Keep it private: token download works, sha256 matches, recipe.json and RUN-LOCALLY.md served; link expires",
  "goal": "A visitor takes the lesson home: 7-day token links for the file, the recipe and the run-locally guide, with the hardware notice shown before any command (spec §13.2 AZ-098, §5.10, §10).",
  "preconditions": [
   "Teach-mode node with `teach.enabled: true`, `backend: 'stub'` and `stubOffline: true` (dev node :3412: `node packages/cli/dist/bin.js --home ~/.ngram-teach/node-t start`, operator password teach-pass) — no GPU job is ever started",
   "A READY lesson owned by this browser key"
  ],
  "steps": [
   "Click 'Keep it private', read the three options, pick 'Download it'",
   "Download the .npz via the link, GET the recipe.json link, click 'RUN-LOCALLY.md'",
   "Pick 'Run it on my machine', read the notice, toggle the commands",
   "GET /api/teach/jobs/<id>/recipe?token=nope",
   "Close and reopen the sheet, keep the default"
  ],
  "expected": [
   "Sheet 'Keep it private' — 'Nothing is published. Pick how you want to keep it.'; default 'Keep it on this node for 7 days' with 'Not private from the operator.'",
   "Link /p2p/blob/<sha>?token=… serves the file whose sha256 equals the displayed sha; recipe.json contains the taught prompt; RUN-LOCALLY.md starts '# Run this knowledge yourself', contains the sha, 'applied: yes' and 'ainize patch import'; size line '… MB · … memory entries · link valid for 7 days'",
   "'There is no laptop version yet.' first; commands (containing the sha and 'patch.py apply') only after the toggle; 'Copy commands' button",
   "401 invalid_signature for a wrong token; links stop working after 7 days (expires_at)",
   "'Kept on this node for 7 days.'; the card stays READY"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach.ts save(), tokenOk(); packages/node/src/teach-recipe.ts renderRunLocally(); packages/node/src/api.ts …/recipe, …/local-run",
   "packages/web/src/components/chat/KeepPrivateSheet.tsx",
   "packages/e2e/tests/web-teach.spec.ts 'AZ-109 …' (formerly AZ-106)"
  ]
 },
 {
  "id": "AZ-110",
  "persona": "Teach mode (operator)",
  "title": "Publish (review mode): PENDING_REVIEW → operator approves on the Teaching tab → ANNOUNCED → verifiers attest → LISTED; anchor carries contributors[].sig that verifies",
  "goal": "In review mode a published lesson waits for the operator; Approve and announce puts it on the market with the visitor as signed data provider (spec §13.2 AZ-099, §5.13, §9.2).",
  "preconditions": [
   "Teach-mode node with `teach.enabled: true`, `backend: 'stub'` and `stubOffline: true` (dev node :3412: `node packages/cli/dist/bin.js --home ~/.ngram-teach/node-t start`, operator password teach-pass) — no GPU job is ever started",
   "Teaching tab setting 'Publish lessons' = 'Review each one' (PATCH /api/me/teach/policy publish=review)",
   "A READY lesson published by its owner (publish-challenge → signed claim → POST …/publish → {status: PENDING_REVIEW})"
  ],
  "steps": [
   "Sign in, open /dashboard?tab=teaching",
   "Find the row in 'Lessons' (chip 'Waiting for review', badge 'N waiting for review'); click 'Decline', type a reason, 'Confirm decline' — on one lesson",
   "On another pending lesson click 'Approve and announce'",
   "GET /api/patches/<patch_id>; on a cluster wait for the verifier attestations"
  ],
  "expected": [
   "Queue columns Lesson · Contributor · Corrections · Status · Started · Actions; the contributor name links to /teacher/<address>, the second line shows the key and IP",
   "Declined row: chip 'Declined' + 'Declined: <reason>'; the contributor's GET /api/teach/jobs/<id> shows status REJECTED and reject_reason",
   "Approved row: chip 'Announced' + link 'Knowledge page →'; the visitor's card flips to 'Announced — verifier nodes are checking it on the real model.' within 30 s",
   "anchor.origin='teach', contributors[0] = {address: visitor key, name, share 0.7, role data_provider, proof signed, sig} and verifyMessage(hashCanonical({patch_sha256, benchmark_hash, address, share}), sig, address) is true; on the demo cluster node-b/c attest and the item becomes LISTED"
  ],
  "priority": "P0",
  "area": "dashboard",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/operator/TeachingTab.tsx (data-testid teach-job / teach-approve / teach-decline / teach-decline-reason / teach-decline-confirm)",
   "packages/node/src/api.ts POST /api/me/teach/jobs/:id/approve|reject; packages/node/src/teach.ts announceJob(), reject()",
   "packages/e2e/tests/web-teach-operator.spec.ts 'AZ-110 …'; packages/e2e/tests/web-teach.spec.ts 'AZ-110/111 …' (review branch approves through the API)"
  ]
 },
 {
  "id": "AZ-111",
  "persona": "Teach mode (visitor)",
  "title": "Publish (auto mode) on the demo cluster; \"Taught by\" chip on catalog and knowledge page",
  "goal": "With publish=auto the signed publish announces immediately and the marketplace credits the data provider everywhere (spec §13.2 AZ-100, §5.9, §5.12).",
  "preconditions": [
   "Teach-mode node with `teach.enabled: true`, `backend: 'stub'` and `stubOffline: true` (dev node :3412: `node packages/cli/dist/bin.js --home ~/.ngram-teach/node-t start`, operator password teach-pass) — no GPU job is ever started with publish=auto (kv override) — or the demo cluster once its config sets teach.publish: 'auto'",
   "A READY lesson owned by this browser key"
  ],
  "steps": [
   "Click 'Publish', fill name and price 0.2, read 'Shown as' and 'You receive 70% of every sale.'",
   "Tick both consents, click Publish",
   "Open the knowledge page, /explore, /teacher/<address> and /chat?mine=1"
  ],
  "expected": [
   "Submit disabled until both consents are ticked; done state 'Announced. Independent verifier nodes are now checking it on the real model…' with a page link and an earnings link /teacher/<address>; the card reads 'Announced — verifier nodes are checking it on the real model.'",
   "Knowledge page: badge 'Taught lesson', line 'Creator node: <node> · Data provider: <name> (70%)', button 'Use it yourself' → Buy tab; /explore item chip 'Taught by <name>' + 'Use it yourself →'; GET /api/catalog?contributor=<address> lists it",
   "/teacher/<address> shows the address, earnings (Earned 0) and the lesson; Your knowledge lists it as 'Being verified' / 'On sale' with 'Earned 0' and the pending hint; DELETE /api/teach/jobs/<id> → 409 published_immutable"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/chat/PublishSheet.tsx; PatchPage.tsx / PatchListItem.tsx (detail.taught_*); TeacherPage.tsx",
   "packages/node/src/teach.ts publish() (validateContributors, origin teach, contributorShare frozen)",
   "packages/e2e/tests/web-teach.spec.ts 'AZ-111 …' (formerly AZ-107)"
  ]
 },
 {
  "id": "AZ-112",
  "persona": "Teach mode (visitor)",
  "title": "Buy a taught knowledge (local credit): settle royalty map contains the contributor; /api/teacher/:address shows earned = paid",
  "goal": "Local-credit sales of a taught lesson credit the data provider directly through the settle record (spec §13.2 AZ-101, §7.3 pass 2, §9.3).",
  "preconditions": [
   "Local-ledger node with a LISTED taught lesson (contributor share 0.7) priced > 0; a buyer node with credit"
  ],
  "steps": [
   "Buy the lesson (ainize use <id> or the Buy tab)",
   "GET /api/ledger?kind=settle (newest record) and GET /api/teacher/<contributor address>"
  ],
  "expected": [
   "settle.royalty contains the contributor address with 70% of the seller remainder (after the 30% lineage pool, if any); the seller keeps the rest",
   "Teacher page: earnings.owed = earnings.paid (local-credit settles are credited by the record itself, no payouts row), sales 1, item status paid"
  ],
  "priority": "P1",
  "area": "x402",
  "automation": "api",
  "evidence": [
   "packages/core/src/catalog.ts royaltySplit (two-pass); packages/core/test/core.test.ts",
   "packages/node/src/market.ts settlePayment (local-credit → creditBalance); packages/node/src/teach.ts teacherProfile()",
   "packages/node/test/payouts.test.ts (GET /api/teacher/:address owed/paid)"
  ]
 },
 {
  "id": "AZ-113",
  "persona": "Teach mode (operator)",
  "title": "Buy on AIN with the transfer forced to fail: payouts row failed, contributor sees pending, operator Retry succeeds",
  "goal": "Failed royalty transfers are recorded, retried every minute, visible to the contributor as pending and fixable from the Teaching tab (spec §13.2 AZ-102, §9.3, §5.13).",
  "preconditions": [
   "AIN-ledger node (dev chain) selling a taught lesson; the node wallet temporarily unable to transfer (empty balance) — never on the shared demo cluster data",
   "Operator signed in; for the UI wiring alone any teach-mode node works (the e2e mocks one failed row)"
  ],
  "steps": [
   "Buy the lesson with AIN; GET /api/me/payouts?status=failed",
   "GET /api/teacher/<contributor> as the visitor",
   "Open /dashboard?tab=teaching → Payouts and click 'Retry' on the row (after funding the wallet)"
  ],
  "expected": [
   "A payouts row {status: failed, attempts ≥ 1, last_error} written BEFORE the transfer attempt; retried every 60 s up to 20 times; events kind payout",
   "Teacher page shows the sale as 'pending' while retries continue ('failed — still owed by <seller>' only after 20 attempts)",
   "Tiles Owed / Paid / Failed reflect GET /api/me/payouts.summary; a local-record node shows 'This node has no chain wallet …'; the row shows the error and 'attempts / 20'; Retry → POST /api/me/payouts/:id/retry → 'Transfer retried.' and the row becomes paid with a tx hash"
  ],
  "priority": "P1",
  "area": "dashboard",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/payouts.ts (enqueue before transfer, 60 s timer, max 20 attempts, retry); packages/node/test/payouts.test.ts",
   "packages/web/src/components/operator/TeachingTab.tsx (data-testid payouts-owed/paid/failed, payout-retry, payouts-no-wallet)",
   "packages/e2e/tests/web-teach-operator.spec.ts 'AZ-113 …' (mocked failed row → retry POST)"
  ]
 },
 {
  "id": "AZ-114",
  "persona": "Teach mode (visitor)",
  "title": "Lineage: knowledge built on a taught lesson pays the data provider their slice (pass 1b)",
  "goal": "When someone publishes knowledge whose parent is a taught lesson, the lesson’s data provider receives their share of the lineage pool (spec §13.2 AZ-103, §7.3).",
  "preconditions": [
   "A taught lesson (contributor share 0.7) as parent of a newer LISTED knowledge sold for a known price"
  ],
  "steps": [
   "Buy the child knowledge",
   "Inspect settle.royalty"
  ],
  "expected": [
   "Pass 1: 30% lineage pool to the parent's author (the node); pass 1b: that slice is shared 70/30 with the parent's contributor; pass 2: the child's own contributors (if any) carve the seller remainder; zero-valued lines omitted"
  ],
  "priority": "P1",
  "area": "x402",
  "automation": "api",
  "evidence": [
   "packages/core/src/catalog.ts royaltySplit pass 1b; packages/core/test/core.test.ts worked examples (spec §7.3)"
  ]
 },
 {
  "id": "AZ-115",
  "persona": "Teach mode (visitor)",
  "title": "Quotas: 4th lesson/day by the same key → 429 quota_key; different key same IP up to 5 → 429 quota_ip",
  "goal": "Per-key and per-IP daily limits both apply and are persisted (spec §13.2 AZ-104, §12).",
  "preconditions": [
   "Teach-mode node with `teach.enabled: true`, `backend: 'stub'` and `stubOffline: true` (dev node :3412: `node packages/cli/dist/bin.js --home ~/.ngram-teach/node-t start`, operator password teach-pass) — no GPU job is ever started with the config defaults (3 per key, 5 per IP) — or set them on the Teaching tab"
  ],
  "steps": [
   "POST /api/teach/jobs four times with one key",
   "POST with fresh keys from the same IP until refused",
   "Read the web copy for the 429"
  ],
  "expected": [
   "4th → 429 'quota_key: …'; the pre-flight quota line counts down '0 of 3 lessons left today for this key'",
   "6th from the IP → 429 'quota_ip: …'; teach_quota rows are day buckets (survive restarts)",
   "UI: 'You have reached today's lesson limit here. Come back tomorrow or run your own node.'"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/src/teach.ts createJob quotas; packages/node/src/store.ts teach_quota; packages/node/test/teach.test.ts 'quotas and bans'",
   "packages/web/src/components/chat/teachUtil.ts mapTeachError (quota_key / quota_ip)"
  ]
 },
 {
  "id": "AZ-116",
  "persona": "Teach mode (operator)",
  "title": "Ban by address → 403 banned; hide name → \"Taught by a visitor\"",
  "goal": "The operator can hide a contributor’s name everywhere and block a key or IP from teaching (spec §13.2 AZ-105, §5.13).",
  "preconditions": [
   "Teach-mode node with `teach.enabled: true`, `backend: 'stub'` and `stubOffline: true` (dev node :3412: `node packages/cli/dist/bin.js --home ~/.ngram-teach/node-t start`, operator password teach-pass) — no GPU job is ever started",
   "A contributor with an ANNOUNCED lesson (AZ-110/111); operator signed in"
  ],
  "steps": [
   "Open /dashboard?tab=teaching → Contributors; click 'Hide name' on the contributor",
   "Open the knowledge page as a visitor; click 'Show name' afterwards",
   "Click 'Block key', confirm the dialog; POST /api/teach/jobs with that key",
   "Click 'Unblock' (row or 'Blocked keys and IPs' table)"
  ],
  "expected": [
   "Row gains the chip 'name hidden'; GET /api/patches/<id> no longer returns contributors[0].name; the knowledge page reads 'Data provider: Taught by a visitor', /explore 'Taught by a visitor'",
   "After 'Show name' the name is back (ledger unchanged — only the response is redacted)",
   "Confirm text 'Block 0x…? New lessons from it will be refused with “This node is not accepting lessons from this key.”'; row chip 'blocked', a row in 'Blocked keys and IPs'; POST → 403 'banned: …'; 'Block IP' uses the last IP seen on that key's jobs",
   "Unblock removes the ban (GET /api/me/teach/bans) and the key can teach again"
  ],
  "priority": "P1",
  "area": "dashboard",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/operator/TeachingTab.tsx (contrib-toggle-hidden, contrib-block-key, contrib-block-ip, contrib-unblock-key, teach-ban)",
   "packages/node/src/api.ts /api/me/teach/contributors/:address, /api/me/teach/bans; hidden names stripped in catalog/detail/teacher",
   "packages/e2e/tests/web-teach-operator.spec.ts 'AZ-116 …'"
  ]
 },
 {
  "id": "AZ-117",
  "persona": "Teach mode (visitor)",
  "title": "Trainer slot busy (pgrep returns an operator job) → job stays QUEUED with the 'fully booked' copy; frees when the job ends",
  "goal": "Visitor lessons never compete with the operator’s own training: the worker leases the trainer slot and waits (spec §13.2 AZ-106, §8.1, §8.5).",
  "preconditions": [
   "Gradient backend node whose `docker exec flashtrain pgrep -f train/` finds a running operator job (or the unit test's fake exec)"
  ],
  "steps": [
   "Queue a lesson",
   "Read GET /api/teach/jobs/<id> and the card",
   "End the operator job"
  ],
  "expected": [
   "status QUEUED with blocked='slot'; card copy 'The trainer is fully booked right now — your lesson is queued…'; GET /api/teach/policy trainer='busy'",
   "When pgrep no longer matches (and nvidia-smi shows ≥ minFreeGpuMb on GPUs 4–6) the job moves to PREFLIGHT/TRAINING; the lease dir <repo>/ple_patch/.ainize-teach.lock is released afterwards"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/src/teach.ts trainer-slot lease (mkdir lock, pgrep, nvidia-smi); packages/node/test/teach.test.ts 'slot busy → blocked=slot'",
   "packages/web/src/i18n/pages/teach.ts teach.card.blocked"
  ]
 },
 {
  "id": "AZ-118",
  "persona": "Teach mode (visitor)",
  "title": "vLLM restart during CHECKING (simulated status revert) → re-apply once, reverted_and_reapplied: true",
  "goal": "A serving-model restart in the middle of the side-effect check does not corrupt the measurement (spec §13.2 AZ-107, §8.3).",
  "preconditions": [
   "Unit-test harness: fake runtime whose patch.py `status` reports the lesson as not applied once after applyRaw"
  ],
  "steps": [
   "Run CHECKING",
   "Read checks"
  ],
  "expected": [
   "The worker re-applies the lesson once and re-measures; checks.reverted_and_reapplied=true; the stack is restored and the operator-pinned set re-asserted in the finally block; a second revert fails the job with a clear error"
  ],
  "priority": "P2",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/src/teach.ts CHECKING revert check; packages/node/test/teach.test.ts 'reverted_and_reapplied'"
  ]
 },
 {
  "id": "AZ-119",
  "persona": "Teach mode (visitor)",
  "title": "Draft expiry after draftTtlDays → EXPIRED, files and tokens removed",
  "goal": "Unsaved private lessons do not accumulate on the node (spec §13.2 AZ-108, §12).",
  "preconditions": [
   "A READY or NEEDS_MORE lesson older than draftTtlDays (unit test: clock advanced)"
  ],
  "steps": [
   "Let the expiry sweep run",
   "GET /api/teach/jobs/<id>; GET a previously issued download link"
  ],
  "expected": [
   "status EXPIRED; the draft, its files and download tokens are gone (link → 401/404); the card reads the expired copy; ANNOUNCED lessons are never expired"
  ],
  "priority": "P2",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/src/teach.ts expiry sweep; packages/node/test/teach.test.ts 'expiry sweep'"
  ]
 },
 {
  "id": "AZ-120",
  "persona": "Teach mode (visitor)",
  "title": "Owner mismatch: another key reads the job → redacted body; publish → 403 not_owner",
  "goal": "Only the teaching key that created a lesson can see or act on its contents (spec §13.2 AZ-109, §6.1).",
  "preconditions": [
   "Teach-mode node with `teach.enabled: true`, `backend: 'stub'` and `stubOffline: true` (dev node :3412: `node packages/cli/dist/bin.js --home ~/.ngram-teach/node-t start`, operator password teach-pass) — no GPU job is ever started",
   "A lesson created by browser A"
  ],
  "steps": [
   "In a fresh browser open /chat?lesson=<id>",
   "With a different key: GET /api/teach/jobs/<id>, POST …/save, POST …/publish"
  ],
  "expected": [
   "Card: 'This lesson belongs to a different teaching key — only its status is visible' and no question text",
   "GET returns {id, status, position?, eta_s?}; save/publish → 403 'not_owner: …'; the web maps it to 'This lesson belongs to a different teaching key. Restore that key to manage it.'"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach.ts view() (owner/operator vs stranger); packages/node/src/api.ts requireOwner",
   "packages/e2e/tests/web-teach.spec.ts 'AZ-120 …' (formerly AZ-108)"
  ]
 },
 {
  "id": "AZ-121",
  "persona": "Teach mode (visitor)",
  "title": "ainize patch import of a downloaded lesson creates a DRAFT and patch apply works",
  "goal": "A lesson taken home with \"Keep it private\" runs through the visitor’s own node (spec §13.2 AZ-110, §10 option B).",
  "preconditions": [
   "A downloaded lesson.npz + recipe.json (AZ-109 'Keep it private' → token links, or `POST /api/teach/jobs/:id/save` with the teaching key)",
   "A second node home on the same machine / same model: `ainize --home ~/.ngram-teach/node-u init --ledger local --runtime-repo /mnt/newdata/qwen3.8 --runtime-api http://localhost:8000` (PR-8)"
  ],
  "steps": [
   "`ainize --home <home> patch import ./lesson-<slug>.npz --recipe ./recipe.json` (operator login on that node first)",
   "`ainize --home <home> patch get taught-<slug>`, `patch records taught-<slug>`",
   "`ainize --home <home> patch apply taught-<slug>`; `ainize chat taught-<slug> \"<taught question>\"`; `patch remove`",
   "`ainize teach status <node-url>` / `… <node>/chat?lesson=<id> --key-file <backup.json>` / `… <node>/teacher/<address>`"
  ],
  "expected": [
   "Import prints 'imported taught-<slug> as a private draft (N rows, sha256 … matches recipe)'; a wrong file for the recipe is refused with 'sha256 mismatch' before anything reaches the node",
   "The DRAFT has the recipe's benchmark (schema taught/<slug>, format template/chat), model id from the recipe, origin `teach`, the data provider as a credit-only contributor (share 0, proof declared), the file registered in place (no copy); `patch records` → none, the local ledger holds no anchor",
   "apply loads it (2 s); chat answers the taught fact after the lesson (before/after compare) and remove restores the rows; RUN-LOCALLY.md's Option B commands are exactly these",
   "`teach status` shows the node policy (accepting lessons, publish mode, trainer, queue, quotas, share), the owner's full lesson body with the key vs status-only without it, and the teacher page (lessons, earned/paid/pending)"
  ],
  "priority": "P2",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/patch.ts patchImport/draftFromRecipe, packages/cli/src/commands/teach.ts teachStatus; packages/cli/test/cli.test.ts ('patch import: …', 'teach status: …', 'publish --contributor …') against an in-process stub node; PR-8 dev round-trip node-t :3412 → node-u :3413 (spec CHANGES PR-8)"
  ]
 },
 {
  "id": "AZ-122",
  "persona": "Teach mode (visitor)",
  "title": "AIN round-trip of an anchor with empty contributors → array restored (withEmptyArrays fix)",
  "goal": "Anchors without contributors survive the AIN blockchain round-trip with contributors: [] instead of a missing field, and more than four contributors are refused (spec §13.2 AZ-111, §7.2).",
  "preconditions": [
   "AIN-ledger node on the dev chain (unit test)"
  ],
  "steps": [
   "Append an anchor with contributors: [] and one with a single contributor; read them back",
   "Append an anchor with five contributors"
  ],
  "expected": [
   "contributors is [] / the one-entry array after the round-trip (withEmptyArrays); the five-entry anchor throws before it is written (MAX_CONTRIBUTORS 4)"
  ],
  "priority": "P2",
  "area": "ledger",
  "automation": "api",
  "evidence": [
   "packages/core/src/ain-ledger.ts withEmptyArrays, contributors cap; packages/core/test/core.test.ts / packages/node/test/ain.test.ts"
  ]
 },
 {
  "id": "AZ-123",
  "persona": "Dataset uploader (visitor)",
  "title": "/teach entry choice: two doors, one pipeline — the conversation card leads and one sentence says the five steps are the same for both",
  "goal": "A stranger landing on /teach can see immediately that a conversation is the primary way in (no file, no preparation, value in one question — ux-critique-owner O-1), that a file is the clearly secondary door for people who already have their data, and that both meet in the same five steps (design §5.2).",
  "preconditions": [
   "node-u on http://localhost:3422 in its current publish-leg mode: teach.enabled true, backend 'stub', teach.stubOffline true. No step in this scenario calls the model server.",
   "Fresh browser context, locale en-US, no localStorage (no teaching key)."
  ],
  "steps": [
   "Open http://localhost:3422/teach and wait for [data-testid=teach-entry].",
   "Read the header and the footer: /teach/* runs under the slim teaching chrome (logo · Exit · language; copyright · Terms · Contact), not the marketplace one.",
   "Read the two door cards, the policy alert and the 'What happens next' sentence.",
   "Read the two limit chips on the file card.",
   "Operator-only variant: PATCH /api/me/teach/policy {enabled:false} with the operator bearer token, reload /teach, then restore {enabled:true}."
  ],
  "expected": [
   "Slim chrome (ux-critique-owner O-3 / O-6): [data-testid=focused-header] holds the 'Ainize home' logo link, [data-testid=link-mine] 'My datasets and lessons' → /teach/mine (the route back to existing work lives in the header's secondary position on every /teach/* page, never in the action group), [data-testid=teach-exit] 'Exit' → /explore and the 'language' toggle (the only one on the page — none inside [data-testid=teach-entry], O-8), and NO marketplace link ('Explore knowledge', 'Live test', …); [data-testid=focused-footer] has 'Terms and Policies' → /terms and 'Contact us' and neither 'ain-js' nor 'aindrive'. /explore keeps the full header with all seven links.",
   "Title 'Teach the model something new'; ONE line of intro (ux-critique-owner O-7): 'No account, no code: your questions and answers become knowledge you can test, keep private or publish.' — the old two-paragraph intro ('Two ways in, one result …' / 'No account, no server of your own, no code.') is gone from the top of the page. Below the doors a native <details data-testid=how-it-works>, collapsed by default, with summary 'How it works' opens to 'Whichever door you pick: your questions and answers are saved as a dataset on this node, checked line by line, trained into the model, and the result is checked again. What comes out is knowledge you can try in Live test, keep private, download, or publish for others to buy. No account, no server of your own, no code.'",
   "Each door is ONE link (ux-critique-owner O-9): the whole card is an <a> with no button or link inside it; its accessible name is heading + label ('Teach it in a conversation Start a conversation' / 'Upload a dataset file Choose a file'); hovering darkens the border to #5b1ca8; Tab reaches the chat card then the file card, each with a solid 3 px focus ring; clicking the card's top-left corner (nowhere near the label) and pressing Enter on it both navigate. Left card [data-testid=door-chat] (primary: 2 px purple border #8b3eeb, tinted background, wider): 'Teach it in a conversation', body 'Ask the model a question and correct its answer. No file needed.' (one sentence + one benefit, O-7), with the contained label [data-testid=door-chat-cta] 'Start a conversation' → /chat?teach=1. Right card [data-testid=door-file] (secondary: 1 px grey border #dadada, white): eyebrow 'Already have a file?', 'Upload a dataset file', body 'Already have questions and answers in a file? Train them straight away.', outlined label [data-testid=door-file-cta] 'Choose a file' → /teach/upload.",
   "The file door's limits are two chips (ux-critique-owner O-4), <ul data-testid=door-file-limits aria-label='File limits'>, directly above the 'Choose a file' label: the node's own format list 'jsonl · json · csv · tsv · txt' (GET /api/teach/policy limits.formats joined with ' · ') and 'up to 2000 questions' (limits.dataset_max_rows — NOT the 4 MB number the upload page shows). They are badges (12 px, weight 600, #f5eefc background), and the card has no <small> helper text at all.",
   "Readiness line (ux-critique-owner O-5), above the doors, info tone: [data-testid=teach-ready] reads 'You can start now — nobody is waiting.' (queue.depth 0; '… 1 lesson is ahead of you.' / '… N lessons are ahead of you.' otherwise; 'The queue is full right now — try again in a little while.' in warning tone when depth ≥ max). A minute figure ('Recent lessons took about N min each.' / 'N–M min' / 'finished in under a minute') is appended ONLY when this node measured ≥ 3 real lessons and timing.simulated is false — node-u is a demo node, so nothing here says 'min'. The technical detail sits behind a collapsed <details data-testid=teach-ready-detail> 'Details': the node's own sentence 'Teaching on this node: open · 0 waiting · demo node — lessons are simulated, nothing is trained' and 'Queue: 0 of 10 lessons' (plus the measured percentiles when there are any).",
   "'What happens next' is a sentence, not a stepper (ux-critique-owner O-2): <p data-testid=teach-next> reads exactly 'What happens next: Dataset → Check → Settings → Training → Result — the same five steps, whichever door you pick'; the entry page contains no <ol>, no <nav>, no aria-current and no [data-testid=teach-stepper], the sentence contains no button or link, and no step carries a border or a background. The real Stepper (current step highlighted) starts on /teach/upload (AZ-124: 'Step 1 of 5 · Dataset').",
   "With teach.enabled false both door-chat and door-file carry aria-disabled=true and tabindex=-1, clicking one stays on /teach, and the readiness line switches to warning tone with the sentence alone — 'This node does not accept lessons. Try another node or run your own.' — and no 'Details' disclosure."
  ],
  "priority": "P0",
  "area": "teach-ui",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/ui/FocusedHeader.tsx, components/base/Layout.tsx (FocusedLayout), components/ui/Footer.tsx (minimal), App.tsx (/teach/* routes)",
   "packages/web/src/components/teach/util.ts (readiness), components/chat/teachUtil.ts (policyLine: a simulated node prints no minutes), packages/web/test/readiness.test.ts",
   "packages/web/src/pages/TeachPage.tsx:54,59,60,65,70 (door-chat / door-file / entry.file.formats / teach-policy / teach-next)",
   "packages/web/src/i18n/pages/teach.ts:290-301 (teach.entry.*), :304-309 (teach.step.*)",
   "GET http://localhost:3422/api/teach/policy → limits.dataset_max_rows 2000",
   "packages/web/src/components/chat/teachUtil.ts policyLine()"
  ]
 },
 {
  "id": "AZ-124",
  "persona": "Dataset uploader (visitor)",
  "title": "/teach/upload first look: three ways in are all present at once, and the privacy sentence is above the fold before any file is chosen",
  "goal": "Step 1 offers drop + a native file input + a paste box simultaneously (dropping is impossible on a phone), and tells a stranger what happens to their file BEFORE they hand it over (design §5.3, §12.2).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US, empty localStorage."
  ],
  "steps": [
   "Open /teach/upload; wait for [data-testid=teach-upload].",
   "Assert the stepper says step 1, then inspect [data-testid=drop-zone], the nested [data-testid=file-input], [data-testid=paste-table], [data-testid=retention], [data-testid=privacy], [data-testid=format-help], [data-testid=samples].",
   "Read the accept attribute of the file input and the DOM order of privacy vs. the intake controls.",
   "Assert [data-testid=key-note] is absent and localStorage has no 'ainize.teacher.key'."
  ],
  "expected": [
   "Stepper aria-label 'Step 1 of 5 · Dataset'; H1 'Upload your dataset'; subtitle 'One question and one right answer per line. The model is taught the answers exactly as you write them.'",
   "Language toggle placement (ux-critique-owner O-8): the 'language' button is inside the header (the slim [data-testid=focused-header] here; the full header on marketplace pages), never inside the page content — and at 360 px it sits on the logo row (its top edge is above the logo's bottom edge), top-right, the same system-setting corner at both widths.",
   "Drop zone is role=button, tabIndex=0, aria-label 'Drop a file here'; it contains a visible 'or' and a real <input type=file aria-label='Choose a file'> with accept='.jsonl,.json,.csv,.tsv,.txt'; the footer line reads 'jsonl, csv, tsv or txt · up to 4 MB' (4 = limits.dataset_max_bytes / 1e6, rounded).",
   "<details data-testid=paste-table> is present and collapsed on a 1280 px viewport with summary 'Paste a table instead'; on a 360 px viewport it is open by default.",
   "Checkbox [data-testid=retention] labelled 'Delete my file as soon as training finishes' is unchecked.",
   "[data-testid=privacy] reads exactly 'Your file is stored on this node while it trains, and the node operator can see it. Do not upload personal data or anything you are not allowed to share.' and appears in the DOM before the format help and the sample list — i.e. it is visible without scrolling past the intake controls.",
   "Format help section 'What the file should look like' renders four boxes headed .JSONL / .CSV / .TSV / .TXT with runnable example bodies, closed by the aliases line 'Other names work too: \"question\" / \"q\" / \"질문\" for the question, \"completion\" / \"output\" / \"a\" / \"정답\" for the answer, \"alt\" / \"paraphrase\" / \"다른표현\" for another way to ask.'",
   "No key note is rendered and localStorage contains no key — a visitor who has not yet chosen a file has no identity on this node."
  ],
  "priority": "P0",
  "area": "teach-ui",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachUploadPage.tsx:107-140 (order: DropZone → PasteTable → retention → privacy → key-note → FormatHelp → samples)",
   "packages/web/src/components/teach/DropZone.tsx:40-52; packages/web/src/lib/teachDataset.ts:41-42 (ACCEPT_ATTR)",
   "packages/web/src/components/teach/PasteTable.tsx:24; TeachUploadPage.tsx:44 narrow() (<480 px opens it)",
   "packages/web/src/i18n/pages/teach.ts:313-336 (teach.up.*)"
  ]
 },
 {
  "id": "AZ-125",
  "persona": "Dataset uploader (visitor)",
  "title": "jsonl through the file picker: chip → node report → preview, and a teaching key is created silently with a backup link",
  "goal": "The golden path of the file door: pick a .jsonl, the node parses it, the preview shows the node's numbers (not the browser's guess), and the visitor is told a key now exists and must be backed up (design §5.3, §5.4, §D2, §D14).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US, empty localStorage."
  ],
  "steps": [
   "Create az-facts.jsonl with exactly:\n{\"prompt\":\"Who founded Ainize?\",\"answer\":\"Comcom\",\"alt_prompt\":\"Which company is behind Ainize?\"}\n{\"prompt\":\"When did Ainize start?\",\"answer\":\"2020\"}\n{\"prompt\":\"What does a lesson produce?\",\"answer\":\"A knowledge file\"}",
   "Open /teach/upload and setInputFiles([data-testid=file-input], az-facts.jsonl).",
   "Wait for the URL to become /teach/dataset/<uuid> and [data-testid=teach-dataset] to render.",
   "Go back to /teach/upload and read [data-testid=key-note]; then open /teach/mine.",
   "Cleanup: /teach/mine → 'Delete dataset' on the card, accept the confirm."
  ],
  "expected": [
   "Between pick and navigation [data-testid=file-chip] appears with 'Reading your file…' and then 'az-facts.jsonl · 200 B · 3 questions found' (browser-side, display only).",
   "The browser navigates to /teach/dataset/<uuid>; POST /api/teach/datasets answered 201 with {dataset, report, created:true} and carried headers x-ngram-auth (v2) + x-ngram-dataset-sha256 = sha256 of the raw file bytes.",
   "Preview page: stepper 'Step 2 of 5 · Check'; H1 'Check your dataset'; subtitle '3 questions from az-facts.jsonl. Fix anything marked in red, then see which ones the model already knows.'",
   "Bar: 'Fingerprint <12 hex>' (dataset.sha256 over the CANONICAL rows.jsonl, not the uploaded bytes) and 'Saved as az-facts.jsonl — you can train from it again any time.'",
   "[data-testid=row-counts] reads '3 will train · 0 already known · 0 duplicates · 0 need a fix'. Three [data-testid=dataset-row] rows with lines 1,2,3, the alt_prompt shown only on row 1, status pill 'Will train' + help 'Not checked yet'.",
   "Buttons present: 'Check (simulated on this node)' (node-u answers the check itself — policy.simulated_checks true; a node with a model server reads 'Check what the model already knows'), 'Add a question', 'Download this dataset (.jsonl)', 'Wrong columns or separator?' (the last only because dataset.status === 'staged'), and a sticky 'Continue to settings'.",
   "Back on /teach/upload, [data-testid=key-note] reads \"Your dataset was signed with this browser's teaching key (0x….…). Lose the key and you lose access to your datasets and lessons — back it up.\" followed by the link 'Back up the key' → /chat?mine=1; localStorage now holds 'ainize.teacher.key' and 'ainize.teach.datasets'.",
   "/teach/mine lists one [data-testid=dataset-card] named az-facts.jsonl with '3 questions', 'Uploaded file', 'Not trained yet'.",
   "After the delete the card is gone and GET /api/teach/datasets/<id> (signed) answers 404 'dataset_not_found: no such dataset on this node'."
  ],
  "priority": "P0",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachUploadPage.tsx:69-87 (send: acceptedFile → size → ensureKey → sha256Hex → upload → rememberDataset → navigate)",
   "packages/node/src/api.ts:470-495 (POST /api/teach/datasets, x-ngram-dataset-sha256)",
   "packages/node/src/teach-datasets.ts:160-215 (create: declared sha check, canonical bytes, 201/created)",
   "packages/web/src/pages/TeachDatasetPage.tsx:194-232; packages/web/src/i18n/pages/teach.ts:339-340 (teach.up.key_made / key_backup)"
  ]
 },
 {
  "id": "AZ-126",
  "persona": "Dataset uploader (visitor)",
  "title": "Drag-and-drop onto the zone, and the same zone opened from the keyboard",
  "goal": "Dropping a file is a real path, not decoration, and the zone is operable without a mouse (design §5.3).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US."
  ],
  "steps": [
   "Open /teach/upload.",
   "Build a DataTransfer in the page containing a File named az-dragdrop.jsonl with the AZ-125 three-line body, dispatch DragEvent('dragover') then DragEvent('drop') on [data-testid=drop-zone].",
   "Separately (fresh page): focus [data-testid=drop-zone] and press Enter; assert the native picker was requested (spy on HTMLInputElement.prototype.click) — the same for Space.",
   "Cleanup: delete the dataset the drop created."
  ],
  "expected": [
   "On dragover the zone's border switches to the primary colour and its background to PALE_GREY (the $over state); on dragleave it reverts.",
   "The drop uploads without any click on the file input: URL becomes /teach/dataset/<uuid>, subtitle '3 questions from az-dragdrop.jsonl…', counts '3 will train · 0 already known · 0 duplicates · 0 need a fix'.",
   "Enter and Space on the focused zone each call input.click() exactly once and do not scroll the page (the handler calls preventDefault).",
   "A drop while an upload is in flight (disabled) is ignored — no second POST /api/teach/datasets."
  ],
  "priority": "P1",
  "area": "teach-ui",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/teach/DropZone.tsx:29-46 (onDrop reads e.dataTransfer.files[0]; onKey handles Enter/Space; disabled guards)",
   "packages/web/src/pages/TeachUploadPage.tsx:110 (DropZone onFile → send)",
   "Verified in a real browser against :3422 — a synthetic DataTransfer drop produced dataset 'az-dragdrop.jsonl', 3 questions"
  ]
 },
 {
  "id": "AZ-127",
  "persona": "Dataset uploader (visitor)",
  "title": "Paste a table instead: two spreadsheet columns become a .tsv the NODE parses",
  "goal": "A visitor with no file can paste two columns and get the identical pipeline — and the browser never becomes a second parser that can disagree with the node (design §5.3, §D2).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US."
  ],
  "steps": [
   "Open /teach/upload, click the [data-testid=paste-table] summary 'Paste a table instead'.",
   "Assert the hint and the placeholder, then fill [data-testid=paste-box] with two tab-separated lines:\nAZ paste question one?\\tAnswer One\nAZ paste question two?\\tAnswer Two",
   "Click [data-testid=paste-use] 'Use this text'.",
   "Repeat in a second context with newline-separated 'Q:'/'A:' text instead of tabs and observe the filename extension.",
   "Cleanup: delete both datasets."
  ],
  "expected": [
   "Hint: 'Copy two columns from a spreadsheet and paste them here — the question in the first column, the right answer in the second.'; placeholder is the literal 'Who founded Ainize?\\tComcom'.",
   "'Use this text' is disabled while the box is empty or whitespace-only.",
   "Tabbed text is uploaded as a File named pasted-<YYYY-MM-DD>.tsv (today's ISO day); non-tabbed text is uploaded as pasted-<YYYY-MM-DD>.txt. Nothing is parsed in the browser — the multipart POST carries the raw text.",
   "Preview: '2 questions from pasted-<YYYY-MM-DD>.tsv…', 'Saved as pasted-<YYYY-MM-DD>.tsv (today’s UTC day) — you can train from it again any time.', counts '2 will train · 0 already known · 0 duplicates · 0 need a fix', rows at lines 1 and 2 with the exact pasted strings (no header row was consumed — the first line is data).",
   "GET /api/teach/datasets/<id> reports format 'tsv', delimiter '\\t', has_header false, source 'upload', source_name 'pasted-<YYYY-MM-DD>.tsv (today’s UTC day)'."
  ],
  "priority": "P1",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachUploadPage.tsx:88-91 (sendPaste: looksTabbed ? .tsv : .txt, new File([text], `pasted-${isoDay()}…`))",
   "packages/web/src/components/teach/PasteTable.tsx:22-31",
   "packages/node/src/teach-dataset.ts:334-346 sniffTxtLayout (tsv when ≥60 % of lines contain a tab); :290 planDelimited/looksLikeHeader",
   "Verified on :3422 — dataset 'pasted-2026-09-01.tsv', 2 questions, lines 1 and 2"
  ]
 },
 {
  "id": "AZ-128",
  "persona": "Dataset uploader (visitor)",
  "title": "Format help and the three sample datasets: download one, or start from it in one click",
  "goal": "A visitor who has nothing to upload can still get to step 2, and the downloadable sample round-trips to the same fingerprint (design §5.3, PR-D1 deviation 3).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US, acceptDownloads: true."
  ],
  "steps": [
   "Open /teach/upload and read [data-testid=samples].",
   "Click the 'Download a sample dataset' link on 'Five English facts' and capture the download.",
   "Click [data-testid=sample-en-facts] 'Start from this sample'.",
   "Re-upload the downloaded bytes through the file input in the SAME browser and compare the resulting dataset id and fingerprint.",
   "Cleanup: delete the dataset(s) created."
  ],
  "expected": [
   "The hint line reads 'A handful of questions in jsonl — open it, replace the text with yours, upload it back.' and three rows are listed: '한국어 사실 5개 / 5 questions', 'Five English facts / 5 questions', 'Mixed Korean and English / 6 questions'.",
   "The download comes from GET /api/teach/samples/en-facts with content-type 'application/x-ndjson; charset=utf-8' and content-disposition filename=\"sample-en-facts.jsonl\"; the file is canonical rows.jsonl (5 lines, LF, no BOM, one trailing LF) whose sha256 is ed73eb39ab65e1ec19326f731ffd54e44a58dafe3022918395e98fb94c1b2fe4 — the same value GET /api/teach/samples reports for that kind. The endpoint needs no teaching key (public, cache-control public max-age=3600).",
   "'Start from this sample' POSTs {source:'sample', sample:'en-facts'} and lands on /teach/dataset/<id>: subtitle '5 questions from a sample dataset.', 'Fingerprint ed73eb39ab65', 'Saved as Five English facts.jsonl — you can train from it again any time.', counts '5 will train · 0 already known · 0 duplicates · 0 need a fix'.",
   "Rows 1–5 are exactly the en-facts questions, with 'Which company is behind Ainize?' on row 1 and 'What exactly does a patch modify?' on row 3 in the 'Another way to ask (optional)' column.",
   "No 'Wrong columns or separator?' button on this page — a sample dataset is created with status 'ready', not 'staged', and there are no original bytes to re-read.",
   "Re-uploading the downloaded sample returns HTTP 200 with created:false and lands on the SAME dataset id and fingerprint (idempotent by canonical sha256; no dataset-count quota charged)."
  ],
  "priority": "P1",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/api.ts:463-468 (GET /api/teach/samples, /api/teach/samples/:kind)",
   "packages/node/src/teach-samples.ts:14-54 (TEACH_SAMPLES)",
   "packages/node/src/teach-datasets.ts:225-234 (findTeachDatasetBySha → created:false), :273-284 (samples/sampleBytes)",
   "packages/web/src/pages/TeachUploadPage.tsx:93-101,134-144; packages/web/src/components/teach/FormatHelp.tsx"
  ]
 },
 {
  "id": "AZ-129",
  "persona": "Dataset uploader (visitor)",
  "title": "CSV with a header and values containing commas inside quotes",
  "goal": "RFC 4180 quoting survives the upload: a comma inside a quoted cell is data, not a column break, and the header row is not trained as a question (design §8.2).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US."
  ],
  "steps": [
   "Create az-quoted.csv with exactly:\nprompt,answer,alt_prompt\n\"Which cities, in order, are on the AZ line?\",\"Seoul, Busan, Daegu\",\"\"\nWho founded Ainize?,Comcom,Which company is behind Ainize?",
   "Upload it through [data-testid=file-input] and read the preview table.",
   "GET /api/teach/datasets/<id> and /rows (signed with the browser key) and check the parse plan.",
   "Cleanup: delete the dataset."
  ],
  "expected": [
   "Preview subtitle '2 questions from az-quoted.csv…'; counts '2 will train · 0 already known · 0 duplicates · 0 need a fix'.",
   "Row 1 is LINE 2 (not 1 — the header was consumed): question 'Which cities, in order, are on the AZ line?' and answer 'Seoul, Busan, Daegu' — both commas preserved, no stray extra row. Row 2 is line 3 with 'Who founded Ainize?' / 'Comcom' / alt 'Which company is behind Ainize?'.",
   "The empty quoted alt_prompt on line 2 renders as an empty cell, not the literal '\"\"'.",
   "GET /api/teach/datasets/<id> → format 'csv', delimiter ',', has_header true, columns {prompt:0, answer:1, alt_prompt:2}, encoding 'utf-8', rows 2, invalid_rows 0.",
   "GET …/rows → summary.source_rows 3, summary.accepted 2, summary.not_parsed 0, and items[0].line === 2."
  ],
  "priority": "P0",
  "area": "teach-parser",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:125-155 parseDelimited (\"\" escape, embedded delimiters/newlines inside quotes)",
   "packages/node/src/teach-dataset.ts:177-191 looksLikeHeader; :286-314 planDelimited; :316-329 recordsFromDelimited (header row skipped)",
   "packages/node/src/api.ts:501-507 (GET …/rows paginated report)",
   "Verified on :3422 — rows rendered at lines 2 and 3 with both commas intact"
  ]
 },
 {
  "id": "AZ-130",
  "persona": "Dataset uploader (visitor)",
  "title": "Header detection both ways: Korean column names are recognised, a headerless TSV keeps its first line as data",
  "goal": "The parser must not eat a real question as a header, and must map 질문/정답/다른표현 as readily as prompt/answer/alt_prompt (design §8.2, FIELD_ALIASES).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US."
  ],
  "steps": [
   "Upload az-korean-header.csv:\n질문,정답,다른표현\nAinize를 만든 곳은?,Comcom,Ainize는 어느 회사인가요?\nAIN 토큰 이름은?,AIN,",
   "Read the preview, then GET the dataset JSON.",
   "Upload az-noheader.tsv (tab separated, no header row):\nAinize를 만든 곳은?\\tComcom\nAIN 토큰 이름은?\\tAIN",
   "Read the preview and the dataset JSON.",
   "Cleanup: delete both datasets."
  ],
  "expected": [
   "Korean-header CSV: preview shows 2 questions at LINES 2 and 3 — the 질문,정답,다른표현 line was recognised as a header and never became a question. Row 1 carries alt 'Ainize는 어느 회사인가요?'.",
   "Its dataset JSON: format 'csv', delimiter ',', has_header true, columns {prompt:0, answer:1, alt_prompt:2} (mapped through the aliases 질문 → prompt, 정답 → answer, 다른표현 → alt_prompt).",
   "Headerless TSV: preview shows 2 questions at LINES 1 and 2 — nothing was consumed as a header. Counts '2 will train · 0 already known · 0 duplicates · 0 need a fix'.",
   "Its dataset JSON: format 'tsv', delimiter '\\t', has_header false, columns {prompt:0, answer:1, alt_prompt:2, note:3} (the positional fallback that only applies when there is no header).",
   "The English equivalents (prompt,answer,alt_prompt) and the mixed aliases question/completion/output/q/a produce the same mapping — the alias table is casefolded and whitespace/underscore-insensitive."
  ],
  "priority": "P1",
  "area": "teach-parser",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:76-85 FIELD_ALIASES + aliasKey/ALIAS_TO_FIELD",
   "packages/node/src/teach-dataset.ts:177-191 looksLikeHeader (every cell non-numeric, ≤64 chars, unique, at least one known alias)",
   "packages/node/src/teach-dataset.ts:286-314 planDelimited (positional fallback only when !hasHeader)",
   "Verified on :3422 — korean-header.csv rows at lines 2/3; noheader.tsv rows at lines 1/2"
  ]
 },
 {
  "id": "AZ-131",
  "persona": "Dataset uploader (visitor)",
  "title": "Plain text: the Q:/A: layout the format help promises",
  "goal": "The .txt example printed on the upload page is the file the parser actually reads, and the line numbers point back into the visitor's file (design §8.4).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US."
  ],
  "steps": [
   "Copy the .TXT block shown in [data-testid=format-help] verbatim into az-qa.txt:\nQ: Who founded Ainize?\nA: Comcom\n\nQ: When did Ainize start?\nA: 2020",
   "Upload it and read the preview.",
   "Also upload az-korean-qa.txt using the Korean markers:\n질문: Ainize를 만든 곳은?\n답: Comcom",
   "Cleanup: delete both datasets."
  ],
  "expected": [
   "az-qa.txt: '2 questions from az-qa.txt…'; counts '2 will train · 0 already known · 0 duplicates · 0 need a fix'. The two rows are reported at LINES 1 and 4 — the line of the Q: that opened each pair, with the blank line counted, so the number matches the visitor's editor.",
   "Questions are stored without the 'Q: ' / 'A: ' markers: 'Who founded Ainize?' → 'Comcom'.",
   "Dataset JSON: format 'txt', layout 'qa', encoding 'utf-8'.",
   "az-korean-qa.txt parses the same way — 질문:/문제: open a question and 답:/답변:/정답: close it; layout is still 'qa'.",
   "An A: line with no Q: before it is reported as not_parsed with detail 'an answer line with no question before it' and appears under 'N line(s) could not be read and were left out.' — never silently dropped."
  ],
  "priority": "P1",
  "area": "teach-parser",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:330-331 Q_RE/A_RE (Q|질문|문제 and A|답변|정답|답)",
   "packages/node/src/teach-dataset.ts:348-380 recordsFromTxt layout 'qa' (pending prompt keeps the Q line number)",
   "packages/web/src/components/teach/FormatHelp.tsx (the .txt block is generated from the same rows shown)",
   "Verified on :3422 — qa.txt produced rows at lines 1 and 4"
  ]
 },
 {
  "id": "AZ-132",
  "persona": "Dataset uploader (visitor)",
  "title": "Alpaca and ChatML: the two shapes people already have on disk are read without an export step",
  "goal": "A visitor who downloaded an instruction-tuning set from elsewhere can upload it as-is: Alpaca's input joins the instruction (it is never used as the answer) and a ChatML system message is ignored, not trained (design §8.3).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US."
  ],
  "steps": [
   "Upload az-alpaca.json:\n[{\"instruction\":\"Who founded Ainize?\",\"input\":\"\",\"output\":\"Comcom\"},{\"instruction\":\"Name the token\",\"input\":\"AIN blockchain\",\"output\":\"AIN\"}]",
   "Read the preview and [data-testid=fixed-note].",
   "Upload az-chatml.jsonl:\n{\"messages\":[{\"role\":\"system\",\"content\":\"You are helpful\"},{\"role\":\"user\",\"content\":\"Who founded Ainize?\"},{\"role\":\"assistant\",\"content\":\"Comcom\"}]}\n{\"messages\":[{\"role\":\"user\",\"content\":\"What token does AIN use?\"},{\"role\":\"assistant\",\"content\":\"AIN\"}]}",
   "Read the preview and the report notes.",
   "Cleanup: delete both datasets."
  ],
  "expected": [
   "Alpaca: '2 questions from az-alpaca.json…', format 'json', counts '2 will train · 0 already known · 0 duplicates · 0 need a fix'. Row 1 = 'Who founded Ainize?' / 'Comcom'. Row 2's question is 'Name the token AIN blockchain' — instruction + '\\n' + input, then whitespace-collapsed — and its answer is 'AIN'; the input is NEVER used as the answer.",
   "Row 2's status pill reads 'Will train — tidied up' and [data-testid=fixed-note] says '1 question(s) were tidied up (extra spaces and line breaks removed).' (summary.fixed = 1, fixes include 'whitespace_collapsed').",
   "ChatML: '2 questions from az-chatml.jsonl…', format 'jsonl', 2 rows at lines 1 and 2 — last user message → question, last assistant message → answer.",
   "The system message is neither trained nor dropped in silence: report.json notes contains 'system_messages_ignored'.",
   "A jsonl line that is valid JSON but carries none of the known keys is reported not_parsed with 'no question/answer keys in this object' and its raw text is shown under 'See the lines that were left out'."
  ],
  "priority": "P1",
  "area": "teach-parser",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:217-245 fieldsFromObject (ChatML branch counts system messages; Alpaca instruction+input join)",
   "packages/node/src/teach-dataset.ts:247-272 recordsFromJsonl; :274-292 recordsFromJsonArray",
   "packages/node/src/teach-dataset.ts:661 notes 'system_messages' → 'system_messages_ignored'",
   "Verified on :3422 — alpaca.json row 2 rendered 'Name the token AIN blockchain' with 'Will train — tidied up'"
  ]
 },
 {
  "id": "AZ-133",
  "persona": "Dataset uploader (visitor)",
  "title": "Encodings: UTF-8 BOM + CRLF is silent, EUC-KR/cp949 and UTF-16 are read and SAID so",
  "goal": "A file saved by Excel on a Korean Windows machine works, and the node names the encoding it guessed — a short CP949 file that decodes as valid UTF-8 is the most likely silent corruption in the pipeline (design §8.1).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US.",
   "Three fixtures written with explicit bytes (not by a text editor)."
  ],
  "steps": [
   "Upload az-bom-crlf.csv = UTF-8 bytes with a leading EF BB BF and CRLF line endings:\nprompt,answer\\r\\nAinize를 만든 곳은?,Comcom\\r\\nAIN 토큰 이름은?,AIN\\r\\n",
   "Upload az-euckr.csv = the same text encoded cp949 with LF endings and a Korean header (질문,정답).",
   "Upload az-utf16.csv = UTF-16LE bytes with a FF FE BOM.",
   "On each preview read [data-testid=encoding-note] and the row text.",
   "Cleanup: delete the datasets."
  ],
  "expected": [
   "UTF-8 + BOM + CRLF: 2 questions, rows at lines 2 and 3, Korean text intact, NO encoding note is rendered (dataset.encoding === 'utf-8' — the BOM is stripped and the CRLFs are one row each, not two).",
   "EUC-KR: 2 questions with correct Hangul, and [data-testid=encoding-note] reads 'Read as euc-kr. If the text looks wrong, save the file as UTF-8 and upload it again.'; dataset.encoding === 'euc-kr'.",
   "UTF-16LE with BOM: 2 questions with correct Hangul, note 'Read as utf-16le. If the text looks wrong, save the file as UTF-8 and upload it again.'; dataset.encoding === 'utf-16le'. A FE FF (UTF-16BE) file gives 'utf-16be' the same way.",
   "The canonical rows.jsonl written for all three is UTF-8 without BOM with LF endings — so the same questions in a different encoding produce the SAME fingerprint. Uploading az-utf16.csv after az-bom-crlf.csv in the same browser returns HTTP 200 created:false and lands on the first dataset (its source_name stays 'az-bom-crlf.csv'). Use distinct question text per encoding if the scenario needs three distinct datasets.",
   "A file that is neither valid UTF-8 nor CP949 falls back to latin1 and says so, rather than failing."
  ],
  "priority": "P0",
  "area": "teach-parser",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:96-114 decodeBuffer (BOM → hint → strict utf-8 → euc-kr → shift_jis → latin1)",
   "packages/node/src/teach-dataset.ts:125-155 parseDelimited (\\r\\n / \\r / \\n all end one logical row)",
   "packages/web/src/pages/TeachDatasetPage.tsx:204 (note shown only when encoding !== 'utf-8'); i18n teach.ts:338 teach.up.encoding",
   "packages/node/src/teach-datasets.ts:225-234 (idempotent by canonical sha256 — the UTF-16 twin lands on the earlier dataset)",
   "Verified on :3422 — euckr.csv and utf16u.csv both rendered their note; bom-crlf.csv rendered none"
  ]
 },
 {
  "id": "AZ-134",
  "persona": "Dataset uploader (visitor)",
  "title": "'Wrong columns or separator?' re-reads the bytes the node already has — no re-upload, new revision, new fingerprint",
  "goal": "When the guess is wrong (mojibake, wrong separator, a header eaten or missed) the visitor fixes it from the preview instead of going back to their file manager (design §5.4).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "A staged dataset from an UPLOADED file (status 'staged' — the reparse button is absent for chat/sample datasets)."
  ],
  "steps": [
   "Upload az-euckr.csv (cp949 bytes) and note the fingerprint on the preview.",
   "Click [data-testid=open-reparse]; read the sheet.",
   "Select Text encoding = 'latin1' and click [data-testid=reparse-go] 'Read it again'; observe the table and the fingerprint.",
   "Reopen the sheet, select 'euc-kr', apply again.",
   "Cleanup: delete the dataset."
  ],
  "expected": [
   "The sheet [data-testid=reparse-sheet] is titled 'Wrong columns or separator?' with the note 'Tell this node how to read your file and it will try again. Nothing is re-uploaded.' and four controls: File format (Decide automatically | jsonl | json | csv | tsv | txt), Separator (Decide automatically | , | tab | ; | |), Text encoding (Decide automatically | utf-8 | euc-kr | utf-16le | utf-16be | latin1) and the checkbox 'The first line is a header'.",
   "'Read it again' POSTs /api/teach/datasets/<id>/reparse — no multipart, no bytes re-sent (the node reads source.<ext> it kept because status is 'staged').",
   "Forced latin1: the same two rows now read as mojibake ('Ainize¸¦ ¸¸µç °÷Àº?' / 'ÇÈ¼¿ÇÃ·¯½º Á¾¸ñÄÚµå´Â?'), the encoding note switches to 'Read as latin1. …', and the Fingerprint changes (revision 2) — the sha256 follows the bytes it now produces.",
   "Any pre-flight result on the page is discarded when the revision changes — no green 'already known' tick survives next to text that just changed.",
   "Forcing 'euc-kr' restores the Hangul and the ORIGINAL fingerprint of the first parse.",
   "A reparse that yields zero usable questions is refused with 400 'dataset_empty: read that way, the file has no usable questions' and the previous revision is left intact."
  ],
  "priority": "P1",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/teach/ReparseSheet.tsx:12-16,30-50; packages/web/src/pages/TeachDatasetPage.tsx:229 (button only when dataset.status === 'staged'), :92 (revision change clears preflight)",
   "packages/node/src/api.ts:508-512 (POST …/reparse)",
   "packages/node/src/teach-datasets.ts:296-307 (re-reads source bytes; 409 if gone; 400 dataset_empty)",
   "Verified on :3422 — latin1 reparse produced mojibake and fingerprint d20259989da3 → e2cfe94732aa"
  ]
 },
 {
  "id": "AZ-135",
  "persona": "Dataset uploader (visitor)",
  "title": "Size cap: a 5.1 MB file is refused in the browser before a byte is uploaded, and the node refuses it independently",
  "goal": "The 4 MB ceiling is enforced twice — once so the visitor is not made to wait for an upload that cannot succeed, once so the node cannot be filled by a client that skips the check (design §7.1, §12.5).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true; policy limits.dataset_max_bytes = 4000000.",
   "Fresh browser context, locale en-US.",
   "az-huge.jsonl ≈ 5.1 MB (e.g. 45 000 lines of {\"prompt\":\"Huge Q%06d padded pppp…?\",\"answer\":\"%06d\"})."
  ],
  "steps": [
   "Open /teach/upload and setInputFiles az-huge.jsonl; capture network requests.",
   "Read [data-testid=upload-error].",
   "Independently: POST the same file to /api/teach/datasets with a valid v2 teaching-key signature and inspect the status and body.",
   "Also POST a 4 MB-under file with a deliberately wrong x-ngram-dataset-sha256."
  ],
  "expected": [
   "No POST /api/teach/datasets is made at all — the guard runs before file.arrayBuffer().",
   "[data-testid=upload-error] (role=alert) reads exactly 'That file is 5.1 MB, over the 4 MB limit. Split it, or upload fewer questions.' — the size string comes from the browser (MB above 1e6, kB below) and the limit from policy limits.dataset_max_bytes / 1e6.",
   "No file chip appears and the page stays on /teach/upload; no teaching key is created by a refused file (ensureKey runs only after the two guards pass).",
   "The direct POST is refused by the node with HTTP 413 'dataset_too_large: this node accepts files up to 4 MB' and details {bytes, max_bytes} — refused by datasetGate on content-length, BEFORE multer writes anything to <dataDir>/teach/incoming.",
   "The wrong-sha256 POST is refused with 400 'dataset_hash: the file changed while it was being uploaded — try again'; the web maps it to 'The file changed while it was being uploaded. Try again.'"
  ],
  "priority": "P0",
  "area": "teach-limits",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachUploadPage.tsx:58-59,72 (maxBytes / err_big before any fetch)",
   "packages/web/src/lib/teachDataset.ts fileSize() (MB / kB / B)",
   "packages/node/src/api.ts:429-444 datasetGate (registered BEFORE datasetUpload; content-length checked first)",
   "packages/node/src/teach-datasets.ts:137-150 gate(); :163-168 (declaredSha256 mismatch → dataset_hash)",
   "Verified on :3422 — 'That file is 5.1 MB, over the 4 MB limit. Split it, or upload fewer questions.'"
  ]
 },
 {
  "id": "AZ-136",
  "persona": "Dataset uploader (visitor)",
  "title": "Row caps: the 'Choose which 200' lesson banner, and the over-2000 dataset note that says nothing was hidden",
  "goal": "Two different caps must not be confused: how many questions fit in ONE lesson (rows_per_job, 200 here) and how many the node keeps in one DATASET (dataset_max_rows, 2000). Neither may drop a line in silence (design §5.4, §9, PR-D1 deviation 1).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true; policy limits.rows_per_job = 200, limits.dataset_max_rows = 2000.",
   "Fresh browser context, locale en-US."
  ],
  "steps": [
   "Upload az-big250.jsonl — 250 distinct lines of the shape {\"prompt\":\"Q%03d what is the AZ code number %03d?\",\"answer\":\"%03d\"} for i in 0..249.",
   "Read [data-testid=row-counts], [data-testid=cap-banner], the pager, and confirm [data-testid=over-cap-note] is absent.",
   "Click [data-testid=cap-pick]; tick three row checkboxes; read [data-testid=cap-selected].",
   "Upload az-big2005.jsonl — 2005 distinct lines; read the counts, the over-cap note and the cap banner; page to the tail of the report.",
   "Cleanup: delete both datasets."
  ],
  "expected": [
   "250 rows: counts '250 will train · 0 already known · 0 duplicates · 0 need a fix' — the DATASET keeps all 250. [data-testid=cap-banner] (info tone) reads 'This node teaches up to 200 questions in one lesson. The first 200 are selected; the rest stay in your dataset for the next lesson.' with the button 'Choose which 200'. No over-cap note (summary.over_cap = 0).",
   "Pager reads '1–50 of 250' with Previous disabled and Next enabled (the report is read paginated, never inlined).",
   "After 'Choose which 200' a checkbox column appears and the banner grows ' · 0 of 200 selected', becoming ' · 3 of 200 selected' after three ticks; a 201st tick is ignored (the Set stops at cap). 'Continue to settings' carries the chosen indexes in sessionStorage under ainize.teach.selection.<dsId>.",
   "2005 rows: counts '2000 will train · …' and [data-testid=over-cap-note] reads 'That file has 2005 questions; this node accepts up to 2000 in one dataset. The first 2000 were loaded.' — the 5 refused lines are NOT invisible: they appear in report.json with status 'over_cap' and render as the pill \"Over this node's dataset limit — not loaded\".",
   "GET /api/teach/datasets/<id> for the 2005-row file → rows 2000, invalid_rows 5, summary.over_cap 5, summary.accepted 2000.",
   "The cap banner also shows on the 2005-row dataset (2000 > 200) — both messages are true at once and neither replaces the other.",
   "GET /api/teach/datasets/<id>/rows?status=over_cap on the 2005-row dataset returns exactly the 5 refused lines (2001..2005) with detail \"this node keeps up to 2000 questions in one dataset\"; they are reachable only by paging to the end of the report (50 rows a page) because the web table has no status filter.",
   "An over-the-limit line is never sold as something to fix: it is counted in invalid_rows and summary.over_cap but NOT in the \"need a fix\" figure of [data-testid=row-counts]."
  ],
  "priority": "P0",
  "area": "teach-limits",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachDatasetPage.tsx:202,205,216-220 (row-counts / over-cap-note / cap-banner / cap-pick / cap-selected), :153-158 toggle(), :167-170 goSettings/saveSelection",
   "packages/web/src/components/teach/util.ts rowsPerJob() (policy.limits.rows_per_job)",
   "packages/node/src/teach-dataset.ts:627-631 (over_cap status + summary)",
   "packages/web/src/i18n/pages/teach.ts:376,392-394 (status.over_cap, rows.cap, cap_pick, cap_selected), :344 (up.err_many)",
   "Verified on :3422 — 250-row and 2005-row uploads produced exactly these strings"
  ]
 },
 {
  "id": "AZ-137",
  "persona": "Dataset uploader (visitor)",
  "title": "Nothing usable in the file: 0 bytes, binary rubbish, a wrong file type, and a header with no data rows",
  "goal": "Four different kinds of nothing must each produce a sentence a stranger can act on, and none of them may create a dataset or a teaching key (design §5.11, §9).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context per case, locale en-US, empty localStorage."
  ],
  "steps": [
   "Upload az-empty.csv — a 0-byte file.",
   "Upload az-blob.txt — 2048 bytes of every byte value 0x00–0xFF repeated, named .txt.",
   "Upload az-pic.png — a PNG.",
   "Upload az-headeronly.csv — the single line 'prompt,answer\\n' and nothing else.",
   "After each, read [data-testid=upload-error], confirm the URL is still /teach/upload and check whether a dataset appeared under /teach/mine."
  ],
  "expected": [
   "az-pic.png is refused in the BROWSER before any request: 'This node reads jsonl, csv, tsv and plain text. \"az-pic.png\" is none of those.' (the accept attribute already filters the picker; a drop of the same file hits the same guard).",
   "az-empty.csv → the node answers 400 dataset_format and the page shows 'This node could not read that file as a dataset. See the format examples.'",
   "az-blob.txt → the node answers 400 dataset_empty and the page shows 'That file has no usable questions. Every line needs a question and a right answer.' (the bytes decoded to something, the lines produced report entries, none of them a question and an answer).",
   "az-headeronly.csv → the node answers 400 and the page shows one of the two sentences above. TODAY it is the dataset_format one ('could not read that file as a dataset'), because the header row is consumed and the report ends up empty — see notes: dataset_empty is the honest sentence for a file that WAS read and simply had no rows.",
   "In every case the URL stays /teach/upload, no dataset is created (GET /api/teach/datasets signed with that browser's key returns an empty items list), and /teach/mine still shows 'Nothing here yet. …'.",
   "The error box has role='alert'; no console error is logged."
  ],
  "priority": "P0",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachUploadPage.tsx:71 (err_type before any request), :112 (upload-error alert)",
   "packages/node/src/teach-datasets.ts:180-184 (parsed.report.length ? dataset_empty : dataset_format)",
   "packages/web/src/components/chat/teachUtil.ts:49-50 (dataset_empty / dataset_format mapping); i18n teach.ts:569-570",
   "Verified on :3422 — empty.csv → dataset_format, blob.txt → dataset_empty, headeronly.csv → dataset_format, pic.png → client-side err_type"
  ]
 },
 {
  "id": "AZ-138",
  "persona": "Dataset uploader (visitor)",
  "title": "A file that is all duplicates: one question kept, every later copy shown with the line it repeats",
  "goal": "Deduplication is visible arithmetic, not a disappearance — the visitor sees which line each dropped copy repeats (design §8.5).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US."
  ],
  "steps": [
   "Upload az-dupes.jsonl — the SAME line five times:\n{\"prompt\":\"Who founded Ainize?\",\"answer\":\"Comcom\"} (×5, one per line).",
   "Read the subtitle, [data-testid=row-counts] and each [data-testid=dataset-row].",
   "GET /api/teach/datasets/<id> and …/rows?status=duplicate.",
   "Also upload a variant where the five lines share a prompt but give two DIFFERENT answers, and compare.",
   "Cleanup: delete the datasets.",
   "Repeat with az-dupes2.jsonl — two pairs (lines 1 and 2 identical, lines 3 and 4 identical) — and download the dataset to count the lines."
  ],
  "expected": [
   "Subtitle '1 questions from az-dupes.jsonl…' and counts '1 will train · 0 already known · 4 duplicates · 0 need a fix'.",
   "Line 1 keeps the pill 'Will train' with 'Not checked yet' and the Remove action. Lines 2–5 each show the muted pill 'Same as line 1 — skipped' and expose only 'Edit' (no Remove — they have no dataset index).",
   "Dataset JSON: rows 1, invalid_rows 4, summary.accepted 1, summary.duplicates 4, summary.rejected 4, summary.source_rows 5. …/rows?status=duplicate returns exactly the four entries, each with detail 'the same question and answer as line 1'.",
   "'Continue to settings' is enabled — a dataset of one question is a legal dataset (the button is disabled only at dataset.rows === 0).",
   "The different-answers variant is NOT deduplicated: those rows come back as 'conflict' (see AZ-139) with the bad tone and a 'Keep this answer' action, because the model can only learn one of them.",
   "Duplicates are decided on the NORMALISED prompt+answer, so two lines differing only by trailing spaces still collapse to one.",
   "The canonical file holds only what trains: \"Download this dataset (.jsonl)\" saves exactly 1 line, and a fixture of two duplicate PAIRS (lines 1=2, 3=4) saves 2 lines with the report reading \"the same question and answer as line 1\" and \"… as line 3\" — the later copies never reach rows.jsonl."
  ],
  "priority": "P1",
  "area": "teach-parser",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:620-626 (dupKey = `${prompt} ${answer}`, detail 'the same question and answer as line N')",
   "packages/web/src/components/teach/util.ts fileStatus case 'duplicate' → teach.rows.status.dupe",
   "packages/web/src/components/teach/DatasetTable.tsx:130-138 (Remove only when row.index !== null)",
   "Verified on :3422 — '1 will train · 0 already known · 4 duplicates · 0 need a fix', rows 2–5 'Same as line 1 — skipped'"
  ]
 },
 {
  "id": "AZ-139",
  "persona": "Dataset uploader (visitor)",
  "title": "A messy real-world file: contradictions, over-length, a missing answer, an unreadable line — all counted, none hidden",
  "goal": "G3, the rule the whole preview exists for: every source row that did not become a trained question is reported with a status, a reason and its line number (design §8.5, §5.4).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US."
  ],
  "steps": [
   "Upload az-messy.jsonl with exactly seven lines:\n1 {\"prompt\":\"Who founded Ainize?\",\"answer\":\"Comcom\"}\n2 {\"prompt\":\"Who founded Ainize?\",\"answer\":\"Anthropic\"}\n3 {\"prompt\":\"What is the AZ long answer?\",\"answer\":\"<205 × x>\"}\n4 {\"prompt\":\"<405 × y>\",\"answer\":\"short\"}\n5 {\"prompt\":\"No answer here?\",\"answer\":\"\"}\n6 not json at all\n7 {\"prompt\":\"  Spaced   question   here?  \",\"answer\":\"  tidy  me  \"}",
   "Read [data-testid=row-counts], [data-testid=fixed-note], every row's pill and help text, and expand [data-testid=dropped].",
   "Cleanup: delete the dataset."
  ],
  "expected": [
   "Counts: '1 will train · 0 already known · 0 duplicates · 6 need a fix' (6 = 2 conflicts + 2 too_long + 1 empty + 1 not_parsed); [data-testid=fixed-note] '1 question(s) were tidied up (extra spaces and line breaks removed).'",
   "Lines 1 and 2: bad-tone pill 'Two answers for this question — pick one' with help 'Lines 1 and 2 ask the same question but give different answers. The model can only learn one.' (and the mirrored 'Lines 2 and 1 …'); both offer [data-testid=row-keep] 'Keep this answer'.",
   "Line 3: 'The answer is 205 characters; keep it under 200. Teach a long explanation as several short facts.' Line 4: 'The question is 405 characters; keep it under 400.' (limits from policy.limits.answer_max 200 / prompt_max 400).",
   "Line 5: 'No answer — type the right answer', with an em dash in the answer cell.",
   "Line 6 is NOT in the table; it is counted in the summary and listed under the collapsed '1 line(s) could not be read and were left out. — See the lines that were left out', whose entry reads 'Line 6 could not be read as a question and an answer.' followed by the raw text 'not json at all' in a <code> block.",
   "Line 7 is the one accepted question: 'Spaced question here?' / 'tidy me' with the pill 'Will train — tidied up' — whitespace collapsed, never rejected for it.",
   "Every status pill is rendered from the machine-readable row.status, not from the server's English detail sentence — switching to 한국어 changes the pill text and keeps the same statuses.",
   "This scenario owns the ARITHMETIC — every source row is in exactly one bucket and the four counts plus the dropped-lines count add back up to the 7 source lines. The per-status copy and the repair actions are each asserted once in their own scenario (conflict, too long, empty, unreadable, duplicate, tidied-up)."
  ],
  "priority": "P0",
  "area": "teach-parser",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:589-631 (not_parsed / empty / too_long / blocked / conflict / duplicate / over_cap, each with a detail and a line)",
   "packages/web/src/components/teach/util.ts fileStatus(); packages/web/src/components/teach/DatasetTable.tsx:112-125",
   "packages/web/src/pages/TeachDatasetPage.tsx:203,246-253 (fixed-note, dropped details), :161-165 keepAnswer",
   "Verified on :3422 — exactly these strings, counts '1 will train · 0 already known · 0 duplicates · 6 need a fix'"
  ]
 },
 {
  "id": "AZ-140",
  "persona": "Dataset uploader (visitor)",
  "title": "Privacy notice and retention: 'Delete my file as soon as training finishes' is offered before the upload and honoured in the record",
  "goal": "A stranger is told the operator can read their file, and is given the one control that limits it — before the file leaves their machine, not after (design §5.3, §12.2, §6.2).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true; policy limits.dataset_ttl_days = 7.",
   "Fresh browser context, locale en-US."
  ],
  "steps": [
   "Open /teach/upload; assert [data-testid=privacy] is rendered while no file has been chosen and sits above the format help.",
   "Leave [data-testid=retention] unchecked and upload az-keep.jsonl (3 lines); GET /api/teach/datasets/<id>.",
   "In a second context, TICK [data-testid=retention] first, then upload az-drop.jsonl (3 lines); GET the dataset.",
   "Open /teach/mine for each and read the retention line on [data-testid=dataset-card].",
   "Cleanup: delete both datasets."
  ],
  "expected": [
   "[data-testid=privacy] is a standing warning-coloured block reading 'Your file is stored on this node while it trains, and the node operator can see it. Do not upload personal data or anything you are not allowed to share.' It is present on first paint — not revealed after a file is chosen.",
   "Unchecked → the multipart body carries retention='keep'; dataset.retention === 'keep' and dataset.expires_at ≈ created_at + 7 days. The card shows 'Kept on this node until <date>.' and the page-level line 'Datasets you have not trained are deleted after 7 days.'",
   "Checked → the body carries retention='delete_after_training'; dataset.retention === 'delete_after_training' and the card shows 'Deleted as soon as training finishes.'",
   "The checkbox label is exactly 'Delete my file as soon as training finishes'.",
   "The uploaded original is on disk as <dataDir>/teach/datasets/<id>/source.<format> alongside rows.jsonl and report.json, with 0600 file mode and 0700 directory mode; the operator moderation view GET /api/me/teach/datasets lists the dataset (which is what the privacy sentence is warning about).",
   "No question or answer text appears in /api/events or the node log for either upload — the log line carries only counts and the dataset id."
  ],
  "priority": "P1",
  "area": "teach-privacy",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachUploadPage.tsx:60,116-120 (retention state, Checkbox, Privacy block position)",
   "packages/node/src/teach-datasets.ts:196-215 (source.<format>, FILE_MODE 0600 / DIR_MODE 0700, retention default 'keep', expires_at), :376-382 (delete_after_training)",
   "packages/node/src/api.ts:472-479 (retention parsed from the multipart fields); :693 GET /api/me/teach/datasets",
   "packages/web/src/i18n/pages/teach.ts:336 (up.privacy), :546-548 (data.retention / retention_delete / retention_set)"
  ]
 },
 {
  "id": "AZ-141",
  "persona": "Dataset uploader (visitor)",
  "title": "A visitor with no teaching key: nothing is owned, nothing is 401-ing in their face, and the key is created at the exact moment it is needed",
  "goal": "'No account needed' has to be literally true: browsing costs nothing and reveals nothing, and the identity appears at the first act that stores data — with a backup route offered in the same breath (design §5.3, §7.7, §D14).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US, empty localStorage."
  ],
  "steps": [
   "Open /teach, /teach/upload and /teach/mine in order without uploading anything; watch the network panel.",
   "Assert localStorage has no 'ainize.teacher.key' and [data-testid=key-note] is absent on /teach/upload.",
   "From a terminal: GET /api/teach/policy, GET /api/teach/samples, GET /api/teach/samples/en-facts, GET /api/teach/datasets and POST /api/teach/datasets — all with NO x-ngram-auth header.",
   "Back in the browser, upload a 3-line jsonl; re-check localStorage and [data-testid=key-note].",
   "Open /teach/dataset/<id> in a THIRD, empty browser context.",
   "Cleanup: delete the dataset with the first browser's key (or the operator token)."
  ],
  "expected": [
   "/teach and /teach/upload render fully with no key: the policy line, both doors, the drop zone, the format help and the three samples are all present, and no request 401s (the pages only call the public GET /api/teach/policy and GET /api/teach/samples).",
   "/teach/mine shows [data-testid=mine-empty] 'Nothing here yet. Teach in a conversation, or upload a dataset file to start.' and issues NO /api/teach/datasets request at all — the query is skipped while there is no key (a stranger never sees a 401).",
   "Unsigned API: /api/teach/policy 200 (cache-control public, max-age=10), /api/teach/samples 200, /api/teach/samples/en-facts 200 with the .jsonl attachment; GET and POST /api/teach/datasets both 401 'invalid_signature: x-ngram-auth header missing, expired, replayed or invalid …'.",
   "The first upload creates the key inline (no modal, no interruption): localStorage gains 'ainize.teacher.key' and 'ainize.teach.datasets', and [data-testid=key-note] appears reading \"Your dataset was signed with this browser's teaching key (0x….…). Lose the key and you lose access to your datasets and lessons — back it up.\" with the link 'Back up the key' → /chat?mine=1.",
   "A refused file (wrong type, over 4 MB) creates NO key — ensureKey runs only after both guards pass.",
   "The third, keyless context opening /teach/dataset/<id> gets 404 'dataset_not_found' from the node (a stranger's dataset is never confirmed to exist) and the page shows the mapped sentence 'That dataset is no longer on this node. Upload it again — you can also download it from My datasets.'"
  ],
  "priority": "P0",
  "area": "teach-auth",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachMinePage.tsx:33-36,76-77 (skip: !hasKey; mine-empty)",
   "packages/web/src/pages/TeachUploadPage.tsx:56,63-67,73,121-127 (keyShort state, ensureKey after the guards, key-note + backup link)",
   "packages/node/src/api.ts:395-400 requireTeacher (401 invalid_signature), :463-468 (public samples), :539 (public policy)",
   "packages/node/src/teach-datasets.ts:104-111 owned() — a stranger gets the same 404 as a missing id",
   "Verified on :3422 — /teach/mine with no key showed the empty state and localStorage []; unsigned GET/POST /api/teach/datasets → 401"
  ]
 },
 {
  "id": "AZ-142",
  "persona": "Dataset uploader (visitor)",
  "title": "한국어 toggle: the file door, the format examples and the error copy all switch, and the pipeline does not",
  "goal": "English-first with a working Korean translation across the whole file door — including the code examples in the format help, which are the part most likely to be left in English (design §5.12, i18n rule).",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true.",
   "Fresh browser context, locale en-US (the app defaults to 'en' regardless of the browser locale)."
  ],
  "steps": [
   "Open /teach/upload; click the header button[aria-label=language] labelled '한국어'.",
   "Re-read the whole [data-testid=teach-upload] page.",
   "Try to upload az-pic.png and read the error in Korean; then upload az-messy.jsonl and read the preview pills in Korean.",
   "Reload the page and confirm the choice stuck; click the button (now labelled 'English') to switch back.",
   "Cleanup: delete the dataset."
  ],
  "expected": [
   "Toggle flips localStorage 'ainize.locale' to 'ko' and survives a reload; the button label swaps between '한국어' and 'English'.",
   "Korean upload page: stepper '1 데이터셋 · 2 확인 · 3 설정 · 4 학습 · 5 결과'; H1 '데이터셋 올리기'; subtitle '한 줄에 질문 하나, 정답 하나. 적어 준 그대로 모델이 배웁니다.'; drop zone '여기에 파일을 놓으세요' / '또는' / 'jsonl, csv, tsv, txt · 최대 4 MB'; paste '표를 붙여넣기'; checkbox '학습이 끝나면 내 파일 삭제하기'.",
   "Privacy in Korean: '학습하는 동안 파일이 이 노드에 저장되고 노드 운영자가 볼 수 있습니다. 개인정보나 공유할 수 없는 내용은 올리지 마세요.'",
   "The format help EXAMPLES themselves switch language — the .jsonl block becomes {\"prompt\":\"Ainize를 만든 곳은?\",\"answer\":\"Comcom\",\"alt_prompt\":\"Ainize는 어느 회사가 만들었나요?\"} and the .txt block becomes 'Q: Ainize를 만든 곳은?' / 'A: Comcom' — the box headings stay .JSONL/.CSV/.TSV/.TXT and the aliases line becomes '다른 이름도 됩니다: 질문은 question / q / 질문, …'.",
   "Sample rows read '질문 5개' / '예시 데이터셋 내려받기' / '이 예시로 시작하기'.",
   "Wrong file type in Korean: '이 노드는 jsonl, csv, tsv, 일반 텍스트를 읽습니다. \"az-pic.png\"은(는) 해당하지 않습니다.'",
   "Preview pills in Korean for az-messy.jsonl: '학습 1개 · 이미 알고 있음 0개 · 중복 0개 · 고칠 것 6개', '이 질문에 정답이 둘입니다 — 하나를 고르세요', '정답 없음 — 정답을 적어 주세요', '학습합니다 — 다듬음' — the same statuses, only the words change.",
   "No key, dataset id, fingerprint, format, encoding or line number changes with the locale."
  ],
  "priority": "P1",
  "area": "i18n",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/i18n/index.ts:14-23 (LocaleProvider, default 'en', localStorage 'ainize.locale')",
   "packages/web/src/components/ui/Header.tsx:78,121 (button[aria-label=language], t('common.locale'))",
   "packages/web/src/components/teach/FormatHelp.tsx:8-21,38-47 (EXAMPLES.ko drives the code blocks)",
   "packages/web/src/components/teach/util.ts fileStatus() — pills come from row.status, never the server's English detail",
   "Verified on :3422 — the full Korean upload page rendered exactly these strings"
  ]
 },
 {
  "id": "AZ-143",
  "persona": "Dataset uploader (visitor)",
  "title": "The whole file door on a 360 px phone: nothing scrolls sideways and the question table becomes cards",
  "goal": "The owner's file door has to work where dragging a file is impossible. At phone width every one of the five steps must fit: the stepper collapses to one line, the paste box opens by itself, the preview table turns into one card per question with its column names inline, and no page ever scrolls horizontally (design §5.3, §5.12).",
  "preconditions": [
   "node-u on http://localhost:3422 as it stands: teach.enabled true, backend 'stub', teach.stubOffline true, publish auto. No model call.",
   "Playwright `mobile` project — viewport 360 × 740, deviceScaleFactor 2, hasTouch true; locale en-US, empty localStorage. Tag the test @mobile.",
   "The run deletes the dataset and the lesson it creates."
  ],
  "steps": [
   "Open /teach, then /teach/upload, and at each page evaluate `document.documentElement.scrollWidth <= window.innerWidth + 1`.",
   "Read [data-testid=teach-stepper] and [data-testid=teach-step-small]; read the open/closed state of <details data-testid=paste-table>.",
   "Upload az-mob-<TAG>.jsonl (3 lines: a long Korean question with an alt_prompt, one plain fact, and one line whose answer is empty) through [data-testid=file-input].",
   "On /teach/dataset/<id> read one [data-testid=dataset-row]: whether <thead> is in the accessibility tree, what each cell renders, and where the line number sits; check the sticky bar and the horizontal-scroll assertion again.",
   "Continue to settings, press Train, and repeat the horizontal-scroll check on /teach/dataset/<id>/settings, /teach/lesson/<jobId> (while TRAINING and after it finishes) and /teach/mine.",
   "Switch the header language button to 한국어 and repeat the scroll check on all five pages.",
   "Cleanup: delete the lesson and the dataset."
  ],
  "expected": [
   "On all five pages, in both locales, `document.documentElement.scrollWidth` is at most `window.innerWidth + 1` — the page body never scrolls sideways; any wide element (the question table, the log, the run-locally commands) scrolls inside its own box.",
   "Below 600 px the Stepper renders only [data-testid=teach-step-small] — \"Step 1 of 5 · Dataset\", \"Step 2 of 5 · Check\", … — plus a five-segment bar; the five spelled-out labels are not rendered at this width, and [data-testid=teach-stepper] keeps its aria-label so the step is still announced.",
   "On /teach/upload the <details data-testid=paste-table> is OPEN on arrival (the page opens it under 480 px, because a phone cannot drop a file), while the drop zone still renders its real <input type=file> — the two intake paths are both usable with a thumb.",
   "On the preview, below 720 px [data-testid=dataset-table] drops its header row from view (thead is clipped to 1 px) and every row becomes one bordered card: each cell prints its own column name from `td::before content: attr(data-label)` — \"Right answer: Comcom\", \"Another way to ask (optional): …\", \"Status: Will train\" — while the question cell prints no label and the line number sits inline in front of it. An empty cell is not rendered at all (`td:empty { display: none }`), so the empty-answer row shows its red pill instead of a blank labelled box.",
   "The sticky bars stay on screen at the bottom of the viewport and are pressable without any sideways scrolling: \"Continue to settings\" on step 2 and \"Train this lesson (3 questions)\" on step 3.",
   "On the result screen the \"What it learned\" table stacks the same way (thead clipped, one card per question, Before/After/Other wording labelled inline), and the counters and the stage rail wrap instead of overflowing.",
   "No text is clipped or truncated mid-word in either locale — the cells break on `word-break: break-word`, and the long Korean question wraps inside its card."
  ],
  "priority": "P1",
  "area": "teach-ui",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/teach/DatasetTable.tsx:27-38 (@media max-width 720px: thead clipped, tr as a card, td::before data-label, td:empty hidden, td.n inline)",
   "packages/web/src/pages/TeachUploadPage.tsx:43 narrow() (< 480 px) → :114 <PasteTable open={narrow()}>",
   "packages/web/src/components/teach/Stepper.tsx:6,41,49 (below 600 px only teach-step-small + the bar)",
   "packages/web/src/pages/TeachDatasetPage.tsx:54 and packages/web/src/pages/TeachSettingsPage.tsx:34 (position: sticky action bars)",
   "packages/web/src/pages/TeachLessonPage.tsx:44-55 (FactTable stacks below 720 px), :40-43 (Counters wrap)"
  ]
 },
 {
  "id": "AZ-144",
  "persona": "Dataset uploader (visitor)",
  "title": "Preview table: ok vs tidied-up rows, the tidy-up receipt and the same verdicts in Korean",
  "goal": "A row that trains says so, and a row the node changed says what it changed — in both locales, from the machine-readable status, never from the server's English detail sentence (design §8.5, §D2).",
  "preconditions": [
   "Dev node-u http://localhost:3422 (home ~/.ngram-teachable/node-u, teach enabled, backend stub, teach.stubOffline true, publish auto) — no model call is needed for this scenario",
   "Fresh browser context (a new teaching key; datasets_per_key_per_day is 10)",
   "Test deletes the dataset it creates (ds-delete on /teach/mine, or DELETE /api/teach/datasets/:id)"
  ],
  "steps": [
   "Open /teach/upload and drop az144-fixes.jsonl with exactly these four lines:\n{\"prompt\":\"  Who   founded\\tAinize?  \",\"answer\":\"Comcom\\nof Seoul\"}\n{\"prompt\":\"Q: What is 2+2?\",\"answer\":\"4\"}\n{\"prompt\":\"Zero​width?\",\"answer\":\"yes\"}\n{\"prompt\":\"When did Ainize start?\",\"answer\":\"2020\"}",
   "On /teach/dataset/<id> read the counts pill ([data-testid=row-counts]), the tidy-up note ([data-testid=fixed-note]) and every row of [data-testid=dataset-table]",
   "GET /api/teach/datasets/<id>/rows?limit=50 with the teaching key and read each item's status + fixes",
   "Click the header language button (aria-label=\"language\", label 한국어) and re-read the pill and the first three status pills",
   "Click 'Download this dataset (.jsonl)' and inspect the saved bytes"
  ],
  "expected": [
   "Four rows, Line 1–4; rows 1–3 show the green pill 'Will train — tidied up', row 4 shows 'Will train'; every row also shows the grey help line 'Not checked yet'",
   "[data-testid=fixed-note] reads '3 question(s) were tidied up (extra spaces and line breaks removed).'",
   "[data-testid=row-counts] reads '4 will train · 0 already known · 0 duplicates · 0 need a fix'",
   "API report: line 1 status 'fixed' fixes ['answer_flattened','whitespace_collapsed']; line 2 'fixed' ['qa_prefix_stripped']; line 3 'fixed' ['controls_stripped']; line 4 'ok' with no fixes",
   "Downloaded rows.jsonl holds the normalised text and nothing else: {\"prompt\":\"Who founded Ainize?\",\"answer\":\"Comcom of Seoul\"} / {\"prompt\":\"What is 2+2?\",\"answer\":\"4\"} / {\"prompt\":\"Zerowidth?\",\"answer\":\"yes\"} / {\"prompt\":\"When did Ainize start?\",\"answer\":\"2020\"}",
   "In Korean the same rows read '학습합니다 — 다듬음' / '학습합니다' and the pill reads '학습 4개 · 이미 알고 있음 0개 · 중복 0개 · 고칠 것 0개' — no English leaks into the table"
  ],
  "priority": "P0",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:408 normalizeRow (controls_stripped / whitespace_collapsed / answer_flattened / qa_prefix_stripped)",
   "packages/web/src/components/teach/util.ts:59 fileStatus (ok/fixed)",
   "packages/web/src/i18n/pages/teach.ts:370-371,391 teach.rows.status.new / .fixed / teach.rows.fixed",
   "packages/web/src/pages/TeachDatasetPage.tsx:187 fixed-note, :186 row-counts"
  ]
 },
 {
  "id": "AZ-145",
  "persona": "Dataset uploader (visitor)",
  "title": "Two answers for one question block both copies, and 'Keep this answer' resolves the contradiction",
  "goal": "A contradiction is never guessed away: every copy is refused with the lines that disagree, and the visitor picks the answer to learn (design §8.5, §5.4).",
  "preconditions": [
   "Dev node-u :3422, backend stub, teach.stubOffline true",
   "Fresh browser context; the dataset is deleted at the end",
   "A file made only of contradicting rows is refused at upload (dataset_empty), so the fixture carries one usable question"
  ],
  "steps": [
   "Upload az145-conflict.csv:\nprompt,answer\nWho founded Ainize?,Comcom\nWho founded Ainize?,Comcom\nWho founded Ainize?,ComcomAI\nWhen did Ainize start?,2020",
   "Read the three conflict rows, their help lines and the counts pill",
   "Click 'Keep this answer' on the Line 4 row (answer ComcomAI)",
   "Re-read the table, the pill, the dataset revision and fingerprint (GET /api/teach/datasets/<id>)"
  ],
  "expected": [
   "Lines 2, 3 and 4 all carry the red pill 'Two answers for this question — pick one' and sit on the pink row background (tr[data-bad='1']); each shows the help line 'Lines 2 and 3 ask the same question but give different answers. The model can only learn one.' (each row names the OTHER lines, e.g. line 3 → 'Lines 3 and 2 …')",
   "Each conflict row offers 'Edit' and 'Keep this answer' and no 'Remove'",
   "Before the click: [data-testid=row-counts] = '1 will train · 0 already known · 0 duplicates · 3 need a fix'; the dataset holds 1 question",
   "After 'Keep this answer': dataset revision 1 → 2, a new fingerprint, 2 questions; the table shows Line 1 'When did Ainize start? / 2020' and Line 2 'Who founded Ainize? / ComcomAI', both 'Will train'; the pill reads '2 will train · 0 already known · 0 duplicates · 0 need a fix'",
   "API report: summary.conflicts = 3 before, 0 after; the chosen pair is the last line of rows.jsonl"
  ],
  "priority": "P0",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:613-617 conflict branch (conflictOf built over the whole file)",
   "packages/web/src/components/teach/DatasetTable.tsx:123 row-keep button; packages/web/src/pages/TeachDatasetPage.tsx:151 keepAnswer → rows_op append",
   "packages/web/src/i18n/pages/teach.ts:377,386,387 status.conflict / bad.conflict / conflict_keep"
  ]
 },
 {
  "id": "AZ-146",
  "persona": "Dataset uploader (visitor)",
  "title": "Too long: the pill names the real length and the node's limit, and the edit sheet refuses the same text",
  "goal": "Length limits are the node's (policy), stated in characters with the over-shoot, and the same rule is enforced before a new question is sent (design §8.5, §5.4).",
  "preconditions": [
   "Dev node-u :3422 (policy limits prompt_max 400, answer_max 200 — read from GET /api/teach/policy, never hard-coded in the test)",
   "Fresh browser context; the dataset is deleted at the end"
  ],
  "steps": [
   "Upload az146-long.jsonl with three lines: {\"prompt\":\"Explain the whole history of the Korean peninsula in one line\",\"answer\":\"<210 × 'x'>\"}, {\"prompt\":\"<431 × 'Q'>\",\"answer\":\"x\"}, {\"prompt\":\"Who founded Ainize?\",\"answer\":\"Comcom\"}",
   "Read the two red rows and the counts pill",
   "GET /api/teach/datasets/<id>/rows?status=too_long",
   "Click 'Add a question', type 'Q?' as the question and a 230-character answer, press 'Add this question'"
  ],
  "expected": [
   "Line 1 pill: 'The answer is 210 characters; keep it under 200. Teach a long explanation as several short facts.'; Line 2 pill: 'The question is 431 characters; keep it under 400.' — both red, both offering only 'Edit'",
   "[data-testid=row-counts] = '1 will train · 0 already known · 0 duplicates · 2 need a fix'",
   "API details: 'the answer is 210 characters, 10 over the 200 limit' and 'the question is 431 characters, 31 over the 400 limit'; summary.too_long = 2",
   "In the sheet the answer counter reads '230/200' in the error colour and the alert says 'The answer is 230 characters; keep it under 200. Teach a long explanation as several short facts.'; no PATCH is sent (dataset revision and fingerprint unchanged)"
  ],
  "priority": "P0",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:599-606 too_long branch",
   "packages/web/src/components/teach/util.ts:70-75 fileStatus too_long (renders from the row's own lengths + policy limits)",
   "packages/web/src/components/teach/RowEditSheet.tsx:33-37 submit() length guards",
   "packages/web/src/i18n/pages/teach.ts:383-384 bad.answer_long / bad.question_long"
  ]
 },
 {
  "id": "AZ-147",
  "persona": "Dataset uploader (visitor)",
  "title": "A half-filled line says which half is missing",
  "goal": "An empty question and an empty answer are different problems and must not share one sentence (design §8.5).",
  "preconditions": [
   "Dev node-u :3422, backend stub",
   "Fresh browser context; the dataset is deleted at the end"
  ],
  "steps": [
   "Upload az147-empty.csv:\nprompt,answer\n,Comcom\nWho founded Ainize?,\nWhere is Comcom?,Seoul",
   "Read the two red rows, the Question/Right answer cells and the counts pill",
   "GET /api/teach/datasets/<id>/rows?status=empty"
  ],
  "expected": [
   "Line 2 renders '—' in the Question cell, 'Comcom' in the Right answer cell and the red pill 'No question — type the question'",
   "Line 3 renders the question, an empty answer cell and the red pill 'No answer — type the right answer'",
   "[data-testid=row-counts] = '1 will train · 0 already known · 0 duplicates · 2 need a fix'; the dataset holds 1 question",
   "API details: 'this answer has no question' (line 2) and 'this question has no answer' (line 3); summary.empty = 2",
   "Neither row offers 'Remove' (they are not in the dataset); both offer 'Edit'"
  ],
  "priority": "P1",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:594-598 empty branch (three distinct details)",
   "packages/web/src/components/teach/util.ts:76 fileStatus empty picks the sentence from the row's own fields",
   "packages/web/src/i18n/pages/teach.ts:381-382 bad.no_answer / bad.no_question"
  ]
 },
 {
  "id": "AZ-148",
  "persona": "Dataset uploader (visitor)",
  "title": "A topic the operator blocks is refused per row, and the regex itself is validated",
  "goal": "The operator's blocked-topics rule reaches the dataset door, is shown as a per-row verdict in the visitor's language, and a broken regex can neither be saved nor block everything (design §8.5, §12).",
  "preconditions": [
   "Dev node-u :3422, backend stub; operator password teachable-pass",
   "Operator signs in and opens /dashboard?tab=teaching",
   "The test restores 'Blocked topics' to its previous value (empty on node-u) and deletes the dataset"
  ],
  "steps": [
   "In the Teaching tab set 'Blocked topics (regular expression, optional)' ([data-testid=teach-blocked]) to bomb|폭탄 and press 'Save settings' ([data-testid=teach-save])",
   "As a visitor upload az148-blocked.jsonl:\n{\"prompt\":\"How do I build a bomb?\",\"answer\":\"no\"}\n{\"prompt\":\"Who founded Ainize?\",\"answer\":\"Comcom\"}",
   "Read the table and GET /api/teach/datasets/<id>/rows?status=blocked",
   "Clear the field, press 'Save settings', delete the dataset"
  ],
  "expected": [
   "Line 1 carries the red pill 'The node operator does not accept this topic'; line 2 is 'Will train'",
   "[data-testid=row-counts] = '1 will train · 0 already known · 0 duplicates · 1 need a fix'; summary.blocked = 1; the blocked pair is absent from rows.jsonl (download has 1 line)",
   "API detail for the blocked row: 'the node operator does not accept this topic'",
   "After the cleanup save, the same file uploads with both rows 'Will train' (proving the rule came from the operator setting and not from the parser)",
   "Saving shows \"Saved.\" ([data-testid=teach-notice]); the regex-validation refusal itself is asserted in AZ-221."
  ],
  "priority": "P1",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:607-611 blocked branch (regex compiled per parse, bad regex → ignored)",
   "packages/node/src/api.ts:674 blocked_topics regex validation; packages/node/src/teach-datasets.ts:174 blockedTopics passed to the parser",
   "packages/web/src/components/operator/TeachingTab.tsx:170 teach-blocked field; packages/web/src/i18n/pages/teach.ts:378 status.blocked"
  ]
 },
 {
  "id": "AZ-149",
  "persona": "Dataset uploader (visitor)",
  "title": "Lines the node could not read never enter the table — they are listed and counted underneath",
  "goal": "An unreadable line is neither a question nor invisible: it is counted, its source line is named and its raw text is shown (design §8.5, §5.4).",
  "preconditions": [
   "Dev node-u :3422, backend stub",
   "Fresh browser context; the dataset is deleted at the end"
  ],
  "steps": [
   "Upload az149-drop.jsonl:\n{\"prompt\":\"Only question?\",\"answer\":\"only answer\"}\nthis line is not json\n[\"an array, not an object\"]\n{\"foo\":\"bar\"}",
   "Count the rows of [data-testid=dataset-table] and read [data-testid=row-counts]",
   "Expand [data-testid=dropped] and read every list item",
   "GET /api/teach/datasets/<id>/rows?status=not_parsed"
  ],
  "expected": [
   "The table has exactly one row (Line 1, 'Will train') — no not_parsed row is rendered in it",
   "[data-testid=row-counts] = '1 will train · 0 already known · 0 duplicates · 3 need a fix'",
   "The disclosure summary reads '3 line(s) could not be read and were left out. — See the lines that were left out'; expanded it lists 'Line 2 could not be read as a question and an answer.' + <code>this line is not json</code>, 'Line 3 …' + [\"an array, not an object\"], 'Line 4 …' + {\"foo\":\"bar\"}",
   "API: three items with status 'not_parsed', line 2/3/4, details 'line is not valid JSON', 'line is not a JSON object', 'no question/answer keys in this object', each with `raw` (capped at 200 characters); summary.not_parsed = 3"
  ],
  "priority": "P0",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:588-591 not_parsed branch; :247-262 recordsFromJsonl error texts",
   "packages/web/src/pages/TeachDatasetPage.tsx:236-244 droppedRows are excluded from the table and rendered in the disclosure",
   "packages/web/src/i18n/pages/teach.ts:385,389,390 bad.parse / dropped / dropped_show"
  ]
 },
 {
  "id": "AZ-150",
  "persona": "Dataset uploader (visitor)",
  "title": "The Line column is the line of the uploaded file: header, blank lines and a quoted newline all counted the node's way",
  "goal": "'Line N' must point at something the visitor can find in their own file — one logical row per line, a header that is not a question, blank lines that keep their place (design §8.2, §8.5, docs line 334).",
  "preconditions": [
   "Dev node-u :3422, backend stub",
   "Fresh browser context; the dataset is deleted at the end"
  ],
  "steps": [
   "Upload az150-lines.csv with exactly this content (line 3 is blank, the B row's quoted value spans two physical lines):\nprompt,answer\nA one?,1\n\n\"B, two?\ncontinued\",\"2\"\nC three?,3",
   "Read the Line cells and the status pills of the three rows",
   "GET /api/teach/datasets/<id>/rows?limit=50 and read report notes + each item's line",
   "Download the dataset"
  ],
  "expected": [
   "Three rows with Line 2, Line 4 and Line 5 — the header is line 1 and is never a question, the blank line 3 consumes a number and appears nowhere",
   "The B row is ONE row (the quoted newline does not split it) and is marked 'Will train — tidied up' — the embedded newline was collapsed into a space, so the question reads 'B, two? continued'",
   "report.json notes contain 'blank_rows:2' (the blank line and the trailing newline); has_header = true, delimiter = ',', columns {prompt:0, answer:1}",
   "Downloaded rows.jsonl holds 3 lines in file order",
   "Documented consequence the test asserts explicitly: the number is the LOGICAL row, so after a quoted embedded newline the physical text line of 'C three?' is 6 while the column says 5"
  ],
  "priority": "P0",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:125 parseDelimited (logical rows, blank rows kept as []), :311-320 recordsFromDelimited (header row skipped, line = i + 1)",
   "packages/web/src/components/teach/DatasetTable.tsx:107 <td class=\"n\">{row.line}</td>",
   "docs/teachable-dataset-design.md:334 'line N — the position in the uploaded file'"
  ]
 },
 {
  "id": "AZ-151",
  "persona": "Dataset uploader (visitor)",
  "title": "After the first edit the report is rebuilt: rejected lines disappear and the numbers stop being file lines",
  "goal": "An edit rewrites the canonical file, so the screen must not keep claiming that the numbers and the counts still describe the uploaded file (design §7.2 rewrite, §5.4).",
  "preconditions": [
   "Dev node-u :3422, backend stub",
   "Fresh browser context; the dataset is deleted at the end"
  ],
  "steps": [
   "Upload az151-mix.csv:\nprompt,answer\nWho founded Ainize?,Comcom\nWho founded Ainize?,ComcomAI\n,Nothing\nnot two columns\n픽셀플러스 종목코드는?,087600\n삼성전자 종목코드는?,005930",
   "Record the table (lines, statuses), the counts pill and the dropped disclosure",
   "Press 'Remove' on the last row and wait for the table to refresh",
   "Re-read the table, the pill, the disclosure, the fingerprint and the revision (GET /api/teach/datasets/<id>)"
  ],
  "expected": [
   "Before: rows on Line 2 and 3 are conflicts, Line 4 is 'No question — type the question', Line 5 is listed under '1 line(s) could not be read and were left out.', Lines 6 and 7 are 'Will train'; pill '2 will train · 0 already known · 0 duplicates · 3 need a fix'",
   "After the Remove: the table holds ONLY the kept question, numbered from 1; every conflict / empty / unreadable row is gone from the table AND from the API report (GET …/rows?status=rejected returns 0 items); the dropped disclosure is gone; the pill reads '1 will train · 0 already known · 0 duplicates · 0 need a fix'",
   "dataset.revision 1 → 2 with a new sha256; the download is named dataset-<id>-r2.jsonl",
   "Honesty requirement the scenario asserts: after the rewrite the screen no longer presents these numbers as lines of the uploaded file — the column heading / subtitle must say the position in the dataset (today the heading still reads 'Line' and the page still says 'Saved as az151-mix.csv', which is the product fix this scenario forces)"
  ],
  "priority": "P1",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-datasets.ts:318-323 patch() re-parses canonicalBytes(next); :336-350 rewrite() overwrites report.json",
   "packages/web/src/pages/TeachDatasetPage.tsx:105-112 removeRow, :226 DatasetTable, :236 dropped",
   "packages/web/src/i18n/pages/teach.ts:355 teach.rows.h.n = 'Line', :402 saved_note"
  ]
 },
 {
  "id": "AZ-152",
  "persona": "Dataset uploader (visitor)",
  "title": "Changing a question throws away every model verdict on the screen",
  "goal": "A green 'already known' tick must never sit next to text the visitor just changed — the model-side status is measured against bytes, and the bytes changed (design §5.4).",
  "preconditions": [
   "Dev node-u :3422 with backend stub and teach.stubOffline true (the offline stub answers a question whose prompt contains its answer, so 'already known' is deterministic without any GPU)",
   "Fresh browser context; the dataset is deleted at the end"
  ],
  "steps": [
   "Upload az152-check.jsonl:\n{\"prompt\":\"종목코드 087600은 픽셀플러스인가요?\",\"answer\":\"픽셀플러스\"}\n{\"prompt\":\"Who founded Ainize?\",\"answer\":\"Comcom\",\"alt_prompt\":\"Which company is behind Ainize?\"}\n{\"prompt\":\"When did Ainize start?\",\"answer\":\"2020\"}",
   "Read the simulated-checks banner and the button, then press 'Check (simulated on this node)' ([data-testid=run-check]) and wait for the verdicts",
   "Record every row's status pill, its simulated-answer help line, [data-testid=checked-note] and the counts pill",
   "Press 'Edit' on row 2, change the answer to 'Comcom Inc.' and press 'Save'",
   "Re-read every row, the checked note and the pill",
   "Repeat the invalidation check for 'Remove' on row 3 and for 'Read it again' ([data-testid=open-reparse] → [data-testid=reparse-go])"
  ],
  "expected": [
   "Before the check: [data-testid=checks-simulated] reads 'Demo node — these checks were simulated, not measured in a live model.' and the button reads 'Check (simulated on this node)'. After it: row 1 grey 'Already known — skipped' with 'Simulated answer (no model was asked): 픽셀플러스'; rows 2 and 3 green 'Will train' with 'Simulated answer (no model was asked): (stub model) I do not know: …' in the warning tone (#8a4b00); [data-testid=checked-note] 'Simulated check: 2 of 3 are marked to train — nothing was measured in a live model.'; pill '2 will train · 1 already known · 0 duplicates · 0 need a fix'",
   "After the edit: EVERY row (not only the edited one) is back to 'Not checked yet', the checked-note is gone, the pill reads '3 will train · 0 already known · 0 duplicates · 0 need a fix', and dataset.revision is 2 with a new fingerprint",
   "The same clearing happens after a Remove and after a re-read (reparse) — no verdict survives a revision change",
   "No verdict is ever shown for a row that was not measured, and the model's own sentence is quoted verbatim under the pill"
  ],
  "priority": "P0",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachDatasetPage.tsx:92-93 useEffect on [revision] clears flight/sampled/partial/selection; :115-124 saveRow, :105-112 removeRow",
   "packages/web/src/components/teach/DatasetTable.tsx:90-92 model status wins only for trainable rows; packages/web/src/components/teach/util.ts:92 modelStatus",
   "packages/node/src/teach.ts:453 stubAnswer, :540-544 already_known / will_train"
  ]
 },
 {
  "id": "AZ-153",
  "persona": "Dataset uploader (visitor)",
  "title": "Editing a refused row adds the corrected question instead of pretending to repair the file",
  "goal": "A rejected source line is not part of the dataset, so 'Edit' on it must produce a new, valid question and say so honestly (design §7.2 rows_op, §5.4).",
  "preconditions": [
   "Dev node-u :3422, backend stub",
   "Fresh browser context; the dataset is deleted at the end"
  ],
  "steps": [
   "Upload az153-long.jsonl:\n{\"prompt\":\"Explain the whole history of the Korean peninsula in one line\",\"answer\":\"<210 × 'x'>\"}\n{\"prompt\":\"Who founded Ainize?\",\"answer\":\"Comcom\"}",
   "Press 'Edit' on the too-long row, read the sheet title, replace the answer with 'It is long.' and press 'Save'",
   "Watch the request the page sends and re-read the table, the pill and GET /api/teach/datasets/<id>"
  ],
  "expected": [
   "The sheet opens titled 'Question, line 1' with the refused text prefilled and the counters '61/400' and '210/200'",
   "Saving sends PATCH /api/teach/datasets/<id> with rows_op {op:'append', rows:[{prompt:…, answer:'It is long.'}]} — never {op:'replace'}, because a refused row has no index in the dataset",
   "After saving: dataset.rows 1 → 2, revision 1 → 2, new fingerprint; the corrected question is the LAST row of the table and of rows.jsonl",
   "No too_long row remains anywhere (the report was rebuilt from the canonical rows) and the pill reads '2 will train · 0 already known · 0 duplicates · 0 need a fix'"
  ],
  "priority": "P1",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachDatasetPage.tsx:115-124 saveRow (replace only when target.index !== null, else append)",
   "packages/node/src/teach-datasets.ts:426 applyRowsOp append; :318 patch() revalidates the whole set",
   "packages/web/src/components/teach/RowEditSheet.tsx:57 title teach.rows.edit_cell '{field}, line {n}'"
  ]
 },
 {
  "id": "AZ-154",
  "persona": "Dataset uploader (visitor)",
  "title": "Remove a question, undo it, and see exactly what changed each time",
  "goal": "Removing is reversible for as long as the toast is on screen, and every change to the questions is a new revision with a new fingerprint (design §5.4, §7.2).",
  "preconditions": [
   "Dev node-u :3422, backend stub",
   "Fresh browser context; the dataset is deleted at the end"
  ],
  "steps": [
   "Upload az154-krx.csv:\nprompt,answer\n픽셀플러스 종목코드는?,087600\n삼성전자 종목코드는?,005930\n카카오 종목코드는?,035720",
   "Note the fingerprint and revision, press 'Remove' on the 카카오 row",
   "Read [data-testid=undo-toast], the table and the pill; re-read GET /api/teach/datasets/<id>",
   "Press 'Undo' inside the toast and re-read the table, the pill and the dataset",
   "Repeat the removal and let the toast time out without clicking"
  ],
  "expected": [
   "The toast reads 'Removed \"카카오 종목코드는?\".' next to an 'Undo' button",
   "After the removal: 2 rows, numbered 1 and 2, pill '2 will train · 0 already known · 0 duplicates · 0 need a fix', dataset.rows 2, revision 2, a different sha256",
   "After Undo: 3 rows again, revision 3, another new sha256, and the restored question is the LAST row (it is appended, not put back in place) — the test asserts the order explicitly rather than assuming it returns to position 3",
   "The toast disappears on its own after ~8 s and the removal stays; a second Remove after the toast is gone offers no way back except 'Add a question'"
  ],
  "priority": "P0",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachDatasetPage.tsx:96-103 toast + 8 s timer, :105-112 removeRow, :263-272 undo toast (append)",
   "packages/node/src/teach-datasets.ts:336-350 rewrite (revision++, new sha256)",
   "packages/web/src/i18n/pages/teach.ts:363-365 remove / removed / undo"
  ]
 },
 {
  "id": "AZ-155",
  "persona": "Dataset uploader (visitor)",
  "title": "The last question cannot be removed, and the refusal must be about the removal",
  "goal": "A dataset needs at least one question; refusing that is right, but the sentence the visitor reads must match what they just did (design §7.2, §5.11 error copy).",
  "preconditions": [
   "Dev node-u :3422, backend stub",
   "Fresh browser context; the dataset is deleted at the end"
  ],
  "steps": [
   "Upload az155-one.jsonl:\n{\"prompt\":\"Only question?\",\"answer\":\"only answer\"}\nthis line is not json",
   "Press 'Remove' on the only row",
   "Read [data-testid=dataset-error], the table, the pill, and GET /api/teach/datasets/<id>"
  ],
  "expected": [
   "PATCH /api/teach/datasets/<id> answers 400 'dataset_empty: a dataset needs at least one question'",
   "The row is still there, no undo toast appears, dataset.rows stays 1 and the revision does not change",
   "The page shows the refusal in the red box above the table; the sentence must describe the removal ('a dataset needs at least one question — add another one first' or equivalent, in both locales). Today it renders the upload-time copy 'That file has no usable questions. Every line needs a question and a right answer.', which does not describe the action — the scenario fails until the mapping has a removal-specific sentence"
  ],
  "priority": "P1",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-datasets.ts:320 patch() → 'dataset_empty: a dataset needs at least one question'",
   "packages/web/src/components/chat/teachUtil.ts:52 mapTeachError case 'dataset_empty' → teach.err.dataset_empty",
   "packages/web/src/i18n/pages/teach.ts:569 teach.err.dataset_empty (file-time wording)"
  ]
 },
 {
  "id": "AZ-156",
  "persona": "Dataset uploader (visitor)",
  "title": "Questions that end the same way get an advisory that never blocks and disappears when it stops being true",
  "goal": "The one failure the measurement predicts — questions the model learns as one because their endings are identical — is reported per row without refusing anything (design §8.5 advisory).",
  "preconditions": [
   "Dev node-u :3422, backend stub",
   "Fresh browser context; the dataset is deleted at the end"
  ],
  "steps": [
   "Upload az156-endings.csv:\nprompt,answer\n픽셀플러스 종목코드는?,087600\n삼성전자 종목코드는?,005930\n카카오 종목코드는?,035720\nWho founded Ainize?,Comcom",
   "Read the four rows: pills, help lines and [data-testid=advisory]",
   "GET /api/teach/datasets/<id>/rows and read advisory / detail / summary.shared_ending",
   "Press 'Remove' on one of the three KRX rows and re-read the remaining rows",
   "Switch the header language to Korean and re-read the advisory line",
   "Run `ainize --home ~/.ngram-teachable/cli teach dataset ./az156-endings.csv` against :3422 and read the heads-up line"
  ],
  "expected": [
   "All three KRX rows keep the green pill 'Will train' — the advisory changes no status and no count (the pill stays '4 will train · 0 already known · 0 duplicates · 0 need a fix')",
   "Each of the three shows an advisory help line naming how many OTHER questions share the ending: for a group of three that is 'Ends the same way as 2 others'. (Today the build renders 'Ends the same way as 3 others' because it prints the group size — the off-by-one is the product fix this scenario forces.) The fourth question has no advisory line",
   "API: the three rows carry advisory:['shared_ending'] and detail '3 questions in this dataset end the same way'; summary.shared_ending = 3",
   "After removing one KRX row the advisory is gone from the remaining two (a group of two is not reported) — it is recomputed on every revision, never cached",
   "Korean renders '다른 {n}개와 끝이 같습니다' with the same number as English",
   "The CLI prints the dataset heads-up line '3 questions end the same way — the model may answer them all alike'"
  ],
  "priority": "P1",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach-dataset.ts:467 endingKey (last up to 3 tokens, never the whole prompt), :643-650 advisory pass (groups of ≥ 3)",
   "packages/web/src/components/teach/util.ts:85-90 sharedEnding (firstNumber(detail) = group size)",
   "packages/web/src/i18n/pages/teach.ts:379 status.shared_end; packages/cli/src/commands/teach-dataset.ts:139 'heads-up' line"
  ]
 },
 {
  "id": "AZ-157",
  "persona": "Dataset uploader (visitor)",
  "title": "The counts pill after a sampled check never claims more than was measured",
  "goal": "The check probes only the head of a big dataset; the pill and the note must let the visitor tell a measured 'will train' from an unmeasured one (design §5.4, §6.2 preflight.sampleRows).",
  "preconditions": [
   "Dev node-u :3422 with backend stub and teach.stubOffline true (deterministic verdicts, no GPU); policy preflight sampleRows 24, perCall 8",
   "Fresh browser context; the dataset is deleted at the end; the check spends 3 of the hourly free live-test units for this IP and key"
  ],
  "steps": [
   "Upload az157-sample.jsonl with 30 lines: rows 1, 5, 9 and 13 are self-answering ({\"prompt\":\"종목코드 087600은 픽셀플러스인가요?\",\"answer\":\"픽셀플러스\"} and three more of the same shape with different codes), the other 26 are ordinary facts the stub cannot answer",
   "Press 'Check (simulated on this node)' and wait for all three batches",
   "Read [data-testid=checked-note], [data-testid=row-counts], and the status of a row inside the sampled head and one beyond it (rows 25–30)",
   "Watch the network: POST /api/teach/preflight is called with {dataset_id, offset:0|8|16, limit:8}"
  ],
  "expected": [
   "Exactly three preflight calls are made; the last response carries sampled {checked:24, of:30}",
   "[data-testid=checked-note] reads 'Simulated check of 24 of 30 questions — not measured in a live model.' — never 'Checked: … of 30' as if all were measured, and never a claim about a live model on a node whose checks are simulated",
   "[data-testid=row-counts] reads '26 will train · 4 already known · 0 duplicates · 0 need a fix' (rows − known), and the rows beyond the sampled head still show the grey help line 'Not checked yet'",
   "No row outside the sampled head is given a model verdict or a simulated-answer line",
   "With every sampled row known (a 3-row all-self-answering dataset), the pill shows 0 will train and the warning 'The model already answers all of these correctly, so there is nothing to teach. Add a question it gets wrong.' appears"
  ],
  "priority": "P0",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach.ts:505-512 preflightSlice (perCall 8, sampleRows 24, sampled{checked,of})",
   "packages/web/src/pages/TeachDatasetPage.tsx:132-159 check() batching, :186 row-counts (train = rows − known), :199-206 checked notes",
   "packages/web/src/i18n/pages/teach.ts:354,397-400 counts / checked / checked_sample / none"
  ]
 },
 {
  "id": "AZ-158",
  "persona": "Dataset uploader (visitor)",
  "title": "The free checks run out halfway: what was measured is kept and the visitor is told the rest still trains",
  "goal": "Losing 8 measured answers to a red box would be the worse outcome — a partial check is a note, not an error (design §5.4, §6.2 quota).",
  "preconditions": [
   "Dev node-u :3422, backend stub, teach.stubOffline true",
   "The hourly free live-test budget is 20 units per IP and per teaching key; this scenario deliberately exhausts it, so it runs LAST in a suite (the bucket is shared by every visitor on this IP for the rest of the hour)",
   "Fresh browser context; the dataset is deleted at the end"
  ],
  "steps": [
   "With the browser's teaching key, POST /api/teach/preflight 19 times with a single-fact body {patch_ids:[], facts:[{prompt:'AZ158 filler <i>?', answer:'x'}]} to leave one unit",
   "Upload az158-quota.jsonl with 24 ordinary question/answer lines and press 'Check (simulated on this node)'",
   "Read the note, the row statuses and the counts pill",
   "Press Check again with the bucket fully empty and read the error box"
  ],
  "expected": [
   "The first batch of 8 lands and its verdicts stay on screen; the second batch answers 429 'quota_chat: free live-test quota exhausted for this hour (this pre-flight needs 1 unit(s)) — try again later' and the loop stops",
   "The page shows the partial note (not a red box): 'Simulated check of 8 questions, then this hour’s free checks ran out. Nothing was measured in a live model; the rest still train.'",
   "The 8 measured rows keep their pill and their simulated-answer line; the other 16 stay 'Not checked yet'; the counts pill counts only what was measured as known",
   "With nothing measured at all, the red box shows 'You used this hour’s free checks. You can check again in an hour — or just train: the check only tells you what the model already knows.' and no row gets a verdict",
   "'Continue to settings' stays enabled throughout — a spent check never blocks training"
  ],
  "priority": "P2",
  "area": "teach-dataset",
  "automation": "api",
  "evidence": [
   "packages/node/src/api.ts:560-566 preflight quota units charged to ip: and key: buckets",
   "packages/web/src/pages/TeachDatasetPage.tsx:141-155 partial handling (isQuotaError && done > 0 → keep, set partial)",
   "packages/web/src/i18n/pages/teach.ts:399 checked_partial, :274 err.quota_check"
  ]
 },
 {
  "id": "AZ-159",
  "persona": "Dataset uploader (visitor)",
  "title": "Pre-flight against the live model: the verdict quotes the model's own answer, and an unreachable model says so",
  "goal": "'Already known' is only believable because the model's current answer is shown next to it — and when the model cannot be asked, no row may be given a verdict (design §5.4, §6.2).",
  "preconditions": [
   "LIVE-MODEL MODE on node-u :3422 — set config runtime.api=http://localhost:8002 (container flashnext-e2e, GPUs 4+5, patch hook on) and teach.stubOffline=false, then restart the node WITH ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e in its environment (`kill` the pid from `ss -ltnp | grep 3422`, then `ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e nohup node packages/cli/dist/bin.js --home $HOME/.ngram-teachable/node-u start &`). There is NO runtime.patchDir config key — the hook reads that env var. node-u ships runtime.api=http://localhost:8000, which the owner forbids: never :8000 or :8001, and no work on GPUs 0-3. Teardown restores runtime.api and teach.stubOffline=true and restarts.",
   "NEVER http://localhost:8000 or :8001, and no work on GPUs 0–3",
   "Restore teach.stubOffline true and runtime.api afterwards, restart, and delete the dataset"
  ],
  "steps": [
   "Confirm GET /api/teach/policy reports the node is not simulating and GET /api/runtime (or the node's status log) shows the model available with the hook up",
   "Upload az159-live.jsonl:\n{\"prompt\":\"대한민국의 수도는?\",\"answer\":\"서울\"}\n{\"prompt\":\"AZ-159 테스트 코드는?\",\"answer\":\"Z7Q4K\"}\n{\"prompt\":\"물의 화학식은?\",\"answer\":\"H2O\"}",
   "Press 'Check what the model already knows' (on the live model the button drops the '(simulated on this node)' label and the simulated banner is gone) and read every pill, its 'It answered: …' line, the checked note and the counts pill",
   "Read the POST /api/teach/preflight response body",
   "Stop the e2e model container (or point runtime.api at an unused port), press Check again and read the error box"
  ],
  "expected": [
   "Every row gets a verdict whose help line quotes a real sentence from the model — it must NOT start with '(stub model) I do not know:' (that string proves the offline stub answered)",
   "The run-unique row 'AZ-159 테스트 코드는?' is 'Will train' (the model cannot know Z7Q4K); the general-knowledge rows are 'Already known — skipped' whenever the quoted answer contains the given answer, and the pill count matches: known = number of already_known rows, train = dataset.rows − known",
   "The response is {facts:[{index, status:'will_train'|'already_known'|'overlaps_listing'|'invalid', base_answer}], trainable, sampled:{checked:3, of:3}, quota:{key_remaining, ip_remaining}} and the checked note reads 'Checked: <trainable> of 3 are wrong today and will train.' — with [data-testid=checks-simulated] absent, because on this run nothing was simulated",
   "With the model server down the page shows 'The model server is off or restarting — try again in a minute. Your corrections are kept in this browser.' (the pre-flight wording, not the lesson wording), the API answers 503 'runtime unavailable: …', and every row stays 'Not checked yet' — no fabricated verdict",
   "After restoring stubOffline true the same dataset checks again with stub answers, proving the mode switch is what changed"
  ],
  "priority": "P0",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach.ts:514-547 preflight (runtime.status → 503, askChat per fact, known = normAnswer(base).includes(normAnswer(answer)))",
   "packages/node/src/runtime.ts:162 spawn env (only ENGRAM_API forwarded), :181-198 hookAvailable/status",
   "packages/web/src/components/chat/teachUtil.ts:28 preflight stage → teach.pre.err_runtime; packages/web/src/i18n/pages/teach.ts:77,380"
  ]
 },
 {
  "id": "AZ-160",
  "persona": "Dataset uploader (visitor)",
  "title": "'Already known' from the preview to the settings promise to the result",
  "goal": "A question the model already answers is skipped by training; the number the settings screen promises and the number the result screen reports must be reconcilable on screen (design §5.5, §12.6).",
  "preconditions": [
   "Dev node-u :3422, backend stub, teach.stubOffline true, publish auto (the stub trains without a GPU; jobs_per_key_per_day is 3)",
   "Fresh browser context; the test cancels/deletes the lesson and the dataset afterwards"
  ],
  "steps": [
   "Upload az160-known.jsonl (the same three lines as AZ-152) and press 'Check (simulated on this node)'",
   "Read [data-testid=row-counts] and the row 1 verdict",
   "Press 'Continue to settings' and read [data-testid=settings-dataset], [data-testid=settings-summary] and the train button label",
   "Press 'Train this lesson (…)' and follow the lesson to a terminal state",
   "Read [data-testid=skipped-known] on /teach/lesson/<jobId> and GET /api/teach/jobs/<jobId>"
  ],
  "expected": [
   "Preview: pill '2 will train · 1 already known · 0 duplicates · 0 need a fix'; row 1 'Already known — skipped' with 'Simulated answer (no model was asked): 픽셀플러스'",
   "Settings: 'Dataset: az160-known · 3 questions · fingerprint <12 hex>'; the summary line reads '3 questions · Balanced (recommended) · side-effect check on'; the button reads 'Train this lesson (3 questions)' — the already-known question is counted in what this screen promises, and nothing on this screen repeats the '1 already known' figure from the previous step",
   "Result: [data-testid=skipped-known] reads '1 of your 3 questions were left out: the model already answered them correctly, so only the rest were taught.' and GET /api/teach/jobs/<jobId> has preflight {checked, of:3, known:1}",
   "The scenario passes only when the two numbers reconcile on screen: either the settings screen states how many will actually be taught, or the result screen accounts for the difference — the test asserts the result sentence exists and that its `known` matches the preview pill's known count",
   "A dataset where every question is already known is refused at job creation with 409 'already_known: …', which the settings screen shows as 'The model already answers this correctly, so there is nothing to train.'"
  ],
  "priority": "P0",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachSettingsPage.tsx:48 trained = min(selection ?? dataset.rows, cap); :162 train button teach.set.train",
   "packages/node/src/teach.ts:697-712 job creation drops known facts only when `facts` carry base_answer; :1100-1128 preflightJob writes {checked, of, known}",
   "packages/web/src/pages/TeachLessonPage.tsx:233-235 skipped-known; packages/web/src/i18n/pages/teach.ts:516"
  ]
 },
 {
  "id": "AZ-161",
  "persona": "Dataset uploader (visitor)",
  "title": "Taking it home: the dataset download is the fingerprinted file, and the per-line report is read through the API/CLI",
  "goal": "What the visitor downloads is byte-for-byte what the lesson trains on, and everything the node decided about their file can be got out of the node (design §6.1, §6.7, §8.7).",
  "preconditions": [
   "Dev node-u :3422, backend stub",
   "Fresh browser context with a teaching key; a CLI teaching key file exported from that browser (or the CLI's own key at ~/.ngram-teachable/cli/teaching-key.json)",
   "The dataset is deleted at the end"
  ],
  "steps": [
   "Upload az161-mix.jsonl:\n{\"prompt\":\"Who founded Ainize?\",\"answer\":\"Comcom\",\"alt_prompt\":\"Which company is behind Ainize?\"}\n{\"prompt\":\"When did Ainize start?\",\"answer\":\"2020\"}\nnot json at all",
   "Note the 'Fingerprint <12 hex>' line, press 'Download this dataset (.jsonl)' and inspect the saved file and the response headers",
   "Re-upload the downloaded file in the same browser"
  ],
  "expected": [
   "The download is a signed request (x-ngram-auth on GET /api/teach/datasets/<id>/download); the file is named dataset-<id>-r1.jsonl and its Content-Type is application/x-ndjson; charset=utf-8",
   "Its bytes are the canonical form: keys in the order prompt, answer, alt_prompt, note (absent when empty), LF endings, no BOM, exactly one trailing LF; sha256(bytes) equals the response header x-content-sha256 and begins with the 12 hex shown as 'Fingerprint …'",
   "Re-uploading that file answers HTTP 200 with created:false and the SAME dataset id (no second copy, no dataset quota spent)",
   "?format=csv returns the same questions with the header prompt,answer,alt_prompt,note and the same x-content-sha256 — the csv is a rendering, not the fingerprint subject",
   "There is no control anywhere in the web UI that saves report.json — the test asserts that the only download button on the screen is 'Download this dataset (.jsonl)' (see notes)",
   "The per-line report itself is not tested here — the API contract is AZ-216 and the CLI round-trip is AZ-211; this scenario ends at the fingerprinted bytes."
  ],
  "priority": "P1",
  "area": "teach-dataset",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/api.ts:531-538 download route (x-content-sha256, content-disposition), :501-507 rows route",
   "packages/node/src/teach-datasets.ts:246 download() jsonl/csv; :191-196 idempotent re-upload by sha256; packages/node/src/teach-dataset.ts:480 canonicalJsonl",
   "packages/web/src/lib/teachDataset.ts signedDownload; packages/cli/src/commands/teach-dataset.ts:247-280 datasetGet/renderDatasetGet"
  ]
 },
 {
  "id": "AZ-162",
  "persona": "Dataset uploader (visitor)",
  "title": "Three effort cards, no numbers: pick how hard it should try",
  "goal": "Step 3 offers exactly three choices a non-expert can judge — Quick / Balanced (recommended) / Thorough — as one radio group whose only visible difference is words, plus one honest duration line per card; the choice reaches the node as `training.effort`.",
  "preconditions": [
   "node-u on http://localhost:3422 (home $HOME/.ngram-teachable/node-u), teach enabled, publish auto, backend stub, teach.stubOffline true",
   "Fresh browser context, en-US locale (the teaching key is created by the first upload and lives in that context's localStorage)"
  ],
  "steps": [
   "Upload az162-<TAG>.jsonl at /teach/upload (drop zone / file input) with exactly these three lines (<TAG> = run-unique 5 chars, as web-teach.spec.ts does):\n{\"prompt\":\"AZ162 <TAG> 사내 와이파이 비밀번호는?\",\"answer\":\"pw-<TAG>-01\",\"alt_prompt\":\"AZ162 <TAG> 와이파이 암호 알려줘\"}\n{\"prompt\":\"AZ162 <TAG> 본사 우편번호는?\",\"answer\":\"06236\"}\n{\"prompt\":\"AZ162 <TAG> What is the support email?\",\"answer\":\"help-<TAG>@example.com\"}",
   "On /teach/dataset/<dsId> click \"Continue to settings\" ([data-testid=to-settings]) → /teach/dataset/<dsId>/settings",
   "Read [data-testid=effort-cards] and each of effort-quick / effort-balanced / effort-thorough; read [data-testid=settings-summary]",
   "Select \"Thorough\", read the summary again, then select \"Balanced (recommended)\" and press \"Train this lesson (3 questions)\"",
   "Read the created job with GET /api/teach/jobs/<jobId> (teaching key)",
   "Clean up: DELETE /api/teach/jobs/<jobId> then DELETE /api/teach/datasets/<dsId>"
  ],
  "expected": [
   "[data-testid=effort-cards] has role=radiogroup with aria-label \"How hard should it try?\" and exactly three radios named `effort`; \"Balanced (recommended)\" is checked on arrival",
   "Card texts, verbatim: \"Quick\" / \"A few passes over your questions. Good for one or two easy facts.\"; \"Balanced (recommended)\" / \"Keeps going until the model answers your questions, up to a sensible limit.\"; \"Thorough\" / \"Tries the longest. Use it for numbers, codes and facts that keep slipping.\"",
   "Every card's [data-testid=effort-time-*] shows one duration sentence — on this node all three read \"this node has not timed a lesson yet\" (see AZ-172); no card shows a step, epoch or pass count",
   "Summary line changes with the choice: \"3 questions · Balanced (recommended) · side-effect check on\" → \"3 questions · Thorough · side-effect check on\" (each followed by \" · \" and the same duration sentence)",
   "GET /api/teach/jobs/<jobId> → job.training = {effort:\"balanced\", max_steps:20, eval_every:2, lr:0.002, check_side_effects:true, use_alt:true, selected_indexes:[0,1,2]}"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/teach/EffortCards.tsx:23-40 (radiogroup, effort-time-<id>); packages/web/src/components/teach/util.ts:17-22 (EFFORTS, label/body keys)",
   "packages/web/src/pages/TeachSettingsPage.tsx:58 (default 'balanced'), :117-121, :154-164",
   "packages/web/src/i18n/pages/teach.ts:422-432, 441-445",
   "POST /api/teach/jobs (packages/node/src/api.ts:566-588 trainingSchema); packages/node/src/teach.ts:670-694",
   "Observed on node-u 2026-09-01: settings page rendered all three cards, summary flipped to \"3 questions · Thorough · side-effect check on\", job training.effort=balanced max_steps=20"
  ]
 },
 {
  "id": "AZ-163",
  "persona": "Dataset uploader (visitor)",
  "title": "No raw \"epoch\" anywhere; the trainer numbers live only in \"For developers\"",
  "goal": "The settings screen never shows a training knob in machine words: the word \"epoch\" appears nowhere, and the only numbers (steps, eval interval, learning rate) sit inside the collapsed \"For developers\" disclosure and equal what GET /api/teach/policy reports for the selected effort.",
  "preconditions": [
   "node-u :3422, teach enabled, backend stub, stubOffline true",
   "A dataset on the node (reuse AZ-162's az162-<TAG>.jsonl)"
  ],
  "steps": [
   "Open /teach/dataset/<dsId>/settings",
   "Assert the full rendered text of [data-testid=teach-settings] contains no case-insensitive \"epoch\" while [data-testid=advanced] is collapsed, and also after opening it",
   "Assert [data-testid=advanced] collapsed text is exactly \"For developers\" (the <summary>); open the <summary> and read the body",
   "Select each effort in turn and re-read the disclosure body",
   "GET /api/teach/policy and compare `effort[]`",
   "Switch the header language button (button[aria-label=\"language\"], label \"한국어\") to Korean and re-read the disclosure"
  ],
  "expected": [
   "\"epoch\" occurs nowhere on the screen in either language (grep of the page text) — the word does not exist in packages/web/src at all",
   "Collapsed, the developer section shows only the summary \"For developers\"; opened it reads: \"\\\"Balanced (recommended)\\\" means up to 20 training steps, a check every 2 steps and learning rate 2e-3. These are the values sent to the trainer.\"",
   "Quick → \"up to 8 training steps, a check every 2 steps\"; Thorough → \"up to 40 training steps, a check every 4 steps\"; the numbers equal policy.effort = [{quick,8,2},{balanced,20,2},{thorough,40,4}] — nothing is hard-coded in the bundle",
   "Korean renders the same disclosure: \"개발자용\" / \"\\\"보통 (권장)\\\"는 학습 단계 최대 20회, 2단계마다 확인, 학습률 2e-3을 뜻합니다. 학습기에 그대로 전달되는 값입니다.\"",
   "No number from the disclosure leaks into the three cards or the summary line"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachSettingsPage.tsx:142-145 (Advanced details, advanced_body vars from presetOf)",
   "packages/web/src/components/teach/util.ts:22-27 (presetOf reads policy.effort, DEFAULT_STEPS only as fallback)",
   "packages/web/src/i18n/pages/teach.ts:439-440 (en + ko)",
   "GET /api/teach/policy → effort[] (packages/node/src/teach.ts:420)",
   "Observed on node-u 2026-09-01: page text had no \"epoch\"; disclosure printed 20/2 for Balanced, 40/4 for Thorough; ko variant identical in structure"
  ]
 },
 {
  "id": "AZ-164",
  "persona": "Dataset uploader (visitor)",
  "title": "The side-effect check is locked on wherever publishing is possible",
  "goal": "On a node that can publish (publish = auto | review) the \"Check it does not break other answers\" box is checked, disabled and explained as the publish gate, and the job is created with check_side_effects: true whatever the visitor clicks.",
  "preconditions": [
   "node-u :3422 with publish = auto (its normal setting), backend stub, stubOffline true",
   "A dataset on the node (az164-<TAG>.jsonl, 3 lines, same shape as AZ-162)"
  ],
  "steps": [
   "Open /teach/dataset/<dsId>/settings",
   "Read [data-testid=check-side]: checked state, disabled state, its label and the hint paragraph under it, and the helper line",
   "Try to click the checkbox and re-read its state",
   "Read [data-testid=settings-summary]",
   "Press \"Train this lesson (3 questions)\" and read the job over GET /api/teach/jobs/<jobId>",
   "Clean up the job and the dataset"
  ],
  "expected": [
   "The checkbox is checked AND disabled; label \"Check it does not break other answers\"",
   "Hint, verbatim: \"Before and after training, the model is asked a fixed set of unrelated questions, plus the questions the knowledge you loaded answers. Anything that changed is shown on the result screen.\"",
   "Helper under it, verbatim: \"Required if you want to publish this lesson.\" (rendered only while the node can publish)",
   "Clicking it changes nothing; the summary keeps \"· side-effect check on\"",
   "The created job has training.check_side_effects = true, and the result screen later shows the Side effects panel with a measured locality line (on the stub: \"Unrelated questions unchanged: 12/12\")"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachSettingsPage.tsx:63-65 (publishable → sideLocked → sideOn), :123-131",
   "packages/web/src/i18n/pages/teach.ts:433-435, 443",
   "packages/node/src/teach.ts:692 (check_side_effects defaults true), :1519-1526 (skipped path)",
   "Observed on node-u 2026-09-01: check-side isChecked=true isDisabled=true, helper present, job training.check_side_effects=true"
  ]
 },
 {
  "id": "AZ-165",
  "persona": "Dataset uploader (visitor)",
  "title": "Where publishing is off the visitor may switch the check off — and the result says so and offers to run it",
  "goal": "When the node cannot publish (publish = never) the side-effect box becomes editable; switching it off sends check_side_effects: false, and the result screen must admit nothing was measured and offer \"Run the check now\" instead of printing a locality score.",
  "preconditions": [
   "LIVE-MODEL MODE on node-u :3422 — set config runtime.api=http://localhost:8002 (container flashnext-e2e, GPUs 4+5, patch hook on) and teach.stubOffline=false, then restart the node WITH ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e in its environment (`kill` the pid from `ss -ltnp | grep 3422`, then `ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e nohup node packages/cli/dist/bin.js --home $HOME/.ngram-teachable/node-u start &`). There is NO runtime.patchDir config key — the hook reads that env var. node-u ships runtime.api=http://localhost:8000, which the owner forbids: never :8000 or :8001, and no work on GPUs 0-3. Teardown restores runtime.api and teach.stubOffline=true and restarts.",
   "Operator sets publish=never for the run: PATCH /api/me/teach/policy {\"publish\":\"never\"} — the test MUST restore {\"publish\":\"auto\"} in its teardown",
   "Dataset az165-<TAG>.jsonl:\n{\"prompt\":\"AZ165 <TAG> 사내 헬프데스크 내선번호는?\",\"answer\":\"4180\"}\n{\"prompt\":\"AZ165 <TAG> Which room is the design review in?\",\"answer\":\"Room <TAG>-2\"}\n{\"prompt\":\"AZ165 <TAG> 백업 서버 이름은?\",\"answer\":\"bak-<TAG>\"}"
  ],
  "steps": [
   "Wait out the node's 10 s policy cache, reload /teach/dataset/<dsId>/settings",
   "Read [data-testid=check-side] state and check whether the helper \"Required if you want to publish this lesson.\" is gone",
   "Uncheck it, read [data-testid=settings-summary], press Train",
   "Wait for the lesson to leave the active statuses on /teach/lesson/<jobId>; read [data-testid=side-effects] and the state of [data-testid=go-publish]",
   "GET /api/teach/jobs/<jobId> and read training.check_side_effects and checks",
   "Press \"Run the check now\" ([data-testid=run-check-now]), watch POST /api/teach/jobs/<jobId>/recheck and re-read the panel and the publish button",
   "Clean up the job + dataset and restore publish=auto"
  ],
  "expected": [
   "With publish=never the checkbox is checked but NOT disabled, and the \"Required if you want to publish this lesson.\" helper is absent",
   "After unchecking, the summary reads \"3 questions · Balanced (recommended) · side-effect check off\" and the POST body carries training.check_side_effects=false",
   "GET job → training.check_side_effects=false and checks.skipped=true, checks.locality={ok:false,same:0,total:<n prompts>}, checks.note=\"the side-effect check was turned off for this lesson — nothing was measured about unrelated answers\"",
   "The Side effects panel says exactly \"You switched the side-effect check off, so this lesson has not been measured and cannot be published yet.\" and shows the button \"Run the check now\" — it must NOT print an \"Unrelated questions unchanged: m/n\" score",
   "\"Publish so others can use it\" stays disabled; after \"Run the check now\" succeeds the panel shows the measured locality line and checks.skipped is gone",
   "This scenario owns the whole check-off path end to end (settings box → job flag → checks.skipped → the panel sentence → recheck). It has to run in live-model mode because the OFFLINE STUB IGNORES the flag: packages/node/src/teach.ts:1396-1409 returns executed:true with locality 12/12 whatever check_side_effects says, so on a stub node the screen would claim a measurement the visitor switched off — a defect this scenario also pins."
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachSettingsPage.tsx:63-66, :123-131",
   "packages/web/src/pages/TeachLessonPage.tsx:277-295 (side_off / run-check-now), :312 (publish gate: gated = !c.ok || !c.executed || c.skipped)",
   "packages/node/src/teach.ts:1396 (sideEffects), :1519-1526 (checks.skipped, live path only), :1643 (publish refused with checks_failed)",
   "packages/web/src/i18n/pages/teach.ts:496-497",
   "DEFECT observed on node-u 2026-09-01 (stub + stubOffline true, publish=never): the box was editable and the POST carried check_side_effects=false, but the offline stub branch (packages/node/src/teach.ts:1397-1409) ignores the flag and returned executed:true, locality 12/12 — the screen claimed a measurement the visitor had switched off"
  ]
 },
 {
  "id": "AZ-166",
  "persona": "Dataset uploader (visitor)",
  "title": "\"Test with a different wording\" counts the rows that have one, and off means off",
  "goal": "The alt-wording toggle reflects the dataset (count of rows with an \"Another way to ask\" value, disabled when none), is held out of training when on, and reaches the node as `use_alt`.",
  "preconditions": [
   "node-u :3422, backend stub, stubOffline true",
   "Two datasets: az167a-<TAG>.jsonl with alt_prompt on exactly one of three rows (AZ-162's file works), and az167b-<TAG>.jsonl with no alt_prompt at all:\n{\"prompt\":\"AZ166 <TAG> 사무실 도어락 번호는?\",\"answer\":\"<TAG>#41\"}\n{\"prompt\":\"AZ166 <TAG> Who signs expense reports?\",\"answer\":\"Team lead <TAG>\"}"
  ],
  "steps": [
   "Open the settings page for az167a: read [data-testid=check-alt] state and the hint under it",
   "Uncheck it, press Train, and read the job over the API",
   "Open the settings page for az167b: read [data-testid=check-alt] state and the hint",
   "Clean up both jobs and datasets"
  ],
  "expected": [
   "az167a: the box is checked and enabled; hint reads \"The \\\"Another way to ask\\\" column is kept out of training and used only to check the model learned the fact, not the sentence. 1 of your questions have one.\" (the count is the number of picked rows with alt_prompt, capped at the per-lesson question cap)",
   "With it unchecked the job has training.use_alt=false and job.facts[].alt_prompt is absent for every question; with it checked the row that had one keeps alt_prompt and the result screen's \"Other wording\" column shows ✓/— for it",
   "az167b: the box is disabled and the hint reads \"None of your questions have another wording yet. Add one on the previous step to switch this on.\"; the job is created with training.use_alt=false",
   "Turning it off never removes the alt wording from the DATASET — GET /api/teach/datasets/<dsId>/rows still returns alt_prompt on that row"
  ],
  "priority": "P2",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachSettingsPage.tsx:51-54 (altCount over picked rows), :66, :133-140",
   "packages/node/src/teach.ts:686-694 (use_alt), :704-711 (alt_prompt copied into facts only when useAlt)",
   "packages/web/src/i18n/pages/teach.ts:436-438",
   "packages/web/src/pages/TeachLessonPage.tsx:248-258 (\"Other wording\" column)",
   "Observed on node-u 2026-09-01: with 1 alt row the hint said \"1 of your questions have one.\"; a dataset with none rendered the disabled box and the alt_none hint"
  ]
 },
 {
  "id": "AZ-167",
  "persona": "Dataset uploader (visitor)",
  "title": "Lesson name, dataset fingerprint and the per-lesson question cap on the settings screen",
  "goal": "Step 3 restates what is about to be trained — name, question count, dataset fingerprint — and, over the node's cap, says honestly how many of the file's questions are in THIS lesson and why the cap is conservative; a hand-picked selection drives the same numbers.",
  "preconditions": [
   "node-u :3422 (limits.rows_per_job = 200, rows_per_job_source = \"default\"; limits.dataset_max_rows = 2000), backend stub",
   "A 250-question dataset az167-<TAG>.jsonl, generated: line i (1..250) = {\"prompt\":\"AZ167 <TAG> handbook rule <i>?\",\"answer\":\"rule-<TAG>-<i>\"}"
  ],
  "steps": [
   "Upload it, land on /teach/dataset/<dsId>, then Continue to settings",
   "Read [data-testid=settings-dataset], [data-testid=rows-cap], [data-testid=settings-summary] and the Train button label",
   "Type \"AZ167 handbook <TAG>\" into [data-testid=lesson-name]",
   "Go back to /teach/dataset/<dsId>, open the cap banner's \"Pick the 200 to teach\" ([data-testid=cap-pick]), tick 5 rows, Continue to settings and re-read the same four places",
   "Press Train and read GET /api/teach/jobs/<jobId>",
   "Clean up job + dataset"
  ],
  "expected": [
   "[data-testid=settings-dataset] reads \"Dataset: az167-<TAG> · 250 questions · fingerprint <first 12 hex of sha256>\" and that fingerprint equals GET /api/teach/datasets/<dsId> dataset.sha256.slice(0,12)",
   "[data-testid=rows-cap] reads \"This node teaches up to 200 questions in one lesson, so 200 of your 250 are in this one.\" followed by \"This node has not timed a real training run yet, so the limit is set conservatively.\" (the second sentence only while limits.rows_per_job_source === \"default\")",
   "Summary reads \"200 questions · …\" and the button reads \"Train this lesson (200 questions)\"",
   "After picking 5 rows the summary and button say 5 questions, and the POST body carries selected_indexes with exactly those 5 zero-based dataset indexes",
   "The job has name \"AZ167 handbook <TAG>\", dataset.rows = 250, dataset.trained_rows = the trained count, and training.selected_indexes matching the pick; the untrained rows stay in the dataset (GET rows still returns 250)"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachSettingsPage.tsx:46-54 (cap, selection, trained), :99-108, :110-115, :155-163",
   "packages/web/src/components/teach/util.ts:30-32 (rowsPerJob from policy, never hard-coded)",
   "packages/web/src/pages/TeachDatasetPage.tsx:216-222 (cap banner + cap-pick), :166-169 (saveSelection → settings)",
   "packages/node/src/teach.ts:670-684 (cap applied, the rest kept for the next lesson), :103-104 (name)",
   "GET /api/teach/policy on node-u 2026-09-01: rows_per_job=200, rows_per_job_source=\"default\""
  ]
 },
 {
  "id": "AZ-168",
  "persona": "Dataset uploader (visitor)",
  "title": "Press Train: one POST, 202, and the progress screen owns the lesson",
  "goal": "The Train button is the only thing that starts training: it sends exactly one POST /api/teach/jobs with the settings shown, shows \"Sending…\" while in flight, and hands over to /teach/lesson/<jobId>, which is findable again from \"My datasets and lessons\".",
  "preconditions": [
   "node-u :3422, backend stub, stubOffline true, trainer ready, daily quota left for the key and the IP",
   "Dataset az168-<TAG>.jsonl (3 lines, AZ-162 shape)"
  ],
  "steps": [
   "Open /teach/dataset/<dsId>/settings and record the network traffic",
   "Press \"Train this lesson (3 questions)\" ([data-testid=train-lesson])",
   "Watch the button while the request is in flight; then follow the navigation",
   "Open /teach/mine in the same context",
   "Clean up job + dataset"
  ],
  "expected": [
   "Exactly one POST /api/teach/jobs is sent, signed with the browser's teaching key (x-ngram-auth), body {patch_ids:[], builds_on_context:false, dataset_id:\"<dsId>\", training:{effort:\"balanced\", check_side_effects:true, use_alt:true}} (plus `name` when the field was filled and `selected_indexes` when rows were picked)",
   "The node answers 202 with {job:{id,status:\"QUEUED\",…}, quota:{key_remaining,ip_remaining,rows_remaining,rows_ip_remaining}}",
   "While in flight the button reads \"Sending…\" and cannot be pressed twice (a double click must not create a second lesson — assert only one job with that dataset_id exists afterwards)",
   "The browser navigates to /teach/lesson/<job.id> and [data-testid=teach-lesson] carries data-status=QUEUED with the step rail at \"4 · Training\"",
   "/teach/mine lists the dataset card az168-<TAG> with \"Lessons from this dataset (1)\" and a link \"Open\" to that lesson"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachSettingsPage.tsx:70-82 (create → rememberJob → navigate), :161-163 (loadingText)",
   "packages/node/src/api.ts:572-588 (POST /api/teach/jobs, res.status(202))",
   "packages/web/src/pages/TeachMinePage.tsx:80-88; packages/web/src/components/teach/DatasetCard.tsx:186-202",
   "Observed on node-u 2026-09-01: POST returned 202, browser landed on /teach/lesson/<id> in QUEUED, the mine page listed the dataset with its lesson"
  ]
 },
 {
  "id": "AZ-169",
  "persona": "Dataset uploader (visitor)",
  "title": "Train refused (daily limit): a plain sentence, and nothing on the screen is lost",
  "goal": "When the node refuses the lesson the visitor gets one plain sentence in the visitor's language, stays on the settings screen with the name/effort/toggles exactly as they were, and no job or draft is created.",
  "preconditions": [
   "node-u :3422; the run must exhaust a quota. Either use up limits.jobs_per_key_per_day (3) with the same teaching key, or have the operator set it to 0 for the run: PATCH /api/me/teach/policy {\"jobs_per_key_per_day\":0} — teardown MUST restore 3 (node-u defaults: jobs_per_key_per_day 3, jobs_per_ip_per_day 5)",
   "Dataset az169-<TAG>.jsonl (2 lines)"
  ],
  "steps": [
   "Open /teach/dataset/<dsId>/settings, type a lesson name, choose Thorough",
   "Press Train and capture the POST response",
   "Read [data-testid=settings-error], then re-read the name field, the selected effort card and [data-testid=settings-summary]",
   "GET /api/teach/jobs (teaching key) to confirm nothing was queued",
   "Restore the quota and delete the dataset"
  ],
  "expected": [
   "POST /api/teach/jobs answers 429 with error \"quota_key: daily lesson limit reached for this key\" (or \"quota_ip: daily lesson limit reached for this address\" with ip_remaining:0)",
   "[data-testid=settings-error] shows exactly \"You have reached today's lesson limit here. Come back tomorrow or run your own node.\" with role=alert; the raw code never appears on screen",
   "The URL is still /teach/dataset/<dsId>/settings; the typed name, the Thorough selection and the summary line are unchanged; the Train button is pressable again",
   "GET /api/teach/jobs contains no job for <dsId>; the dataset status is unchanged (not `in_use`)",
   "The same refusal in Korean reads \"오늘 이 노드의 수업 한도에 도달했습니다. 내일 다시 오거나 직접 노드를 운영하세요.\""
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachSettingsPage.tsx:81 (setError(mapTeachError)), :152",
   "packages/web/src/components/chat/teachUtil.ts:33 (quota_key/quota_ip → teach.err.quota)",
   "packages/node/src/teach.ts:715-721 (quota checks before the insert)",
   "packages/web/src/i18n/pages/teach.ts:273",
   "Observed on node-u 2026-09-01: POST returned 429 {\"error\":\"quota_ip: daily lesson limit reached for this address\",\"ip_remaining\":0} and the page showed the English sentence above"
  ]
 },
 {
  "id": "AZ-170",
  "persona": "Dataset uploader (visitor)",
  "title": "The progress screen names the stage it is in — and a demo node says \"Starting…\"",
  "goal": "Step 4 shows the six-stop stage rail (Waiting → Preparing → Warming up → Teaching → Double-checking → Done) with exactly one current stop that follows the job status, and a stub-backend node replaces \"Warming up the model\" with \"Starting…\" because nothing is warmed up.",
  "preconditions": [
   "node-u :3422, backend stub, stubOffline true (policy.backend = \"stub\")",
   "Dataset az170-<TAG>.jsonl (4 lines, AZ-162 shape)"
  ],
  "steps": [
   "Press Train and poll /teach/lesson/<jobId> every ~1 s until the status leaves the active set",
   "At each poll record [data-testid=teach-lesson]@data-status, [data-testid=stage-rail]@data-stage and the rail's six labels",
   "Also record the Stepper: [data-testid=teach-stepper] current item while running, and after it finishes",
   "Clean up job + dataset"
  ],
  "expected": [
   "The rail labels are, in order: \"Waiting for a free training slot\", \"Preparing your dataset\", \"Starting…\", \"Teaching\", \"Double-checking in the live model\", \"Done\" — the third is \"Warming up the model\" only when policy.backend !== \"stub\"",
   "data-stage follows the status map: QUEUED→queued, PREFLIGHT→prep, LOADING→warm, TRAINING→train, EXPORTED|CHECKING→check, anything else→done; exactly one <li> carries aria-current=\"step\" and the earlier ones are marked done",
   "The Stepper reads \"Step 4 of 5 · Training\" while the lesson runs and \"Step 5 of 5 · Result\" once it stops",
   "Below 600 px the Stepper collapses to the single line [data-testid=teach-step-small] plus a five-segment bar, and the page never scrolls sideways"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/teach/StageRail.tsx:9-39 (KEYS, stub → teach.run.stage.start)",
   "packages/web/src/components/teach/util.ts:103-115 (stageOf)",
   "packages/web/src/components/teach/Stepper.tsx:49, 78-92",
   "packages/web/src/i18n/pages/teach.ts:456-463",
   "Observed on node-u 2026-09-01: rail printed \"Waiting for a free training slot / Preparing your dataset / Starting… / Teaching / Double-checking in the live model / Done\", data-stage went queued → train → (result)"
  ]
 },
 {
  "id": "AZ-171",
  "persona": "Dataset uploader (visitor)",
  "title": "A real step bar and real counters — no invented percentage",
  "goal": "While training, the visible bar is the trainer's own `step / max_steps` (never a computed percent), the counters come from the same progress object the API returns, and \"{n} questions in this lesson\" equals the questions actually being trained.",
  "preconditions": [
   "node-u :3422, backend stub, stubOffline true",
   "Dataset az171-<TAG>.jsonl with exactly 3 lines (AZ-162 shape)"
  ],
  "steps": [
   "Press Train; while data-status is TRAINING read [data-testid=train-bar] (aria-valuenow / aria-valuemax / aria-valuemin), [data-testid=step-line], [data-testid=hits-line] and the \"questions in this lesson\" line",
   "At the same moment GET /api/teach/jobs/<jobId> and compare with job.progress",
   "Assert no element on the progress screen renders a per-cent sign or the value of job.progress.percent",
   "Clean up job + dataset"
  ],
  "expected": [
   "The bar has role=progressbar with aria-valuemin=0, aria-valuemax = job.progress.max_steps and aria-valuenow = job.progress.step; its width is round(step/max_steps*100)% and it is shown only in the `train` stage with max_steps > 0",
   "[data-testid=step-line] reads \"Step <step> of up to <max>\" with the same two numbers (on the stub: \"Step 3 of up to 3\", because the stub trainer emits max_steps 3 whatever the effort — the effort's 8/20/40 is what the real trainer receives)",
   "No \"%\" and no rendering of job.progress.percent anywhere on the screen (percent exists in the API for §D5 but must stay out of the visitor's view)",
   "The last line reads \"3 questions in this lesson\" and equals job.facts.length",
   "[data-testid=hits-line] must count QUESTIONS, matching its own sentence \"<hits> of <total> questions answered correctly so far\" — DEFECT to fix first: on node-u a 3-question lesson renders \"6 of 6 questions answered correctly so far\" because the stub seeds progress.total = facts.length * 2 (model probes, not questions); either the stub must emit question counts or the counter must read the index-aligned facts the way the result screen does"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:140-167 (frac from step/max_steps, train-bar, step-line, hits-line, rows line)",
   "packages/node/src/teach.ts:1207-1216 (bumpPercent — the additional percent is API-only), :1224-1233 (step event)",
   "packages/node/src/teach.ts:1323 (stub: progress.total = job.facts.length * 2), :1331 (stub max_steps 3)",
   "packages/web/src/api/types.ts:141-145 (phase, percent, rows_total, rows_touched)",
   "Observed on node-u 2026-09-01: TRAINING screen showed \"Step 3 of up to 3\" + \"6 of 6 questions answered correctly so far\" for a 3-question lesson; API progress {step:3,max_steps:3,hits:8,total:8,rows_total:4,percent:85} for a 4-question lesson"
  ]
 },
 {
  "id": "AZ-172",
  "persona": "Dataset uploader (visitor)",
  "title": "Elapsed always, minutes only after three measured gradient lessons",
  "goal": "The progress screen always shows a truthful elapsed clock, and it never invents a \"time left\": a minute figure appears only when this node measured at least three lessons on the gradient backend AND the node sent an eta for this job; otherwise the visitor is told, in words, that the node cannot know yet.",
  "preconditions": [
   "node-u :3422, backend stub, stubOffline true → GET /api/teach/policy timing = {p50_s:null,p90_s:null,samples:0,backend:\"gradient\",simulated:true,load_s_p50:null,s_per_row_p50:null}",
   "For the branch coverage: a unit test beside packages/web/test/teacherKey.test.ts (run with `npm test -w packages/web`), since no node in this environment has three gradient samples"
  ],
  "steps": [
   "e2e: press Train on az172-<TAG>.jsonl (3 lines) and read [data-testid=elapsed] and [data-testid=eta] in QUEUED and again in TRAINING; read the three [data-testid=effort-time-*] lines on the settings screen beforehand",
   "e2e: watch [data-testid=elapsed] over 3 s and confirm it advances (it re-renders once a second while the job is active)",
   "unit (new packages/web/test/teachTime.test.ts): exercise minutesFor()/effortTime()/etaLine() from packages/web/src/components/teach/util.ts with hand-built policies",
   "Clean up job + dataset"
  ],
  "expected": [
   "Elapsed renders as mm:ss (h:mm:ss past an hour) from job.started_at, starting at \"Elapsed 00:00\", and ticks while the job is active",
   "[data-testid=eta] on node-u reads exactly \"No time estimate yet — this node has not finished enough lessons to know. The first one may take up to 30 minutes.\" in QUEUED and in TRAINING — no minutes anywhere on the screen",
   "Every effort card reads \"this node has not timed a lesson yet\" while policy.timing.simulated is true or samples < 3",
   "unit minutesFor: null when timing.simulated is true; null when samples < 3; null when s_per_row_p50 is null; with {simulated:false, samples:3, load_s_p50:30, s_per_row_p50:0.5} and balanced (20 passes) over 4 questions → 1 → effortTime renders \"about 1 min for 4 questions on this node\"",
   "unit etaLine: with a measured policy and job.eta_s=45 → \"less than a minute left\"; eta_s=600 → \"about 10 min left\"; eta_s null/0 → the eta_none sentence; unmeasured policy + eta_s=600 → the eta_none sentence (the node's number is discarded, never shown)"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/teach/util.ts:36-51 (minutesFor, effortTime), :117-135 (elapsedText, etaLine)",
   "packages/web/src/pages/TeachLessonPage.tsx:93-94 (1 s tick while active), :123 (elapsed from started_at), :163-167",
   "packages/node/src/teach.ts:390-397, 415-419 (timing from gradient samples only, simulated on a stub), :798-810 (eta_s only with ETA_MIN_SAMPLES gradient samples; null while blocked on the slot)",
   "packages/core/src/config.ts:120 (ETA_MIN_SAMPLES = 3)",
   "packages/web/src/i18n/pages/teach.ts:432, 466-469",
   "Observed on node-u 2026-09-01: eta line exactly as quoted in QUEUED and TRAINING; all three effort cards said \"this node has not timed a lesson yet\""
  ]
 },
 {
  "id": "AZ-173",
  "persona": "Dataset uploader (visitor)",
  "title": "\"This lesson's log\" — the trainer's own lines, with nothing private in them",
  "goal": "The progress screen carries a collapsed log that shows this lesson's real events (queued, started, per-step loss/hits, exported, final verdict) with local timestamps, and leaks no draft id, key or question text.",
  "preconditions": [
   "node-u :3422, backend stub, stubOffline true",
   "Dataset az173-<TAG>.jsonl (4 lines)"
  ],
  "steps": [
   "Press Train; open the <details> \"This lesson's log\" ([data-testid=event-log]) while the lesson runs and again after it finishes",
   "GET /api/teach/jobs/<jobId>/events with the teaching key and compare line for line",
   "Assert the log text does not contain the job's draft_id, the teaching key/address, or the answers from the dataset",
   "Clean up job + dataset"
  ],
  "expected": [
   "Before any event exists the log body reads \"Nothing logged yet.\"; otherwise each line is \"<local time>  <message>\" in seq order",
   "The messages match GET …/events, e.g. \"lesson queued (4 of 4 question(s), context -)\", \"training started (stub) for <jobId>\", \"step 1/3 loss 1 hits 1/4\", \"step 3/3 loss 0.33 hits 4/4\" (the step lines count QUESTIONS after the AZ-171 fix; the closing line keeps the check’s own probe count), \"exported 4 memory entries (0.01 MB, sha 78b4e120bfcb…)\", \"READY: taught 8/8, locality 12/12, parents 0/0\"",
   "The events response is the redacted public shape (seq, ts, level, message, data) — the owner is not the operator, so draft ids/keys/prompts stay out of both the API and the rendered log",
   "The log keeps polling every 5 s while the job is active and stops when it is not"
  ],
  "priority": "P2",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:95 (events poll), :180-183",
   "packages/node/src/api.ts:611-621 (GET /api/teach/jobs/:id/events, publicEvents redaction)",
   "packages/node/src/teach.ts:746 (queued message keeps the name and key out), :1232 (step line), :1073 (final verdict line)",
   "Observed on node-u 2026-09-01 for job e70531e1: the six messages quoted above, in that order"
  ]
 },
 {
  "id": "AZ-174",
  "persona": "Dataset uploader (visitor)",
  "title": "Cancel training: confirm, stop, and the dataset survives",
  "goal": "\"Cancel training\" asks once, can be backed out of, and when confirmed leaves the lesson CANCELLED with an honest one-word result screen — while the dataset it was trained from is kept and immediately trainable again.",
  "preconditions": [
   "node-u :3422, backend stub, stubOffline true (a stub lesson finishes in ~4 s, so the target lesson is queued BEHIND a first one to keep it cancellable: train az175a-<TAG>.jsonl first, then az175b-<TAG>.jsonl)",
   "Two datasets of 4 lines each, AZ-162 shape"
  ],
  "steps": [
   "Train az175a, then immediately train az175b and stay on /teach/lesson/<jobB>",
   "Press \"Cancel training\" ([data-testid=cancel-training]); read [data-testid=cancel-confirm]; press \"Keep training\" and confirm the confirm bar closes and the lesson keeps running",
   "Press \"Cancel training\" again and press \"Stop it\" ([data-testid=cancel-yes])",
   "Reload the page and read data-status and the result screen; GET /api/teach/jobs/<jobB>",
   "Open /teach/mine and check the az175b dataset card; then press its \"Train again\"",
   "Clean up both jobs and both datasets"
  ],
  "expected": [
   "The confirm bar reads \"Stop teaching this lesson? Your dataset is kept, so you can train it again.\" with the two buttons \"Stop it\" and \"Keep training\"",
   "\"Keep training\" only closes the bar — no DELETE is sent and the lesson keeps its status",
   "\"Stop it\" sends DELETE /api/teach/jobs/<jobB> → {ok:true,status:\"CANCELLED\"}; the page becomes the result screen with data-status=CANCELLED, the title \"Your lesson: <name>\" and [data-testid=result-failed] reading exactly \"Cancelled.\"",
   "No learned / not-learned / side-effects / Try-it-here block is rendered for a cancelled lesson; \"Publish so others can use it\" and \"Keep it private\" are disabled and \"Change settings and re-train\" stays enabled",
   "The dataset is untouched: GET /api/teach/datasets/<dsB> still returns it with its row count and status back to \"ready\", /teach/mine shows the card with the lesson pill \"Cancelled\", and \"Train again\" opens /teach/dataset/<dsB>/settings ready to run",
   "No draft is left behind: the cancelled job has no draft_id and its job directory is gone"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:127-130 (doCancel), :171-179",
   "packages/node/src/api.ts:596 (DELETE /api/teach/jobs/:id); packages/node/src/teach.ts:848-869 (cancel: files cleaned, draft deleted, dataset markStatus('ready'), \"the dataset is deliberately NOT deleted\")",
   "packages/web/src/i18n/pages/teach.ts:115, 472-475",
   "packages/web/src/pages/TeachLessonPage.tsx:197, 208-211, 242-243 (failedTone suppresses the result blocks), :312, :319",
   "Observed on node-u 2026-09-01: cancelling a queued lesson gave data-status=CANCELLED, the screen showed \"Your lesson: az-c2-<TAG>\" + \"Cancelled.\", and /teach/mine still listed the dataset with \"Train again\""
  ]
 },
 {
  "id": "AZ-175",
  "persona": "Dataset uploader (visitor)",
  "title": "Close the tab while it trains — the lesson keeps its place and is findable again",
  "goal": "The promise \"You can close this tab\" / \"Your place in the queue is kept even if you close this tab\" is true: the lesson keeps running (or keeps its queue position) without an open page, and the visitor finds it again from My datasets and lessons in the same browser.",
  "preconditions": [
   "node-u :3422, backend stub, stubOffline true",
   "Dataset az175-<TAG>.jsonl (6 lines) — enough that the lesson is still active for a moment after the tab closes"
  ],
  "steps": [
   "Press Train; on the progress screen read the two sentences under the counters",
   "Close the page (not the browser context — the teaching key lives in its localStorage) while data-status is QUEUED or TRAINING",
   "Open a new page in the same context on /teach/mine",
   "Open the lesson from the card link and confirm it shows the same job (progress or result)",
   "Clean up job + dataset"
  ],
  "expected": [
   "The progress screen carries \"Training runs on spare hardware here, so it can pause and pick up again.\" (hidden on a stub node) and \"You can close this tab. Find the lesson again under My datasets and lessons.\"",
   "With no page open the job keeps advancing: polling GET /api/teach/jobs/<jobId> from the API shows it reaching a terminal status on its own",
   "/teach/mine shows the az175-<TAG> dataset card with \"Lessons from this dataset (1)\" and the status pill from the same vocabulary as the card (Queued / Training / Ready / Needs more…), plus \"<hits> of <total> learned\" once measured",
   "Following \"Open\" lands on /teach/lesson/<jobId> and shows the same lesson; while it is active the page polls every 3 s and after it finishes every 30 s"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:93 (poll 3 s active / 30 s idle), :168-169",
   "packages/web/src/i18n/pages/teach.ts:447, 470, 476",
   "packages/web/src/pages/TeachMinePage.tsx:43-53 (lessons grouped by dataset), packages/web/src/components/teach/DatasetCard.tsx:186-202",
   "packages/web/src/lib/teachStore.ts rememberJob (called at packages/web/src/pages/TeachSettingsPage.tsx:79)",
   "Observed on node-u 2026-09-01: /teach/mine listed \"az-c2-<TAG> … Lessons from this dataset (1) … Cancelled … Open\" after the lesson page was closed"
  ]
 },
 {
  "id": "AZ-176",
  "persona": "Dataset uploader (visitor)",
  "title": "FAILED, honestly: \"there was nothing to teach\"",
  "goal": "A lesson that cannot be taught ends as a plain sentence, not a stack trace: the result screen shows the mapped explanation, hides every block that would describe a lesson that does not exist, and still offers to train again.",
  "preconditions": [
   "node-u :3422, backend stub with teach.stubOffline true — the offline stub \"knows\" any question whose prompt already contains its answer, so the worker pre-flight drops every row and the job fails with `already_known`",
   "Dataset az176-<TAG>.jsonl:\n{\"prompt\":\"종목코드 087600은 픽셀플러스인가요? <TAG>\",\"answer\":\"픽셀플러스\"}\n{\"prompt\":\"Is the answer yes-<TAG>? <TAG>\",\"answer\":\"yes-<TAG>\"}"
  ],
  "steps": [
   "Press Train and wait for the status to leave the active set",
   "Read data-status, the page title, [data-testid=result-failed] and which panels exist",
   "Read the state of go-publish / go-keep / go-retrain",
   "GET /api/teach/jobs/<jobId> and read job.error",
   "Clean up job + dataset"
  ],
  "expected": [
   "data-status=FAILED; the title is \"Your lesson: az176-<TAG>\" (the lesson name, not \"Your lesson is ready\")",
   "[data-testid=result-failed] reads exactly \"The model already answered this correctly, so there was nothing to teach.\" — the raw error \"already_known: the model already answers all of this correctly\" never reaches the screen",
   "No learned-block, missed-block, side-effects panel or Try-it-here box is rendered; the dataset link and the \"Download the dataset this lesson was trained on\" button remain",
   "\"Publish so others can use it\" and \"Keep it private\" are disabled; \"Change settings and re-train\" is enabled",
   "The other three mapped sentences exist for the other error families and must be used where they apply (node restart → \"This node restarted while teaching. Nothing was charged. Please try again.\"; GPU OOM → \"This node ran out of training GPU memory. …\"; anything else → \"Something went wrong while teaching. Nothing was charged. Try again in a moment.\")"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach.ts:1032 (no facts left → FAILED already_known), :1100-1101 (offline pre-flight)",
   "packages/web/src/components/chat/teachUtil.ts:69-75 (failedKey mapping)",
   "packages/web/src/pages/TeachLessonPage.tsx:197, 206-211, 242-243, 312, 319",
   "packages/web/src/i18n/pages/teach.ts:110-113",
   "Observed on node-u 2026-09-01: job 44eeef36 ended FAILED and the screen read \"Your lesson: az-known-<TAG>\" + \"The model already answered this correctly, so there was nothing to teach.\" with only the dataset line and the three next-step cards"
  ]
 },
 {
  "id": "AZ-177",
  "persona": "Dataset uploader (visitor)",
  "title": "NEEDS_MORE: the misses are listed and publishing stays shut",
  "goal": "When fewer than 75 % of the trained questions answer right, the node says NEEDS_MORE and the result screen says so in the title, counts the misses from the index-aligned questions (never from probe counts), lists what did not stick with what the model said instead, and blocks publishing while still allowing keep / re-train.",
  "preconditions": [
   "LIVE-MODEL MODE on node-u :3422 — set config runtime.api=http://localhost:8002 (container flashnext-e2e, GPUs 4+5, patch hook on) and teach.stubOffline=false, then restart the node WITH ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e in its environment (`kill` the pid from `ss -ltnp | grep 3422`, then `ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e nohup node packages/cli/dist/bin.js --home $HOME/.ngram-teachable/node-u start &`). There is NO runtime.patchDir config key — the hook reads that env var. node-u ships runtime.api=http://localhost:8000, which the owner forbids: never :8000 or :8001, and no work on GPUs 0-3. Teardown restores runtime.api and teach.stubOffline=true and restarts.",
   "Dataset az177-<TAG>.jsonl (3 lines, run-unique wording, AZ-162 shape)"
  ],
  "steps": [
   "Press Train and wait for a terminal status",
   "Read data-status, the title, [data-testid=result-learned], [data-testid=missed-block] and [data-testid=simulated]",
   "Read the state of go-publish / go-keep / go-retrain",
   "GET /api/teach/jobs/<jobId> and compare facts[].hit with what the screen counted",
   "Clean up job + dataset"
  ],
  "expected": [
   "data-status=NEEDS_MORE; on this node (stub trainer) the title reads \"Demo run finished — nothing was trained\" — a node that really trains shows \"Your lesson needs a bit more\" for the same status",
   "[data-testid=result-learned] reads \"It learned <hits> of <total> questions.\" where <total> = job.facts.length and <hits> = the number of facts with hit === true — NOT checks.taught (which counts model probes, two per question)",
   "The missed panel is headed \"What it did not learn\" with the hint \"The ones it missed are listed below. Add another wording for them and train again — your dataset is saved.\" and a two-column table (Question / After) showing what the model answered instead",
   "On this node the demo banner reads \"Demo node — no real training happened. The answers below were measured in the live model, but the knowledge file itself is a placeholder.\"",
   "Publishing is disabled (status is not READY): on a demo node it is the [data-testid=publish-demo] \"Publish anyway (demo)\" link and it is disabled; \"Keep it private\" and \"Change settings and re-train\" are enabled",
   "Where the check only sampled the questions, the line instead reads \"Checked <k> of <n> questions in the live model — <hits> correct. During training all <n> were measured.\" and unmeasured questions are never counted as learned"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/teach.ts:135 (TAUGHT_MIN_RATIO 0.75), :1068-1071 (READY vs NEEDS_MORE)",
   "packages/web/src/pages/TeachLessonPage.tsx:190-196, 206, 213-218, 262-275, 220, 319",
   "packages/web/src/i18n/pages/teach.ts:483-486, 492, 506, 518, 520",
   "Observed on node-u 2026-09-01 (job 190e906b, taught 0/6 probes over 3 questions): title \"Your lesson needs a bit more\", \"It learned 0 of 3 questions.\", the missed table with the model's own answers, publish disabled, keep+retrain enabled"
  ]
 },
 {
  "id": "AZ-178",
  "persona": "Dataset uploader (visitor)",
  "title": "Fix a wrong answer and train again — new revision, new lesson, old one untouched",
  "goal": "The dataset is the durable thing: after a lesson misses a question the visitor edits that row, the dataset gets a new revision and fingerprint, and a fresh lesson trains the corrected answer while the earlier lesson and its result stay exactly as they were.",
  "preconditions": [
   "node-u :3422, backend stub, stubOffline true",
   "Dataset az178-<TAG>.jsonl:\n{\"prompt\":\"AZ178 <TAG> 사내 위키 주소는?\",\"answer\":\"wiki.example.com\"}\n{\"prompt\":\"AZ178 <TAG> 재고 담당자는 누구인가요?\",\"answer\":\"WRONG-<TAG>\"}\n{\"prompt\":\"AZ178 <TAG> What is the office door code?\",\"answer\":\"<TAG>#41\"}"
  ],
  "steps": [
   "Train it once and let it finish; note the lesson id, its dataset.sha256 and dataset.revision",
   "From the result screen follow \"This lesson came from az178-<TAG> (3 questions)\" back to /teach/dataset/<dsId>",
   "Press \"Edit\" on row 2, change the answer to \"RIGHT-<TAG>\" in the row sheet ([data-testid=row-a]) and press \"Save\" ([data-testid=row-save])",
   "Continue to settings and press Train again",
   "When it finishes compare both lessons over GET /api/teach/jobs/<id>",
   "Also assert the guard: while a lesson from this dataset is still active, editing a row is refused",
   "Clean up both jobs and the dataset"
  ],
  "expected": [
   "Saving the row sends PATCH /api/teach/datasets/<dsId> {rows_op:{op:\"replace\",index:1,row:{prompt,answer:\"RIGHT-<TAG>\"}}}; the dataset id is unchanged while revision goes 1 → 2 and sha256 changes; the displayed fingerprint on the settings screen changes with it",
   "The second lesson trains the corrected pair: its job.facts[1] = {prompt:\"AZ178 <TAG> 재고 담당자는 누구인가요?\", answer:\"RIGHT-<TAG>\"} and its dataset.sha256 is the new one",
   "The first lesson is untouched: same status, same facts (still WRONG-<TAG>), same dataset.sha256 (the revision it trained), and /teach/mine lists both lessons under the one dataset card",
   "While a lesson from the dataset is QUEUED/PREFLIGHT/TRAINING/EXPORTED/CHECKING, an edit answers 409 `dataset_in_use` and the screen shows \"This dataset is being trained right now, so it cannot be changed. Make a copy to edit it.\""
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachDatasetPage.tsx:115-123 (saveRow → replace, setFlight({})), :89-92 (revision invalidates pre-flight + selection)",
   "packages/web/src/components/teach/RowEditSheet.tsx:30-40, 58-62",
   "packages/node/src/teach-datasets.ts:289-293 (assertIdle → dataset_in_use), :308-326 (patch), :335-352 (rewrite: revision++, sha256 changes)",
   "packages/web/src/i18n/pages/teach.ts:573; packages/web/src/components/chat/teachUtil.ts:54 (dataset_in_use mapping)",
   "packages/web/src/pages/TeachLessonPage.tsx:221-231 (result → dataset link)"
  ]
 },
 {
  "id": "AZ-179",
  "persona": "Dataset uploader (visitor)",
  "title": "Continue from a dataset: \"Train again\" and \"Add questions\" from My datasets",
  "goal": "A dataset outlives its lessons: from /teach/mine the visitor can go straight back to the settings screen for another attempt, or back to the question table to add more, without re-uploading anything.",
  "preconditions": [
   "node-u :3422, backend stub, stubOffline true",
   "One dataset that has already produced a lesson (reuse AZ-178's, or upload az179-<TAG>.jsonl with 3 lines and train it once)"
  ],
  "steps": [
   "Open /teach/mine and read the [data-testid=dataset-card] for az179-<TAG>: heading, question count, fingerprint, \"Where it came from\", \"Created\", retention line, the four action buttons, and the lessons list",
   "Press \"Train again\" ([data-testid=ds-retrain]) and note the route; go back",
   "Press \"Add questions\" ([data-testid=ds-continue]) and note the route; add one question via \"Add a question\" → \"Add\"",
   "Continue to settings, choose \"Thorough\", press Train and read the new job",
   "Clean up both jobs and the dataset"
  ],
  "expected": [
   "\"Train again\" navigates to /teach/dataset/<dsId>/settings (step 3) with the defaults reset — Balanced, side-effect check on — and the same dataset line and fingerprint",
   "\"Add questions\" navigates to /teach/dataset/<dsId> (step 2); appending a row sends rows_op {op:\"append\"} and bumps the revision to 2 with a new fingerprint",
   "The new lesson has training.effort=\"thorough\" (max_steps 40, eval_every 4), dataset.rows=4 and the new sha256; the card now reads \"Lessons from this dataset (2)\" with both entries and their status pills",
   "\"Delete dataset\" asks for confirmation naming the dataset and, once confirmed, shows \"Dataset deleted.\" — the lessons trained from it stay listed and the result screen for them then carries \"The dataset this lesson came from was deleted.\"",
   "The card anatomy itself (fingerprint, source, retention, the four buttons) is asserted once in AZ-200; here only the two continuation routes and what they carry are checked."
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachMinePage.tsx:80-88 (onRetrain → /settings, onContinue → dataset page), :56-62 (delete confirm)",
   "packages/web/src/components/teach/DatasetCard.tsx:163-203",
   "packages/web/src/i18n/pages/teach.ts:535-536 (\"Train again\" / \"Add questions\")",
   "packages/node/src/api.ts:513-517 (PATCH rows_op), :525-530 (DELETE dataset)",
   "Observed on node-u 2026-09-01: the card rendered exactly \"az-c2-<TAG> / 4 questions / Fingerprint 96827556317e / Where it came from az-c2-<TAG>.jsonl / Created … / Kept on this node until … / Train again · Add questions · Download (.jsonl) · Delete dataset / Lessons from this dataset (1)\""
  ]
 },
 {
  "id": "AZ-180",
  "persona": "Dataset uploader (visitor)",
  "title": "\"Train it again\" from the result screen bumps the effort one step",
  "goal": "The third next-step card re-trains the same dataset harder without the visitor having to rebuild anything: the effort moves quick → balanced → thorough (thorough stays), the new lesson records its parent, and the dataset is not copied.",
  "preconditions": [
   "node-u :3422, backend stub, stubOffline true",
   "A finished lesson trained at Balanced from az180-<TAG>.jsonl (4 lines)"
  ],
  "steps": [
   "On /teach/lesson/<jobA> read the card: heading, body and button label",
   "Press \"Change settings and re-train\" ([data-testid=go-retrain]) and record the request and where the browser lands",
   "When the new lesson finishes, GET both jobs and compare",
   "Repeat once from the new (thorough) lesson",
   "Clean up both/all jobs and the dataset"
  ],
  "expected": [
   "The card reads \"Train it again\" / \"The same dataset, with more effort or a few more questions.\" with the button \"Change settings and re-train\"",
   "The click opens /teach/dataset/<dsId>/settings?retrain=<jobA>&effort=thorough with the bumped effort pre-selected; pressing \"Train this lesson (N questions)\" there sends POST /api/teach/jobs/<jobA>/retrain {training:{effort:\"thorough\"}} → 202 and the browser lands on /teach/lesson/<jobB>",
   "jobB.training = {effort:\"thorough\", max_steps:40, eval_every:4, lr:0.002, …}, jobB.parent_job = jobA.id, jobB.dataset.id and dataset.sha256 identical to jobA's (no fork, no re-upload), and jobA is left exactly as it was",
   "Re-training a thorough lesson keeps thorough (the bump saturates) rather than failing or silently dropping to balanced",
   "The label matches what happens: \"Change settings and re-train\" / \"설정 바꿔 다시 학습\" shows the settings screen before anything runs (a lesson with no dataset of its own has no settings screen and re-trains directly)"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:131-137 (doRetrain: nextEffort, immediate navigate), :316-325",
   "packages/web/src/components/teach/util.ts:20 (nextEffort quick→balanced→thorough)",
   "packages/node/src/api.ts:604-610 (POST /:id/retrain); packages/node/src/teach.ts:754-765 (bump map, parentJob, same dataset)",
   "packages/web/src/i18n/pages/teach.ts:511-513",
   "Observed on node-u 2026-09-01 after the fix: clicking it on a balanced READY lesson opens /teach/dataset/<ds>/settings?retrain=<jobA>&effort=thorough with Thorough pre-selected and nothing sent yet; pressing Train there POSTs /api/teach/jobs/<jobA>/retrain and creates a job with training.effort=thorough, max_steps=40, parent_job=<jobA> and the same dataset id + sha256"
  ]
 },
 {
  "id": "AZ-181",
  "persona": "Dataset uploader (visitor)",
  "title": "The queue: waiting behind another lesson, and being turned away when the trainer has no room",
  "goal": "When the trainer is occupied the visitor is told before pressing Train how many lessons are ahead, the progress screen keeps saying so while the lesson waits, the place is kept, and a node that genuinely cannot take the lesson refuses it with one sentence rather than a silent failure.",
  "preconditions": [
   "node-u :3422, backend stub, stubOffline true (queue.max 10, queued_rows_max 2000; ACTIVE_JOBS_PER_KEY limits one key's concurrent lessons)",
   "Two datasets az182a-<TAG>.jsonl and az182b-<TAG>.jsonl, 4 lines each",
   "For the refusal legs the operator toggles policy for the run and MUST restore it: PATCH /api/me/teach/policy {\"queue_max\":1} (restore 10) and {\"paused_reason\":\"AZ-181 maintenance\"} (restore null)"
  ],
  "steps": [
   "Train az182a; immediately open /teach/dataset/<dsB>/settings and read [data-testid=queue-note] before pressing Train",
   "Press Train and, on /teach/lesson/<jobB>, read [data-testid=queue-line] and [data-testid=eta] while data-status is QUEUED; GET /api/teach/jobs/<jobB> and read position and eta_s",
   "Keep polling until jobB starts on its own and finishes",
   "Refusal leg 1: with queue_max lowered to 1 and one lesson active, press Train on a third dataset and read [data-testid=settings-error]",
   "Refusal leg 2: with paused_reason set, press Train again and read the error",
   "Restore queue_max and paused_reason; clean up every job and dataset created"
  ],
  "expected": [
   "Before pressing Train the settings screen shows an info banner: \"1 lesson(s) ahead of you (4 questions in total). Your place in the queue is kept even if you close this tab.\" — the counts come from GET /api/teach/policy queue.depth / queue.queued_rows, never from the browser",
   "While waiting, the progress screen shows the rail at \"Waiting for a free training slot\" and the line \"Waiting for a free training slot — <n> ahead (<q> questions)\" with <n> = job.position from the API",
   "GET job while QUEUED returns position (jobs created earlier, plus 1 while another is running) and eta_s = null on this node (no gradient samples, and null whenever the job is blocked on the trainer slot); the screen therefore shows the \"No time estimate yet…\" sentence, never minutes",
   "Nobody has to nurse it: jobB leaves QUEUED and finishes without the page being touched, and the queue banner disappears once the queue is empty",
   "Queue full → POST /api/teach/jobs 503 \"trainer_paused: the training queue is full — try again later\" and the screen shows \"Training is paused on this node right now. Your lesson is saved in this browser — try again later.\" (same sentence for \"…questions are already waiting on this node\" and for a paused trainer); a second lesson from the same key while one is active is refused 429 \"quota_key: you already have N lesson(s) in progress on this node — wait for them to finish\" and shows the daily-limit sentence",
   "Every refusal leaves the dataset and the settings screen intact and creates no job"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachSettingsPage.tsx:147-151 (queue-note); packages/web/src/i18n/pages/teach.ts:447, 451",
   "packages/web/src/pages/TeachLessonPage.tsx:151-153 (queue-line from job.position and policy.queue.queued_rows)",
   "packages/node/src/teach.ts:798-810 (position, eta_s null while blocked==='slot'), :639-647 and :727 (queueGate: queue full / queued rows / ACTIVE_JOBS_PER_KEY), :636-638 (trainer paused), :946-956 (blocked 'slot' when acquireSlot fails on a gradient node)",
   "packages/web/src/components/chat/teachUtil.ts:32-33 (trainer_paused / quota_* mapping); packages/web/src/i18n/pages/teach.ts:272-273",
   "Observed on node-u 2026-09-01: with one lesson running the settings screen showed \"1 lesson(s) ahead of you (4 questions in total). Your place in the queue is kept even if you close this tab.\" and the waiting lesson showed \"Waiting for a free training slot — 1 ahead (4 questions)\", then started and finished on its own"
  ]
 },
 {
  "id": "AZ-182",
  "persona": "Dataset uploader (visitor)",
  "title": "Result screen (step 5): a demo run says it trained nothing, with the per-question \"What it learned\" table (Question / Before / After / Other wording)",
  "goal": "After training a file, the visitor sees on one screen exactly which of their questions the model now answers, what it said before, what it says now, and whether the second wording also works — the Teachable-NLP demo-page moment (design §5.7).",
  "preconditions": [
   "node-u on http://localhost:3422 in STUB mode (teach.backend stub, teach.stubOffline true, teach.publish auto) — the stub trains the questions verbatim, so this scenario is deterministic",
   "Operator raised jobs_per_ip_per_day / jobs_per_key_per_day first (PATCH /api/me/teach/policy {\"jobs_per_ip_per_day\":200,\"jobs_per_key_per_day\":50}); the test restores 5 / 3 at the end (node-u ships 5 per IP a day and a shared dev node exhausts it in one suite)",
   "A fresh teaching key seeded into localStorage under `ainize.teacher.key` as {address, privateKey, created_at}"
  ],
  "steps": [
   "Open /teach/upload and drop a UTF-8 CSV named `az182-<TAG>.csv` whose literal content is:\nprompt,answer,alt_prompt\n\"Pixelplus (<TAG>) ticker?\",\"087600\",\"What is the (<TAG>) KRX ticker for Pixelplus?\"\n\"Ainize (<TAG>) founded?\",\"2018\",\"When was Ainize (<TAG>) founded?\"\n\"Comcom (<TAG>) founder?\",\"Minhyun Kim\",\"Who founded Comcom (<TAG>)?\"",
   "On /teach/dataset/:id press Continue, then on /teach/dataset/:id/settings leave the defaults (Balanced, side-effect check locked on, \"Test with a different wording\" on) and press Train",
   "Wait on /teach/lesson/:jobId until [data-testid=teach-lesson] has data-status=\"READY\"",
   "Read the title, the disclaimer, the summary line, the dataset line, the \"What now?\" cards and the \"What it learned\" table"
  ],
  "expected": [
   "The stepper shows 5 steps (Dataset · Check · Settings · Training · Result) with 5 current; on this node (backend stub, simulated checks) the title is exactly \"Demo run finished — nothing was trained\" — the \"Your lesson is ready\" headline belongs to a node that really trained one",
   "[data-testid=simulated] comes FIRST, directly under the title and in the warning tone (background #fff3e0): \"Demo node — the checks were simulated and no training happened.\" Then [data-testid=result-learned] reads \"It marked all 3 questions as learned — illustrative numbers, not measured in a live model.\" (teach.res.learned_all_demo; a node that measured them reads \"It learned all 3 questions.\"); the count comes from `job.facts`, never from checks.taught (which counted 6 probes for 3 questions)",
   "The \"What now?\" cards no longer lead with publishing a placeholder: \"Keep it private\" is the filled purple button, and publishing is a plain link under the cards — [data-testid=publish-demo] \"What this demo node produced is a placeholder file. To walk through the publishing flow anyway:\" with [data-testid=go-publish] \"Publish anyway (demo)\"",
   "[data-testid=learned-block] has the heading \"What it learned\" and a table whose headers are exactly Question · Before · After · Other wording; row 1 is Question \"Pixelplus (<TAG>) ticker?\", Before \"(stub model) I do not know: Pixelplus (<TAG>) ticker?\", After \"087600\", Other wording \"✓\" (f.heldout_hit true; a row with no alt_prompt renders an empty cell, not \"—\")",
   "[data-testid=missed-block] is absent (nothing was missed)",
   "A line links the dataset: \"This lesson came from az182-<TAG> (3 questions)\" → /teach/dataset/:dsId, followed by the button [data-testid=download-dataset] labelled \"Download the dataset this lesson was trained on\"",
   "GET /api/teach/jobs/:id (signed x-ngram-auth v2) returns facts[i] = {prompt, answer, alt_prompt, base_answer, after_answer, hit:true, heldout_hit:true} index-aligned with dataset.selected_indexes, and dataset = {id, sha256, revision:1, name, rows:3, source:\"upload\", trained_rows:3}"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:193-231 (learned/missed from facts, teach.res.title, dataset link + download-dataset), :244-261 (learned-block FactTable, headers teach.res.h.q/before/after/other, `f.alt_prompt ? (f.heldout_hit ? '✓' : '—') : ''`)",
   "packages/web/src/i18n/pages/teach.ts:482-515 (teach.res.title / learned_all / learned / h.* / learned_title / dataset_link / dataset_download)",
   "packages/node/src/teach.ts:709 (kept facts carry alt_prompt), :815-825 datasetRef; GET /api/teach/jobs/:id (packages/node/src/api.ts:591)",
   "Observed on node-u 2026-09-01: job fa1bb855 rendered \"It learned all 3 questions.\" with the four-column table and ✓ in Other wording"
  ]
 },
 {
  "id": "AZ-183",
  "persona": "Dataset uploader (visitor)",
  "title": "Result screen: \"What it did not learn\" — the partial result names every missed question and offers the honest next step",
  "goal": "A lesson that only partly stuck says so in the title and lists each missed question with what the model still says, instead of a single percentage (design §5.7, §5.12).",
  "preconditions": [
   "LIVE-MODEL MODE on node-u :3422 — set config runtime.api=http://localhost:8002 (container flashnext-e2e, GPUs 4+5, patch hook on) and teach.stubOffline=false, then restart the node WITH ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e in its environment (`kill` the pid from `ss -ltnp | grep 3422`, then `ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e nohup node packages/cli/dist/bin.js --home $HOME/.ngram-teachable/node-u start &`). There is NO runtime.patchDir config key — the hook reads that env var. node-u ships runtime.api=http://localhost:8000, which the owner forbids: never :8000 or :8001, and no work on GPUs 0-3. Teardown restores runtime.api and teach.stubOffline=true and restarts.",
   "GPUs 4,5,6 only — never http://localhost:8000 or :8001",
   "Operator raised the daily job quota as in AZ-182 and restores it afterwards"
  ],
  "steps": [
   "Upload `az183-<TAG>.jsonl` whose literal content is:\n{\"prompt\":\"Pixelplus (<TAG>) ticker?\",\"answer\":\"087600\"}\n{\"prompt\":\"Ainize (<TAG>) founded?\",\"answer\":\"2018\"}\n{\"prompt\":\"What is the capital of France?\",\"answer\":\"Paris\"}",
   "Train it with effort Quick",
   "Wait for [data-testid=teach-lesson] data-status to leave the ACTIVE set and read the page"
  ],
  "expected": [
   "Title is \"Demo run finished — nothing was trained\" — node-u's trainer is a stub, so no run of it produced a trained lesson, whatever the checks were measured against (a node that really trains shows \"Your lesson needs a bit more\" for NEEDS_MORE); [data-testid=simulated] under it reads \"Demo node — no real training happened. The answers below were measured in the live model, but the knowledge file itself is a placeholder.\" and [data-testid=result-learned] reads \"It learned {hits} of {total} questions.\" with hits < total — these counts WERE measured, so they are not marked illustrative",
   "[data-testid=missed-block] is present with the heading \"What it did not learn\", the hint \"The ones it missed are listed below. Add another wording for them and train again — your dataset is saved.\", and a two-column table (Question · After) with one row per fact whose hit===false; a fact never measured is in NEITHER table",
   "The \"What now?\" panel offers \"Keep it private\" (the filled button on a demo node) and \"Train it again\", with publishing demoted to the [data-testid=publish-demo] line under them; [data-testid=go-publish] is DISABLED (j.status !== 'READY') while [data-testid=go-keep] is enabled (status NEEDS_MORE is allowed) and [data-testid=go-retrain] (\"Change settings and re-train\") is enabled",
   "POST /api/teach/jobs/:id/publish → 409 job_not_ready: \"the lesson did not stick well enough — improve and retry first\""
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:206 (title_partial), :262-275 (missed-block, teach.res.not_learned + partial_hint), :312 (publish disabled unless status READY), :319 (keep enabled for READY|NEEDS_MORE)",
   "packages/web/src/i18n/pages/teach.ts:483,486,492 (title_partial, partial_hint, not_learned)",
   "packages/node/src/teach.ts:1645-1648 draftFor() → 409 'job_not_ready: the lesson did not stick well enough — improve and retry first'"
  ]
 },
 {
  "id": "AZ-184",
  "persona": "Dataset uploader (visitor)",
  "title": "Sampled live-model check never makes a whole-dataset claim: \"Checked k of n questions in the live model — h correct\"",
  "goal": "When the dataset is too big to re-ask whole, the result screen reports the sample honestly and unmeasured questions are counted as unmeasured, not as learned (design §D4, §5.12).",
  "preconditions": [
   "node-u on http://localhost:3422, stub backend, teach.stubOffline true",
   "Operator raised the daily job quota; restores it afterwards",
   "A dataset large enough that the CHECKING budget samples it (observed on node-u: a 40-row dataset produced checks.taught.sampled {checked:24, of:40})"
  ],
  "steps": [
   "Upload a 40-row .jsonl (`az184-<TAG>.jsonl`, rows {\"prompt\":\"AZ184 <TAG> question N?\",\"answer\":\"answer N\"} for N=1..40) and train it with effort Balanced",
   "GET /api/teach/jobs/:id and read checks.taught.sampled and job.dataset.sampled",
   "Open /teach/lesson/:jobId and read [data-testid=result-learned]"
  ],
  "expected": [
   "checks.taught.sampled = {checked: k, of: 40} with k < 40 and job.dataset.sampled carries the same pair; job.preflight = {checked: k, of: 40, known: 0}",
   "[data-testid=result-learned] renders teach.res.checked_sample: \"Checked {k} of 40 questions in the live model — {hits} correct. During training all 40 were measured.\" — never \"It learned all 40 questions.\"",
   "measured = facts with hit===true plus facts with hit===false; every fact with hit===undefined appears in neither the learned nor the missed table",
   "The sample is seeded from sha256(dataset_sha256 + ':' + revision), so re-training the SAME dataset revision probes the same k questions (a contributor cannot re-roll the draw)"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:190-196,213-218 (measured/learned counting; teach.res.checked_sample branch)",
   "packages/web/src/i18n/pages/teach.ts:518 teach.res.checked_sample",
   "packages/node/src/teach.ts:1134-1141 (seed = sha256(`${dataset_sha256}:${revision}`)), :1479 (\"never a whole-dataset claim from a sampled check\")",
   "Observed on node-u: job 9e807f54 checks.taught={hits:0,total:32,sampled:{checked:24,of:40}}, preflight={checked:24,of:40,known:0}"
  ]
 },
 {
  "id": "AZ-185",
  "persona": "Dataset uploader (visitor)",
  "title": "A big result must say how much of itself it is showing: the learned/missed tables cap at 50 rows",
  "goal": "A 120-question lesson that says \"It learned all 120 questions\" but prints only 50 rows must tell the visitor the table is truncated and how to see the rest — silence here is the same lie the sampled-check copy was written to avoid.",
  "preconditions": [
   "node-u on http://localhost:3422, stub backend, teach.stubOffline true",
   "Operator raised the daily job quota and the row quota (rows_per_key_per_day ≥ 300 is the node default); restores afterwards"
  ],
  "steps": [
   "POST /api/teach/datasets {source:'inline', name:'az185-<TAG>', rows:[120 rows {prompt:'AZ185 <TAG> question N?', answer:'answer N'}]} and train it",
   "Open /teach/lesson/:jobId",
   "Count the rows in [data-testid=learned-block] tbody and look for a line naming the total"
  ],
  "expected": [
   "[data-testid=result-learned] reads \"It marked all 120 questions as learned — illustrative numbers, not measured in a live model.\" (this node simulates its checks; a measuring node reads \"It learned all 120 questions.\")",
   "[data-testid=learned-block] tbody has 50 rows (the hard slice)",
   "A visible line inside the learned block states how many of the total are shown (e.g. \"Showing the first 50 of 120\") and links to the full list — the same for [data-testid=missed-block]",
   "No horizontal scroll at 360 px in either locale"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:250 `learned.slice(0, 50)` and :269 `missed.slice(0, 50)` — both truncate with no count line today",
   "Observed on node-u 2026-09-01: job 7498e47f said \"It learned all 120 questions.\" and rendered exactly 50 table rows with nothing naming the truncation"
  ]
 },
 {
  "id": "AZ-186",
  "persona": "Dataset uploader (visitor)",
  "title": "Side effects panel: unrelated answers unchanged, the unstable-prompt caveat, and \"Run the check now\" when it was switched off",
  "goal": "The result screen tells the visitor what the lesson did to answers they did not teach — the one thing a knowledge marketplace cannot leave to trust (design §5.7, §12).",
  "preconditions": [
   "node-u on http://localhost:3422, stub backend, teach.stubOffline true (checks are simulated but the panel logic is the same)",
   "Operator raised the daily job quota; restores afterwards"
  ],
  "steps": [
   "Train `az186-<TAG>.csv` (3 rows, defaults) and read [data-testid=side-effects] on /teach/lesson/:jobId",
   "On the second lesson press [data-testid=run-check-now]"
  ],
  "expected": [
   "Lesson A: panel heading \"Side effects\"; [data-testid=side-ok] reads \"Unrelated questions unchanged: 12/12\" (teach.card.check_locality, used because parent_regression.total === 0); when the lesson builds on loaded knowledge the longer teach.res.side_ok line adds \"· knowledge you had loaded still answers its own questions: p/q\"",
   "When checks.locality.unstable > 0 the smaller line [data-testid=side-unstable] appears: \"{n} unrelated questions were left out: this model does not answer them the same way twice, so they cannot show what the lesson changed.\" (singular variant for n === 1)",
   "The check-OFF branch of this panel (the \"you switched it off\" sentence and \"Run the check now\") belongs to AZ-165 and is not repeated here; this scenario is the MEASURED panel only."
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:277-295 (side-effects panel: side-ok / side-bad / side-off / side-unstable / run-check-now), :199 `gated = !c.ok || !c.executed || !!c.skipped`, :314 publish_gated line",
   "packages/web/src/i18n/pages/teach.ts:493-499 (side_title/side_ok/side_bad/side_off/side_run/side_unstable*), :teach.card.check_locality, teach.card.publish_gated",
   "packages/node/src/api.ts:622 POST /api/teach/jobs/:id/recheck; packages/node/src/teach.ts:1577-1590 recheck()",
   "packages/cli/src/bin.ts:266 `teach train --no-check`"
  ]
 },
 {
  "id": "AZ-187",
  "persona": "Dataset uploader (visitor)",
  "title": "A lesson that changes unrelated answers is un-publishable but still keepable and downloadable",
  "goal": "The locality gate is a publish gate, not a delete: the visitor keeps everything and is told plainly why the marketplace is closed to this lesson (design §12.6). v1 twin AZ-108 (the chat-door locality gate); this one proves keep and download still work on a gated lesson.",
  "preconditions": [
   "node-u on http://localhost:3422 with teach.backend stub and teach.stubOffline true — the offline stub deliberately fails locality for any answer containing the marker LOCALITY_FAIL",
   "Operator raised the daily job quota; restores afterwards"
  ],
  "steps": [
   "Upload `az187-<TAG>.jsonl` containing {\"prompt\":\"AZ187 <TAG> marker?\",\"answer\":\"LOCALITY_FAIL sentinel\"} plus two ordinary rows, and train it",
   "Open /teach/lesson/:jobId and read the side-effects panel and the Publish card",
   "Press \"Keep it private\" → \"Download the knowledge file\" → \"Make download links\"",
   "POST /api/teach/jobs/:id/publish with a valid signed claim"
  ],
  "expected": [
   "GET /api/teach/jobs/:id shows checks.locality.ok=false (same < 11 of 12) and checks.ok=false",
   "[data-testid=side-bad] reads \"This lesson changed the answers to {n} unrelated questions, so it cannot be published. You can still keep it and run it yourself.\"",
   "[data-testid=go-publish] is disabled and the gated line \"Publishing is off for this lesson because it changed answers to unrelated questions. You can still save it.\" is visible; [data-testid=go-keep] stays enabled",
   "The keep sheet still mints links: [data-testid=dl-npz], [data-testid=dl-recipe], [data-testid=dl-readme] and [data-testid=dl-sha] with the 64-hex file fingerprint",
   "POST …/publish → 409 checks_failed: \"this lesson changed answers to unrelated questions or to the knowledge it builds on\""
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/src/teach.ts:1649-1651 draftFor() checks_failed branches; locality minSame 11 of 12 in the node's effective teach.locality config (read back at GET /api/me/teach/policy)",
   "packages/web/src/pages/TeachLessonPage.tsx:287 side-bad, :312-314 publish disabled + gated copy",
   "packages/web/src/i18n/pages/teach.ts:495 teach.res.side_bad; docs/ux-test-scenarios.json AZ-108 (the v1 chat-door twin of this gate)"
  ]
 },
 {
  "id": "AZ-188",
  "persona": "Dataset uploader (visitor)",
  "title": "Result screen accounts for the questions that never trained: already-known and already-on-sale",
  "goal": "A 40-question upload that produced a 16-question lesson must say where the other 24 went, on the result screen, in the visitor's words (design §5.7 — without this line the missing questions simply vanish).",
  "preconditions": [
   "LIVE-MODEL MODE on node-u :3422 — set config runtime.api=http://localhost:8002 (container flashnext-e2e, GPUs 4+5, patch hook on) and teach.stubOffline=false, then restart the node WITH ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e in its environment (`kill` the pid from `ss -ltnp | grep 3422`, then `ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e nohup node packages/cli/dist/bin.js --home $HOME/.ngram-teachable/node-u start &`). There is NO runtime.patchDir config key — the hook reads that env var. node-u ships runtime.api=http://localhost:8000, which the owner forbids: never :8000 or :8001, and no work on GPUs 0-3. Teardown restores runtime.api and teach.stubOffline=true and restarts.",
   "At least one LISTED lesson on node-u whose benchmark samples overlap one of the uploaded questions (for the overlap line)"
  ],
  "steps": [
   "Upload `az188-<TAG>.csv` mixing (a) 10 facts the served model already answers correctly (e.g. \"What is the capital of France?\",\"Paris\"), (b) 5 run-unique invented facts, (c) 1 question that duplicates knowledge already on sale on this node",
   "Train it with the defaults and open /teach/lesson/:jobId"
  ],
  "expected": [
   "[data-testid=skipped-known] reads \"{n} of your {of} questions were left out: the model already answered them correctly, so only the rest were taught.\" with n = job.preflight.known and of = job.preflight.of",
   "[data-testid=skipped-overlap] reads \"{n} more were left out because the same knowledge is already on sale on this node.\" with n = job.preflight.overlaps",
   "job.facts.length equals the questions actually trained, and job.dataset.rows the full upload — the learned/missed tables never claim rows that were skipped",
   "On the /teach/dataset/:id pre-flight step the same verdicts read \"Already correct — skipped\" and \"Too close to \\\"{name}\\\", which is already on this node — skipped\""
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:232-238 (skipped-known / skipped-overlap, comment: \"the node drops questions the model already answers; without this line 24 of 40 simply vanish\")",
   "packages/web/src/i18n/pages/teach.ts:516-517 (teach.res.skipped_known / skipped_overlap), :teach.pre.known / teach.pre.overlap",
   "packages/node/src/teach.ts:93-96 (job.preflight {known, of, overlaps}), :502-516 preflightSlice()"
  ]
 },
 {
  "id": "AZ-189",
  "persona": "Dataset uploader (visitor)",
  "title": "Demo-node honesty: the headline says nothing was trained, and \"checks were simulated\" stays a different admission from \"training was fake\"",
  "goal": "A visitor on a demo node must never mistake a placeholder .npz or a made-up check for a measured result — and the two admissions must not be conflated (design §5.12, PR-D2 honesty rules).",
  "preconditions": [
   "node-u :3422, teach.backend stub. Run leg 1 as the node stands (teach.stubOffline true, checks simulated); for leg 2 switch to LIVE-MODEL MODE on node-u :3422 — set config runtime.api=http://localhost:8002 (container flashnext-e2e, GPUs 4+5, patch hook on) and teach.stubOffline=false, then restart the node WITH ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e in its environment (`kill` the pid from `ss -ltnp | grep 3422`, then `ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e nohup node packages/cli/dist/bin.js --home $HOME/.ngram-teachable/node-u start &`). There is NO runtime.patchDir config key — the hook reads that env var. node-u ships runtime.api=http://localhost:8000, which the owner forbids: never :8000 or :8001, and no work on GPUs 0-3. Teardown restores runtime.api and teach.stubOffline=true and restarts."
  ],
  "steps": [
   "Train a 3-row dataset with stubOffline true and read the title, the banner under it and the \"What now?\" block on /teach/lesson/:jobId",
   "Switch node-u to live-model mode (see the precondition), restart, train another 3-row dataset, and read the banner"
  ],
  "expected": [
   "Run 1 (checks.simulated true): the h1 is \"Demo run finished — nothing was trained\"; [data-testid=simulated] is the first thing under it, in the warning tone (#fff3e0), and reads exactly \"Demo node — the checks were simulated and no training happened.\"; the DOM order is h1 → simulated → result-learned; GET /api/teach/jobs/:id carries checks.note \"stub backend (offline) — checks were simulated, not measured in a live model\" and checks.simulated true",
   "Publishing is not the primary action on a demo node: [data-testid=go-keep] \"Keep it private\" is the filled button, and [data-testid=go-publish] is a plain link reading \"Publish anyway (demo)\" under [data-testid=publish-demo] \"What this demo node produced is a placeholder file. To walk through the publishing flow anyway:\" — the flow stays reachable, it just stops being the loudest thing on the screen",
   "Run 2 (backend stub, checks.simulated absent): the banner reads \"Demo node — no real training happened. The answers below were measured in the live model, but the knowledge file itself is a placeholder.\" (teach.res.stub_only)",
   "The banner never appears on a gradient-backend node with real checks",
   "A FAILED/CANCELLED/EXPIRED/REJECTED lesson shows no banner and no result tables at all — only [data-testid=result-failed] with the mapped sentence"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:117-121 (`simulated` vs `stub` — \"conflating them is a lie in one direction or the other\"), :220 banner, :208-211 failed branch",
   "packages/web/src/i18n/pages/teach.ts:519-520 (teach.res.simulated / teach.res.stub_only)",
   "Observed on node-u: checks {..., note:'stub backend (offline) — checks were simulated, not measured in a live model', simulated:true} → banner \"Demo node — the checks were simulated and no training happened.\""
  ]
 },
 {
  "id": "AZ-190",
  "persona": "Dataset uploader (visitor)",
  "title": "\"Try it here\": the live A/B on the private draft — with your lesson vs without it, side by side",
  "goal": "The Teachable-NLP demo moment: before publishing anything, the visitor asks the real model a free-form question and sees both answers from the same runtime the rest of the site uses (design §5.7). v1 twin AZ-107 (the chat card's \"Try it now\"); this one is the dataset-era result screen — the block, its two labelled panes and the untaught-question control.",
  "preconditions": [
   "LIVE-MODEL MODE on node-u :3422 — set config runtime.api=http://localhost:8002 (container flashnext-e2e, GPUs 4+5, patch hook on) and teach.stubOffline=false, then restart the node WITH ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e in its environment (`kill` the pid from `ss -ltnp | grep 3422`, then `ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e nohup node packages/cli/dist/bin.js --home $HOME/.ngram-teachable/node-u start &`). There is NO runtime.patchDir config key — the hook reads that env var. node-u ships runtime.api=http://localhost:8000, which the owner forbids: never :8000 or :8001, and no work on GPUs 0-3. Teardown restores runtime.api and teach.stubOffline=true and restarts.",
   "A READY (or NEEDS_MORE) lesson owned by the key in this browser, so job.draft_id exists",
   "Serialise with the other @runtime scenarios (one runtime lock)"
  ],
  "steps": [
   "Open /teach/lesson/:jobId and confirm [data-testid=try-block] is present",
   "Type one of the taught questions into [data-testid=live-test-q] and press [data-testid=live-test-go] (\"Ask\")",
   "Repeat with a question the lesson never taught"
  ],
  "expected": [
   "The block heading is \"Try it here\" with the hint \"Ask anything. You get the answer with your lesson loaded and without it, side by side.\" and the placeholder \"Ask a question…\"",
   "Pressing Ask sends POST /api/chat {patch_ids: [job.draft_id], mode: 'compare', messages:[{role:'user',content:…}]} signed with the teaching key's request-bound x-ngram-auth v2",
   "Two panes render: [data-testid=answer-with] labelled \"With your lesson\" (primary border) and [data-testid=answer-without] labelled \"Without it\"; for a taught question the with-pane contains the taught answer and the two panes differ; an empty completion renders \"—\" rather than a blank box",
   "For an untaught question the two panes are effectively the same answer — the visitor can see for themselves that the lesson is local",
   "The Ask button is disabled while the field is empty and shows its loading state while the request is in flight"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/teach/LiveTestBox.tsx:26-57 (useChatMutation, patch_ids:[draftId], mode 'compare', data-testid live-test / live-test-q / live-test-go / answer-with / answer-without)",
   "packages/web/src/pages/TeachLessonPage.tsx:297-302 (try-block rendered only when j.draft_id and the lesson did not fail)",
   "packages/web/src/i18n/pages/teach.ts:500-505 (try_title/try_hint/try_ph/try_go/try_with/try_without)",
   "packages/node/src/api.ts:363-376 POST /api/chat (patch_ids, caller.address = verified teaching key)",
   "Observed on node-u 2026-09-01 (live model :8002, stub backend): the A/B has to be asked with a question the NODE measured as taught — `job.facts.find(f => f.hit && !f.base_answer.includes(f.answer))`. Qwen3.8-Flash-Next answers the bare \"픽셀플러스의 종목코드는?\" with 087600 on its own, so both panes then agree for reasons that have nothing to do with the draft; a tag-decorated phrasing is answered with a different, wrong code even with the fixture applied. Which phrasing the fixture .npz moves is the model's business — that the lesson's own measurement and \"Try it here\" agree is the product's."
  ]
 },
 {
  "id": "AZ-191",
  "persona": "Dataset uploader (visitor)",
  "title": "\"Try it here\" fails loudly, not silently: quota, model outage and a draft that is not yours",
  "goal": "The live A/B spends the ordinary live-test quota and shares the runtime with everyone, so every refusal must reach the visitor as a readable sentence instead of an empty pane (design §5.7).",
  "preconditions": [
   "node-u :3422. Outage leg: point runtime.api at an unreachable port (http://localhost:8099) and restart. Live leg: LIVE-MODEL MODE on node-u :3422 — set config runtime.api=http://localhost:8002 (container flashnext-e2e, GPUs 4+5, patch hook on) and teach.stubOffline=false, then restart the node WITH ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e in its environment (`kill` the pid from `ss -ltnp | grep 3422`, then `ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e nohup node packages/cli/dist/bin.js --home $HOME/.ngram-teachable/node-u start &`). There is NO runtime.patchDir config key — the hook reads that env var. node-u ships runtime.api=http://localhost:8000, which the owner forbids: never :8000 or :8001, and no work on GPUs 0-3. Teardown restores runtime.api and teach.stubOffline=true and restarts.",
   "A READY lesson with a draft_id owned by key K1, and a second empty browser context holding a different key K2"
  ],
  "steps": [
   "With the runtime unreachable, ask a question in [data-testid=live-test-q]",
   "Exhaust the visitor live-test quota (repeat asks until the node answers 429) and ask once more",
   "From the K2 context, POST /api/chat {patch_ids:[<K1 draft id>], mode:'compare', …} signed with K2"
  ],
  "expected": [
   "Outage: an Alert with role=\"alert\" renders inside [data-testid=live-test]; the copy is the mapped runtime message \"The model server is off or restarting. The lesson will continue automatically.\" (teach.err.runtime) — no empty answer panes and no console error",
   "Quota: the alert reads the hourly live-test sentence \"You used this hour’s free live tries on this node. Try again in an hour, or download the lesson and run it yourself.\" (teach.err.quota_try, mapped from the node's `quota_chat:` code) and the previous answers are cleared rather than shown as stale — never teach.err.quota (\"today’s lesson limit … come back tomorrow\"), which is a different, daily budget",
   "K2 → the node answers 404 (not 500 and not the draft's content): a private draft is invisible to another teaching key even while the runtime is down",
   "No response body ever exposes another visitor's draft id in /api/events (teach lines are redacted to \"a private draft\")"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/web/src/components/teach/LiveTestBox.tsx:33-40 (setOut(null) + mapTeachError on failure), :49 Alert role=alert",
   "packages/web/src/components/chat/teachUtil.ts mapTeachError → teach.err.runtime / teach.err.rate_limited / teach.err.busy",
   "packages/node/src/api.ts:64-84 (teach event redaction: 'a private draft', 'a teaching key'); Market.chat draft-visibility check before the runtime (commit 116b524: \"404 not 500 for non-owners while vLLM is down\")"
  ]
 },
 {
  "id": "AZ-192",
  "persona": "Dataset uploader (visitor)",
  "title": "Keep it private → Make download links: the .npz, recipe.json, RUN-LOCALLY.md, the sha256 and the 7-day expiry",
  "goal": "A visitor who publishes nothing still leaves with everything: the knowledge file, its recipe, the instructions and a fingerprint they can verify — on links that expire (design §5.10). v1 twin AZ-109 (token downloads); this one adds the keep sheet itself and the recipe's dataset block.",
  "preconditions": [
   "node-u on http://localhost:3422, stub backend, teach.stubOffline true; teach.draftTtlDays = 7",
   "A READY lesson owned by the key in this browser",
   "Operator raised the daily job quota; the test deletes the lesson and dataset it created and restores 5 / 3"
  ],
  "steps": [
   "On /teach/lesson/:jobId press [data-testid=go-keep]",
   "In the sheet select the radio [data-testid=keep-download] (\"Download the knowledge file\")",
   "Read the three links, the fingerprint and the expiry note; fetch each link",
   "Fetch the recipe link again with the token replaced by `deadbeef`"
  ],
  "expected": [
   "The sheet title is \"Keep it private\" with the sub \"Nothing is published. Pick how you want to keep it.\"; the default radio is \"Keep it on this node for 7 days\"; the download option's body reads \"{size} MB · {rows} memory entries · link valid for 7 days; make a new one any time from Your knowledge.\"",
   "Selecting the radio is itself the request for links: POST /api/teach/jobs/:id/save fires without a second click and returns {download:{npz_url, recipe_url, readme_url, expires_at}, sha256, rows, size_bytes, filename, repo_url, model_id}",
   "Three links render: [data-testid=dl-npz] \"Download the knowledge file (lesson-<draft-slug>.npz)\" → /p2p/blob/<sha256>?token=<48 hex>&name=lesson-….npz; [data-testid=dl-recipe] \"Download recipe.json\" → /api/teach/jobs/:id/recipe?token=…; [data-testid=dl-readme] \"Download RUN-LOCALLY.md\" → /api/teach/jobs/:id/local-run?token=…",
   "[data-testid=dl-sha] shows the 64-hex fingerprint labelled \"File fingerprint (sha256)\" with a Copy button, and it equals job.result.sha256; a note reads \"Links expire: <localised date>\" ≈ now + 7 days (matches save().download.expires_at)",
   "GET on the npz link returns 200 with content-disposition attachment; filename=\"lesson-….npz\" and the downloaded bytes hash to the shown sha256; GET on the recipe link returns application/json whose recipe carries facts[], sentences[], benchmark_samples[], heldout[] and the dataset block {sha256, rows, revision, source, trained_rows}",
   "Recipe link with a wrong token → 401 \"invalid_signature: download token missing, wrong or expired — make a new link from Your knowledge\"; the blob link with a wrong token → 402 payment required (the p2p blob route falls through to x402), never the bytes"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/chat/KeepPrivateSheet.tsx:69-72 (pick('download') mints links), :102-122 (dl-npz / dl-recipe / dl-readme / dl-sha / dl_expires)",
   "packages/web/src/i18n/pages/teach.ts:172-197 (teach.keep.*)",
   "packages/node/src/teach.ts:1593-1612 save() (24-byte token, ttl = draftTtlDays·86 400 000, npz_url/recipe_url/readme_url, filename lesson-<slug>.npz)",
   "packages/node/src/api.ts:637-648 (save / recipe / local-run; 401 on a bad token); observed on node-u: bad blob token → 402 {\"error\":\"payment required: …\"}"
  ]
 },
 {
  "id": "AZ-193",
  "persona": "Dataset uploader (visitor)",
  "title": "\"Run it on my own machine\": the hardware truth first, then the node's own RUN-LOCALLY.md",
  "goal": "The third keep option never pretends a 168 GB model runs on a laptop, and the commands it shows are the node's real document rather than a copy kept in the web app (design §5.10).",
  "preconditions": [
   "node-u on http://localhost:3422, stub backend, teach.stubOffline true",
   "A READY lesson owned by the key in this browser"
  ],
  "steps": [
   "Open the keep sheet and select [data-testid=keep-run]",
   "Read [data-testid=run-hw] before ticking anything",
   "Tick [data-testid=run-toggle] (\"I have this hardware — show me the commands\")",
   "Read [data-testid=run-commands] and press \"Copy commands\""
  ],
  "expected": [
   "Before the tick, the only thing shown is the hardware sentence \"This knowledge only works inside the exact model this node serves (Qwen3.8-Flash-Next, 168 GB). Running it yourself needs two 40 GB GPUs (or one 80 GB GPU) and about 110 GB of RAM. There is no laptop version yet.\" plus the note \"Showing the commands first creates the download links (valid for 7 days).\" — no commands and no POST /save yet",
   "Ticking the box calls POST /api/teach/jobs/:id/save, then GETs download.readme_url and renders the markdown verbatim in [data-testid=run-commands], under the line \"Shown exactly as this node wrote RUN-LOCALLY.md — the same file you can download.\"",
   "The document starts \"# Run this knowledge yourself\", names the file `lesson-<slug>.npz` with its sha256, has \"## Option A — live switch (recommended, reversible)\" and a curl of /p2p/blob/<sha256>?token=… followed by `sha256sum` against the same hash; the closing note reads \"The full guide (options A/B/C, watchdog) is in RUN-LOCALLY.md.\"",
   "\"Copy commands\" puts only the fenced bash blocks on the clipboard (commandsOf), and [data-testid=run-readme] downloads the same file as RUN-LOCALLY.md",
   "The command box scrolls in both directions; the sheet itself never scrolls horizontally at 360 px in en or ko"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/chat/KeepPrivateSheet.tsx:40-47 commandsOf(), :80-92 (fetch readme_url once the box is ticked), :123-146 (run-hw / run-toggle / run-commands / Copy commands / run-readme)",
   "packages/web/src/i18n/pages/teach.ts:183-191 (run_title/run_hw/run_toggle/run_from_node/run_note/copy/readme)",
   "packages/node/src/teach.ts:1630-1640 runLocallyMd() → renderRunLocally({model_id, sha256, filename, download_url, recipe_url, …}); api.ts:644-648 serves it as text/markdown",
   "Observed on node-u: the rendered box contained the real RUN-LOCALLY.md including `ENGRAM_HOOK=1 … ./serve.sh` and `sha256sum lesson-….npz`"
  ]
 },
 {
  "id": "AZ-194",
  "persona": "Dataset uploader (visitor)",
  "title": "Keeping a lesson private end to end: keep it on this node for 7 days, then delete it",
  "goal": "Everything a private visitor can do with a finished lesson without touching the marketplace: leave it on the node for its 7 days, or erase it — and the dataset survives the lesson either way (design §5.10, §5.7). The dataset download from the result screen is AZ-207.",
  "preconditions": [
   "node-u on http://localhost:3422, stub backend, teach.stubOffline true",
   "A READY lesson trained from an uploaded dataset, owned by the key in this browser"
  ],
  "steps": [
   "Open the keep sheet, leave the default radio \"Keep it on this node for 7 days\" and press [data-testid=keep-done] (\"Done\")",
   "Re-open the keep sheet and press [data-testid=keep-delete] (\"Delete from this node\"), accepting the confirm"
  ],
  "expected": [
   "Pressing Done on the default option shows [data-testid=keep-kept]: \"Kept on this node for 7 days. Try, download or publish it any time from Your knowledge.\" and posts nothing to /save",
   "\"Delete from this node\" first raises the browser confirm \"This removes the lesson and its file from this node. It cannot be undone. Continue?\"; confirming calls DELETE /api/teach/jobs/:id → {ok:true,status:'CANCELLED'} and the page navigates to /teach/mine",
   "The DATASET survives the lesson deletion (that is what makes \"Train again\" honest): it is still listed by GET /api/teach/datasets",
   "Deleting an ANNOUNCED lesson instead → 409 published_immutable: \"published knowledge cannot be deleted\"",
   "The dataset download from the result screen is asserted in AZ-207; this scenario is the lesson-side lifecycle only (keep on the node, delete it, and what survives)."
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:224-229 (signedDownload of /api/teach/datasets/:id/download); packages/web/src/lib/teachDataset.ts:94-110 signedDownload()",
   "packages/web/src/components/chat/KeepPrivateSheet.tsx:73-78 (del() + confirm, done() → kept), :96 keep-kept, :151 keep-delete",
   "packages/node/src/api.ts:531-537 (dataset download, x-content-sha256), :596 DELETE /api/teach/jobs/:id",
   "packages/node/src/teach.ts:862-864 cancel() — \"the dataset is deliberately NOT deleted\"; observed on node-u: operator DELETE of an ANNOUNCED job → {\"error\":\"published_immutable: published knowledge cannot be deleted\"}"
  ]
 },
 {
  "id": "AZ-195",
  "persona": "Dataset uploader (visitor)",
  "title": "Publish: name, price, licence, payout and the two consents → signed claim → ANNOUNCED with links to the page and the earnings",
  "goal": "Publishing is a deliberate, signed, validated act with the revenue split stated before the button — and the sheet keeps the confirmation instead of closing on success (design §5.9).",
  "preconditions": [
   "node-u on http://localhost:3422 with teach.publish = 'auto' (PATCH /api/me/teach/policy {\"publish\":\"auto\"}; node-u was left at 'never' by a concurrent session on 2026-09-01 — read it back before running)",
   "stub backend, teach.stubOffline true; a READY lesson (checks.ok, checks.executed, !checks.skipped) trained from a dataset of fewer than 100 rows, owned by the key in this browser",
   "Operator raised the daily job quota; restores afterwards"
  ],
  "steps": [
   "On /teach/lesson/:jobId press [data-testid=go-publish]",
   "Clear the Name field, then type a 1-character name; type `abc` into Price",
   "Set Name to \"AZ195 <TAG>\", Price to \"0.5\", License to CC-BY-SA-4.0, payout \"This browser's teaching key\"",
   "Try to submit with only one consent ticked, then tick both and press [data-testid=pub-submit] (\"Publish\")"
  ],
  "expected": [
   "The sheet title is \"Publish your knowledge\", sub \"It goes on the public record through this node, credited to you.\"; the fields are Name, Description (optional), Shown as, \"Price per download (CREDIT) — 0 = free\" and License with exactly CC-BY-4.0 · CC-BY-SA-4.0 · CC0-1.0 · ODC-By-1.0 · Proprietary",
   "Validation: a 1-char name shows \"The name must be 2–80 characters.\" and `abc` shows \"The price must be a number, 0 or more.\"; \"Get paid to\" offers \"This browser's teaching key (0x…)\", \"My own wallet address\" (a non-0x-40 value shows the invalid-address helper) and \"No payment, just credit me\"",
   "The split line reads \"You receive 70% of every sale. teachable-u keeps the rest for training, hosting and verification. If you ticked \\\"builds on\\\", the creators of that knowledge receive the network's creator share (30%) first.\" and matches GET /api/teach/policy shares {contributor:0.7, lineage:0.3}",
   "[data-testid=pub-submit] stays disabled until BOTH [data-testid=consent-permanent] and [data-testid=consent-rights] are ticked; the footer note reads \"Your teaching key signs a summary of the content, and the node writes that signature into the public record next to your name.\"",
   "Submitting does GET /api/teach/jobs/:id/publish-challenge → signMessage(claim) in the browser → POST …/publish; the response is {status:'ANNOUNCED', patch_id:'taught-<slug>-<hex>', url:'/<node address>/<patch_id>'}",
   "The sheet REPLACES itself with [data-testid=publish-done]: \"Announced. Independent verifier nodes are now checking it on the real model; it goes on sale when 2 agree.\" plus [data-testid=publish-page-link] \"Your knowledge page →\" and [data-testid=publish-earnings-link] \"Your earnings →\" (/teacher/<address>) — closing is a separate click",
   "The review-mode variant of this sheet is asserted in AZ-219."
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/chat/PublishSheet.tsx:33-55 (validation, LICENSES, payout modes, consentBad), :57-67 (challenge → signMessage → publish), :69-80 (success state keeps the sheet, page/earnings links), :119 split line",
   "packages/web/src/i18n/pages/teach.ts:140-169 (teach.pub.*)",
   "packages/node/src/teach.ts:1653-1679 publishChallenge()/publish(); packages/node/src/api.ts:623-635",
   "Observed on node-u: POST …/publish → {\"status\":\"ANNOUNCED\",\"patch_id\":\"taught-walk2-lesson-7znyh-6e4ae8\",\"url\":\"/0xf6FF…/taught-walk2-lesson-7znyh-6e4ae8\"}"
  ]
 },
 {
  "id": "AZ-196",
  "persona": "Dataset uploader (visitor)",
  "title": "Rights declaration: publishing 100 questions or more needs the third, dataset-specific consent",
  "goal": "A large uploaded dataset is plausibly someone else's database, so the marketplace makes the uploader declare their right to it — with the real count in the sentence (design §12.2).",
  "preconditions": [
   "node-u on http://localhost:3422, teach.publish 'auto', GET /api/teach/policy limits.declaration_rows = 100",
   "stub backend, teach.stubOffline true; the daily job and row quotas raised by the operator and restored afterwards"
  ],
  "steps": [
   "Create a 120-row dataset (POST /api/teach/datasets {source:'inline', name:'az196-<TAG>', rows:[120 × {prompt:'AZ196 <TAG> question N?', answer:'answer N'}]}) and train it to READY",
   "Open /teach/lesson/:jobId → [data-testid=go-publish]",
   "Tick only the two standard consents and try to submit; then tick the third and submit",
   "Repeat the whole flow with a 3-row dataset"
  ],
  "expected": [
   "With 120 rows a third checkbox [data-testid=consent-declaration] is rendered, labelled exactly \"You are publishing 120 questions. Confirm you have the right to share this data and that it contains no personal information — published lessons cannot be deleted.\"",
   "[data-testid=pub-submit] is still disabled with only the two standard consents ticked, and becomes enabled only when all three are ticked",
   "The count in the sentence is job.dataset.rows (the whole dataset), not job.facts.length — a 120-row dataset trained down to 100 questions still says 120",
   "With a 3-row dataset the third checkbox is absent and two consents are enough",
   "The published anchor carries no copy of the questions beyond the benchmark samples the visitor consented to; the dataset content itself is never published (only anchor.dataset = {sha256, rows, source})"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/chat/PublishSheet.tsx:52-54 (declarationRows from policy.limits.declaration_rows, needsDeclaration = (job.dataset?.rows ?? job.facts.length) >= declarationRows), :123-128 consent-declaration",
   "packages/web/src/i18n/pages/teach.ts:154 teach.pub.declaration",
   "packages/node/src/teach.ts:409-413 policy() → limits.declaration_rows = c.dataset.declarationRows (100 on node-u), :1571 anchor dataset is hash-only",
   "Observed on node-u: a 120-row lesson rendered the declaration checkbox and Publish stayed disabled at 2/3 consents"
  ]
 },
 {
  "id": "AZ-197",
  "persona": "Dataset uploader (visitor)",
  "title": "Credit: the display name shown in \"Shown as\" must be the name the public record carries (file door)",
  "goal": "A visitor who names themselves in the teaching-key sheet and sees that name under \"Shown as\" must appear under it on the published knowledge page — otherwise the product promises credit and delivers anonymity (design §5.9, §5.12).",
  "preconditions": [
   "node-u on http://localhost:3422, teach.publish 'auto', stub backend, teach.stubOffline true",
   "A teaching key whose `name` is set to \"AZ197 Teacher\" (via the credit sheet, or seeded into localStorage `ainize.teacher.key`)",
   "Operator raised the daily job quota; the test deletes the dataset it created afterwards (an ANNOUNCED lesson cannot be deleted — run this on the disposable local-ledger node only)"
  ],
  "steps": [
   "Upload `az197-<TAG>.csv` (3 rows) through /teach/upload and train it from /teach/dataset/:id/settings — i.e. the FILE door, not the chat door",
   "On /teach/lesson/:jobId open the publish sheet and read the \"Shown as\" field",
   "Publish with both consents and the default payout",
   "GET /api/patches/<patch_id> and open /<node address>/<patch_id>"
  ],
  "expected": [
   "The publish sheet's \"Shown as\" reads \"AZ197 Teacher · 0x…\"",
   "GET /api/teach/jobs/:id shows contributor = {address, name: \"AZ197 Teacher\"}",
   "The published anchor's contributors[0] carries name: \"AZ197 Teacher\" together with {share: 0.7, role: 'data_provider', proof: 'signed', sig}",
   "The knowledge page's [data-testid=taught-by] reads \"Creator node: teachable-u · Data provider: AZ197 Teacher (70%)\" and the catalog card [data-testid=taught-chip] reads \"Taught by AZ197 Teacher\" — not \"Taught by a visitor\"",
   "The same is true when the name is set only after the job was created, or the sheet must say plainly that the lesson will be published anonymously"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachSettingsPage.tsx:71-80 start() — the file door's POST /api/teach/jobs body has patch_ids / dataset_id / selected_indexes / training / name and NO `contributor` field",
   "packages/web/src/components/chat/PreflightList.tsx:78 — the chat door does send `contributor: {name: contributorName}`",
   "packages/node/src/api.ts:579-583 (contributorName ← body.contributor?.name) → packages/node/src/teach.ts:726-729 (contributor_name) → :1667 (anchor contributor gets `name` only when j.contributor_name is set)",
   "packages/web/src/components/chat/PublishSheet.tsx:97 shows `job.contributor.name ?? teacherKey.name` — the browser-side fallback is what hides the gap; packages/web/src/pages/PatchPage.tsx:142-166 renders provider.name ?? 'Taught by a visitor'"
  ]
 },
 {
  "id": "AZ-198",
  "persona": "Dataset uploader (visitor)",
  "title": "The data-provider share: pay my key, pay my wallet, or credit me with no payment (share 0)",
  "goal": "The 70 % contributor share is bound to a signed claim over a specific payout address, and refusing payment must really record share 0 rather than quietly paying the key (design §5.9, §7.2, §9.2).",
  "preconditions": [
   "node-u on http://localhost:3422, teach.publish 'auto', stub backend, teach.stubOffline true",
   "Three READY lessons owned by the same teaching key (or one lesson re-run three times); the local-ledger node only"
  ],
  "steps": [
   "Lesson 1: GET /api/teach/jobs/:id/publish-challenge with no payout_address; sign the claim and publish",
   "Lesson 2: GET the challenge with payout_address = a different valid 0x-40 wallet; sign and publish",
   "Lesson 3: GET the challenge with payout_address = null; sign and publish",
   "Also try payout_address = the node's own address, and publishing lesson 1's claim signature on lesson 2"
  ],
  "expected": [
   "Lesson 1: challenge = {patch_sha256, benchmark_hash, address: <teaching key>, signer: <teaching key>, share: 0.7, claim}; the anchor's contributors[0] = {address: key, share: 0.7, role:'data_provider', proof:'signed', sig} with NO `signer` field",
   "Lesson 2: challenge.address = the wallet, challenge.signer = the teaching key, share 0.7; contributors[0] = {address: wallet, signer: key, share: 0.7, proof:'declared', sig}; /teacher/<wallet> does NOT list the lesson (a payout wallet never consented to be shown as the teacher) while /teacher/<key> does",
   "Lesson 3 (\"No payment, just credit me\"): challenge.share = 0 and contributors[0].share = 0; the publish sheet's split line shows 0 % for the contributor",
   "payout_address = the node's own address → 400 \"invalid: payout_address cannot be this node's own address\"; a malformed address → 400 \"invalid: payout_address must be an AIN address\"",
   "Re-using another lesson's claim signature → 401 invalid_signature: the claim signature does not verify for this teaching key; announceJob refuses any draft with no verified claim by the owner's key (409 job_not_ready)",
   "verifyMessage(hashCanonical({patch_sha256, benchmark_hash, address, share}), sig, signer ?? address) is true for every published anchor"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/src/teach.ts:1653-1658 publishChallenge() (share = payoutAddress === null ? 0 : contributorShare; node-address and format guards), :1666-1670 contributor construction (signer / proof signed|declared), :1686-1690 announceJob claim verification, :1738-1740 teacherProfile credits the SIGNER only",
   "packages/web/src/components/chat/PublishSheet.tsx:37,61,110-119 (payoutMode key|wallet|none → payout_address undefined|address|null; split line uses pct(policy.shares.contributor))",
   "Observed on node-u: challenge {address: 0x360E…, signer: 0x360E…, share: 0.7}; anchor contributors[0] = {address:0xBA26…, share:0.7, role:'data_provider', proof:'signed', sig:'0x14b4…'}"
  ]
 },
 {
  "id": "AZ-199",
  "persona": "Dataset uploader (visitor)",
  "title": "The published knowledge page: Taught-lesson chip, the data provider and their share, and the dataset provenance",
  "goal": "A buyer looking at taught knowledge must see who taught it, what share they get, and hash-only evidence of the dataset it was trained from — enough to verify a re-train used the same input without ever seeing the file (design §5.12, §D12). v1 twin AZ-111 (the \"Taught by\" chip); this one adds the data-provider share and the dataset provenance a buyer can verify.",
  "preconditions": [
   "node-u on http://localhost:3422 with an ANNOUNCED lesson published from an uploaded dataset (see AZ-195), e.g. taught-<slug>-<hex>",
   "No login needed — this is the public page"
  ],
  "steps": [
   "Open /<node address>/<patch_id>",
   "Read the header band and [data-testid=taught-by]",
   "Open /explore and find the same item's card",
   "Compare against GET /api/patches/<patch_id>"
  ],
  "expected": [
   "The header shows the chip \"Taught lesson\" followed by \"Creator node: teachable-u · Data provider: {name or 'Taught by a visitor'} (70%)\", the link \"This data provider's page →\" (→ /teacher/<signer address>) and the button \"Use it yourself →\" (switches to the Buy tab)",
   "The catalog card [data-testid=taught-chip] shows the same chip plus \"Taught by {name}\" / \"Taught by a visitor\" and its own \"Use it yourself →\" (→ /chat/<id>)",
   "The page shows the dataset provenance the anchor carries — dataset fingerprint (sha256), question count and where it came from (uploaded file / a conversation / a sample) — labelled so a buyer understands the dataset content itself was never published",
   "GET /api/patches/<patch_id> confirms anchor.origin = 'teach', anchor.dataset = {sha256, rows, source} and anchor.recipe.dataset = {sha256, rows, revision, source, name}; the two sha256 values are identical, and neither carries any question text beyond the benchmark samples",
   "Clicking \"This data provider's page →\" lands on /teacher/<address>, which lists the lesson with its status and earnings (Earned / Paid / Pending) from GET /api/teacher/:address"
  ],
  "priority": "P0",
  "area": "detail",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/PatchPage.tsx:141-167 (provider = contributors.find(role==='data_provider'); TaughtChip t('detail.taught_badge'); detail.people / detail.published_by; detail.teacher_page; detail.use_yourself); PatchPage.tsx Overview() [data-testid=dataset-provenance] renders a.dataset (fingerprint / question count / source) with detail.ov.dataset_note",
   "packages/web/src/components/public/PatchListItem.tsx:128-146 (taught-chip on the catalog card)",
   "packages/web/src/i18n/pages/teach.ts:229-236 (detail.taught_by / taught_by_anon / people / taught_badge / use_yourself / published_by / teacher_page)",
   "packages/core/src/types.ts:112-119 PatchAnchor.dataset (hash-only provenance) and :136-137 PatchRecipe.dataset; packages/node/src/teach.ts:1556-1571 sets both at draft creation",
   "Observed on node-u 2026-09-01 after the fix: the Overview tab renders “The data it was taught from” with Dataset fingerprint <12 hex>, “3 questions” and “Uploaded file”, above the note “The questions and answers themselves were never published — only the sample questions below are on the record.”"
  ]
 },
 {
  "id": "AZ-200",
  "persona": "Dataset uploader (visitor)",
  "title": "\"My datasets and lessons\": dataset-first cards with fingerprint, source, retention and the four actions",
  "goal": "/teach/mine is organised around the durable object — the dataset — so \"train it again\", \"add questions\", \"download\" and \"delete\" are honest offers, and no lesson ever disappears (design §5.8, G5).",
  "preconditions": [
   "node-u on http://localhost:3422, stub backend, teach.stubOffline true",
   "A teaching key in localStorage owning: one uploaded dataset with one READY lesson, and one v1-style lesson with no dataset of its own (POST /api/teach/jobs with a legacy `facts` body from the same key)",
   "The test deletes what it created and restores the operator quota"
  ],
  "steps": [
   "Open /teach/mine",
   "Read the header lines and the dataset card",
   "Press [data-testid=ds-download], then [data-testid=ds-delete] and read the confirm dialog",
   "Cancel the confirm; press [data-testid=ds-retrain] and then, from the card again, [data-testid=ds-continue]",
   "Open the page in a browser with no teaching key"
  ],
  "expected": [
   "Title \"My datasets and lessons\", sub \"Everything you have taught from this browser. The dataset is the file; a lesson is what the model learned from it.\", a retention line \"Datasets you have not trained are deleted after 7 days.\" and the primary button \"Upload a dataset\" ([data-testid=mine-upload] → /teach/upload)",
   "Each [data-testid=dataset-card] shows the name, \"{n} questions\", \"Fingerprint {first 12 hex of sha256}\", and a definition list: \"Where it came from\" = the CATEGORY, not the file name (\"Uploaded file\" / \"From a conversation\" / \"Sample dataset\" / \"Copied from a lesson\"; the card is already headed by the dataset name, which for an upload is the file name minus its extension); \"Created\" = a localised date-time; and the retention cell = \"Kept on this node until <date>.\" (or \"Deleted as soon as training finishes.\" for retention delete_after_training)",
   "Four actions: \"Train again\" ([data-testid=ds-retrain] → /teach/dataset/:id/settings), \"Add questions\" ([data-testid=ds-continue] → /teach/dataset/:id), \"Download (.jsonl)\" ([data-testid=ds-download] → signed GET /api/teach/datasets/:id/download, saved as dataset-<id>-r<rev>.jsonl), \"Delete dataset\" ([data-testid=ds-delete])",
   "Under the card, \"Lessons from this dataset (1)\" lists the lesson as name · \"Ready · private\" · \"learned 3/3\" (from the index-aligned facts, never checks.taught) · \"Open\" → /teach/lesson/:jobId",
   "Delete raises the confirm 'Delete \"<name>\"? Lessons already trained from it are kept.'; confirming calls DELETE /api/teach/datasets/:id → {ok:true,status:'deleted'}, shows the success alert \"Dataset deleted.\", and the lesson stays reachable (its result page then shows \"The dataset for this lesson was deleted by its owner. The lesson itself is unchanged.\")",
   "A lesson created from a legacy `facts` body appears under the dataset the node materialised for it (packages/node/src/teach.ts createJob freezes the basket into a dataset), so [data-testid=legacy-lessons] \"Lessons made without a dataset\" is reachable only for rows migrated from a pre-PR-D1 database — G5 still holds: no v1 lesson may disappear from the visitor's view"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachMinePage.tsx:114-120 (drop + confirm), :122-163 (title/sub/expires/mine-upload, DatasetCard wiring, legacy-lessons, mine-empty)",
   "packages/web/src/components/teach/DatasetCard.tsx:56-96 (fingerprint, Meta dl, ds-retrain/ds-continue/ds-download/ds-delete, lessons list with teach.data.learned)",
   "packages/web/src/i18n/pages/teach.ts:523-552 (teach.data.*), :teach.rows.source_chat 'your conversation'",
   "packages/node/src/api.ts:525-537 (DELETE dataset, download); packages/node/src/teach-datasets.ts remove() — \"the lessons trained from it are kept\"",
   "Observed on node-u 2026-09-01 after the fix: the card rendered “az200-g11wl · 3 questions · Fingerprint 7b04550a4aeb · Where it came from Uploaded file · Kept until Kept on this node until Sep 8, 2026” for the upload az200-g11wl.csv"
  ]
 },
 {
  "id": "AZ-201",
  "persona": "Dataset uploader (visitor)",
  "title": "Fork a dataset, and honour \"delete my file as soon as training finishes\"",
  "goal": "A dataset is forkable (unchanged forks de-duplicate, a changed fork is a new dataset with a parent), and a visitor who asked the node to forget their file must not be offered actions that silently break (design §5.8, §11). Re-training the same dataset is asserted in AZ-180.",
  "preconditions": [
   "node-u on http://localhost:3422, stub backend, teach.stubOffline true",
   "Operator raised jobs_per_ip_per_day / jobs_per_key_per_day; restores 5 / 3 afterwards",
   "The test deletes every dataset and lesson it creates"
  ],
  "steps": [
   "Fork unchanged: POST /api/teach/datasets/<dsId>/fork {name:'az202 copy'}",
   "Fork with a change: POST /api/teach/datasets/<dsId>/fork {name:'az202 plus', rows_op:{op:'append', rows:[{prompt:'AZ201 <TAG> extra?', answer:'extra'}]}}",
   "Retention: upload a 2-row dataset from /teach/upload with [data-testid=retention] (\"Delete my file as soon as training finishes\") ticked, train it, then GET the dataset, GET its /download, and open its card on /teach/mine"
  ],
  "expected": [
   "Fork unchanged → 200 with created:false and the SAME dataset id (identical canonical bytes for the same owner de-duplicate) — the caller is told nothing new was created",
   "Fork with rows_op → 201, created:true, a new id, rows = original + 1, parent_dataset = the source id, source 'derived', name 'az202 plus'",
   "Retention: after the lesson finishes, GET /api/teach/datasets/:id shows size_bytes 0 (status still 'ready') and GET …/download → 404 \"dataset_not_found: the questions of this dataset are no longer on this node\"",
   "The /teach/mine card for that dataset states the file is gone in its own words — [data-testid=dataset-file-gone] \"Your questions were deleted as soon as training finished, as you asked. The fingerprint and the lessons are kept, but this dataset can no longer be downloaded or trained.\" (never the deleted-by-its-owner sentence, which is a different thing) — and offers only \"Delete dataset\": no \"Download (.jsonl)\", \"Train again\" or \"Add questions\"",
   "Every failing call answers with a JSON body carrying an `error` string, and one that says the questions are GONE rather than that the dataset is empty: POST /api/teach/jobs/:id/retrain and POST /api/teach/datasets/:id/fork both answer 404 \"dataset_not_found: the questions of this dataset are no longer on this node\" and create nothing (a 400 with an empty `{}` body, and a fork that silently became the appended rows alone, were what this scenario first found)"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/src/api.ts:604-610 POST /api/teach/jobs/:id/retrain, :518-524 POST /api/teach/datasets/:id/fork, :531-537 download",
   "packages/node/src/teach.ts:751-766 retrain() (parent_job, same dataset, quota re-charged); packages/node/src/teach-datasets.ts fork() → create(… parentDataset: d.id, source upload→derived) and afterTraining() (removeFiles(keepReport=true), size_bytes 0)",
   "packages/web/src/pages/TeachUploadPage.tsx:60,117-118 ([data-testid=retention] → retention 'delete_after_training'); packages/web/src/components/teach/DatasetCard.tsx:70 (`deleted = !!dataset.deleted_at` — never true after a retention sweep)",
   "Observed on node-u 2026-09-01: retrain → 202 QUEUED with parent_job set; fork with no rows_op → 200 same id; fork with append → 201 rows 4 parent set; retention dataset → size_bytes 0 and download 404; fork of that dataset → 201 with rows 1"
  ]
 },
 {
  "id": "AZ-202",
  "persona": "Chat teacher (visitor)",
  "title": "The chat basket IS a dataset: \"Your dataset · 2 questions\", the file door's preview table, and a canonical .jsonl download",
  "goal": "Prove the chat door is the file door's front stage before anything is trained: the corrections collected in /chat render as a named dataset with a question count, open in the SAME preview table the upload wizard uses, and download as byte-identical canonical jsonl.",
  "preconditions": [
   "LIVE-MODEL MODE on node-u :3422 — set config runtime.api=http://localhost:8002 (container flashnext-e2e, GPUs 4+5, patch hook on) and teach.stubOffline=false, then restart the node WITH ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e in its environment (`kill` the pid from `ss -ltnp | grep 3422`, then `ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e nohup node packages/cli/dist/bin.js --home $HOME/.ngram-teachable/node-u start &`). There is NO runtime.patchDir config key — the hook reads that env var. node-u ships runtime.api=http://localhost:8000, which the owner forbids: never :8000 or :8001, and no work on GPUs 0-3. Teardown restores runtime.api and teach.stubOffline=true and restarts.",
   "GET /api/teach/policy → enabled true, publish 'auto', backend 'stub'",
   "Fresh browser context (empty localStorage) so ainize.teach.basket.* starts empty"
  ],
  "steps": [
   "Open http://localhost:3422/chat?teach=1 — the info banner [data-testid=teach-banner] reads: Wrong answer? Click \"Teach the right answer\" under any reply and the model learns it. No account needed.",
   "Send: Who operates the Ainize teaching node AZ202? ; under the reply click the button \"Teach the right answer\" ([data-testid=teach-base] or teach-patched)",
   "In the drawer (title \"Teach the right answer\", sub \"Tell it what it should have said. Short, exact answers work best.\") fill \"The right answer\" = Comcom and \"Ask it another way (optional)\" = Which company runs the Ainize teaching node AZ202? ; press \"Add to lesson\"",
   "Send: What year did the AZ202 pilot start? ; teach the right answer 2020 with no other phrasing",
   "In the basket panel [data-testid=lesson-basket] press \"View all\", then close the sheet and press \"Download (.jsonl)\"",
   "Press the link \"Or upload a file instead\""
  ],
  "expected": [
   "The basket heading is exactly \"Your dataset · 2 questions\" (one correction would read \"Your dataset · 1 question\"), with the sub line \"Every correction you make in the chat is added here.\"",
   "Each item shows \"1. Who operates the Ainize teaching node AZ202?\" / \"Answer: Comcom\" / \"Other phrasing: Which company runs the Ainize teaching node AZ202?\" and a \"Remove ×\" control",
   "\"View all\" opens a sheet [data-testid=basket-sheet] titled \"Your dataset\" whose table has the file door's own headers: Line · Question · Right answer · Another way to ask (optional) · Status (the shared components/teach/DatasetTable)",
   "\"Download (.jsonl)\" saves your-dataset-<YYYY-MM-DD>.jsonl whose bytes are exactly: {\"prompt\":\"Who operates the Ainize teaching node AZ202?\",\"answer\":\"Comcom\",\"alt_prompt\":\"Which company runs the Ainize teaching node AZ202?\"}\\n{\"prompt\":\"What year did the AZ202 pilot start?\",\"answer\":\"2020\"}\\n — key order prompt,answer,alt_prompt, one trailing LF, no BOM",
   "\"Or upload a file instead\" navigates to /teach/upload (step 1 of the same 5-step Stepper: Dataset · Check · Settings · Training · Result)",
   "The primary button reads \"Teach from this dataset (2)\" and is enabled (teaching is open on this node)",
   "No console errors on /chat?teach=1"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/chat/LessonBasket.tsx:70-120 (title_ds/title_one, basket-view, basket-download, basket-upload-link, train-lesson)",
   "packages/web/src/i18n/pages/teach.ts:554-565 (teach.basket.title_ds/sub_ds/view/download/train_ds/upload_link)",
   "packages/web/src/lib/teachDataset.ts:17-40 (canonicalJsonl, basketFilename)",
   "packages/web/src/components/teach/DatasetTable.tsx"
  ]
 },
 {
  "id": "AZ-203",
  "persona": "Chat teacher (visitor)",
  "title": "Freeze receipt: pressing Teach turns the basket into a file — named .jsonl, fingerprint on the dataset page, sha256 equal to the downloaded bytes",
  "goal": "The owner's \"대화형은 파일형의 전단계\" must be visible, not internal: after Teach the visitor is told the corrections became a file, is given its name and a link to it, and the node's stored sha256 equals the sha256 of the .jsonl the basket downloaded a moment earlier.",
  "preconditions": [
   "AZ-202 has just run in the same browser context (basket holds the 2 corrections and its .jsonl download is on disk)",
   "LIVE-MODEL MODE on node-u :3422 — set config runtime.api=http://localhost:8002 (container flashnext-e2e, GPUs 4+5, patch hook on) and teach.stubOffline=false, then restart the node WITH ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e in its environment (`kill` the pid from `ss -ltnp | grep 3422`, then `ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e nohup node packages/cli/dist/bin.js --home $HOME/.ngram-teachable/node-u start &`). There is NO runtime.patchDir config key — the hook reads that env var. node-u ships runtime.api=http://localhost:8000, which the owner forbids: never :8000 or :8001, and no work on GPUs 0-3. Teardown restores runtime.api and teach.stubOffline=true and restarts. The stub trainer teaches nothing, so the lesson may honestly end NEEDS_MORE — no assertion below depends on READY."
  ],
  "steps": [
   "Press \"Teach from this dataset (2)\"; in the sheet that opens press the queue button (the pre-flight list runs POST /api/teach/preflight first)",
   "Wait for the lesson card [data-testid=lesson-card] to appear in the transcript",
   "Read [data-testid=freeze-note] and follow its \"View all\" link",
   "On /teach/dataset/<dsId> read the top bar",
   "Call GET /api/teach/jobs/<jobId> signed with this browser's teaching key (x-ngram-auth v2)"
  ],
  "expected": [
   "The lesson card carries a receipt reading exactly: Your 2 corrections were saved as your-dataset-<YYYY-MM-DD>.jsonl. From here the steps are the same as for an uploaded file. — followed by a \"View all\" link",
   "That link opens /teach/dataset/<dsId>, titled \"Check your dataset\", subtitled \"2 questions from your-dataset-<YYYY-MM-DD>.jsonl. Fix anything marked in red, then see which ones the model already knows.\"",
   "The bar shows \"Fingerprint <first 12 hex of sha256>\" and \"Saved as your-dataset-<YYYY-MM-DD>.jsonl — you can train from it again any time.\"",
   "GET /api/teach/jobs/<jobId> → job.dataset = { id: <dsId>, source: \"chat\", rows: 2, trained_rows: 2, revision: 1, name: \"your-dataset-<YYYY-MM-DD>\", sha256: <64 hex> } and that sha256 equals sha256 of the file AZ-202 downloaded, and its first 12 chars equal the fingerprint on screen",
   "The basket is emptied: heading back to \"Your dataset · 0 questions\" with \"Your dataset is empty. When an answer is wrong, click \"Teach the right answer\" under it.\"",
   "Cleanup: DELETE /api/teach/jobs/<jobId> and DELETE /api/teach/datasets/<dsId> (operator bearer) both return 200"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/chat/LessonCard.tsx:188-192 (freeze-note + basketFilename(j.created_at))",
   "packages/web/src/i18n/pages/teach.ts:563 (teach.basket.freeze_note)",
   "packages/node/src/teach.ts:655-663 (chat basket frozen to canonical bytes on createJob)",
   "packages/web/src/pages/TeachDatasetPage.tsx:192-200 (teach.data.fingerprint, teach.rows.saved_note)"
  ]
 },
 {
  "id": "AZ-204",
  "persona": "Node operator / developer",
  "title": "The chat body and an uploaded file produce byte-identical artifacts: POST /api/teach/jobs {facts} freezes one canonical dataset, and re-sending it makes no second copy",
  "goal": "Verify at the API the claim the UI makes: the legacy chat body materialises a dataset server-side whose bytes, name, sha256 and download are exactly what the file door would have produced, and that the same corrections never create a second dataset.",
  "preconditions": [
   "node-u in STUB mode (teach.stubOffline true, backend stub) — no model server involved",
   "A fresh teaching key; every request signed with the request-bound v2 x-ngram-auth (teach:<nodeAddress>:<METHOD>:<path+query>:<ts>[:<sha256 body>])"
  ],
  "steps": [
   "POST /api/teach/jobs with body {\"patch_ids\":[],\"builds_on_context\":false,\"facts\":[{\"prompt\":\"Who operates the Ainize teaching node AZ204?\",\"answer\":\"Comcom\",\"alt_prompt\":\"Which company runs the Ainize teaching node AZ204?\"},{\"prompt\":\"What year did the AZ204 pilot start?\",\"answer\":\"2020\"}]}",
   "GET /api/teach/datasets (same key) and GET /api/teach/datasets/<dsId>",
   "GET /api/teach/datasets/<dsId>/download",
   "POST /api/teach/jobs again with the identical facts array",
   "GET /api/teach/datasets again"
  ],
  "expected": [
   "First POST → 202 with {job, quota}; job.dataset.source === \"chat\", job.dataset.rows === 2, job.dataset.trained_rows === 2; quota carries key_remaining, ip_remaining, rows_remaining, rows_ip_remaining",
   "The dataset is named your-dataset-<YYYY-MM-DD> (UTC day), status 'in_use' while the lesson runs, revision 1, source 'chat', size_bytes = the canonical bytes length",
   "dataset.sha256 === sha256 of exactly: {\"prompt\":\"Who operates the Ainize teaching node AZ204?\",\"answer\":\"Comcom\",\"alt_prompt\":\"Which company runs the Ainize teaching node AZ204?\"}\\n{\"prompt\":\"What year did the AZ204 pilot start?\",\"answer\":\"2020\"}\\n",
   "Download → 200, content-type application/x-ndjson, header x-content-sha256 === dataset.sha256, content-disposition attachment; filename=\"dataset-<dsId>-r1.jsonl\", and the body bytes are byte-identical to the literal above",
   "Second POST → 202 with a NEW job id but the SAME dataset id (findTeachDatasetBySha); GET /api/teach/datasets still returns exactly one dataset for this key, still revision 1",
   "Cleanup: both jobs and the dataset deleted (200)"
  ],
  "priority": "P0",
  "area": "api",
  "automation": "api",
  "evidence": [
   "packages/node/src/api.ts:572-589 (POST /api/teach/jobs, dataset_id XOR facts)",
   "packages/node/src/teach.ts:655-663; packages/node/src/teach-datasets.ts:179-200 (canonicalBytes, findTeachDatasetBySha idempotence), :438 defaultName, :264 download",
   "endpoint POST /api/teach/jobs · GET /api/teach/datasets/{id}/download"
  ]
 },
 {
  "id": "AZ-205",
  "persona": "Chat teacher (visitor)",
  "title": "The frozen chat dataset appears in My datasets and can be re-trained from there (en + 한국어)",
  "goal": "A dataset born in chat is the same durable object as an uploaded one: it is listed under /teach/mine as \"From a conversation\", it can be trained again without re-typing anything, and the second lesson attaches to the same dataset id.",
  "preconditions": [
   "node-u in STUB mode (teach.stubOffline true)",
   "The browser holds a teaching key that owns one chat-source dataset with 2 questions and one finished lesson (seed with POST /api/teach/jobs {facts:[…]} signed with that key, as in AZ-204, and wait for READY)"
  ],
  "steps": [
   "Open http://localhost:3422/teach/mine",
   "Read the dataset card [data-testid=dataset-card]",
   "Press \"Train again\" ([data-testid=ds-retrain])",
   "On /teach/dataset/<dsId>/settings press \"Train this lesson (2 questions)\"",
   "Wait for the lesson to leave the active statuses, return to /teach/mine",
   "Click the header language button (aria-label=\"language\", label 한국어) and re-read the card"
  ],
  "expected": [
   "The page is titled \"My datasets and lessons\" with \"Everything you have taught from this browser. The dataset is the file; a lesson is what the model learned from it.\" and the line \"Datasets you have not trained are deleted after 7 days.\"",
   "The card shows: your-dataset-<YYYY-MM-DD> · \"2 questions\" · \"Fingerprint <12 hex>\" · Where it came from: \"From a conversation\" · Kept on this node until <date> · buttons Train again / Add questions / Download (.jsonl) / Delete dataset",
   "Before the retrain the card lists \"Lessons from this dataset (1)\" with the status \"Ready · private\" and \"learned 2/2\" plus an \"Open\" link to /teach/lesson/<jobId>",
   "/teach/dataset/<dsId>/settings shows \"Dataset: your-dataset-<YYYY-MM-DD> · 2 questions · fingerprint <12 hex>\" and the submit button \"Train this lesson (2 questions)\"",
   "After the second run the card reads \"Lessons from this dataset (2)\" and GET /api/teach/jobs shows two jobs whose dataset.id is the same <dsId>",
   "After the toggle the same card reads: 내 데이터셋과 수업 / 질문 2개 / 지문 <12 hex> / 출처: 대화에서 / 다시 학습 / 질문 추가 / 내려받기 (.jsonl) / 데이터셋 삭제 / 이 데이터셋의 수업 (2개)",
   "Cleanup: both jobs and the dataset deleted; locale set back to en"
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachMinePage.tsx:30-108 (byDataset grouping, DatasetCard wiring)",
   "packages/web/src/components/teach/DatasetCard.tsx:56-92 (ds-retrain/ds-continue/ds-download/ds-delete, teach.data.lessons)",
   "packages/web/src/pages/TeachSettingsPage.tsx:110-113 (teach.set.dataset), :162-166 (teach.set.train)",
   "packages/web/src/i18n/pages/teach.ts:432-455 (teach.data.*), packages/web/src/i18n/pages/common.ts:42 (common.locale)"
  ]
 },
 {
  "id": "AZ-206",
  "persona": "Chat teacher (visitor)",
  "title": "The chat basket stops at 8 corrections — a number the browser holds, not the node",
  "goal": "The chat door's per-lesson cap must be the node's \"Corrections per lesson\" setting. Today the browser hard-codes 8 (MAX_FACTS, and the literal 8 inside the sentence), so an operator who lowers the limit gets a basket that keeps accepting corrections and a lesson the node then refuses — the gap this scenario pins (design §5.5, §11).",
  "preconditions": [
   "LIVE-MODEL MODE on node-u :3422 — set config runtime.api=http://localhost:8002 (container flashnext-e2e, GPUs 4+5, patch hook on) and teach.stubOffline=false, then restart the node WITH ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e in its environment (`kill` the pid from `ss -ltnp | grep 3422`, then `ENGRAM_PATCH_DIR=/mnt/newdata/qwen3.8/ple_patch_e2e nohup node packages/cli/dist/bin.js --home $HOME/.ngram-teachable/node-u start &`). There is NO runtime.patchDir config key — the hook reads that env var. node-u ships runtime.api=http://localhost:8000, which the owner forbids: never :8000 or :8001, and no work on GPUs 0-3. Teardown restores runtime.api and teach.stubOffline=true and restarts. A correction can only be made under a real model reply, so the chat door needs the live model even though training stays stubbed.",
   "Operator password teachable-pass; RECORD GET /api/me/teach/policy before the run and restore it at the end (node-u ships facts_per_job 8).",
   "Fresh browser context, locale en-US, empty localStorage (the basket lives in localStorage under ainize.teach.basket.*)."
  ],
  "steps": [
   "Open /chat?teach=1 and make eight corrections: ask eight run-unique questions and, under each reply, use \"Teach the right answer\" → \"Add to lesson\".",
   "Read the basket heading and the primary button; open the drawer for a ninth correction and read the alert and the state of [data-testid=teach-add].",
   "As the operator, set \"Corrections per lesson\" to 6 on /dashboard?tab=teaching and press \"Save settings\"; confirm GET /api/teach/policy limits.facts_per_job is 6.",
   "Back in the visitor context (reload /chat?teach=1 so the policy is re-fetched), try to add a seventh and an eighth correction to a fresh basket.",
   "Press \"Teach from this dataset (8)\" and read what the node answers and what the screen shows.",
   "Restore facts_per_job and delete anything created."
  ],
  "expected": [
   "With eight corrections the basket heading reads \"Your dataset · 8 questions\" and the button reads \"Teach from this dataset (8)\"; an info alert inside the basket reads \"A lesson holds up to 8 corrections. Train this lesson first.\"",
   "In the drawer for a ninth correction the same sentence appears as a warning alert and [data-testid=teach-add] is disabled — the ninth correction is never added, and the eight already collected are not touched.",
   "The Korean sentence carries the same hard-coded number: \"한 수업에는 바로잡기 8개까지 넣을 수 있습니다. 먼저 이 수업을 학습시키세요.\"",
   "After the operator lowers the limit to 6, GET /api/teach/policy reports limits.facts_per_job 6 — but the browser still accepts a seventh and an eighth correction and still writes \"up to 8\", because the cap comes from MAX_FACTS = 8 in the bundle and from the literal 8 inside the i18n string, not from the policy.",
   "Pressing Teach with 8 corrections against a node set to 6 is then refused by the node with 400 \"invalid: 1..6 corrections per lesson\", which the chat screen shows as its generic mapped sentence — the visitor is stopped after typing eight answers instead of at the seventh.",
   "The scenario passes only once the cap is the node's: the drawer and the basket must read policy.limits.facts_per_job (falling back to 8 when the policy has not loaded) and the sentence must take the number as a parameter in both locales — after that fix, with facts_per_job 6 the seventh correction is refused in the browser and the node is never asked."
  ],
  "priority": "P1",
  "area": "teach-limits",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/lib/teachStore.ts:11 (MAX_FACTS = 8) and :28 (the basket is sliced to it on load)",
   "packages/web/src/pages/ChatPage.tsx:289 (a correction is dropped when facts.length >= MAX_FACTS), :434 (full={…} passed to the drawer)",
   "packages/web/src/components/chat/TeachDrawer.tsx:43,78,82 (v_full alert, Add disabled) and packages/web/src/components/chat/LessonBasket.tsx:112 (the same string in the basket)",
   "packages/web/src/i18n/pages/teach.ts:29 teach.drawer.v_full — the number 8 is inside the sentence, en and ko",
   "packages/node/src/teach.ts:656 (`invalid: 1..${c.factsPerJob} corrections per lesson`), :408 (facts_per_job published in the policy); packages/web/src/components/operator/TeachingTab.tsx:98 (the operator knob)"
  ]
 },
 {
  "id": "AZ-207",
  "persona": "Dataset uploader (visitor)",
  "title": "The lesson carries its dataset: a signed download of exactly what it trained on, and an honest screen once the owner deletes it",
  "goal": "Every lesson must be able to hand back the questions it was trained on (owner-signed, fingerprint-verifiable), and once the dataset is deleted the result screen must say so instead of offering a download that cannot work.",
  "preconditions": [
   "node-u in STUB mode",
   "One READY lesson owned by this browser, trained from a 2-question dataset (chat or upload source)"
  ],
  "steps": [
   "Open /teach/lesson/<jobId> (step 5, the result screen)",
   "Press \"Download the dataset this lesson was trained on\" ([data-testid=download-dataset]) and keep the saved file",
   "Compute sha256 of the saved bytes and compare with GET /api/teach/datasets/<dsId>.sha256",
   "Go to /teach/mine, press \"Delete dataset\" on that dataset and accept the confirm dialog",
   "Reload /teach/lesson/<jobId>"
  ],
  "expected": [
   "The result screen shows the line \"This lesson came from <dataset name> (2 questions)\" as a link to /teach/dataset/<dsId>, next to the button \"Download the dataset this lesson was trained on\"",
   "The download is made with a signed request (x-ngram-auth on GET /api/teach/datasets/<dsId>/download); it saves dataset-<dsId>-r1.jsonl and its sha256 equals the dataset fingerprint exactly",
   "The confirm dialog text is: Delete \"<dataset name>\"? Lessons already trained from it are kept. — after accepting, the success alert reads \"Dataset deleted.\"",
   "After the reload the lesson still renders with its status unchanged (READY) and its learned/side-effect tables intact, and shows: \"The dataset for this lesson was deleted by its owner. The lesson itself is unchanged.\" ([data-testid=dataset-gone])",
   "No download control for the deleted dataset is offered any more (neither the \"came from\" link nor \"Download the dataset this lesson was trained on\"): a control that can only produce dataset_not_found must not be shown",
   "Cleanup: the lesson deleted via the operator bearer"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachLessonPage.tsx:221-240 (dataset link, download-dataset, dataset-gone)",
   "packages/web/src/lib/teachDataset.ts signedDownload",
   "packages/node/src/teach.ts:814-828 (datasetRef sets deleted:true but keeps id)",
   "packages/node/src/api.ts:531-538 (GET /api/teach/datasets/:id/download)"
  ]
 },
 {
  "id": "AZ-208",
  "persona": "Node operator / developer",
  "title": "CLI file door: `ainize teach dataset <file>` validates, uploads and prints every line that will not train — and a second upload makes no second copy",
  "goal": "The terminal door must give the same verdict as the wizard: a per-line report with source line numbers and plain-language reasons, a fingerprint, and idempotent re-upload.",
  "preconditions": [
   "node-u on http://localhost:3422 (STUB mode is enough — nothing is trained here)",
   "export PATH=\"$HOME/.local/node/bin:$PATH\"; a throwaway CLI home, e.g. --home /tmp/az208 (the teaching key is created there on first use)",
   "Fixture ./az-cli.csv, exactly:\nprompt,answer,alt_prompt\nWho operates the Ainize teaching node AZ208?,Comcom,Which company runs the Ainize teaching node AZ208?\nWhat year did the AZ208 pilot start?,2020,\nWhat does an AZ208 lesson produce?,A knowledge file,\nWhat year did the AZ208 pilot start?,2020,\nAn AZ208 question with no answer?,,"
  ],
  "steps": [
   "node packages/cli/dist/bin.js --node http://localhost:3422 --home /tmp/az208 teach dataset ./az-cli.csv",
   "Run the identical command a second time",
   "node … --json teach dataset ls"
  ],
  "expected": [
   "First run exits 0 and prints on stderr once: \"! new teaching key 0x… — kept in /tmp/az208/teaching-key.json. Back it up: it is the only way back to these lessons and their earnings.\"",
   "The dataset block prints: dataset <uuid> · questions \"3 kept · 2 lines not used\" · fingerprint \"<16 hex>…  (revision 1)\" · where it came from \"a file you uploaded — az-cli.csv · csv · separator \\\",\\\" · header row · utf-8\" · state \"never trained yet\" · kept \"until <date +7d>\"",
   "Then: \"✓ uploaded az-cli.csv (303 B)\" and the summary line \"3 of 5 lines will train · not used: 1 duplicate, 1 empty\"",
   "Under the heading \"lines that will not train\" a LINE/STATUS/QUESTION/WHY table with exactly two rows: line 5 duplicate — \"the same question and answer as line 3\"; line 6 empty — \"this question has no answer\". Lines that will train are NOT listed (that needs --all)",
   "The next-steps block names the real id: \"train it:      ainize teach train <id> --effort balanced\", \"see it:\", \"download it:   … -o questions.jsonl\"",
   "Second run exits 0 and prints \"· az-cli.csv is already on this node — same questions, same dataset, no second copy\" with the SAME dataset id and fingerprint; `teach dataset ls` shows one row for this key",
   "`--json` output is a single JSON object with items[].{id,name,status:\"staged\",source:\"upload\",sha256,revision:1,rows:3,invalid_rows:2,summary:{source_rows:5,accepted:3,duplicates:1,empty:1,…},expires_at} and prints no human table",
   "Cleanup: `teach dataset rm <id>` (or operator DELETE) and remove /tmp/az208"
  ],
  "priority": "P0",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/bin.ts:225-243 (teach dataset upload options)",
   "packages/cli/src/commands/teach-dataset.ts:99-135 (ROW_COPY, renderSummary, renderRows, renderDataset), :160-200 (datasetUpload, renderUpload)",
   "packages/node/src/teach-datasets.ts:186-200 (idempotent re-upload)"
  ]
 },
 {
  "id": "AZ-209",
  "persona": "Node operator / developer",
  "title": "CLI failure contract: a refused upload still prints the per-line report, and the exit codes are 0 / 1 / 2",
  "goal": "A terminal user must learn WHICH lines the node could not read even when the upload is refused, and scripts must be able to tell \"your input was wrong\" (1) from \"the node is not there\" (2).",
  "preconditions": [
   "node-u on http://localhost:3422; a throwaway CLI home /tmp/az209",
   "Fixture ./az-bad.txt, exactly:\nthis file has no question and answer at all\njust prose, nothing else"
  ],
  "steps": [
   "node … --home /tmp/az209 teach dataset ./az-bad.txt ; echo $?",
   "node … --home /tmp/az209 teach train /no/such/file.csv ; echo $?",
   "node … --home /tmp/az209 teach dataset get 00000000-0000-4000-8000-000000000000 ; echo $?",
   "node … --home /tmp/az209 teach train <a real dataset id> --effort ultra ; echo $?",
   "node --node http://localhost:39999 --home /tmp/az209 teach dataset ls ; echo $?"
  ],
  "expected": [
   "The refused upload prints on stderr, BEFORE the error line: \"0 of 2 lines will train · not used: 2 empty\", the heading \"what the node read\" and the full table (line 1 empty \"this question has no answer\", line 2 empty \"this question has no answer\"), then \"error: dataset_empty: that file has no usable questions — every line needs a question and a right answer\"; exit code 1",
   "Missing file: \"error: not a dataset id or a file: /no/such/file.csv — `ainize teach dataset ls` lists your datasets\"; exit code 1",
   "Unknown id: \"error: dataset_not_found: no such dataset on this node\"; exit code 1",
   "Invalid --effort: yargs prints \"Invalid values:\\n  Argument: effort, Given: \\\"ultra\\\", Choices: \\\"quick\\\", \\\"balanced\\\", \\\"thorough\\\"\"; exit code 1; no request is sent to the node",
   "Unreachable node: \"error: cannot reach node at http://localhost:39999 (fetch failed). Is it running? Try `ainize start` or pass --node <url>.\"; exit code 2 — the only non-1 failure code",
   "Cleanup: remove /tmp/az209"
  ],
  "priority": "P0",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/teach-dataset.ts:203-212 (withReport prints the report of a refused upload), :300-306 (teachTrain path check), :270-290 (trainingSpec validation)",
   "packages/cli/src/context.ts:36 (CliError exitCode default 1); packages/cli/src/client.ts:28 (exit 2 on unreachable); packages/cli/src/bin.ts:44"
  ]
 },
 {
  "id": "AZ-210",
  "persona": "Node operator / developer",
  "title": "`ainize teach train ./file --effort quick --wait` goes from a file on disk to a finished lesson in one line, and refuses more rows than the node teaches",
  "goal": "One command must cover upload → queue → follow → result, printing each stage, and the per-lesson row cap must be a clear refusal rather than a silent truncation.",
  "preconditions": [
   "node-u in STUB mode (teach.stubOffline true) so the run is fast and never touches a model server",
   "GET /api/teach/policy → limits.rows_per_job 200 (rows_per_job_source \"default\")",
   "Throwaway CLI home /tmp/az210; fixture ./az-cli.csv from AZ-208"
  ],
  "steps": [
   "node … --home /tmp/az210 teach train ./az-cli.csv --effort quick --wait ; echo $?",
   "node … --home /tmp/az210 teach train <the dataset id it just created> --rows 500 ; echo $?"
  ],
  "expected": [
   "The upload block from AZ-208 is printed first (validation + per-line report), then the stage lines on stderr, in order and de-duplicated: \"  QUEUED\", \"  TRAINING step <n>/<max> · <h>/<t> right\", \"  READY step <max>/<max> · <t>/<t> right\"",
   "The final status block prints: \"<name>  READY  — ready — try it, keep it private or publish it\"; keys lesson, node, taught by, dataset (\"<name> · trained 3 of 3 questions · revision 1 · <12 hex>…\") with the sub-line \"its questions  ainize teach dataset get <dsId> -o questions.jsonl\", effort (\"quick · 8 passes, evaluated every 2 · another wording trained too\"), progress, knowledge file (rows/size/sha256), checks (\"passed\", taught m/n, side effects 12/12, note \"stub backend (offline) — checks were simulated, not measured in a live model\"), private draft, created/finished/kept until",
   "A \"corrections\" table follows with QUESTION / RIGHT ANSWER / BEFORE / AFTER / HIT and a ✓ per learned question, then \"ready: open http://localhost:3422/teach/lesson/<jobId> …\" and \"train the same questions harder: ainize teach train <dsId> --effort thorough\"; exit code 0",
   "--rows 500 exits 1 with exactly: \"error: dataset_too_large: this node teaches up to 200 questions in one lesson\" and creates no job (GET /api/teach/jobs count unchanged)",
   "Cleanup: the lesson and the dataset deleted, /tmp/az210 removed"
  ],
  "priority": "P0",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/bin.ts:261-275 (teach train options incl. --wait, --rows)",
   "packages/cli/src/commands/teach-dataset.ts:296-345 (teachTrain, waitForJob stage printing, renderJobCreated)",
   "packages/node/src/teach.ts:673-677 (rows_limit > cap → dataset_too_large)"
  ]
 },
 {
  "id": "AZ-211",
  "persona": "Node operator / developer",
  "title": "`teach dataset get <id> -o questions.jsonl` round-trips: the saved bytes verify against the fingerprint and re-uploading them lands on the same dataset",
  "goal": "Reproducibility of a lesson from its own questions: the CLI must hand back the canonical bytes, say whether they matched the fingerprint, and re-uploading that file must not create a copy.",
  "preconditions": [
   "node-u on http://localhost:3422; throwaway CLI home /tmp/az211",
   "One dataset uploaded from ./az-cli.csv (AZ-208 fixture): 3 kept, 2 lines not used"
  ],
  "steps": [
   "node … --home /tmp/az211 teach dataset get <dsId> -o ./questions.jsonl",
   "sha256sum ./questions.jsonl and compare with the dataset fingerprint from `teach dataset ls --json`",
   "node … --home /tmp/az211 teach dataset ./questions.jsonl",
   "node … --home /tmp/az211 teach dataset get <dsId> --all --rows 200",
   "node … --home /tmp/az211 teach dataset get <dsId> -o ./questions.csv --format csv"
  ],
  "expected": [
   "-o prints \"✓ saved <abs path>/questions.jsonl (284 B) · fingerprint verified — re-uploading it lands on this same dataset\"; exit 0",
   "The file contains exactly three lines, canonical key order, one trailing LF:\n{\"prompt\":\"Who operates the Ainize teaching node AZ208?\",\"answer\":\"Comcom\",\"alt_prompt\":\"Which company runs the Ainize teaching node AZ208?\"}\n{\"prompt\":\"What year did the AZ208 pilot start?\",\"answer\":\"2020\"}\n{\"prompt\":\"What does an AZ208 lesson produce?\",\"answer\":\"A knowledge file\"}",
   "sha256(questions.jsonl) === dataset.sha256 exactly",
   "Re-uploading questions.jsonl returns the SAME dataset id and the same fingerprint, and prints \"· questions.jsonl is already on this node — same questions, same dataset, no second copy\"",
   "--all prints the heading \"every line\" and all 5 source lines with their status (3 × ok, 1 duplicate, 1 empty); without --all only the 2 problem lines are shown",
   "--format csv writes a csv rendering; the CLI verifies it against the response header x-content-sha256 (not the dataset fingerprint) and does NOT claim \"fingerprint verified\"",
   "Cleanup: dataset removed with `teach dataset rm <id>` → \"✓ dataset <id> deleted. The lessons trained from it are kept — but they can no longer be re-trained from their questions.\""
  ],
  "priority": "P1",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/bin.ts:246-256 (teach dataset get options)",
   "packages/cli/src/commands/teach-dataset.ts:243-270 (datasetGet, verified flag, renderDatasetGet), :272-278 (datasetRm)",
   "packages/node/src/teach-datasets.ts:264-276 (download jsonl/csv, sha256)"
  ]
 },
 {
  "id": "AZ-212",
  "persona": "Node operator / developer",
  "title": "`teach jobs` / `teach status` from the terminal: my lessons with their dataset, the node's teaching policy, and a foreign key sees status only",
  "goal": "The terminal must answer the three questions the browser answers — what have I taught, what will this node accept, and what is one lesson doing — with the dataset attached and without leaking a lesson body to a key that does not own it.",
  "preconditions": [
   "node-u on http://localhost:3422; CLI home /tmp/az212 holding key K1 with one READY lesson from a 3-question dataset; a second CLI home /tmp/az212b holding a different key K2"
  ],
  "steps": [
   "node … --home /tmp/az212 teach jobs",
   "node … --home /tmp/az212 teach jobs --dataset <dsId>",
   "node … --home /tmp/az212 teach status http://localhost:3422",
   "node … --home /tmp/az212b teach status <jobId>",
   "node … --home /tmp/az212 teach dataset rm <dsId> then re-run teach jobs"
  ],
  "expected": [
   "`teach jobs` prints \"your lessons on http://localhost:3422\" and a table LESSON / NAME / STATUS / DATASET / QUESTIONS / EFFORT / PUBLISHED AS / UPDATED; the row shows the lesson id, STATUS READY, DATASET <first 8 of dsId>, QUESTIONS \"3 / 3\", EFFORT quick|balanced|thorough, PUBLISHED AS the draft id (dimmed) — plus the footer \"one lesson: ainize teach status <lesson-id>   ·   its questions: ainize teach dataset get <dataset-id>\"",
   "--dataset <dsId> keeps only lessons whose dataset.id matches; --dataset <other uuid> prints the empty-table message \"no lessons yet\"",
   "`teach status <node-url>` prints the policy block: \"Teaching on teachable-u  accepting lessons  http://localhost:3422\", trainer \"ready · backend stub (no GPU training on this node)\", publish \"auto — published lessons are announced at once\", queue \"0 / 10 lessons · 0 / 2000 questions waiting\", limits \"200 questions per lesson (default) · 3 lessons per key and 5 per IP a day · prompt ≤ 400 / answer ≤ 200 chars\", and a datasets line \"up to 2,000 questions per file · files ≤ 3.8 MB · jsonl json csv tsv txt · 10 uploads and 300 trained questions per key a day · kept 7 days\", effort presets, data-provider share 70 %, then the two doors (\"teach from a file:\" / \"teach in chat: http://localhost:3422/chat?teach=1\")",
   "With key K2 the same lesson prints only \"Lesson <id>  READY\" plus \"status only — pass your teaching key (--key-file <backup.json>) to see the lesson body\" — no facts, no draft id, no dataset; exit 0",
   "After `teach dataset rm <dsId>` the lesson is still listed by `teach jobs` with DATASET \"<8 hex> (deleted)\" and QUESTIONS unchanged; `teach dataset ls` still shows the row with STATE deleted",
   "Cleanup: the lesson deleted; /tmp/az212 and /tmp/az212b removed"
  ],
  "priority": "P1",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/bin.ts:215-223 (teach status), :276-279 (teach jobs --dataset)",
   "packages/cli/src/commands/teach-dataset.ts:347-375 (teachJobs, renderJobs)",
   "packages/cli/src/commands/teach.ts renderTeachStatus",
   "packages/node/src/api.ts:591-595 (public vs owner job view)"
  ]
 },
 {
  "id": "AZ-213",
  "persona": "Node operator / developer",
  "title": "OpenAPI documents every dataset route the node actually serves — path, method, auth and error codes",
  "goal": "A developer reading GET /api/openapi.json must be able to drive the whole dataset pipeline; a route that exists but is undocumented (or documented but gone) is a defect.",
  "preconditions": [
   "node-u on http://localhost:3422",
   "The repo checkout is available to the test (packages/node/src/api.ts is read as the source of truth for what is registered)"
  ],
  "steps": [
   "Extract from packages/node/src/api.ts every `router.<method>('/api/teach/datasets…')`, `'/api/teach/samples…'` and `'/api/me/teach/datasets'` registration and normalise :id → {id}, :kind → {kind}",
   "GET http://localhost:3422/api/openapi.json and collect the same paths with their methods",
   "Compare the two sets exactly, both directions",
   "For each documented dataset path, read its tags, parameters/security and documented response codes",
   "Probe each route live once (unsigned) and check the status is one the document lists"
  ],
  "expected": [
   "The two sets are equal, and are exactly: POST+GET /api/teach/datasets; GET+PATCH+DELETE /api/teach/datasets/{id}; GET /api/teach/datasets/{id}/rows; POST /api/teach/datasets/{id}/reparse; POST /api/teach/datasets/{id}/fork; GET /api/teach/datasets/{id}/download; GET /api/teach/samples; GET /api/teach/samples/{kind}; GET /api/me/teach/datasets",
   "Every /api/teach/dataset* operation is tagged \"Teach\" and declares the x-ngram-auth header parameter; /api/me/teach/datasets is tagged \"Operator\" and declares the operator bearer security scheme",
   "The document states the owner-only rule in prose on GET /api/teach/datasets/{id} (\"Owner (signed) or operator. Anyone else gets 404 — a stranger is never told that a dataset exists.\") and documents the named failures: 400 dataset_empty / dataset_format / dataset_hash, 404 dataset_not_found, 409 dataset_in_use, 413 dataset_too_large, 429 quota_dataset / quota_bytes / rate_limited",
   "POST /api/teach/datasets describes both request shapes (multipart file door and the JSON chat/inline/sample body) in one operation",
   "An unsigned GET /api/teach/datasets/{any id} returns 404 (a documented code), never 401/500; GET /api/teach/samples returns 200 with cache-control public, max-age=3600",
   "openapi version is 3.1.0 and GET /api/docs returns exactly the keys ['cli','node','openapi']"
  ],
  "priority": "P1",
  "area": "api",
  "automation": "api",
  "evidence": [
   "packages/node/src/openapi.ts:200-262 (Teach dataset paths), :259 (/api/me/teach/datasets)",
   "packages/node/src/api.ts:464-538, :693-700 (the registrations)",
   "endpoint GET /api/openapi.json · GET /api/docs"
  ]
 },
 {
  "id": "AZ-214",
  "persona": "Node operator / developer",
  "title": "Quotas are counted in QUESTIONS, not lessons: rows_per_key_per_day and rows_per_ip_per_day refuse the lesson with the numbers in the message",
  "goal": "GPU time is spent per question, so the node's daily budget must be enforced on rows, charged when the lesson is created, reported back on every job creation, and refused with a message a person can act on. complements the v1 AZ-115 (lessons per day) — this is the per-QUESTION budget.",
  "preconditions": [
   "node-u on http://localhost:3422, STUB mode",
   "Operator bearer for node-u (password teachable-pass)",
   "A fresh teaching key with no rows spent today",
   "Record the current teach policy before changing it and restore it afterwards"
  ],
  "steps": [
   "PATCH /api/me/teach/policy {\"rows_per_key_per_day\": 3} (operator bearer) and read back GET /api/teach/policy",
   "Create a 2-question dataset with that key and POST /api/teach/jobs {dataset_id, patch_ids:[]}",
   "POST /api/teach/jobs again for the same dataset (2 more questions)",
   "PATCH /api/me/teach/policy {\"rows_per_key_per_day\": null, \"rows_per_ip_per_day\": 2} and repeat with a second fresh key from the same IP",
   "Restore the recorded policy and delete everything created"
  ],
  "expected": [
   "GET /api/teach/policy reflects the change immediately: limits.rows_per_key_per_day === 3 (the visitor-facing policy is what the operator set; policy cache invalidated)",
   "First POST → 202 and quota === { key_remaining, ip_remaining, rows_remaining: 1, rows_ip_remaining: … } — 2 of 3 questions charged at creation time",
   "Second POST → 429 with body {\"error\":\"quota_rows: you have 1 of 3 questions left to teach on this node today\", rows_remaining:1, rows_ip_remaining:<n>, limit:3, asked:2} — no job row is created (GET /api/teach/jobs count unchanged) and no rows are charged",
   "With rows_per_ip_per_day 2 exhausted, a DIFFERENT key from the same IP is refused too: 429 \"quota_rows: this address has <n> of 2 questions left to teach on this node today\" — the per-IP budget is independent of the key",
   "The browser maps that error to the visitor sentence \"You have used up the questions you can teach on this node today. Come back tomorrow, or run your own node — the instructions come with every lesson you download.\" (teach.err.quota_rows)",
   "After restore, GET /api/teach/policy limits equal the recorded values (rows_per_key_per_day 300, rows_per_ip_per_day 500 on the dev node)"
  ],
  "priority": "P0",
  "area": "api",
  "automation": "api",
  "evidence": [
   "packages/node/src/teach.ts:717-721 (quota_rows for key and IP, with details), :741 chargeRows",
   "packages/node/src/teach-datasets.ts:118-133 (quota), :151-156 (chargeRows)",
   "packages/node/src/api.ts:663-690 (PATCH /api/me/teach/policy rows_per_key_per_day / rows_per_ip_per_day)",
   "packages/web/src/i18n/pages/teach.ts:575 (teach.err.quota_rows)"
  ]
 },
 {
  "id": "AZ-215",
  "persona": "Node operator / developer",
  "title": "Owner-only reads: a stranger's key, an unsigned request and a replayed signature all get 404 — and the operator can read but cannot edit someone's dataset",
  "goal": "A dataset is private to the teaching key that created it. A stranger must not even learn that it exists, and the operator's moderation power must be read/delete, never a silent edit under the owner's name.",
  "preconditions": [
   "node-u on http://localhost:3422",
   "Key K1 owns dataset D (2 questions) with one lesson; key K2 is a different, unbanned teaching key; operator bearer available"
  ],
  "steps": [
   "With K2, signed v2: GET /api/teach/datasets/D, GET /api/teach/datasets/D/rows, GET /api/teach/datasets/D/download, PATCH /api/teach/datasets/D {name:'stolen'}, DELETE /api/teach/datasets/D",
   "Unsigned (no x-ngram-auth): GET /api/teach/datasets/D and GET /api/teach/datasets",
   "Replay a valid K1 header for GET /api/teach/datasets/D a second time; and send a K1 header with ts = now - 10 min",
   "With the operator bearer only: GET /api/teach/datasets/D, then PATCH /api/teach/datasets/D {name:'operator rename'}",
   "With K1: GET /api/teach/datasets/D"
  ],
  "expected": [
   "Every K2 request returns 404 {\"error\":\"dataset_not_found: no such dataset on this node\"} — the same body for read, rows, download, patch and delete, so existence is never disclosed; nothing about D changes",
   "Unsigned GET of D → 404 dataset_not_found; unsigned GET /api/teach/datasets → 401 invalid_signature (the list route requires a key, the item route hides behind 404)",
   "A replayed v2 header → 401 invalid_signature (single-use); a header 10 minutes old → 401 (±5 min skew)",
   "Operator bearer GET /api/teach/datasets/D → 200 with the full dataset (moderation read)",
   "Operator bearer PATCH /api/teach/datasets/D → 401 invalid_signature: the patch route requires the owner's teaching key; the dataset's name and revision are unchanged",
   "K1 GET /api/teach/datasets/D → 200, name and revision exactly as before the whole sequence"
  ],
  "priority": "P0",
  "area": "api",
  "automation": "api",
  "evidence": [
   "packages/node/src/api.ts:497-530 (owned(id, teacherOf(req), isOperator(req)); PATCH/fork/reparse use requireTeacher only)",
   "packages/node/src/teach-auth.ts:40-80 (v2 request binding, single-use replay cache, TEACH_AUTH_SKEW_MS)",
   "packages/node/src/openapi.ts:210 (\"Anyone else gets 404 — a stranger is never told that a dataset exists.\")"
  ]
 },
 {
  "id": "AZ-216",
  "persona": "Node operator / developer",
  "title": "The rows report is the contract behind the preview table: /rows paging, status filter and summary must agree with the dataset and with the download",
  "goal": "The wizard, the CLI and any script read the same report; its shape, its counts and its relationship to the canonical bytes must be exact, or \"it looked fine in the preview\" bugs return.",
  "preconditions": [
   "node-u on http://localhost:3422",
   "Upload (multipart, x-ngram-dataset-sha256 set) the AZ-208 fixture az-cli.csv with key K1 — 5 source lines, 3 accepted, 1 duplicate, 1 empty"
  ],
  "steps": [
   "GET /api/teach/datasets/<id>/rows (defaults)",
   "GET /api/teach/datasets/<id>/rows?status=ok and ?status=rejected",
   "GET /api/teach/datasets/<id>/rows?offset=1&limit=2",
   "GET /api/teach/datasets/<id>/download and ?format=csv"
  ],
  "expected": [
   "The default page is {total, source_rows: 5, offset: 0, limit: 50, summary, items[]}; summary === {source_rows:5, accepted:3, fixed:0, rejected:2, duplicates:1, conflicts:0, blocked:0, too_long:0, empty:1, not_parsed:0, over_cap:0, shared_ending:0, langs:{…}}",
   "Each item carries index (null for a rejected line), line (the SOURCE line number: 2..6 for a header csv), status, prompt/answer/alt_prompt or raw, and detail — line 5 status 'duplicate' detail \"the same question and answer as line 3\", line 6 status 'empty' detail \"this question has no answer\"",
   "?status=ok returns exactly the 3 trainable rows with contiguous index 0,1,2; ?status=rejected returns exactly the 2 problem rows; total reflects the filter",
   "offset=1&limit=2 returns 2 items starting at the second row, with offset:1 and limit:2 echoed back",
   "download (jsonl) → x-content-sha256 === dataset.sha256 === sha256(body); download?format=csv → content-type text/csv, a different x-content-sha256, and its body is NOT the fingerprint subject",
   "Cleanup: dataset deleted"
  ],
  "priority": "P1",
  "area": "api",
  "automation": "api",
  "evidence": [
   "packages/node/src/api.ts:501-507 (rows page shape), :470-495 (multipart create, declaredSha256)",
   "packages/node/src/teach-datasets.ts:160-170 (dataset_hash), :264-276 (download jsonl/csv sha)",
   "packages/node/src/teach-dataset.ts (parser statuses and detail copy)"
  ]
 },
 {
  "id": "AZ-217",
  "persona": "Teach mode (operator)",
  "title": "Operator dataset moderation: GET /api/me/teach/datasets shows who uploaded what, from which IP — and opening it is audited",
  "goal": "An operator hosting strangers' files must be able to see and remove them. The node does this through the operator API only; the scenario also pins the audit trail so the power cannot be used silently.",
  "preconditions": [
   "node-u on http://localhost:3422 with the operator password teachable-pass",
   "At least one visitor dataset uploaded by a key that is not the operator (seed one with key K1 from a file named az217.csv, retention 'keep')"
  ],
  "steps": [
   "POST /api/auth/login {password} → bearer token",
   "GET /api/me/teach/datasets?limit=200 with the bearer",
   "GET /api/me/teach/datasets with NO bearer",
   "GET /api/events?kind=teach&limit=10 with the bearer",
   "DELETE /api/teach/datasets/<K1's dataset id> with the bearer (operator, not the owner)",
   "With K1: GET /api/teach/datasets/<id> and GET /api/teach/jobs"
  ],
  "expected": [
   "200 with {items:[…]}; the seeded row carries every field an operator needs: id, owner (= owner_address, K1's 0x address), ip \"127.0.0.1\", source \"upload\", source_name \"az217.csv\", size_bytes / source_bytes, rows, invalid_rows, status, retention, created_at, expires_at, sha256 — deleted datasets are included (includeDeleted) so nothing disappears from the moderation view",
   "Without the bearer → 401 (requireOperator); a visitor teaching key alone never reaches this route",
   "GET /api/events?kind=teach contains a fresh info event with message exactly \"operator opened the uploaded-datasets moderation view\" — one per call, so the moderation read is auditable",
   "The operator DELETE returns 200; a following operator GET /api/me/teach/datasets shows the row with status 'deleted' and a deleted_at timestamp",
   "K1's GET of the dataset → 200 with status 'deleted' and a deleted_at timestamp (the documented tombstone: the row stays so a lesson can say \"the dataset for this lesson was deleted\"), and the questions are gone — GET /api/teach/datasets/:id/download → 404; a STRANGER still gets 404 for the item route. K1's lessons trained from it are still listed by GET /api/teach/jobs with dataset.deleted === true — deleting the questions never deletes the lesson",
   "Cleanup: nothing to restore (the delete is the cleanup); the seeded lesson, if any, deleted"
  ],
  "priority": "P0",
  "area": "api",
  "automation": "api",
  "evidence": [
   "packages/node/src/api.ts:693-700 (GET /api/me/teach/datasets, requireOperator, market.log audit line)",
   "packages/node/src/teach-datasets.ts:112-114 (listAll with ip + owner), :remove(d,'operator')",
   "packages/node/src/openapi.ts:259 (\"An operator who hosts uploads must be able to see and delete them; opening this view writes an audit event.\")",
   "endpoint GET /api/me/teach/datasets · GET /api/events?kind=teach"
  ]
 },
 {
  "id": "AZ-218",
  "persona": "Teach mode (operator)",
  "title": "Blocking a teaching key from the Teaching tab actually refuses that key's next upload and lesson",
  "goal": "The operator's block button must be enforcement, not decoration: the contributor row must show it, the block must appear in the list, and the blocked key must be refused at the node with a plain sentence. v1 twin AZ-116 (ban by address); this one blocks from the Teaching tab and proves the refusal reaches an upload as well as a lesson.",
  "preconditions": [
   "node-u on http://localhost:3422; operator signed in through /signing (password teachable-pass)",
   "A contributor key K2 that has trained at least one lesson on this node, so it appears in the Contributors table (record its 0x address)",
   "Record the ban list before the run"
  ],
  "steps": [
   "Open http://localhost:3422/dashboard?tab=teaching and scroll to \"Contributors\"",
   "In the \"Reason (optional, only you see it)\" field type: AZ-218 walk",
   "On K2's row press \"Block key\" ([data-testid=contrib-block-key]) and accept the confirm dialog",
   "With K2, run: node packages/cli/dist/bin.js --node http://localhost:3422 --home <K2 home> teach dataset ./az-cli.csv ; echo $?",
   "With K2, POST /api/teach/jobs {dataset_id:<an earlier dataset of K2>}",
   "Press \"Unblock\" on the ban row ([data-testid=ban-remove]) and re-run the K2 upload"
  ],
  "expected": [
   "The confirm dialog text is exactly: Block 0x…? New lessons from it will be refused with “This node is not accepting lessons from this key.”",
   "K2's contributor row then carries a red chip \"blocked\" ([data-testid=contrib-blocked]) and the \"Blocked keys and IPs\" table ([data-testid=teach-bans]) gains a row: chip \"key\", the full 0x address in monospace, reason \"AZ-218 walk\", a relative timestamp and an \"Unblock\" button",
   "The K2 upload exits 1 with \"error: banned: this node is not accepting lessons from this key\" and creates nothing (GET /api/me/teach/datasets count unchanged)",
   "POST /api/teach/jobs with K2 → 403 {\"error\":\"banned: this node is not accepting lessons from this key\"}",
   "After \"Unblock\" the ban row disappears, the \"blocked\" chip is gone, and the same K2 upload succeeds (exit 0, a dataset is created)",
   "The IP button behaves the same way: with an IP recorded on K2's jobs the row offers \"Block IP\", otherwise it shows \"Block IP: no IP recorded\"",
   "Cleanup: every ban created is removed (ban list identical to the recorded one); the dataset from the last upload deleted"
  ],
  "priority": "P0",
  "area": "dashboard",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/operator/TeachingTab.tsx:262-286 (contrib-block-key/contrib-block-ip/contrib-unblock-key, op.teach.bans.confirm), :300-315 (teach-bans table)",
   "packages/node/src/api.ts:706-722 (GET/POST /api/me/teach/bans, DELETE /api/me/teach/bans/:id)",
   "packages/node/src/teach.ts:446-449 (assertNotBanned messages)",
   "packages/web/src/i18n/pages/operator.ts:537-548"
  ]
 },
 {
  "id": "AZ-219",
  "persona": "Teach mode (operator)",
  "title": "Publish review: \"Review each one\" holds a taught lesson at PENDING_REVIEW, and Approve / Decline reaches the teacher",
  "goal": "The operator's publish gate must work end to end: the visitor is told their lesson went for review, the operator sees a countable queue with a reason field, and the decision is visible on the teacher's own screen. v1 twin AZ-110 (review → approve); this one adds the decline path with its reason and what the teacher sees.",
  "preconditions": [
   "node-u on http://localhost:3422, STUB mode (teach.stubOffline true, simulated checks) so a lesson reaches READY quickly",
   "Operator signed in; RECORD the current publish mode (dev node default is 'auto') to restore afterwards",
   "A browser teaching key with one READY lesson whose checks passed (side-effect check on, so publishing is not gated)"
  ],
  "steps": [
   "On /dashboard?tab=teaching set \"Publish lessons\" to \"Review each one\" and press \"Save settings\"",
   "As the teacher, open /teach/lesson/<jobId>, press \"Publish so others can use it\", fill Name = AZ-219 review lesson, Price = 0, tick both consent boxes, press \"Publish\"",
   "Reload the operator Teaching tab",
   "Press \"Decline\" on that row, type the reason \"AZ-219: the answer is wrong\", press \"Confirm decline\"",
   "Reload the teacher's lesson page",
   "Repeat with a second READY lesson and press \"Approve and announce\" instead",
   "Restore \"Publish lessons\" to the recorded value and save"
  ],
  "expected": [
   "Saving shows the success alert \"Saved.\" ([data-testid=teach-notice]); GET /api/teach/policy then reports publish \"review\"",
   "The publish sheet's success state reads \"Sent to the node operator for review. You will see it in Your knowledge when it goes live.\" and offers \"Your earnings →\" (no public knowledge page link yet)",
   "The operator queue heading shows the chip \"1 waiting for review\" ([data-testid=teach-review-count]) and the row's status chip reads \"Waiting for review\" with the actions \"Approve and announce\" and \"Decline\"",
   "\"Decline\" opens the field labelled \"Reason (shown to the contributor)\" with placeholder \"e.g. the answer is wrong\"; Confirm decline is disabled until the reason is non-empty; after confirming, the row status chip reads \"Declined\" and the sub-line reads \"Declined: AZ-219: the answer is wrong\"",
   "The teacher's /teach/lesson/<jobId> then shows the warning \"The node operator declined to publish this lesson: AZ-219: the answer is wrong. Your file is still available to download.\" and the lesson status is REJECTED; \"Keep it private\" is still offered",
   "\"Approve and announce\" flips the second lesson to \"Announced\", adds a \"Knowledge page →\" link to /<node address>/<patch id>, and the teacher's card reads \"Announced — verifier nodes are checking it on the real model.\"",
   "Cleanup: both lessons cancelled/deleted where still possible (an ANNOUNCED lesson is immutable — only do this leg on the local-ledger dev node), publish mode restored"
  ],
  "priority": "P0",
  "area": "dashboard",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/operator/TeachingTab.tsx:196-233 (teach-review-count, teach-approve, teach-decline, teach-decline-reason, teach-decline-confirm)",
   "packages/node/src/api.ts:702-706 (approve/reject/cancel)",
   "packages/web/src/i18n/pages/teach.ts:159-161 (teach.pub.done_review, teach.pub.rejected), packages/web/src/i18n/pages/operator.ts:488-506",
   "packages/web/src/components/chat/PublishSheet.tsx:72-77"
  ]
 },
 {
  "id": "AZ-220",
  "persona": "Teach mode (operator)",
  "title": "Payouts to the data provider: the published anchor names the teacher with the node's share, and the operator's Payouts panel is honest about a node with no chain wallet",
  "goal": "The money path must be traceable from the published record to the operator's payout ledger and to the teacher's own earnings page — and where no transfer can happen, the UI must say why rather than show zeros without explanation.",
  "preconditions": [
   "node-u on http://localhost:3422, STUB mode, publish 'auto', local ledger (GET /api/me/payouts → wallet false)",
   "One lesson published by teaching key K1 with the payout radio left on \"This browser's teaching key (0x…)\"",
   "Operator signed in"
  ],
  "steps": [
   "Publish the lesson from /teach/lesson/<jobId> (Name = AZ-220 payout lesson, Price = 1, both consents ticked)",
   "Read the published anchor: GET /api/patches/<patchId> (or the knowledge page /<node address>/<patchId>)",
   "Open /dashboard?tab=teaching and scroll to \"Payouts\"",
   "Open /teacher/<K1 address>",
   "Cross-check GET /api/me/payouts?limit=100 and GET /api/teacher/<K1 address>"
  ],
  "expected": [
   "The Payouts section description reads \"Data-provider shares this node owes from sales of taught lessons. Transfers retry automatically every minute (up to 20 times); Retry sends one now.\" with three tiles labelled Owed / Paid / Failed",
   "Because this node has no chain wallet, the info alert [data-testid=payouts-no-wallet] reads exactly \"This node has no chain wallet (local record), so payouts to visitors are only recorded here and stay pending until it runs on the AI Network.\", and with no sales yet the table shows \"No payouts yet — they appear when a taught lesson sells.\"",
   "/teacher/<K1 address> lists the lesson under \"Lessons\" with its status and the earnings tiles Owed / Paid / Pending / Sales; GET /api/teacher/<K1> returns lessons[] containing the patch id and earnings.{owed,paid,pending,failed,sales} that match the tiles digit for digit",
   "No private draft id ever appears on the public teacher page (a PENDING_REVIEW lesson is referenced by JOB id only)",
   "Cleanup: on the local-ledger dev node only — the announced lesson stays (immutable by design); note it in the run log rather than forcing a delete",
   "The anchor contributor block and its share are asserted in AZ-198; here the anchor is only read to get the address the payout ledger must name."
  ],
  "priority": "P1",
  "area": "dashboard",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/operator/TeachingTab.tsx:317-360 (payouts tiles, payouts-no-wallet, teach-payout rows, payout-retry)",
   "packages/node/src/api.ts:264-271 (GET /api/me/payouts, POST /api/me/payouts/:id/retry)",
   "packages/node/src/teach.ts teacherProfile (owed from settle records, paid from payouts rows; credited address only)",
   "packages/web/src/pages/TeacherPage.tsx:50-101; packages/web/src/i18n/pages/operator.ts:551-570"
  ]
 },
 {
  "id": "AZ-221",
  "persona": "Teach mode (operator)",
  "title": "Teach settings on the Teaching tab: every visible knob saves, a pause reason reaches the visitor immediately, and a bad blocked-topics regex is refused",
  "goal": "The operator's settings form must be the real policy, applied at once and reflected in what visitors read — and must not be able to store a regular expression that would later throw inside the parser.",
  "preconditions": [
   "node-u on http://localhost:3422; operator signed in",
   "RECORD GET /api/me/teach/policy (policy + effective) before touching anything and restore it at the end"
  ],
  "steps": [
   "Open /dashboard?tab=teaching and read the Settings block",
   "Change: Corrections per lesson 8→6, Lessons per key per day 3→4, Queue size 10→12, Days an unsaved lesson is kept 7→5, Data-provider share slider 70→65; press \"Save settings\"",
   "Type into \"Pause reason shown to visitors\": AZ-221 GPU maintenance until Monday ; press \"Save settings\"",
   "In another context open http://localhost:3422/teach and http://localhost:3422/chat?teach=1",
   "Clear the pause reason, put `[` into \"Blocked topics (regular expression, optional)\" and press \"Save settings\"",
   "Restore every recorded value and save"
  ],
  "expected": [
   "The form header shows \"Trainer: ready · backend stub (demo — no real training)\"; the fields present are exactly: Accept lessons from visitors (checkbox), Publish lessons (radios Review each one / Automatically / Never), Corrections per lesson, Lessons per key per day, Lessons per IP per day, Queue size, Days an unsaved lesson is kept, Data-provider share of each sale (slider, %) with helper \"The rest stays with this node for GPU time and hosting.\", Pause reason shown to visitors, Blocked topics (regular expression, optional)",
   "\"Save settings\" is disabled until something changes; after saving, the alert \"Saved.\" appears and GET /api/teach/policy shows limits.facts_per_job 6, limits.jobs_per_key_per_day 4, queue.max 12, draft_ttl_days 5, shares.contributor 0.65 — the visitor policy changed with no restart",
   "With the pause reason set, /teach shows the warning \"Teaching is paused on this node right now. AZ-221 GPU maintenance until Monday\" and the chat basket's policy line shows the same sentence; \"Teach from this dataset (n)\" is disabled and POST /api/teach/jobs returns 503",
   "The invalid regex is refused: the error alert shows \"blocked_topics must be a valid regular expression\" (HTTP 400), the stored policy is unchanged, and no visitor request starts failing",
   "After restore, GET /api/me/teach/policy.effective equals the recorded object field for field"
  ],
  "priority": "P1",
  "area": "dashboard",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/operator/TeachingTab.tsx:139-190 (teach-settings form, teach-save, teach-share, teach-paused, teach-blocked)",
   "packages/node/src/api.ts:663-691 (PATCH /api/me/teach/policy, blocked_topics regex validation, invalidatePolicy)",
   "packages/web/src/i18n/pages/teach.ts:42 (teach.basket.policy_paused); packages/web/src/i18n/pages/operator.ts:457-483"
  ]
 },
 {
  "id": "AZ-222",
  "persona": "Teach mode (operator)",
  "title": "The dataset-era limits are operator-settable only through the API, and the visitor UI obeys them: file size, dataset cap, per-lesson cap and the publish declaration",
  "goal": "Every number the dataset wizard shows must come from the node's policy, not from the bundle — and the operator must be able to move each one, even though the Teaching tab form does not yet expose them.",
  "preconditions": [
   "node-u on http://localhost:3422, STUB mode; operator bearer",
   "RECORD GET /api/me/teach/policy first and restore it at the end",
   "A browser teaching key ready to upload"
  ],
  "steps": [
   "PATCH /api/me/teach/policy (operator bearer) {\"dataset_max_bytes\":1000000, \"dataset_max_rows\":4, \"rows_per_job\":2, \"declaration_rows\":2, \"dataset_ttl_days\":3}",
   "GET /api/teach/policy and compare limits",
   "Open /teach and /teach/upload as a visitor",
   "Upload this exact 5-question file (az222.jsonl):\n{\"prompt\":\"AZ222 fact one?\",\"answer\":\"one\"}\n{\"prompt\":\"AZ222 fact two?\",\"answer\":\"two\"}\n{\"prompt\":\"AZ222 fact three?\",\"answer\":\"three\"}\n{\"prompt\":\"AZ222 fact four?\",\"answer\":\"four\"}\n{\"prompt\":\"AZ222 fact five?\",\"answer\":\"five\"}",
   "Continue to /teach/dataset/<id>/settings and start the lesson; when it is READY open the publish sheet",
   "Restore the recorded policy and delete everything created"
  ],
  "expected": [
   "GET /api/teach/policy limits echo the patch exactly: dataset_max_bytes 1000000, dataset_max_rows 4, rows_per_job 2 with rows_per_job_source no longer \"default\" (an explicit override disables the measured derivation), declaration_rows 2, dataset_ttl_days 3",
   "/teach/upload's drop zone reads \"jsonl, csv, tsv or txt · up to 1 MB\" and /teach's file-door chips read \"jsonl · json · csv · tsv · txt\" and \"up to 4 questions\" — both numbers come from the policy, none are hard-coded",
   "The 5-question upload is accepted with a note: \"That file has 5 questions; this node accepts up to 4 in one dataset. The first 4 were loaded.\" ([data-testid=over-cap-note]) and the dataset holds 4 rows",
   "The dataset page shows the cap banner \"This node teaches up to 2 questions in one lesson. The first 2 are selected; the rest stay in your dataset for the next lesson.\" with a \"Choose which 2\" button; the settings screen shows \"This node teaches up to 2 questions in one lesson, so 2 of your 4 are in this one.\" and its button reads \"Train this lesson (2 questions)\"",
   "With declaration_rows 2 the publish sheet shows a THIRD consent box: \"You are publishing 4 questions. Confirm you have the right to share this data and that it contains no personal information — published lessons cannot be deleted.\" and \"Publish\" stays disabled until all three are ticked",
   "The Teaching tab settings form does NOT contain any of these fields — they are reachable only through PATCH /api/me/teach/policy (recorded as a product gap, not a test failure)",
   "After restore, GET /api/teach/policy limits equal the recorded ones (dataset_max_bytes 4000000, dataset_max_rows 2000, rows_per_job 200 source \"default\", declaration_rows 100, dataset_ttl_days 7)"
  ],
  "priority": "P1",
  "area": "teach",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/api.ts:672-690 (dataset_max_bytes / dataset_max_rows / rows_per_job / declaration_rows / dataset_ttl_days on PATCH /api/me/teach/policy)",
   "packages/node/src/teach.ts:405-415 (policy limits, declaration_rows)",
   "packages/web/src/pages/TeachUploadPage.tsx:60-64 (maxBytes/maxMb from policy), packages/web/src/pages/TeachDatasetPage.tsx:205 (over-cap-note), :216-220 (cap-banner)",
   "packages/web/src/components/chat/PublishSheet.tsx:51-53 + :124-127 (declarationRows → consent-declaration)"
  ]
 },
 {
  "id": "AZ-223",
  "persona": "Visitor (knowledge user)",
  "title": "A runaway answer is cut off with a plain explanation, not shown as an endless loop",
  "goal": "When the model gets stuck repeating itself, the visitor sees the useful part of the answer, one sentence explaining what happened, and can still open the raw text.",
  "preconditions": [
   "Model server available; krx-all-2761 testable on the node",
   "No operator session needed"
  ],
  "steps": [
   "Open http://localhost:3402/chat/krx-all-2761, choose 'After only'",
   "Send the one-character prompt '드' — with the shipped stop sequences the chat path answers normally; confirm the answer does NOT run away and is NOT auto-scored",
   "As the operator, POST /api/runtime/complete {\"prompt\":\"드\",\"max_tokens\":200} — the completion path still degenerates and is where the guard is observable end to end",
   "Repeat that call with {\"raw\": true} to see the same request without the stop sequences and without the guard",
   "Back in the UI, on a bubble that carries the note, click 'Show the raw answer (all {n} characters)'"
  ],
  "expected": [
   "The answer is cut at the point the repetition starts; underneath it a yellow note reads 'The model started repeating itself, so the answer is cut off here — that usually means the question is outside what this knowledge covers.' with 'showing {shown} of {raw} characters'",
   "'Show the raw answer' reveals the model's full output unchanged — nothing is ever deleted, only truncated",
   "POST /api/chat returns patched.truncated = 'repetition' with shown_chars < raw_chars and raw_content carrying the full text",
   "On /api/runtime/complete the guarded call returns truncated='repetition' while the same call with raw:true returns the whole loop, unflagged",
   "The chat path is the quiet one: over PROBE A's 73 prompts re-run through this node, 0 of 73 chat answers ran away while the completion path caught and cut 8. '드' answering normally in the UI is the expected result of this scenario, not a failure of it",
   "A short correct answer (e.g. '087600') is never flagged: truncated is null and no note is shown",
   "Nor is any other complete short answer: a big round number ('1000000000000000'), a 60-dash rule, a dot leader, a markdown table separator row, or a run of ㅋ the visitor asked for",
   "A markdown table, CSV, JSON dump or numbered list that runs out of budget is labelled 'The answer stopped at the length limit before it was finished.' (truncated = 'length', shown_chars = raw_chars) — never 'repetition', and never shortened",
   "The prompt is no longer auto-scored against an unrelated benchmark sample: '드' shows 'Free question — not auto-scored' instead of '✗ Wrong'"
  ],
  "priority": "P1",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/degenerate.ts detectDegenerate()/guardAnswer(); packages/node/test/degenerate.test.ts + fixtures/degenerate-corpus.json (293 real answers) + fixtures/degenerate-legit-adversarial.json (57 legitimate answers written to trip the guard)",
   "packages/node/src/runtime.ts chat()/completeDetailed() sampling + guard; DEFAULT_CHAT_SAMPLING stop ['\\n\\n\\n\\n','<think>']",
   "packages/web/src/components/chat/TurnView.tsx Truncation; packages/web/src/i18n/pages/chat.ts chat.trunc.*"
  ]
 },
 {
  "id": "AZ-224",
  "persona": "Visitor (knowledge user)",
  "title": "A sample question is sent exactly as the knowledge was trained, trailing space included",
  "goal": "Clicking a sample chip asks the model the prompt the knowledge was actually trained on — the marketplace never silently rewrites it.",
  "preconditions": [
   "Model server available; krx-all-2761 testable on the node"
  ],
  "steps": [
   "Open http://localhost:3402/chat/krx-all-2761",
   "Click the chip '종목코드 삼성전자' and look at the input box",
   "Send it"
  ],
  "expected": [
   "The chip label stays trimmed ('종목코드 삼성전자') and carries a small ␣ marker whose tooltip reads 'The trailing space is part of the trained prompt — clicking inserts it, and it is sent, exactly as trained.'",
   "The textarea contains '종목코드 삼성전자 ' with the trailing space, and POST /api/chat sends exactly that string",
   "The turn is auto-scored against its own benchmark sample ('✓ Correct', expected 005930)",
   "Sample chips of a knowledge whose benchmark format has no chat form show 'This knowledge was trained and verified in the completion form … the answer can differ from its verified score.' above the transcript (krx-all-2761-ep6 / -ep12)",
   "Benchmark verification is unaffected: it still sends BenchmarkSpec.samples verbatim through /v1/completions with no stop sequences and no guard"
  ],
  "priority": "P1",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/components/chat/ChatComposer.tsx insert()/submit() (no trim), ␣ marker with aria-label unchanged",
   "packages/node/src/market.ts matchBenchmarkSample() (trimmed equality first, containment only ≥ 8 chars); packages/web/src/components/chat/util.ts matchSample mirror",
   "packages/node/src/runtime.ts verify() — sampling: null on every generation"
  ]
 },
 {
  "id": "AZ-225",
  "persona": "Visitor (knowledge user)",
  "title": "A question asked while another process holds the shared model is queued, not lost, and giving up costs nothing",
  "goal": "When someone else — another visitor, a verifier, another node on the same machine — is using the shared model, the visitor is told they are in line, who is ahead of them and for how long, and can stop waiting without spending a free try.",
  "preconditions": [
   "Model server available; krx-all-2761 testable on node-a",
   "A SECOND process holds the cross-process runtime lease of the same patch mailbox (<runtime.patchDir>/.ainize-runtime.lock with a live holder pid) — the e2e helper holdRuntimeLock() takes it exactly the way a node does"
  ],
  "steps": [
   "Run one live test on http://localhost:3402/chat/krx-all-2761 so the free-trial counter shows a number",
   "From another process, take the shared runtime lease on the same mailbox",
   "Read the knowledge picker",
   "Ask a question while the lease is held",
   "Press 'Stop waiting' and read the free-trial counter",
   "Release the lease and ask the same question again"
  ],
  "expected": [
   "The picker says 'Someone else is testing on the shared model right now.' with 'Another test in progress (chat:krx-all-2761, node process <pid>) — started {n}s ago' and the one-at-a-time explanation — naming the FOREIGN holder, never 'Your test has the shared model' (that line is for this tab's own in-flight request only)",
   "The transcript's own pending turn says 'Queued behind another test — your question has not been lost.' within a couple of seconds, names the holder ('Someone else has the shared model (chat:krx-all-2761, started {n}s ago).') and shows a ticking 'waiting {n}s' counter",
   "The queue position line is shown only when someone is ahead in this node's own queue (alone in line it stays quiet)",
   "The pending row's button reads 'Stop waiting' rather than 'Cancel'",
   "Pressing it returns {cancelled: true, reason: 'queued', charged: false}; the turn says 'You stopped waiting. The node had not started this test yet, so no free try was used.' and offers Retry",
   "The free-trial counter is unchanged — nothing was sent to the model, so nothing was charged (POST /api/chat answers HTTP 499)",
   "Once the holder lets go the banner disappears and the same question is answered normally, charging exactly one free try"
  ],
  "priority": "P1",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "packages/node/src/chat-queue.ts (ticket registry: queued/running/gone, per-visitor, free cancel while queued); packages/node/src/runtime.ts lockHolder()/queueState()/serial() onEnter",
   "packages/node/src/api.ts GET /api/chat/status, POST /api/chat/cancel, quota consumed only after a successful chat",
   "packages/web/src/pages/ChatPage.tsx lockIsMine={queue?.state === 'running'} (RTK Query keeps `data` after a query is skipped, so reading `qs` directly made every later holder read as 'your test'); packages/web/src/components/chat/TurnView.tsx QueuePending; packages/web/src/components/chat/util.ts lockKind()"
  ]
 },
 {
  "id": "AZ-226",
  "persona": "Visitor (knowledge user)",
  "title": "Ask a follow-up in Compare mode and confirm each column replays only its own earlier answers",
  "goal": "The comparison stays a comparison past turn 1: the un-patched column is never told it produced the knowledge's answer, so the 'Before loading' column keeps being wrong and the visitor sees the real difference the knowledge makes.",
  "preconditions": [
   "No operator session; quota not exhausted",
   "Model server available; krx-all-2761 testable on node-a",
   "Browser DevTools open on the Network panel"
  ],
  "steps": [
   "Open http://localhost:3402/chat/krx-all-2761 in the default 'Compare' view",
   "Click the chip '종목코드 픽셀플러스', press Enter and wait for both columns",
   "Type '방금 말한 종목코드를 숫자만 다시 알려줘' and press Enter",
   "Read both columns of the second turn and the line under the transcript",
   "In DevTools open the second POST http://localhost:3402/api/chat request and read its JSON payload"
  ],
  "expected": [
   "Turn 1: 'Before loading' answers with a wrong ticker (✗ Wrong, Expected: 087600) and 'After loading' with 087600 (✓ Correct)",
   "Turn 2's payload carries three arrays: `messages` and `messages_patched` replay the PATCHED answer of turn 1, `messages_base` replays the BASE answer of turn 1, and all three end with the same new question — POST /api/chat rejects (400) a `messages_base`/`messages_patched` whose last message differs from `messages`",
   "The response reports what was replayed: history {base: 3, patched: 3, split: true}",
   "Turn 2's 'Before loading' column repeats its OWN wrong ticker and does not contain 087600 — the un-patched model was never shown the knowledge's answer",
   "Turn 2's 'After loading' column still answers 087600",
   "A line under the transcript says 'On follow-up questions each column replays only its own earlier answers — the \"Before loading\" model is never shown what the knowledge answered.' (ko: '이어지는 질문에서 각 열은 자기가 한 답만 다시 보게 됩니다 …'), shown in Compare mode once a turn has finished"
  ],
  "priority": "P0",
  "area": "chat",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/ChatPage.tsx buildHistory(prior, text, column) + the compare-mode split sent as messages_base / messages_patched",
   "packages/node/src/api.ts POST /api/chat (messages_base / messages_patched, both refined to end with the same message as messages); packages/node/src/market.ts chatInner() msgsBase / msgsPatched and the reported `history`",
   "packages/cli/src/commands/chat.ts chatRepl(): one transcript per column (state.transcript / state.baseTranscript)",
   "packages/web/src/i18n/pages/chat.ts: chat.history.split"
  ]
 },
 {
  "id": "AZ-227",
  "persona": "Node operator / developer",
  "title": "Challenge a listed knowledge and confirm it stops selling until it is re-verified",
  "goal": "A verifier that disputes a result can take the knowledge off sale, everyone can read why, and a re-verification puts it back — instead of the item selling at full price with a red chip.",
  "preconditions": [
   "A PRIVATE three-node cluster on the local ledger (NGRAM_CLUSTER_HOME=<tmp> NGRAM_PORT_BASE=3512 NGRAM_LEDGER=local NGRAM_SEED=0 scripts/cluster-restart.sh). Never the shared demo chain: a challenge record is permanent and would take a demo knowledge off sale for everyone",
   "One knowledge published by node-a and LISTED (2/2) — GET /x402/patch/<id> answers 402",
   "Operator sessions on node-b and node-c"
  ],
  "steps": [
   "GET /x402/patch/<id> on the seller and note the 402",
   "On node-c: `ainize patch challenge <id> --reason \"the ticker codes are wrong\"`",
   "GET /x402/patch/<id> again",
   "On node-b: `ainize patch buy <id>`",
   "Open the knowledge page and the seller's manage page in the browser",
   "Wait for the verifiers' next automatic round (verifier.intervalMs) and repeat the GET"
  ],
  "expected": [
   "Before the challenge the gateway answers 402 with the payment requirements",
   "GET /api/patches/<id> reports status CHALLENGED, quorum_ok true (the quorum is still met) and sellable false, with open_challenge {challenger, reason, created_at}",
   "The gateway answers 423 'a verifier has challenged this knowledge — re-verification pending, so it is not for sale (0x…: \"the ticker codes are wrong\")' — not a discounted 402",
   "`ainize patch buy` refuses with the same sentence and no settlement record is written",
   "The knowledge page shows a warning banner naming the challenger, the reason, when, and what happens next; the Buy tab says 'Challenged knowledge is not on sale at any price — the sale is stopped, not discounted.'; the seller's manage page shows the same banner",
   "`ainize patch get <id>` prints a 'challenges' block with the challenger, reason and which challenge is open",
   "After a verifier re-runs the benchmark (the automatic round now re-verifies a knowledge it has already attested when a challenge is open), the newer attestation replaces its earlier one, the status returns to LISTED, sellable is true again and the gateway answers 402"
  ],
  "priority": "P0",
  "area": "x402",
  "automation": "api",
  "evidence": [
   "packages/core/src/catalog.ts deriveCatalog (sellable, open_challenge, effectiveAttestation)",
   "packages/node/src/api.ts GET /x402/patch/:id (423 + challengedMessage), packages/node/src/market.ts buy()",
   "packages/node/src/verifier.ts round() (re-verify a CHALLENGED anchor it already attested)",
   "packages/web/src/pages/PatchPage.tsx challenge banner + Buy tab, packages/web/src/pages/ManagePage.tsx challenged banner",
   "packages/node/test/trust.test.ts item 153 (the same flow on three nodes, automated)"
  ]
 },
 {
  "id": "AZ-228",
  "persona": "CLI user / node operator",
  "title": "A machine with no node config is told so, instead of being shown whatever answers port 3402",
  "goal": "The first command a newcomer types after installing never reports a stranger's node as theirs, and `login` never claims a node the user did not name.",
  "preconditions": [
   "Demo cluster running (node-a on :3402) — it is the stranger's node this scenario must NOT report",
   "Shell: `export PATH=\"$HOME/.local/node/bin:$PATH\"; cd /mnt/newdata/ainize/knowledge-marketplace; X=\"node packages/cli/dist/bin.js --home /no/such/dir\"`",
   "A throwaway node of your own for step 7: `node packages/cli/dist/bin.js --home $S/t3 init --name t3 --port 3592 --ledger local --runtime-api http://127.0.0.1:1` then `… --home $S/t3 start -d` (never logged in, so it has no operator password)"
  ],
  "steps": [
   "Run `$X status`",
   "Run `$X patch ls`",
   "Run `$X wallet`",
   "Run `$X config show`",
   "Run `$X --node http://localhost:3402 status`",
   "Run `$X teach status http://localhost:3402`",
   "Init a second home `$S/t4` on the same port 3592 as the throwaway node, then run `NGRAM_PASSWORD=hack-me node packages/cli/dist/bin.js --home $S/t4 login`",
   "Repeat step 7 with `--node http://localhost:3592` added"
  ],
  "expected": [
   "Steps 1-3 each fail with `error: no node configured in /no/such/dir — run \\`ainize init\\` to create one, or pass --node <url> to talk to an existing node`, exit code 2; no request is made and nothing about node-a (its name, address, ledger height or catalog) is printed",
   "Step 4 fails with `error: no node config at /no/such/dir/config.json — run \\`ainize init\\` first`, exit code 1 (the local config commands answer for themselves)",
   "Step 5 prints node-a's full status block and exits 0 — an explicitly named node is a target the user aimed",
   "Step 6 prints node-a's teaching policy and exits 0 — `teach status <url>` names its own node, so it needs no config",
   "Step 7 fails with `error: http://localhost:3592 is answered by \"t3\" (0x…), which has no operator password yet — and it is not the node in $S/t4 (0x…). Refusing to claim someone else's node; re-run with --node http://localhost:3592 if that is really what you want.`, exit code 2; GET /api/auth/me on :3592 still reports needsSetup true",
   "Step 8 succeeds: `✓ operator password set and logged in to http://localhost:3592` — claiming a node is allowed only when the user names it"
  ],
  "priority": "P0",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/context.ts buildContext (nodeSource) + requireNodeTarget",
   "packages/cli/src/bin.ts run() (the NoConfig exemptions: init, config, keys, start/stop/seed, chain, logout, `teach status <target>`)",
   "packages/cli/src/commands/auth.ts login() (refuses /api/auth/setup against a node the user did not name)",
   "packages/cli/test/operator.test.ts (nodeSource + requireNodeTarget unit tests)",
   "docs/ux-critique-2.json item 101"
  ]
 },
 {
  "id": "AZ-229",
  "persona": "Node operator",
  "title": "`start -d` on a busy port fails loudly, `status` refuses to pass off the stranger holding it, and `stop` never reports a process it did not stop",
  "goal": "The three commands an operator lives by report what actually happened: a node that died on startup is not announced as started, a health check does not describe someone else's node, and a stop that did not stop is not a tick.",
  "preconditions": [
   "Two throwaway homes of your own under the scratchpad — never the demo cluster: `S=<scratch>`; `N=\"node packages/cli/dist/bin.js\"`",
   "Home A: `$N --home $S/a init --name a --port 3593 --ledger local --runtime-api http://127.0.0.1:1` then `$N --home $S/a start -d`",
   "Home B on the SAME port: `$N --home $S/b init --name b --port 3593 --ledger local --runtime-api http://127.0.0.1:1`"
  ],
  "steps": [
   "Run `$N --home $S/b start -d` (port 3593 is already node a's)",
   "Check whether $S/b/node.pid exists",
   "Run `$N --home $S/b status`",
   "Run `$N --home $S/b stop`",
   "SIGSTOP node a: `kill -STOP $(cat $S/a/node.pid)` — it now ignores SIGTERM",
   "Run `NGRAM_STOP_GRACE_MS=3000 $N --home $S/a stop`",
   "Check the process is gone (`ps -o stat -p <pid>`) and $S/a/node.pid is removed",
   "Run `$N --home $S/a start -d` again",
   "Remove $S/a/node.pid (node a keeps running — a foreground or supervisor-started node has no pid file) and run `$N --home $S/a start -d`; then put the pid file back"
  ],
  "expected": [
   "Step 1 fails before anything is spawned: `error: port 3593 is already answered by \"a\" (0x…), not the node in $S/b — stop that node, or move this one: `ainize config set port <1-65535>``; exit code 1; no green tick, no pid file and no $S/b/node.log. (When the port is held by something that is not an ainize node the child is spawned and its death reported with the log's own reason: `error: node exited while starting (exit code 1) — it is not running.` followed by `$S/b/node.log (last N lines):` and `error: listen EADDRINUSE: address already in use 0.0.0.0:3593`.)",
   "Step 2: $S/b/node.pid does not exist — a pid file is written only for a child that was spawned, and kept only for one that answered",
   "Step 3 prints on stderr, above the block, `! http://localhost:3593 is answered by \"a\" (0x…), not the node in $S/b (0x…) — that node is not running.` with the dim second line `everything below belongs to that other node.`; the block that follows is node a's; exit code 2 (not 0)",
   "Step 4 prints `no background node running for this NGRAM_HOME` and exits 0 — home b really has no node, and the node on the port is not b's, so nothing else is claimed",
   "Step 6 prints `! node <pid> is still running 3 s after SIGTERM — sending SIGKILL` and then `✓ stopped node (pid <pid>) — it ignored SIGTERM, so it was killed`; exit code 0",
   "Step 7: the process is gone and $S/a/node.pid is removed — the pid file survives only a stop that did not confirm the exit, in which case the command exits non-zero with `did not stop (still running after SIGTERM and SIGKILL)`",
   "Step 8 succeeds with `✓ node started in the background (pid N) — port 3593`, printed only after the new node answered /api/info with home a's own address",
   "Step 9 fails with `error: this node is already serving on http://localhost:3593 — it was not started by `ainize start -d` (foreground, or a supervisor), so stop it where it was started before starting it here`; exit code 1; no tick — the start-up probe never mistakes the node already on the port for the child it spawned"
  ],
  "priority": "P0",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/node.ts start() (pre-flight /api/info probe naming whoever holds the port; poll /api/info, identity match, startFailed + logTail), stop() (waitGone, SIGKILL escalation), status() (stranger warning + exit 2)",
   "packages/cli/test/operator.test.ts (items 118/119 unit tests)",
   "docs/ux-critique-2.json items 118, 119"
  ]
 },
 {
  "id": "AZ-230",
  "persona": "Node operator",
  "title": "`config set` validates against the config schema, `config get`/`unset` exist, and a node refuses to boot on a config it cannot use",
  "goal": "The CLI's only configuration verb can tell an operator whether the thing they just set is real, and a mistyped value never produces a running node bound nowhere findable.",
  "preconditions": [
   "A throwaway home of your own: `$N --home $S/c init --name c --port 3594 --ledger local --runtime-api http://127.0.0.1:1` (never the demo cluster)"
  ],
  "steps": [
   "Run `$N --home $S/c config set port notanumber`",
   "Run `$N --home $S/c config set verifier.stak 5`, `… market.defaultprice 0.5`, `… ledger.knid ain`",
   "Run `$N --home $S/c config set typo.that.does.not.exist hello`",
   "Run `$N --home $S/c config set host 999.999.999.999`, `… roles admin`, `… verifier.quorum -3`, `… market.royaltyShare 47`",
   "Run `$N --home $S/c config set identity.privateKey dead` and `… config set market {}`",
   "Run `$N --home $S/c config set market.defaultPrice 9.99` and read the value back with `$N --home $S/c config get market.defaultPrice`",
   "Run `$N --home $S/c config set teach.trainer.gpus 0,1` then `$N --home $S/c config unset teach.trainer.gpus`",
   "Hand-edit config.json to `\"port\": \"notanumber\"` and `\"roles\": [\"admin\"]`, then run `$N --home $S/c start`",
   "With config.json still holding `\"port\": \"notanumber\"`, run `$N --home $S/c config show`, `$N --home $S/c status` and `$N --home $S/c config set port 3594`",
   "Hand-edit config.json to add `\"strayKey\": 1`, start the node and run `$N --home $S/c logs --kind config`"
  ],
  "expected": [
   "Step 1 fails with `error: port must be a number — got \"notanumber\"`, exit 1; config.json is unchanged",
   "Step 2 fails with the nearest real key each time: `error: unknown config key 'verifier.stak' — did you mean 'verifier.stake'?`, `… 'market.defaultprice' — did you mean 'market.defaultPrice'?`, `… 'ledger.knid' — did you mean 'ledger.kind'?`; exit 1",
   "Step 3 fails with `error: unknown config key 'typo.that.does.not.exist'; \\`ainize config show\\` lists every key this node has`; no key is created",
   "Step 4 fails with, respectively, `host must be an interface to bind: an IP address (0.0.0.0, 127.0.0.1, ::) or a hostname`, `roles must be a comma list of 'seller', 'verifier', 'serving', 'gateway'`, `verifier.quorum must be at least 1`, `market.royaltyShare must be a fraction between 0 and 1` — each naming what the key wants and what it got",
   "Step 5 fails with `refusing to set identity.privateKey: the identity is this node's only key pair — see \\`ainize keys\\`` and `market is a group of keys, not a value — set one of: market.currency, market.defaultPrice, market.royaltyShare, market.initialCredit`",
   "Step 6 prints `✓ market.defaultPrice = \"9.99\"  (the node reads config.json when it starts)` — a price is stored as the decimal STRING the rest of the product uses, never a JSON number — and `config get` prints `9.99`",
   "Step 7: `config unset` on a key the config cannot do without resets it to the built-in default (`✓ teach.trainer.gpus reset to the default \"4,5,6\" (was \"0,1\"; it cannot be absent)`); on an optional key it removes it and says the node falls back to its default",
   "Step 8: the node refuses to start — `error: this node's config is not usable:` then one line per key (`port must be a number`, `roles.0 must be a comma list of …`) and the path of the config.json to fix; exit 1; nothing binds a port",
   "Step 9: `config show` prints the file (exit 0); `status` fails with `error: port in $S/c/config.json is \"notanumber\", not a port number — fix it with \\`ainize config set port <1-65535>\\`` (exit 2) — never `--node must be a full URL`, a flag nobody passed; and `config set port 3594` succeeds: the fix the refusal names is never itself locked out",
   "Step 10: the node starts (a key this build does not know must not stop an older binary from booting) and logs one `warn config strayKey: unknown config key` line"
  ],
  "priority": "P0",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/core/src/config-schema.ts (nodeConfigSchema, configField, nearestConfigKey, coerceConfigValue, validateConfig)",
   "packages/cli/src/commands/init.ts configSet / configGet / configUnset",
   "packages/node/src/server.ts startNode() (refuses invalid values, warns about unknown keys)",
   "packages/core/test/config.test.ts + packages/cli/test/operator.test.ts (item 123)",
   "docs/ux-critique-2.json item 123",
   "packages/cli/src/context.ts buildContext() (nodeUrlProblem: a broken config port is named by the commands that need a node, not reported as a --node typo)"
  ]
 },
 {
  "id": "AZ-231",
  "persona": "Node operator",
  "title": "A `config set` made while the node runs survives the next console save, and says it needs a restart",
  "goal": "The documented way of configuring a node is not a coin flip: an edit is never silently reverted by someone clicking in the console, and the operator is told the running node still uses the old value.",
  "preconditions": [
   "A throwaway node of your own, started and logged in: `$N --home $S/m init --name m --port 3595 --ledger local --runtime-api http://127.0.0.1:1`, `$N --home $S/m start -d`, `NGRAM_PASSWORD=m-pass-1234 $N --home $S/m login`"
  ],
  "steps": [
   "Note `market.defaultPrice` in $S/m/config.json (0.1)",
   "Run `$N --home $S/m config set market.defaultPrice 9.99`",
   "Read config.json again",
   "Make the console save the config: `curl -s -X POST http://localhost:3595/api/peers -H \"authorization: Bearer <operator token>\" -H 'content-type: application/json' -d '{\"endpoint\":\"http://localhost:9999\"}'`",
   "Read config.json a third time",
   "Remove the peer again: `curl -s -X DELETE http://localhost:3595/api/peers -H \"authorization: Bearer <operator token>\" -H 'content-type: application/json' -d '{\"endpoint\":\"http://localhost:9999\"}'`, then read config.json a fourth time"
  ],
  "expected": [
   "Step 2 prints `✓ market.defaultPrice = \"9.99\"  (the node reads config.json when it starts)` on stdout and, on stderr, `! the node in $S/m is running (pid N) and keeps using the value it started with — restart it to apply this (\\`ainize stop\\` then \\`ainize start -d\\`)`",
   "Step 3: config.json holds \"9.99\"",
   "Step 4 answers {\"ok\":true} — the peer is added",
   "Step 5: config.json still holds \"9.99\" AND the new peer — the node writes only the keys it changed itself (its password, its peers, its display name) on top of what is on disk now, instead of dumping its start-up snapshot over the file",
   "Step 6: the peer is gone from config.json and \"9.99\" is still there — the node diffs against what it LAST WROTE, not its boot snapshot, so an add followed by a remove reaches the file as a remove (diffed against boot it looked like \"no change\" and the earlier save, peer included, stood)"
  ],
  "priority": "P0",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/core/src/config.ts mergeConfigChanges()",
   "packages/node/src/server.ts persistConfig() (last-saved → live diff applied to the current file)",
   "packages/cli/src/commands/init.ts configSet() (runningHere warning)",
   "docs/ux-critique-2.json item 124",
   "packages/node/test/config-persist.test.ts"
  ]
 },
 {
  "id": "AZ-232",
  "persona": "Node operator",
  "title": "The version a node reports is the build it is running, not a string frozen into config.json at init",
  "goal": "After an upgrade, the CLI, the API, the console and every peer agree on which build a node is running — and a config written a year ago cannot make it claim last year's version.",
  "preconditions": [
   "A throwaway node of your own: `$N --home $S/v init --name v --port 3596 --ledger local --runtime-api http://127.0.0.1:1`"
  ],
  "steps": [
   "Run `$N --home $S/v config set version 0.0.1-from-2024`",
   "Run `$N --home $S/v start -d` and then `$N --home $S/v status`",
   "Run `curl -s http://localhost:3596/api/info | python3 -m json.tool` and read node.version / node.build / node.config_version",
   "Run `$N --home $S/v logs --kind config --limit 3`",
   "Run `$N --home ~/.ngram-cluster/node-a status` (a node whose config.json version matches the build)"
  ],
  "expected": [
   "Step 1 succeeds — `version` is a real config key (the schema version config.json was written by)",
   "Step 2 prints `version     0.1.0 · built <YYYY-MM-DD HH:MM:SS>  (config.json written by 0.0.1-from-2024)`: the running build first, the config's claim named as the config's claim",
   "Step 3: `node.version` is the VERSION constant of the running code (0.1.0), `node.build` is an ISO timestamp measured from the running files, `node.config_version` is \"0.0.1-from-2024\" — so every peer and the generated OpenAPI document report the build, not the file — measured once, when the node loaded its code: rebuilding dist under a running node does not change what that node reports until it restarts",
   "Step 4 shows one info line `config: config.json was written by version 0.0.1-from-2024; this node is running 0.1.0` — the hook a future config migration hangs on",
   "Step 5 prints `version     0.1.0 · built <…>` with no config note, because there is nothing to say"
  ],
  "priority": "P1",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/core/src/config.ts VERSION + buildStamp()",
   "packages/node/src/market.ts selfInfo() (version/build/config_version), packages/node/src/api.ts (OpenAPI version)",
   "packages/cli/src/commands/node.ts nodeVersion()",
   "packages/node/src/server.ts (start-up line when the two differ)",
   "docs/ux-critique-2.json item 141",
   "packages/core/test/config.test.ts (buildStamp measured once)"
  ]
 },
 {
  "id": "AZ-233",
  "persona": "Node operator",
  "title": "Health checks that can fail: `/healthz`, `/readyz` and `ainize status --check`",
  "goal": "An operator can wire the node into an uptime check, a Kubernetes probe or a deploy script and have it go red when the node cannot do its job — instead of a permanently green 200 from the web app.",
  "preconditions": [
   "Demo cluster running (node-a on :3402, model server up)",
   "A throwaway node of your own with an unreachable runtime: `$N --home $S/h init --name h --port 3597 --ledger local --runtime-api http://127.0.0.1:1`, `$N --home $S/h start -d`"
  ],
  "steps": [
   "Run `curl -s -o /dev/null -w '%{http_code} %{content_type}\\n' http://localhost:3402/healthz` and the same for /readyz, /status, /metrics and /this-node-is-on-fire",
   "Run `curl -s http://localhost:3402/readyz | python3 -m json.tool`",
   "Run `ainize --home ~/.ngram-cluster/node-a status --check`",
   "Run `curl -s -o /dev/null -w '%{http_code}\\n' http://localhost:3597/readyz` (the node whose model server is unreachable) and read the body",
   "Run `$N --home $S/h status --check`",
   "Stop that node, run `$N --home $S/h config set roles seller`, start it again and repeat `status --check`"
  ],
  "expected": [
   "Step 1: `/healthz` and `/readyz` answer `200 application/json`; `/status` and `/metrics` answer `404 application/json` with `{\"error\":\"not found\",\"hint\":\"health checks: /healthz (the process is alive) and /readyz (ledger + runtime); /api/info for detail\"}`; an ordinary unknown path (`/this-node-is-on-fire`) is still the web app's own 404 page (200 text/html), because that is a person's typo, not a monitor",
   "Step 2: `{ok:true, node:\"node-a\", address, version, checks:{ledger:{ok,kind,height,records}, runtime:{ok,required,available,model}, peers:{ok,configured,unreachable}}}`",
   "Step 3 prints `✓ node-a  http://localhost:3402` with `ledger ok · ain · height <h>`, `runtime ok · Qwen3.8-Flash-Next`, `peers 2 configured`; exit 0",
   "Step 4: HTTP 503, with `checks.runtime` = `{ok:false, required:true, available:false, model:null, error:\"serving API unreachable\"}` while `checks.ledger.ok` is true",
   "Step 5 prints `✗ h  http://localhost:3597  NOT READY` and `runtime  serving API unreachable`; exit code 1 — the failure a monitor must see",
   "Step 6: with roles `seller` only, the runtime is not required — `/readyz` is 200 again and `status --check` prints `runtime  not required by this node's roles` and exits 0"
  ],
  "priority": "P1",
  "area": "api",
  "automation": "cli",
  "evidence": [
   "packages/node/src/server.ts (/healthz, /readyz, the probe-path 404 before the SPA fallback)",
   "packages/cli/src/commands/node.ts statusCheck()",
   "packages/node/src/openapi.ts (/healthz + /readyz documented), deploy/README.md §4",
   "docs/ux-critique-2.json item 134"
  ]
 },
 {
  "id": "AZ-234",
  "persona": "Node operator",
  "title": "The node identity survives `init --force`, and replacing it takes a typed confirmation and leaves a backup",
  "goal": "The remedy the CLI's own error recommends cannot destroy the key that owns everything the node published, and an operator who really wants a new identity has to say so in words.",
  "preconditions": [
   "A throwaway home of your own: `$N --home $S/f init --name critic-n1 --port 3577 --ledger local --runtime-api http://127.0.0.1:1`",
   "An operator password on it (or edit operatorPasswordHash into config.json) so you can see it survive"
  ],
  "steps": [
   "Run `$N --home $S/f init --name x` (no --force)",
   "Run `$N --home $S/f init --force --name renamed` and compare address, operatorPasswordHash and the files in $S/f",
   "Run `printf 'nope\\n' | $N --home $S/f init --force --new-identity`",
   "Run `printf '<the current address>\\n' | $N --home $S/f init --force --new-identity`",
   "Run `$N --home $S/f keys backup $S/key.json --passphrase hunter2`, then `$N --home $S/f keys backup $S/key.json` again",
   "On a second home: `$N --home $S/g init --name g --port 3599 --ledger local`, then `printf '<g's address>\\n' | $N --home $S/g keys import $S/key.json --passphrase hunter2`",
   "Run `printf '<the new address>\\n' | $N --home $S/g keys rotate`",
   "Run `$N --home $S/g keys show --reveal` with no answer on stdin"
  ],
  "expected": [
   "Step 1 fails with ``error: config already exists at $S/f/config.json — change one setting with `ainize config set <key> <value>`; `--force` rewrites the file (keeping this node's identity)`` — the error names the safe path first",
   "Step 2 succeeds and prints `keeping this node's identity 0x… (pass --new-identity to replace it)` and `previous config saved as $S/f/config.json.bak-<timestamp>`: the address is unchanged, operatorPasswordHash is unchanged, and the old file is on disk (mode 0600)",
   "Step 3 prints the warning block (`! this replaces the node identity 0x…`, what it costs) and the prompt `Type the current address to replace it (0x…): `, then fails with `error: that is not this node's address — nothing was changed`; the address in config.json is unchanged",
   "Step 4 succeeds: a new address, another config.json.bak-<timestamp>",
   "Step 5 writes `{kind:\"ainize-node-key\", version:1, address, publicKey, node, created_at, cipher:{alg:\"aes-256-gcm\", kdf:\"scrypt\"}}` at mode 0600 with no plaintext key, and says the file is useless without the passphrase; the second call is refused with `already exists — pick another name (or pass --force to overwrite it)`",
   "Step 6 asks for g's current address, then prints `✓ this node is now 0x…` — the backed-up identity, with g's port and every other setting untouched; a wrong passphrase gives `error: wrong passphrase for this backup` and changes nothing",
   "Step 7 asks for the address again and prints `✓ new identity 0x… (was 0x…; previous config saved as …; restart the node to apply)`",
   "Step 8: `keys show --reveal` warns that the key will be in the scrollback and asks you to type \"show\"; with no answer it exits 1 with `error: cancelled — nothing was printed` and prints no key. `--yes` skips the question for scripts"
  ],
  "priority": "P0",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/cli/src/commands/init.ts init() (identity carried forward, backupConfig, --new-identity confirmation), keysBackup/keysImport/keysRotate/keysShow",
   "packages/cli/test/operator.test.ts (items 120/122)",
   "packages/node/src/openapi.ts CLI_REFERENCE group 'Your node: identity, config, backups'; deploy/README.md §5; README.md Run",
   "docs/ux-critique-2.json items 120, 122"
  ]
 },
 {
  "id": "AZ-235",
  "persona": "Knowledge publisher",
  "title": "`patch forget` shows the blast radius before deleting, not after",
  "goal": "Withdrawing one version never silently pulls the knowledge file out from under every other version and draft built from the same training output.",
  "preconditions": [
   "A PRIVATE node of your own (never the demo cluster: forgetting a demo body would un-serve it for everyone): `$N --home $S/fg init --name fg --port 3601 --ledger local --runtime-api http://127.0.0.1:1`, quorum 1 + allowSelfAttest in config.json, `start -d`, `login`",
   "One .npz published twice under different ids and benchmark schemas — the normal case, since v1/v2/v3 of a knowledge are usually the same file re-announced: `adv-v1` and `adv-share`"
  ],
  "steps": [
   "Run `$N --home $S/fg patch forget adv-v1`",
   "Run `$N --home $S/fg patch get adv-v1` and check `body on this node`",
   "Run `$N --home $S/fg patch forget adv-v1 --all-sharing`",
   "Run `$N --home $S/fg patch get adv-share` and check `body on this node`",
   "Run `curl -s -X POST http://localhost:3601/api/patches/adv-v1/forget -H \"authorization: Bearer <operator token>\"` on a body another item shares"
  ],
  "expected": [
   "Step 1 fails (exit 1) with `error: adv-v1 shares its knowledge file with 1 other item(s) on this node — forgetting it stops serving them too:` followed by a table `ALSO STOPS SERVING / NAME / STATUS / SALES` listing `adv-share  adv share  ANNOUNCED  0`, and the line `the public record is untouched either way. To stop serving all of them: `ainize patch forget adv-v1 --all-sharing``",
   "Step 2: `body on this node  yes` — the refusal deleted nothing",
   "Step 3 succeeds: `✓ forgot adv-v1 body 62d0978ccbf0… — file left in place, no longer served from this node` followed by `! same body as adv-share — those are no longer served from here either`",
   "Step 4: `body on this node  no` — the operator asked for exactly this",
   "Step 5: HTTP 409 with `{error: \"… shares its knowledge file with …\", also_affects:[{id,name,status,sales}], sha256}`; passing `{\"all_sharing\": true}` performs the deletion. A body no other item shares still needs no flag"
  ],
  "priority": "P0",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/node/src/market.ts forgetBody() (also_affects resolved and refused BEFORE blobs.remove)",
   "packages/node/src/api.ts POST /api/patches/:id/forget (all_sharing), ConflictError details in the 409 body",
   "packages/cli/src/commands/patch.ts patchForget() (--all-sharing, the blast-radius table)",
   "packages/cli/test/cli.test.ts (item 149)",
   "docs/ux-critique-2.json item 149"
  ]
 },
 {
  "id": "AZ-236",
  "persona": "Dataset uploader (visitor)",
  "title": "/teach trust strip: at the moment of choosing a door the visitor can see what stays private, who can see it, where the key lives and that publishing is a separate step — and reach the full terms",
  "goal": "Trust is visible at the entry point (ux-critique-owner O-10) and says only what the code makes true: private by default, publishing is the visitor's own explicit step, the teaching key never leaves the browser, and the node operator CAN see private drafts.",
  "preconditions": [
   "node-u on http://localhost:3422 with teach.enabled true (backend 'stub'; nothing here calls the model).",
   "Fresh browser context, locale en-US; run at 1280 px and at 360 px (Pixel 5)."
  ],
  "steps": [
   "Open http://localhost:3422/teach and wait for [data-testid=teach-entry].",
   "Read the strip directly under the two door cards; open 'What this means'; press 'Full terms →'.",
   "Toggle the language to 한국어 on /teach and read the strip again."
  ],
  "expected": [
   "<section data-testid=trust-strip aria-label='Trust and privacy'> sits directly below the doors (its top edge is below the file card's bottom edge) and above the 'What happens next' sentence, and lists exactly four facts in this order: 'Private by default' · 'You choose what to publish' · 'Your teaching key stays in this browser' · 'The node operator can see your drafts' ([data-testid=trust-private|trust-publish|trust-key|trust-operator]).",
   "A collapsed native <details data-testid=trust-detail> 'What this means' opens to four definitions: 'What you teach stays a private draft on this node until you publish it. You can delete a draft any time from My datasets and lessons.' / 'Publishing is a separate step you take yourself, with your key's signature and your consent. Once published, a lesson is a permanent public record and cannot be deleted.' / 'The key that signs your lessons and receives your share is created and kept in this browser. The node only ever sees its public address — download a backup from Your knowledge.' / 'This page is served by one node. Its operator can read the questions, answers and files you store here for as long as they exist — private means private from everyone else, not from the operator. Do not upload personal data or anything you are not allowed to share.'",
   "The strip never claims what the code does not do: no 'encrypted', no 'anonymous', no 'nobody can see'.",
   "[data-testid=trust-terms] 'Full terms →' is a link to /terms#teaching; following it lands on the Terms page with the heading [data-testid=terms-teaching] '3.5 What a node stores when you teach it' (id='teaching') scrolled into view, followed by the four paragraphs on storage and deletion, operator visibility, the key, and publishing.",
   "In 한국어 the four facts read '기본은 비공개' · '공개 여부는 내가 정합니다' · '가르치기 키는 이 브라우저에만 있습니다' · '노드 운영자는 초안을 볼 수 있습니다' and the link reads '전체 약관 →'.",
   "At 360 px the strip wraps onto several lines inside the viewport (scrollWidth ≤ 360) and the definitions stack term-over-definition."
  ],
  "priority": "P1",
  "area": "teach-ui",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachPage.tsx (Trust, TRUST, trust-strip / trust-detail / trust-terms)",
   "packages/web/src/i18n/pages/teach.ts (teach.trust.*), i18n/pages/public.ts (terms.s3.h5 / terms.s3.p5)",
   "packages/web/src/pages/TermsPage.tsx (#teaching, hash scroll after ScrollToTop)",
   "packages/node/src/teach.ts (announceJob consent gate: only an owner-published lesson is ever announced), packages/web/src/lib/teacherKey.ts (KEY_STORAGE in localStorage), teach.ts 'teach.keep.node_body' (Not private from the operator)",
   "packages/e2e/tests/web-ds-upload.spec.ts (AZ-236, projects web + mobile)"
  ]
 },
 {
  "id": "AZ-237",
  "persona": "Dataset uploader (visitor)",
  "title": "/teach hierarchy: the conversation door is the one primary action and the file door is clearly secondary, at 1280 and 360 px",
  "goal": "A first-time visitor can tell at a glance what to try first (ux-critique-owner O-1): one door carries the primary weight in position, size and colour, the other is visibly the alternative — and stays one click away.",
  "preconditions": [
   "node-u on http://localhost:3422 with teach.enabled true (backend 'stub'; nothing here calls the model).",
   "Fresh browser context, locale en-US; run once at 1280 px and once at 360 px (Pixel 5)."
  ],
  "steps": [
   "Open http://localhost:3422/teach and wait for [data-testid=teach-entry].",
   "Compare the two door cards: document order, border, background, heading size, button style and box width.",
   "Press the secondary door."
  ],
  "expected": [
   "Order: [data-testid=door-chat] (the card link) precedes [data-testid=door-file] in the DOM — the primary door is what a screen reader and the Tab key reach first.",
   "Weight: the chat card has a 2 px #8b3eeb border and the tinted #f5eefc background; the file card a 1 px #dadada border on white. The chat heading is 22 px, the file heading 16 px.",
   "Labels (visual, inside the card links — O-9): [data-testid=door-chat-cta] is contained (white text on #8b3eeb); [data-testid=door-file-cta] is outlined (#8b3eeb text on a transparent background).",
   "Size and position: at 1280 px the chat card is at least 1.3× wider than the file card (3:2 grid); at 360 px both cards are full width, the chat card sits above the file card, and the page does not scroll horizontally (scrollWidth ≤ 360).",
   "The secondary door still works with one click: clicking the file card's top-left corner (not its label) lands on /teach/upload."
  ],
  "priority": "P1",
  "area": "teach-ui",
  "automation": "e2e",
  "evidence": [
   "packages/web/src/pages/TeachPage.tsx (Doors 3fr 2fr grid, Door = styled(Link) $primary, Cta, Eyebrow)",
   "packages/web/src/i18n/pages/teach.ts (teach.entry.file.eyebrow)",
   "docs/teachable-dataset-design.md §5.2 (the primary-door decision)",
   "packages/e2e/tests/web-ds-upload.spec.ts (AZ-237, projects web + mobile)"
  ]
 },
 {
  "id": "AZ-238",
  "persona": "Teach mode (visitor)",
  "title": "Teach on top of someone else's knowledge: the job carries the base stack, the exported file is a real delta, and the record says what it was built on",
  "goal": "The owner's question — \"why can I not teach ON TOP of someone else's knowledge?\" — is answered by the pipeline, not by a label: the base's questions are loaded as known answers, the trainer is told which knowledge to load first, the exported rows start from the base's values, and the anchor records the base for good (lineage design §3 story A, §5.1, §7.1).",
  "preconditions": [
   "node-u on http://localhost:3422 (local ledger, teach backend 'stub', teach.lineage true) — or the in-process node the suite starts.",
   "A published knowledge whose training set access is 'derivative' (the base), and a second teaching key with its own two-question dataset."
  ],
  "steps": [
   "POST /api/teach/jobs { dataset_id, base_ids: [<base>] } with the second key.",
   "Wait for READY, then read GET /api/teach/jobs/<id>, <job>/job.json, <job>/known.jsonl and <job>/lesson.npz.",
   "Publish it and read the anchor back from GET /api/patches/<child>."
  ],
  "expected": [
   "The job carries `bases` (ordered, ancestors first), `mode: 'extend'` and `export: 'delta'`.",
   "job.json carries `parents[{patch_id, sha256, npz}]` (the base body is copied next to the job), `known_file: 'known.jsonl'`, `max_known`, `export: 'delta'` and `probe_with_parents: true` — the §7.1 trainer contract.",
   "known.jsonl holds the base's questions, so the lesson is trained without undoing them.",
   "In lesson.npz, every address the child shares with the base has `before` == the base's `after` (bf16-exact); a fresh address starts from the disk base. A `meta` member names the export, the base stack and `pre_state_sha256`.",
   "The anchor lists the whole stack in `parents[]`, `derivation: {kind:'extend', bases:[{patch_id, patch_sha256, dataset_sha256, rows}], added_rows}`, `base: {stack, export:'delta', pre_state_sha256}` (the same hash as the npz meta) and `dataset.parents`.",
   "GET /api/patches/<child> answers `requires: [{ id: <base>, name, held, price }]` — a buyer is told what else is needed."
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/test/lineage.test.ts (AZ-238)",
   "packages/node/src/teach.ts (resolveBases, stackFiles, writeStubNpz, lineageFields)",
   "docs/lineage-teach-design.md §3 story A, §5.1, §7.1"
  ]
 },
 {
  "id": "AZ-239",
  "persona": "Teach mode (visitor)",
  "title": "A taught anchor carries at most 32 questions on the ledger, and the hash on the record is recomputable from the list published with the training set",
  "goal": "The ledger stays small (F10: ~100 KB AIN free tier) without the cap becoming a way to hide what was trained: `answers_hash` commits to the full list, and the full list is served with the training set (§5.1, §5.2).",
  "preconditions": [
   "node-u on http://localhost:3422 (local ledger, teach backend 'stub', teach.lineage true) — or the in-process node the suite starts.",
   "A 40-question dataset."
  ],
  "steps": [
   "Train it, publish with dataset access 'public'.",
   "Read the anchor, then <dataDir>/blobs/datasets/<sha>/benchmark.jsonl and manifest.json."
  ],
  "expected": [
   "`benchmark.samples` has exactly 32 entries and they are the first 32 of the full list.",
   "`benchmark.answers_hash` == sha256 over the canonical FULL list (42 entries here), recomputable by anyone who fetches benchmark.jsonl.",
   "manifest.json reports `benchmark_samples` (the full count), `rows`, the chosen access and licence, and `sha256` equal to the anchor's.",
   "rows.jsonl hashes to `dataset.sha256` on the record."
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/test/lineage.test.ts (AZ-239)",
   "packages/core/src/lineage.ts (capBenchmarkSamples, answersHash)",
   "packages/node/src/dataset-blobs.ts (encodeBenchmarkJsonl)",
   "docs/lineage-teach-design.md §5.1"
  ]
 },
 {
  "id": "AZ-240",
  "persona": "Teach mode (visitor)",
  "title": "The training set is served by the access its creator chose: public downloads, 'derivative' needs a key and a derive token, private is refused with the reason",
  "goal": "Inheritance is impossible without content, and content must not leak past what the creator chose (§6.1, §6.6). Each level behaves exactly as the publish sheet promised.",
  "preconditions": [
   "node-u on http://localhost:3422 (local ledger, teach backend 'stub', teach.lineage true) — or the in-process node the suite starts.",
   "Three published knowledges with access public / derivative / private."
  ],
  "steps": [
   "GET /api/patches/<id>/dataset unsigned and signed with a stranger's teaching key, for each level.",
   "GET /api/patches/<id>/dataset/rows for each level.",
   "POST /api/patches/<derivative>/derive-intent, then GET /p2p/dataset/<sha> with and without the token.",
   "GET /p2p/datasets and the node's PeerInfo."
  ],
  "expected": [
   "public: any caller, signed or not, gets the summary and a 20-question preview; /dataset/rows returns ndjson whose sha256 is the one on the record.",
   "derivative: an unsigned request is refused `dataset_derivative_only` with what to do next; a signed key gets the summary and preview; /dataset/rows still refuses — the bytes come through the derive path.",
   "private: refused `dataset_private` for everyone but the credited teaching key and the operator; derive-intent is refused too.",
   "The derive intent returns a token; /p2p/dataset/<sha> serves the bytes with it and refuses without it. The private set is never served over /p2p.",
   "/p2p/datasets lists the sets this node holds with their access, and PeerInfo.datasets advertises them."
  ],
  "priority": "P0",
  "area": "teach-dataset",
  "automation": "api",
  "evidence": [
   "packages/node/test/lineage.test.ts (AZ-240)",
   "packages/node/src/api.ts (datasetOf, /p2p/dataset)",
   "packages/node/src/market.ts (mayReadDataset, deriveIntent)",
   "docs/lineage-teach-design.md §6.1, §6.6, §12.3"
  ]
 },
 {
  "id": "AZ-241",
  "persona": "Teach mode (visitor)",
  "title": "A base whose questions are private cannot be built on, and the whole feature is behind the teach.lineage flag",
  "goal": "Nothing is dropped silently (§1 goal 9): a base that cannot be built on says so at the earliest moment, and a node that has not enabled lineage refuses before any quota is charged (§18 gating).",
  "preconditions": [
   "node-u on http://localhost:3422 (local ledger, teach backend 'stub', teach.lineage true) — or the in-process node the suite starts.",
   "A published knowledge whose training set is private, plus the visitor's own dataset."
  ],
  "steps": [
   "POST /api/teach/jobs with base_ids naming the private knowledge, an unknown id, and two ids.",
   "Set teach.lineage false and repeat; read GET /api/teach/policy."
  ],
  "expected": [
   "`base_private` names the knowledge and says the creator kept the questions private (and that it can still be loaded for comparison).",
   "`base_unknown` for an id this node does not have; `merge_not_available` for two bases until the merge PR lands.",
   "With the flag off: 403 `lineage_disabled`, and GET /api/teach/policy reports `lineage: false` so the UI can hide 'Build on this'.",
   "No job row, no quota charge, no files on disk for any refusal."
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/test/lineage.test.ts (AZ-241)",
   "packages/node/src/teach.ts (createJob, resolveBases)",
   "docs/lineage-teach-design.md §12.1, §18"
  ]
 },
 {
  "id": "AZ-242",
  "persona": "Teach mode (visitor)",
  "title": "Licences travel down the family tree: an unknown one is refused, a CC-BY-SA base forces the child, and a Proprietary base publishes no inherited questions",
  "goal": "A creator cannot accidentally re-licence someone else's questions, and a proprietary base can still be built on without its rows leaking (§6.4).",
  "preconditions": [
   "node-u on http://localhost:3422 (local ledger, teach backend 'stub', teach.lineage true) — or the in-process node the suite starts.",
   "A base published CC-BY-SA-4.0 (derivative), and another published Proprietary (derivative)."
  ],
  "steps": [
   "Train on top of the CC-BY-SA base and publish with licence NOT-A-LICENCE, then CC-BY-4.0, then CC-BY-SA-4.0.",
   "Train on top of the Proprietary base and read the child's on-chain samples."
  ],
  "expected": [
   "`bad_license` names the five licences this network knows.",
   "`license_incompatible` names the parent and its licence; the child publishes only as CC-BY-SA-4.0, with access at least the parent's.",
   "A child of a Proprietary base publishes: no benchmark sample carries that base as `source`, and the base is still in `parents[]` so it is credited and paid."
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/test/lineage.test.ts (AZ-242)",
   "packages/core/src/lineage.ts (licenseCompatible, deltaOnlyParent)",
   "packages/node/src/teach.ts (datasetPublication)",
   "docs/lineage-teach-design.md §6.4"
  ]
 },
 {
  "id": "AZ-243",
  "persona": "Teach mode (visitor)",
  "title": "Personal information blocks a shared training set (never the private one), and a big set cannot be published without saying where it came from",
  "goal": "Sharing questions widens exposure, so the server — not the browser — checks it (§6.5): PII rows are named so they can be removed, and a set at or above the declaration threshold states its source.",
  "preconditions": [
   "node-u on http://localhost:3422 (local ledger, teach backend 'stub', teach.lineage true) — or the in-process node the suite starts.",
   "A dataset with an e-mail in one row, and a 100-question dataset."
  ],
  "steps": [
   "Train the PII dataset; publish with access 'derivative', then with access 'private'.",
   "Train the 100-question dataset; publish without a declaration, then with one."
  ],
  "expected": [
   "`dataset_pii` names the row numbers and the kinds found; the rows still trained (nothing was dropped behind the visitor's back).",
   "The same lesson publishes with access 'private', and the manifest records `pii_scan.ok: false`.",
   "`dataset_declaration` names the count and the threshold; publishing with `declaration: {source, no_pii}` succeeds and the manifest keeps it.",
   "A publish with either consent box off is refused `consent_required` (never assumed)."
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/test/lineage.test.ts (AZ-243)",
   "packages/node/src/teach.ts (datasetPublication)",
   "packages/node/src/teach-dataset.ts (detectPii)",
   "docs/lineage-teach-design.md §6.5"
  ]
 },
 {
  "id": "AZ-244",
  "persona": "Teach mode (visitor)",
  "title": "Announce-time validation: a base that is still a draft blocks publishing, and no lineage field may name a knowledge that is not a parent",
  "goal": "A creator may build on their own unpublished lesson (story A3) — but the record can never claim a base that is not credited and paid (§5.1 invariant, §12.6).",
  "preconditions": [
   "node-u on http://localhost:3422 (local ledger, teach backend 'stub', teach.lineage true) — or the in-process node the suite starts.",
   "The visitor's own READY draft, and a published base."
  ],
  "steps": [
   "Train a lesson on top of the visitor's own draft; try to publish it.",
   "Publish the base draft, then publish the child.",
   "Try to edit the child draft so `derivation.bases` (or `parents`) names something else."
  ],
  "expected": [
   "The job runs with the draft as its base and its questions inherited — nothing is dropped.",
   "Publishing is refused `parent_not_listed` naming the draft; after the base is published the child publishes and lists it in `parents[]`.",
   "An edit whose `derivation.bases`, `base.stack` or `dataset.parents` names a non-parent is refused with the field named; an unknown parent id is refused."
  ],
  "priority": "P0",
  "area": "teach",
  "automation": "api",
  "evidence": [
   "packages/node/test/lineage.test.ts (AZ-244)",
   "packages/core/src/lineage.ts (lineageProblems)",
   "packages/node/src/market.ts (validateLineageForAnnounce)",
   "docs/lineage-teach-design.md §5.1, §12.6, §3 story A3"
  ]
 },
 {
  "id": "AZ-245",
  "persona": "Teach mode (visitor)",
  "title": "A published training set outlives the working dataset it came from",
  "goal": "A knowledge that promises its questions to derivative creators must keep them: the published copy is content-addressed, immutable and exempt from the owner's delete and the 7-day sweep (§5.2).",
  "preconditions": [
   "node-u on http://localhost:3422 (local ledger, teach backend 'stub', teach.lineage true) — or the in-process node the suite starts.",
   "A knowledge published with a public training set."
  ],
  "steps": [
   "DELETE /api/teach/datasets/<the working dataset>.",
   "GET /api/patches/<id>/dataset/rows and read the blob directory."
  ],
  "expected": [
   "The working dataset is deleted (the lesson is unchanged, as before).",
   "The published copy is still served, still hashes to the sha on the record, and its benchmark.jsonl is still beside it."
  ],
  "priority": "P1",
  "area": "teach-dataset",
  "automation": "api",
  "evidence": [
   "packages/node/test/lineage.test.ts (AZ-245)",
   "packages/node/src/dataset-blobs.ts",
   "docs/lineage-teach-design.md §5.2"
  ]
 },
 {
  "id": "AZ-246",
  "persona": "Node operator / developer",
  "title": "Lineage rules as pure functions: the on-chain slice, the licence matrix, the anchor invariant, bf16 pre-state and the merkle root",
  "goal": "Every node — writer, reader and verifier — must agree on these without a network: the same 32 samples, the same hash, the same refusals, the same pre-state for a delta whatever order its rows are in (§5.1, §5.4, §6.4).",
  "preconditions": [
   "No node needed: packages/core unit tests."
  ],
  "steps": [
   "Run `npm test -w packages/core`."
  ],
  "expected": [
   "capBenchmarkSamples: the child's own first, then one per base with `source`, capped at 32, de-duplicated; `answers_hash` is over the FULL list and is order-sensitive.",
   "licenseCompatible: CC0/CC-BY/ODC-By/Proprietary bases accept any child licence; a CC-BY-SA base forces CC-BY-SA and an access floor; an unknown string is `bad_license`.",
   "lineageProblems: bases outside `parents[]`, a delta with an empty stack, a malformed `pre_state_sha256`, a self-parent, an unknown derivation kind, a negative row count and a teach anchor over the 32-sample cap are all named.",
   "preStateSha256 is invariant to row order and sensitive to any bf16-visible change; bf16Bits rounds half to even.",
   "merkleRoot is defined for 0, 1, 2 and odd leaf counts and carries an odd leaf up rather than duplicating it."
  ],
  "priority": "P0",
  "area": "api",
  "automation": "api",
  "evidence": [
   "packages/core/test/lineage.test.ts (AZ-246)",
   "packages/core/src/lineage.ts",
   "docs/lineage-teach-design.md §5.1, §5.4, §6.3, §6.4"
  ]
 },
 {
  "id": "AZ-247",
  "persona": "Node operator / developer",
  "title": "The AIN round trip keeps derivation, base stack and dataset parents (empty arrays included)",
  "goal": "AIN drops empty arrays, so a family tree read back from the chain must not lose an edge — and an anchor written before lineage must read back exactly as it was written (§5.1, §14).",
  "preconditions": [
   "No node needed: packages/core unit tests."
  ],
  "steps": [
   "toAin → JSON → fromAin → withEmptyArrays for an anchor with derivation/base/dataset, for one whose arrays were dropped, and for a pre-lineage anchor."
  ],
  "expected": [
   "All three lineage objects survive unchanged and still satisfy the invariant.",
   "Dropped arrays read back as [] (never undefined) inside the objects that exist.",
   "A pre-lineage anchor reads back with no `derivation`, no `base` and no `dataset` — never as empty objects."
  ],
  "priority": "P1",
  "area": "api",
  "automation": "api",
  "evidence": [
   "packages/core/test/lineage.test.ts (AZ-247)",
   "packages/core/src/ain-ledger.ts (withEmptyArrays)",
   "docs/lineage-teach-design.md §5.1, §14"
  ]
 },
 {
  "id": "AZ-248",
  "persona": "CLI user / node operator",
  "title": "`ainize dataset get <knowledge>`: the questions behind a published knowledge, by access level, from the terminal",
  "goal": "A creator who wants to build on someone's knowledge can get its questions without a browser — and is told plainly when they may not (§13).",
  "preconditions": [
   "node-u on http://localhost:3422 (local ledger, teach backend 'stub', teach.lineage true) — or the in-process node the suite starts.",
   "Knowledges published with public / derivative / private training sets (`ainize patch publish --dataset … --dataset-access …`)."
  ],
  "steps": [
   "`ainize dataset get <id>`; `ainize dataset get <id> -o questions.jsonl`; `ainize dataset get <sha256> --manifest`; `ainize dataset get <private-id>`."
  ],
  "expected": [
   "Without -o: questions, access in plain words, licence, fingerprint, whether this node holds it, what it was built from, and the first questions.",
   "With -o: the exact canonical bytes; the file hashes to the fingerprint on the record, and `--include-notes` is what keeps the publisher's notes in it.",
   "A `derivative` set is fetched through a signed derive intent (the fetch is counted on the creator's knowledge) — the terminal says so.",
   "A sha256 instead of an id resolves to the knowledge that published those bytes.",
   "A private set prints `dataset_private` plus what to do about it and exits 3."
  ],
  "priority": "P1",
  "area": "cli",
  "automation": "cli",
  "evidence": [
   "packages/cli/test/cli.test.ts (AZ-248)",
   "packages/cli/src/commands/dataset.ts",
   "docs/lineage-teach-design.md §13, §12.3"
  ]
 },
 {
  "id": "AZ-249",
  "persona": "Cross-cutting (errors, accessibility, i18n, performance)",
  "title": "No visitor IP leaves this node: live-test events are keyed by an HMAC id, the public feed carries neither, and raw events are purged after 90 days",
  "goal": "Demand signals are only allowed to exist because they cannot identify anyone (§10 privacy preconditions, F11): the id is an HMAC of the address under a node secret, the public feed strips it, and the counters outlive the rows they came from.",
  "preconditions": [
   "node-u on http://localhost:3422 (local ledger, teach backend 'stub', teach.lineage true) — or the in-process node the suite starts."
  ],
  "steps": [
   "Run a live test, then read GET /api/events and the events table; run the retention sweep with a row older than 90 days."
  ],
  "expected": [
   "Stored usage events carry `visitor: 'v:<16 hex>'` — never `ip:<addr>`; the same visitor is stable, a different one differs.",
   "GET /api/events returns no `data.visitor` for any kind, and no ` by …` suffix in the message.",
   "patch_signals_daily counts the test (tests/hits/misses/unscored) and its unique visitors; the raw event row is purged after 90 days while the counters stay."
  ],
  "priority": "P0",
  "area": "api",
  "automation": "api",
  "evidence": [
   "packages/node/test/chat.test.ts",
   "packages/node/test/signals.test.ts",
   "packages/node/src/market.ts (visitorId, publicEvents)",
   "docs/lineage-teach-design.md §5.6, §10"
  ]
 },
 {
  "id": "AZ-250",
  "persona": "Node operator / developer",
  "title": "Royalties along a family tree: every ancestor author is paid, both parents of a merge are paid, and Σ payouts = price",
  "goal": "Money has to follow the tree before the tree exists, and the pass-2 bug (F9: two 0.5 contributors got 50 %/25 %) had to go first (§11).",
  "preconditions": [
   "No node needed: packages/core unit tests."
  ],
  "steps": [
   "royaltySplit over the §11 worked examples 1–8, including a two-parent merge and two 0.5 data providers."
  ],
  "expected": [
   "The pool is split equally among unique ancestor authors reached through `parents[]` (depth 16, cycle-safe); an ancestor authored by the seller folds back into the seller.",
   "Pass 2 carves each seller-side contributor from the FIXED remainder: two 0.5 providers get 50 % / 50 %.",
   "Both parents of a merge are paid; Σ payouts equals the price in every example; Σ contributor shares ≤ 1 is refused at createDraft/publish."
  ],
  "priority": "P0",
  "area": "x402",
  "automation": "api",
  "evidence": [
   "packages/core/test/core.test.ts (royaltySplit)",
   "packages/core/src/catalog.ts",
   "docs/lineage-teach-design.md §11"
  ]
 },
 {
  "id": "AZ-251",
  "persona": "Dataset uploader (visitor)",
  "title": "The parser flags personal information without dropping the row",
  "goal": "A row that looks like an e-mail, a phone number, a 주민등록번호 or a card number still trains — it is publishing it to strangers that is blocked (§6.5).",
  "preconditions": [
   "No node needed: the parser is a pure function."
  ],
  "steps": [
   "Parse a file with an e-mail, a 010- phone number, a resident registration number, a Luhn-valid card number, a ticker code and a date."
  ],
  "expected": [
   "The four are `pii` with the kinds found; the ticker code and the date are `ok` (a six-digit ticker is not an id).",
   "Every flagged row keeps its position in rows.jsonl and is counted in `summary.pii`; nothing is rejected.",
   "The row's detail says it trains but cannot be published above 'private'."
  ],
  "priority": "P1",
  "area": "teach-parser",
  "automation": "api",
  "evidence": [
   "packages/node/test/teach-dataset.test.ts (pii)",
   "packages/node/src/teach-dataset.ts (detectPii)",
   "docs/lineage-teach-design.md §6.5"
  ]
 },
 {
  "id": "AZ-252",
  "persona": "Dataset uploader (visitor)",
  "title": "\"Delete the file after training\" keeps its promise: the training set is forced private and no copy is kept",
  "goal": "Two promises could contradict each other — 'the file is deleted after training' and 'people building on this can have the questions'. The first wins, and nothing is kept anywhere (lineage design §6.1, SC-8 `teach.pub.ds_retention`).",
  "preconditions": [
   "node-u on http://localhost:3422 (local ledger, teach backend 'stub', teach.lineage true) — or the in-process node the suite starts.",
   "A dataset uploaded with retention 'delete_after_training'."
  ],
  "steps": [
   "Train it, then publish asking for dataset access 'derivative'.",
   "Read the anchor, the dataset blob store and GET /api/patches/<id>/dataset with another key."
  ],
  "expected": [
   "The anchor's `dataset.access` is 'private' — the retention choice overrules the sharing choice, and the sheet says so before publishing.",
   "No copy is pinned in the dataset blob store: nothing was kept.",
   "The anchor still carries the sha256, so a re-train from the same questions can still be proven identical.",
   "Asking for the questions answers `dataset_private`, the same as any other private set."
  ],
  "priority": "P1",
  "area": "teach-dataset",
  "automation": "api",
  "evidence": [
   "packages/node/test/lineage.test.ts (AZ-252)",
   "packages/node/src/teach.ts (datasetPublication, publish)",
   "docs/lineage-teach-design.md §6.1, §4 SC-8"
  ]
 }
]
