{
  "name": "@ngram/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --project=web",
    "test:cli": "playwright test --project=cli-api",
    "report": "playwright show-report",
    "typecheck": "tsc -p tsconfig.check.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "@types/node": "^24.10.0"
  }
}
